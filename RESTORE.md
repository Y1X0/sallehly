# استعادة قاعدة البيانات من نسخة احتياطية — دليل طوارئ

اقرأ هذا وأنت بحالة طوارئ فعلية (قرص Render تعطّل/فُقد، أو قاعدة البيانات
تلفت). كل أمر هنا حقيقي ومُختبَر فعلياً من طرف لطرف — ليس افتراضياً. راجع
[DR-FIX-01] بـ`DECISIONS.md` للتفاصيل التصميمية الكاملة.

## أين تعيش النسخ الاحتياطية

**محلياً (على قرص Render نفسه)**: `$DATA_DIR/backups/` — نسخة جديدة كل 6
ساعات تلقائياً (`config/db.js`)، الاحتفاظ بآخر 7 أيام فقط. **المشكلة**: لو
القرص نفسه تعطّل، هذه النسخ تُفقَد معه بنفس اللحظة.

**خارجياً (GitHub Releases)**: إن كانت `BACKUP_GITHUB_TOKEN`/`OWNER`/`REPO`
مضبوطة على Render — نفس النسخة تُرفَع تلقائياً بعد كل نسخة محلية ناجحة،
لمستودع GitHub منفصل مخصَّص لهذا الغرض فقط، بنفس نافذة الاحتفاظ (7 أيام).
هذه هي النسخة التي تنجو حتى لو فُقد القرص بالكامل.

## كيف تجلب نسخة

من **Render Shell** (لوحة التحكم → هذا السيرفس → تبويب Shell):

```bash
node scripts/restore-db.js --list
```

يسرد كل النسخ المتاحة — المحلية (بتوقيتها) والخارجية (بوسمها)، أحدثها أولاً.
لو القرص المحلي فُقد بالكامل، هذا الأمر نفسه سيعرض فقط قسم "نسخ خارجية" —
وهذا متوقَّع ومقصود، أداة الاستعادة تحمّل النسخة الخارجية تلقائياً بالخطوة
التالية.

## كيف تستعيد

**دائماً استعد أولاً لمسار جديد منفصل، تحقق، ثم فقط بعدها استبدل القاعدة
الحيّة:**

```bash
# 1) استعادة لمسار مؤقت للتحقق أولاً (لا يلمس القاعدة الحيّة إطلاقاً)
node scripts/restore-db.js --source <مسار-محلي-أو-وسم-خارجي> --target /tmp/verify.sqlite

# الأداة تطبع تلقائياً: فحص PRAGMA integrity_check، وعدد الصفوف بالجداول
# الجوهرية (users, requests, offers, messages, ledger) — راجعها بعينك قبل المتابعة.

# 2) فقط بعد التحقق: استبدال القاعدة الحيّة فعلياً (يتطلّب --force صراحة)
node scripts/restore-db.js --source <نفس المصدر> --target "$DATA_DIR/sallehly.sqlite" --force

# 3) الملف الحالي بمسار القاعدة الحيّة (إن وُجد) يُنسَخ احتياطياً تلقائياً
#    أولاً إلى sallehly.sqlite.pre-restore-<وقت>.bak قبل أي استبدال — لا حذف
#    صامت أبداً.

# 4) حرج: أعد تشغيل عملية السيرفر يدوياً الآن (Render → Manual Deploy، أو
#    Restart من لوحة التحكم). عملية Node الحيّة تحمل اتصالاً مفتوحاً بالملف
#    القديم بالذاكرة ولن ترى الملف الجديد إلا بعد إعادة تشغيل فعلية.
```

`--source` يقبل إما مساراً محلياً موجوداً فعلاً، أو وسم نسخة خارجية (مثال:
`backup-2026-08-29T23-01-30-198Z`) — لو لم يكن الأول ملفاً محلياً موجوداً،
الأداة تُحمّله تلقائياً من GitHub Releases.

## كم يستغرق

من تمرين استعادة حقيقي كامل نُفِّذ فعلياً (راجع القسم التالي): نسخ الملف
والفحص خلال أداة الاستعادة نفسها — **أقل من ثانية واحدة** لقاعدة بحجم
~200KB. الوقت الحقيقي المستهلَك بكارثة فعلية هو غالباً: الوصول لـRender
Shell (~دقيقة)، تشغيل أمرَي `--list` ثم `--source/--target` (~دقيقة)،
إعادة التشغيل اليدوية من لوحة التحكم (~دقيقة إلى دقيقتين حتى يكتمل النشر).
**بمجمل: توقّع 5-10 دقائق من بداية الكارثة إلى خدمة تعمل بالبيانات
المُستعادة**، وليس ساعات. نسخ SQLite الخام أصلاً سريع حتى لقواعد أكبر
بكثير — الوقت لا يتناسب مع حجم البيانات بشكل ملحوظ إلا لقواعد ضخمة جداً
(عشرات الجيجابايت)، وهذا بعيد كل البعد عن الحجم الحالي.

## الإثبات — تمرين حقيقي كامل، ليس افتراضاً

نُفِّذ تمرين استعادة حقيقي من طرف لطرف (وليس فقط اختبار CI معزول):
سيرفر حقيقي، بيانات حقيقية عبر HTTP فعلي (عميل + فني + طلب + عرض + رسالة
دردشة + عملية شحن رصيد معتمَدة)، نسخة احتياطية حقيقية عبر
`POST /admin/backup`، استعادة حقيقية عبر `scripts/restore-db.js` كعملية
منفصلة تماماً، ثم **سيرفر ثانٍ منفصل كلياً يُشغَّل ضد الملف المُستعاد فقط**
للتأكد أن التطبيق فعلياً يقلع ويعمل عليه، لا فقط أن الملف يُفتَح.

| الجدول | العدد الحي (قبل النسخ) | العدد بالملف المُستعاد |
|---|---|---|
| users | 8 | 8 |
| requests | 1 | 1 |
| offers | 1 | 1 |
| messages | 1 | 1 |
| ledger | 1 | 1 |

`PRAGMA integrity_check` على الملف المُستعاد: `ok`. الطلب الفريد المُنشأ
بهذا التمرين وُجِد فعلاً بالملف المُستعاد (تطابق تام بالوصف). السيرفر الثاني
(ضد الملف المُستعاد فقط) أقلع بنجاح، `/health` أرجع `200`، تسجيل دخول
الأدمن نجح فعلياً ضد البيانات المُستعادة، والطلب الفريد نفسه ظهر عبر
`GET /api/requests` الحقيقي — إثبات أن الاستعادة تُنتج قاعدة بيانات صالحة
للاستخدام الفعلي، لا مجرد ملف يُفتَح.

**تنويه صريح**: هذا التمرين نفَّذته وكيل Claude محلياً بنفس الكود بالضبط
المنشور على الإنتاج (نسخة طازجة من نفس المستودع، خادم حقيقي، لا محاكاة) —
لكن ضد بيانات تمرين أُنشئت لهذا الغرض تحديداً، لا ضد قاعدة الإنتاج الحيّة
فعلياً على Render. الوكيل لا يملك وصولاً لا للوحة تحكم Render ولا لـShell
ولا لبيانات اعتماد الأدمن الحقيقية — ولا يجب أن يملكها. لإثبات مطابق تماماً
ضد الإنتاج الحقيقي، شغّل نفس أوامر هذا الدليل بنفسك عبر Render Shell على
نسخة حقيقية من `$DATA_DIR/backups/`، لمسار مؤقت منفصل، تماماً كما هنا.
