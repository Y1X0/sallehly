# اصطلاح هذا الملف — إلزامي لكل مُدخَل جديد

كل مُدخَل جديد (`# [TAG]` أو `## [TAG]`) يجب أن يحمل قسم `## الخطورة` صريحاً،
مباشرة بعد العنوان — لا تترك الخطورة ضمنية بالسرد فقط. سبب هذا القسم:
تدقيق لاحق (2026-08-30) احتاج تصنيف كل نتيجة مؤجَّلة عبر خمس جولات تدقيق،
ووجد أن الغالبية العظمى (كل شيء خارج قائمة الجولة الثالثة المؤجَّلة تحديداً)
لا تحمل أي كلمة خطورة صريحة — فاضطُرّ التصنيف لاستنتاج الخطورة من صياغة
الأثر، لا الاستشهاد بها مباشرة. لا تكرّر هذه الفجوة.

**المقياس الثابت (أربع درجات فقط، لا تُضِف درجات جديدة بلا نقاش):**
- **حرج (Critical)** — يوقف الخدمة بالكامل لكل المستخدمين، أو يفسد بيانات
  مالية/شخصية بشكل يصعب أو يستحيل التراجع عنه، أو يستغلّه أي مستخدم عادي
  بلا شرط خاص.
- **خطير (High)** — تسريب بيانات حقيقية لأشخاص حقيقيين، تحايل كامل على حماية
  أمنية أو نموذج عمل قائم، أو فساد بيانات مالية لحساب واحد أو أكثر.
- **متوسط (Medium)** — أثر حقيقي لكن محدود النطاق (يحتاج شرطاً خاصاً/توقيتاً
  معيّناً/دوراً إدارياً)، أو مسألة منتج/خصوصية مفتوحة لم تُحسَم بعد.
- **منخفض (Low)** — أثر نظري أو ضيق جداً، أو مسألة أداء/تنظيف بلا أي احتمال
  استغلال حقيقي حالياً.

اكتب الخطورة كخيار واحد من الأربعة أعلاه بالضبط (بالعربي أو الإنجليزي، لا
كلاهما)، ثم جملة واحدة تبرّرها. لو كانت غير محسومة (سؤال منتج مفتوح لا خطأ
برمجي)، اكتب "غير محسومة" صراحة بدل ترك القسم فارغاً أو حذفه.

---

# ملاحظة توثيقية — حذف حساب (DELETE /me و DELETE /admin/users/:id)

## الخطورة
حرج — كان يُسقط عملية Node بأكملها (عطل إنتاج كامل) لأي مستخدم له سجل
استخدام حقيقي يستدعي `DELETE /me`، بلا أي شرط خاص.

## [FIX-DELETE-CRASH-01] تحديث — القرار الأصلي لم يعد قابلاً للتنفيذ حرفياً
القرار الموثَّق أدناه (Hard Delete فعلي لصف `users`) كان يعمل فقط لأن قيود
`FOREIGN KEY` لم تكن مُفعَّلة فعلياً وقت كتابته. بعد ترقية `better-sqlite3`
(تُفعِّل `foreign_keys` افتراضياً لكل اتصال)، أي `DELETE FROM users` لحساب له
سجل واحد فعلي بأي من `topups.technician_id`، `requests.customer_id`/
`technician_id`، `offers.technician_id`، `support_tickets.user_id`، أو
`support_messages.sender_id` (كلها مفاتيح خارجية حقيقية NOT NULL تقريباً)
يفشل بـ`FOREIGN KEY constraint failed`. بما أن `DELETE /me` كانت `async` بلا
`try/catch`، هذا الاستثناء غير الملتقَط كان **يُسقط عملية Node بأكملها** —
عطل إنتاج كامل قابل للتحقيق من أي مستخدم عادي له سجل استخدام حقيقي (تقريباً
كل مستخدم فعلي بالمنصة).

**الحل الحالي المطبَّق (`utils/db-helpers.js` → `anonymizeUser`):** بدل حذف
صف `users` (مستحيل الآن دون كسر القيد)، يُعاد كتابته في مكانه — يبقى
موجوداً (فلا ينكسر أي `FOREIGN KEY` ولا أي `JOIN` حالي بالكود، مثل
`getMessages` في `utils/db-helpers.js` التي تعتمد `JOIN users` لعرض اسم
المرسِل)، لكن كل حقل يعرّف صاحبه فعلياً (`name`, `email`, `phone`,
`password_hash`, `national_number`, `avatar_url`, `city`, `areas`,
`services`, `fcm_token`) يُصفَّى بشكل لا رجعة فيه، مع `is_active=0` (يمنع
تسجيل الدخول)، `token_version+1` (يُبطل أي توكن قائم فوراً)، و`deleted_at`
(علامة زمنية تميّزه عن حساب موقوف عادي). هذا يحقق **نفس هدف القرار الأصلي
بالضبط**: إزالة أي بيانات شخصية تعرّف صاحب الحساب، مع الحفاظ الكامل على كل
السجلات التاريخية/المالية الحقيقية بلا حذف أو تعديل — فقط الآلية تغيّرت من
"احذف الصف" إلى "صفِّ الصف مكانه"، بسبب تفعيل `FOREIGN KEY`.

كلا الـ endpoint الآن مُغلَّفان بـ`try/catch` صريح يُعيد خطأ HTTP نظيف
(500) بدل أي احتمال لإسقاط العملية، بغض النظر عن أي حالة غير متوقَّعة أخرى.

انظر `tests/account-deletion.spec.js` للاختبارات التي تثبت: (1) الحذف
لا يُسقط السيرفر لحساب له تاريخ استخدام حقيقي، (2) لا تُكشَف أي بيانات
شخصية بعد الحذف، (3) القيود الخارجية تبقى سليمة.

## [DATA-INTEGRITY-02] توضيح — الحماية أعلاه لا تغطي كل الجداول، لا تفترض ذلك

الفقرة أعلاه دقيقة لسياقها (حذف `users`): الأعمدة المذكورة فعلاً محمية بقيد
`FOREIGN KEY` حقيقي. لكن صياغتها ("تُفعِّل `foreign_keys` افتراضياً لكل
اتصال") قد تُقرأ خطأً على إنها حماية شاملة لكل الجداول — وهذا غير صحيح.
`PRAGMA foreign_keys` (تحقَّق مباشرة: `1` افتراضياً بنسخة `better-sqlite3`
الحالية بلا أي `db.pragma()` صريح) يحمي فقط العلاقات التي تُصرَّح فعلياً بـ
`FOREIGN KEY` بتعريف الجدول — لا يحمي أي جدول تلقائياً.

**جداول تحمل `FOREIGN KEY` فعلياً** (`config/migrate.js`): `topups`،
`requests`، `offers`، `support_tickets`، `support_messages`، `notifications`.

**جداول تشير لـ`users`/`requests`/`offers` بمعرّف رقمي لكن بلا أي
`FOREIGN KEY` مُصرَّح**: `messages` (`request_id`، `sender_id`)،
`chat_violations`، `chat_reads`، `message_reports`، `user_blocks`،
`ratings`، `ledger`، `complaints`. لو أُضيف مستقبلاً أي مسار `DELETE`
حقيقي على `requests` أو `users` (لا يوجد اليوم — تحقَّق: لا `DELETE FROM
users`/`requests` بأي route حالياً، `anonymizeUser` لا تحذف صفاً أبداً)،
هذه الجداول الثمانية لن تمنع SQLite الحذف ولا سترمي أي خطأ — ستبقى صفوفها
يتيمة بصمت. أي عمل مستقبلي على حذف `requests`/`users` يجب أن يتحقق من هذه
القائمة أولاً، لا أن يفترض أن `PRAGMA foreign_keys` يغطيها.

---

## القرار الأصلي (كما كُتِب قبل هذا التحديث)
`DELETE /admin/users/:id` بيحذف صف المستخدم **فعلياً ونهائياً** من جدول `users`
(Hard Delete)، بشرط ما يكون عنده طلب نشط أو رصيد أكبر من صفر وقت الحذف.

## الأثر الجانبي المقصود (وليس خطأ)
الجداول التانية (`requests`, `offers`, `messages`, `ratings`, `ledger`, `topups`,
`chat_violations`, `complaints`, `support_tickets`) **ما بتنحذف تبعاً له** —
السجلات التاريخية بتضل موجودة بقاعدة البيانات، بس بتصير مرتبطة بـ `user_id`
ما عاد موجود.

**النتيجة العملية:** أي شاشة أو استعلام بيعتمد على `JOIN` مع جدول `users`
(مثل قائمة الطلبات بلوحة الأدمن التي تعرض `customer_name`/`technician_name`)
**رح تخفي هاي السجلات تلقائياً** من العرض، رغم إنها لسا موجودة بقاعدة البيانات.

## ليش هيك بالذات
تم اختيار هذا السلوك (حذف المستخدم فقط، إبقاء السجلات التاريخية يتيمة) بدل:
- **Cascading Delete** (حذف كل شي مرتبط فيه): خطر — بيمسح تاريخ مالي ومعاملات
  حقيقية بشكل نهائي، وممكن يأثر على سجلات أطراف تانية (مثلاً حذف عميل بيمسح
  طلبات فيها تقييمات وسجل عمل لفني تاني).
- **Soft Delete** (عمود `is_deleted`): تم اقتراحه كخيار، لكن تم اختيار الحذف
  الفعلي بناءً على طلب صريح من صاحب المنتج.

## ملاحظة: القسم التالي لم يعد ينطبق حرفياً (راجع التحديث أعلى الملف)
- لإخفاء السجلات اليتيمة من الشاشات: عدّل استعلامات الـ `JOIN` لتستخدم
  `LEFT JOIN` مع قيمة افتراضية ("مستخدم محذوف") بدل `INNER JOIN`.
- للانتقال لـ Soft Delete: أضف عمود `is_deleted` لجدول `users`، وعدّل هذا
  الـ endpoint ليحدّث العمود بدل `DELETE`، وأضف شرط `WHERE is_deleted=0`
  لكل استعلامات تسجيل الدخول وعرض القوائم.

# [FIX-ERRCODE-02] رموز أخطاء ثابتة (code) لتمكين ترجمة العميل — النطاق المتعمَّد

## ما تم
كل استجابة `res.status(...).json({error:...})` بالراوتات المذكورة أدناه صارت
تحمل أيضاً حقل `code` ثابت (`SCREAMING_SNAKE_CASE`، مسبوق باسم النطاق) بجانب
نص `error` العربي الموجود أصلاً — 127 رمزاً مختلفاً عبر 178 موقعاً (127 لا
125: `FILE_TYPE_NOT_ALLOWED`/`AUDIO_TYPE_NOT_ALLOWED` بـ`middleware/security.js`
سقطا سهواً من العدّ الأصلي لأنهما يُسنَدان عبر متغيّر بشرط ternary
`const code = ... ? 'FILE_TYPE_NOT_ALLOWED' : 'AUDIO_TYPE_NOT_ALLOWED'`، لا
حرفية `code: 'X'` مباشرة كباقي المواقع — الكود نفسه صحيح منذ الدمج، والتصحيح
هنا توثيقي فقط). **لم يتغيّر
أي نص عربي حرفياً** (تحقّق آلي: استخراج كل حرفيات `error:` قبل/بعد ومقارنتها
كمجموعة متعددة — مطابقة تامة). المواقع القليلة التي تحمل بيانات ديناميكية
(مثال: `OFFER_ACTIVE_REQUEST_EXISTS`، `DELETE_ACCOUNT_ACTIVE_REQUEST`) تحمل
أيضاً حقل `params` مُتداخِل (`{id, service}` أو ما شابه) — **نفس الشكل بلا
استثناء**: `{error, code, params?}` دائماً، لا حقول مسطّحة إضافية بأي موقع.
`INSUFFICIENT_BALANCE` (routes/offers.routes.js) كان السابقة الوحيدة الحاملة
لحقول مسطّحة (`required_balance`/`current_balance`/`free_quota_used`) — نُقلت
هي أيضاً لنفس شكل `params` (تحقَّق أولاً أن لا شيء يعتمد على الشكل المسطّح
القديم قبل نقلها — لا شيء كان يعتمد عليه).

الملفات المشمولة: `middleware/auth.js`، `middleware/security.js`،
`routes/auth.routes.js`، `routes/requests.routes.js`، `routes/offers.routes.js`،
`routes/chat.routes.js`، `routes/support.routes.js`، `routes/technicians.routes.js`،
`routes/topups.routes.js`، `routes/notifications.routes.js`.

## `routes/admin.routes.js` مُستثنى عمداً — لا تُضِف له رموزاً بلا نقاش أولاً
لوحة الأدمن (~45 موقع خطأ إضافي) **لم تُلمَس بهذا التغيير، وهذا مقصود لا
سهو**. السبب: الهدف الكامل من `code` هو تمكين العميل (تطبيق فلاتر) من ترجمة
رسالة الخطأ حسب لغة الواجهة الحالية (عربي/إنجليزي) بدل عرض النص العربي دائماً
بغض النظر عن اللغة. لوحة الأدمن يستخدمها طاقم داخلي يتحدّث العربية أصلاً —
قيمة الترجمة هناك شبه معدومة، بينما هي أكبر ملف بالمشروع (أكثر مواقع خطأ من
أي ملف آخر). لو احتاج يوماً واجهة أدمن مترجَمة فعلياً، هذا قرار منفصل يستحق
نقاشاً صريحاً وقتها — لا تُعِد فتح هذا الملف لإضافة رموز بشكل عرضي ضمن مهمة
أخرى دون التحقق من أن الحاجة فعلاً تغيّرت.

## طريقة التحقق (نفس الانضباط عبر كل الملفات، لا استثناء)
لكل ملف: استُخرجت كل حرفيات `error:` (نصوص مقتبَسة فقط) من النسخة القديمة
والجديدة، وقورنتا كمجموعة متعددة (multiset) — أي تغيير حرفي واحد في أي رسالة
كان سيُفشِل التحقق ويمنع الكتابة للملف. الرسائل المتطابقة حرفياً عبر عدة مواقع
(مثال: `'الطلب غير موجود'` بأربعة ملفات، `'معرّف غير صحيح'` بخمسة) أخذت نفس
الرمز، لكن **بعد التحقّق الفعلي من التطابق الحرفي لكل موقع على حدة** — نصوص
متشابهة ظاهرياً لكن مختلفة بحرف واحد (مثال: `'كلمة السر طويلة جداً، الحد
الأقصى 72 حرف'` مقابل `'كلمة السر طويلة جداً'` بلا اللاحقة) أُعطيت رموزاً
منفصلة عمداً. مجموعة اختبارات Playwright الكاملة (254 اختباراً) شُغِّلت قبل
وبعد التعديل — نجحت جميعها (اختبار واحد فقط احتاج تحديث سطر واحد ليطابق نقل
`INSUFFICIENT_BALANCE` لشكل `params`، وهو تغيير سلوك مقصود موثَّق أعلاه، لا
انكسار).

# [SEC-FIX-CHATSCOPE-01] + [SEC-FIX-OFFERSCOPE-01] — ثغرتا تسريب بيانات: دردشة مشتركة بين مزايدين، وحصاد مواقع الطلبات

## الاكتشاف
تدقيق أمني شامل (طُلب صراحة، غطّى المصادقة/الصلاحيات/التحقق من المدخلات)
كشف ثغرتين حقيقيتين على تطبيق منشور فعلياً بمستخدمين حقيقيين — كلتاهما
"صلاحية زائدة" لا يستخدمها العميل (Flutter) فعلياً بأي مسار حالي، تحقَّق من
هذا مباشرة بمراجعة كود العميل قبل اقتراح أي إصلاح (لا مجرد افتراض).

### #1 — أي فني قدّم عرضاً (حتى مرفوضاً) يقرأ/يكتب بمحادثة الطلب للأبد
`routes/chat.routes.js` (6 مواقع: messages GET/POST، audio، images،
report-message، block) كانت تسمح بالوصول لأي فني له صف بجدول `offers` لهذا
الطلب (`hasOffer`)، بلا شرط على حالة العرض (pending/rejected) ولا على كون
الفني هو المؤكَّد فعلياً (`r.technician_id`). جدول `messages` نفسه
(`config/migrate.js`) لا يحمل أي عمود يربط رسالة بفني معيّن — فقط
`request_id`. النتيجة: لو قدّم فنيان عرضاً على نفس الطلب، كلاهما يقرأ نفس
سجل المحادثة الواحد المشترك — بما فيه محادثة العميل مع الفني الآخر (تفاوض
سعر، توضيح عنوان) — ويبقى هذا الوصول قائماً حتى بعد رفض عرضه، للأبد، إلى أن
يحظره العميل يدوياً (إجراء لن يخطر لأحد تجاه فني لم يتحدث معه أصلاً).

**الإصلاح:** حُذف شرط `hasOffer` بالكامل من الستة مواقع — الوصول الآن مقصور
على الفني المؤكَّد (`r.technician_id === req.user.id`) فقط، تماماً كالعميل
والأدمن. **بلا أي تغيير على قاعدة البيانات أو العميل** — تحقَّق مباشرة (قراءة
كود `sallehly_app` قبل الإصلاح) أن الواجهة أصلاً لا تفتح شاشة الدردشة إطلاقاً
قبل قبول عرض: `chats_screen.dart` (الشاشة المشتركة لكلا الدورين) تستثني صراحة
حالات الطلب "بانتظار العروض"/"وصلت عروض" من قائمة المحادثات، بطاقة العرض
بشاشة العميل (`offer_card.dart`) لا تحمل أي زر دردشة، والمسار الوحيد لفتح
الدردشة قبل هذا الاستثناء هو مباشرة بعد نجاح `acceptOffer()`. أي اختبار
Playwright حالي لا يعتمد على السلوك القديم (تحقَّق قبل التعديل).

### #2 — أي فني يحصد بيانات موقع كل طلب على المنصة عبر IDs تسلسلية
`GET /requests/:id/offers` (`routes/offers.routes.js`) كانت تمنح أي حساب
فني (`req.user.role === 'technician'`) وصولاً لأي `request_id`، بلا شرط
تطابق خدمة/مدينة ولا علاقة سابقة بالطلب، وترجع كائن الطلب الكامل — يشمل
`lat`/`lng` (إحداثيات دقيقة لبيت الزبون)، `description`، `problem_image_url`.
بما أن الـ IDs تسلسلية (`AUTOINCREMENT`)، حساب فني واحد يقدر يمشي `1,2,3...`
ويحصد موقع كل زبون على المنصة — ليس فقط الطلبات المفتوحة حالياً، بل كل طلب
بتاريخ المنصة. **هذا تسريب خصوصية موقع لأشخاص حقيقيين، لا مجرد فجوة صلاحيات.**

**الإصلاح:** الشرط الآن مطابق للفني المؤكَّد أو من قدّم عرضاً فعلياً على هذا
الطلب بالذات (`hasOffer` — نفس النمط المستخدَم أصلاً بباقي هذا الملف)، بدل
أي فني على الإطلاق. تحقَّق (قراءة `requests_provider.dart`/`offers_screen.dart`)
أن العميل يستدعي هذا الـendpoint من شاشة واحدة فقط (العميل يراجع عروضه) —
لا شاشة فنّي بالتطبيق تستدعيه إطلاقاً؛ تصفّح الفني للطلبات المتاحة يمر عبر
`GET /requests` منفصل تماماً (مصفّى أصلاً حسب الخدمة/المدينة).

## قرار مؤجَّل عمداً — عرض lat/lng قبل قبول العرض
لم يُلمَس شكل الحقول المُرجَعة بهذا الإصلاح (لا تصفية `lat`/`lng` عن الفني
المزايد). هل يجب إخفاء الإحداثيات الدقيقة عن فني قدّم عرضاً لكن لم يُقبَل
بعد (يكتفي بمدينة/منطقة فقط حتى القبول)؟ هذا سؤال منتج منفصل يستحق تفكيراً
لحاله، لا أن يُحسَم ضمن إصلاح أمني — **لا تُغلَق هذه النقطة ضمن مهمة أخرى
بلا نقاش صريح.**

## بيانات تاريخية — سؤال إفصاح لا تنظيف، لم يُحسَم بعد
الثغرة #1 كانت منشورة فعلياً؛ إن كان هناك طلبات تلقّت رسائل من أكثر من فني
مزايد قبل هذا الإصلاح، فتلك المحادثات فعلاً قُرئت من طرف لم يكن يجب أن
يراها — هذا لا يُصحَّح بإصلاح الكود وحده. عدّ (قراءة فقط، بلا كتابة) يحدّد
حجم الأثر التاريخي يُجرى من طرف صاحب المنتج مباشرة على قاعدة الإنتاج
الحقيقية (لم تُمنَح أي جلسة وصولاً لها). النتيجة لم تصل بعد وقت كتابة هذا
القسم — إن كان الرقم غير صفري بشكل ملموس، هذه مسألة إفصاح للمستخدمين
المتأثرين تستحق نقاشاً منفصلاً، وليست مجرد "مهمة تنظيف بيانات".

# [DATA-INTEGRITY-01] تغليف إرسال/قبول العروض بمعاملة (`db.transaction()`)

## ما كان
`POST /requests/:id/offer` (`routes/offers.routes.js`) و`POST
/offers/:id/decision` (فرع القبول تحديداً) كانا يُنفّذان 3 كتابات منفصلة
غير مُغلَّفة بمعاملة لكل عملية أعمال واحدة: إدراج عرض + تحديث عدّاد الحصة +
تحديث حالة الطلب (الإرسال)، أو رفض كل العروض الأخرى + قبول هذا العرض +
تعيين الفني للطلب (القبول). كلا الـroute متزامن تماماً (بلا `await`)، فلا
يوجد أي تسابق (race) مع طلب آخر — الخطر الحقيقي الوحيد كان انهيار العملية
(crash) في المنتصف: بفرع القبول تحديداً، انهيار بين "رفض كل العروض" و"تعيين
الفني" كان يترك الطلب بلا أي عرض مقبول ولا فني معيَّن، بلا أي خطأ يظهر
للعميل — طريق مسدود يحتاج تدخل دعم.

## الإصلاح
كلا الموقعين الآن يستخدمان `db.transaction()` — نفس النمط المستخدَم أصلاً
بـ`doComplete` (`routes/requests.routes.js`) و`doReview`
(`routes/topups.routes.js`) و`doAdjust` (`routes/admin.routes.js`). لا تغيير
بالسلوك الناجح — فقط ضمان أن أي انهيار في المنتصف يُرجع كل الكتابات لحالتها
قبل العملية بدل تركها منتصفة الطريق. مجموعة اختبارات Playwright الكاملة
(255 اختباراً) شُغِّلت بعد التعديل — نجحت جميعها.

## نطاق متروك عمداً
تدقيق شامل لعمليات الأعمال متعددة الكتابات بباقي المشروع لم يُجرَ ضمن هذه
المهمة — هذا الإصلاح مقصور على الموقعين المذكورين أعلاه فقط (كانا الأخطر
مالياً/تشغيلياً من بين ما رُوجِع). أي موقع آخر مشابه يُكتشف لاحقاً يستحق نفس
المعالجة، لا افتراض أن هذا الإصلاح غطّى كل شيء.

# [SEC-FIX-CHATSCOPE-02] نفس ثغرة الدردشة، طريق ثانٍ لم يغطّه [SEC-FIX-CHATSCOPE-01]

## الاكتشاف
[SEC-FIX-CHATSCOPE-01] دقّق REST فقط (`routes/chat.routes.js`) وأبلغ الوضع
"مُغلَق". كان غير مكتمل — `services/socket.js` يحمل فحص صلاحية **منفصل
تماماً** لنفس القرار (هل يقدر هذا المستخدم يشارك بمحادثة هذا الطلب؟)، ولم
يُلمَس بذلك الإصلاح إطلاقاً. `socket.on('join-request', ...)` كان يسمح لأي
فني له عرض بحالة `'pending'` (لم يُقبَل ولم يُرفَض بعد) بالانضمام لغرفة
الطلب اللحظية — وبما أن `messages-updated` يبث سجل الرسائل **كاملاً** غير
مُصفّى لكل من بالغرفة، فني بعرض معلَّق يقدر يقرأ محادثة العميل بأكملها لحظياً
عبر Socket.IO، رغم أنه بعد [SEC-FIX-CHATSCOPE-01] لم يعد يقدر يقرأها عبر
`GET /requests/:id/messages` (REST). لا يقدر يرسل (لا معالج رسائل بالسوكت
أصلاً، الإرسال REST فقط)، لكن القراءة اللحظية بقيت مفتوحة بالكامل.

## الإصلاح
`services/socket.js`، `join-request`: نفس القاعدة بالضبط المطبَّقة الآن
بـREST — الفني المؤكَّد فقط (`r.technician_id === socket.user.id`)، حُذف
فرع العرض `pending`/`accepted` بالكامل. اختبار تراجع مضاف
(`tests/socket-realtime.spec.js`، `[SEC-FIX-CHATSCOPE-02]`) يثبت أن فنياً
بعرض pending لا يستقبل `messages-updated` بعد `join-request`.

## كل نسخة مكرَّرة لفحص REST داخل الطبقة اللحظية (`services/socket.js`) — القائمة الكاملة
تدقيق كامل للملف (86 سطراً) وجد نسختين فقط، لا أكثر:

1. **`io.use(...)` (فحص الاتصال)** — يكرّر منطق `middleware/auth.js` بالكامل:
   فك تشفير JWT، فحص `is_active` حي، فحص `token_version`. **صحيح ومتزامن حالياً**
   مع REST — لم يُكتشَف أي انحراف هنا. لكنه لا يزال نسخة مكرَّرة يدوياً، لا
   استدعاء مشترك لنفس الدالة.
2. **`join-request` (فحص المشاركة بمحادثة طلب)** — يكرّر منطق `routes/chat.routes.js`.
   **كان منحرفاً فعلياً** (هذا الإصلاح) — المصدر المباشر لهذه الحادثة.

هذا ثاني انحراف من هذا النوع بالضبط (الأول [SEC-FIX-CHATSCOPE-01] نفسه) —
راجع [SEC-FIX-CHATSCOPE-03] أدناه: فحص #2 (المشاركة بمحادثة طلب) صار دالة
واحدة مشتركة بعد هذا الإصلاح مباشرة، فالقاعدة "عدّل النسخة المقابلة يدوياً"
لم تعد تنطبق عليه إطلاقاً — عدّل الدالة الواحدة، تتحدّث كلتا الطبقتين تلقائياً.
فحص #1 (الاتصال) **لا يزال** نسختين منفصلتين يدوياً — القاعدة القديمة تبقى
سارية عليه فقط: أي تعديل مستقبلي على `middleware/auth.js` (فحص `is_active`،
`token_version`، أو أي منطق مصادقة إضافي) يجب أن يُراجَع وينعكس يدوياً على
`io.use(...)` بـ`services/socket.js` بنفس النداء، لا كمهمة منفصلة لاحقة.

# [SEC-FIX-CHATSCOPE-03] توحيد فحص المشاركة بمحادثة الطلب — دالة واحدة بدل نسختين

## لماذا
نفس القاعدة ("هل يقدر هذا المستخدم يشارك بمحادثة هذا الطلب؟") انحرفت مرتين
متتاليتين بجلسة تدقيق واحدة ([SEC-FIX-CHATSCOPE-01] REST، ثم
[SEC-FIX-CHATSCOPE-02] Socket.IO) لأنها كانت مكتوبة يدوياً بمكانين منفصلين —
تعديل نسخة لا يضمن تعديل الأخرى تلقائياً، وهذا بالضبط ما حصل. الحل البنيوي:
دالة واحدة تُستدعى من الطرفين، بحيث يصبح النسيان مستحيلاً هيكلياً بدل أن يعتمد
على انضباط كل تعديل مستقبلي.

## التوحيد
`utils/helpers.js` → `canAccessRequestChat(user, request)` — دالة صرفة (بلا
اعتماد على `db` أو `io`، بنفس فلسفة باقي هذا الملف)، تُرجع `true` فقط للأدمن
أو صاحب الطلب (`customer_id`) أو الفني المؤكَّد (`technician_id`). تُستخدَم
الآن من:
- `routes/chat.routes.js`: كل الستة مواقع (بدل الشرط المكرَّر يدوياً بكل موقع).
- `services/socket.js`: `join-request` (بدل النسخة المصححة للتو بـ
  [SEC-FIX-CHATSCOPE-02]).

## نطاق متروك عمداً — فحص الاتصال (`io.use`) لم يُوحَّد
تم التفكير بتوحيد فحص الاتصال (JWT + `is_active` + `token_version`) أيضاً،
وتُرِك عمداً كما هو (نسختان منفصلتان). السبب: هذا الفحص **يحتاج استعلام
db حياً فعلياً** (بعكس فحص المشاركة بالمحادثة، الذي لا يحتاج أي استعلام
إضافي — الصف `r` موجود أصلاً بكلا الطرفين قبل الفحص)، وExpress middleware
(`(req,res,next)`) وSocket.IO middleware (`(socket,next)`) لهما توقيع دالة
مختلف تماماً — توحيدهما فعلياً يعني استخراج طبقة استعلام+مقارنة مشتركة
وإبقاء غلافين (adapter) رفيعين حول كل بروتوكول، لا استبدال نسخة بنداء واحد
كما حدث هنا. هذا الفحص **لم ينحرف أبداً حتى الآن** (لا يزال متطابقاً)، فتكلفة
إعادة الهيكلة الآن أعلى من المخاطرة الفعلية الحالية. مؤجَّل بثقة — ليس منسياً.
راجع الفقرة أعلاه: القاعدة اليدوية ("عدّل النسخة المقابلة") لا تزال سارية على
هذا الفحص تحديداً، وعليه وحده.

---
# [SEC-FIX-UPLOADS-01] الملفات المرفوعة (public/uploads) بلا أي تحقق صلاحية — الخطوة الأولى: avatars/ وpayments/

## الاكتشاف
كل ملفات `public/uploads` (صور شخصية، إيصالات دفع، صور الطلب/الشات، تسجيلات
صوتية) كانت تُقدَّم عبر `express.static(env.UPLOAD_DIR)` بـ`app.js` بلا أي
تحقق صلاحية إطلاقاً — الحماية الوحيدة اسم ملف يصعب تخمينه
(`crypto.randomBytes(8)`، راجع `utils/helpers.js:safeUploadName`). أي رابط
مسرَّب (سجل متصفح، لقطة شاشة، Referer لخادم خارجي مسموح بـCSP) يكشف الملف
بشكل دائم.

**فحص فعلي (لا افتراض) لمين بيعرض كل نوع فعلياً بتطبيق Flutter:**
- `avatars/` — **لا تُعرَض بأي شاشة إطلاقاً**. كل مكان avatar بالتطبيق
  (admin_users_screen، my_reviews_screen، notifications_screen، إلخ) يعرض
  أيقونة عامة (person/engineering) لا الصورة الحقيقية.
- `payments/` (إيصالات) — **لا تُعرَض بأي شاشة إطلاقاً، ولا حتى بمراجعة
  الأدمن نفسها** (`admin_topups_screen.dart` بلا أي `Image.network`). الأدمن
  يوافق/يرفض بلا معاينة الإيصال بالتطبيق أصلاً اليوم.
- `requests/` (صورة المشكلة + صور الشات) — صورة المشكلة تُعرَض بلا أي هيدر
  مصادقة (`customer_request_details_screen.dart:161`،
  `technician_request_details_screen.dart:149`)؛ صور الشات تُعرَض **بهيدر
  Authorization فعلي أصلاً** (`chat_bubble.dart` — `_authHeadersFor`، مقصورة
  على first-party host، من إصلاح سابق [SEC-FIX-C1]).
- `audios/` (تسجيلات الشات) — تُشغَّل عبر `audioplayers`'s `UrlSource(url)`
  بلا أي هيدر (النسخة المستخدَمة `6.1.0`؛ دعمها لخيار `headers` لم يُتحقَّق
  منه بعد — راجع "نطاق متروك عمداً" أدناه).

**الخلاصة المهمة:** أخطر نوعين فعلياً (الإيصالات، والصور الشخصية) هما
بالضبط الأقل استخداماً بالتطبيق — تأمينهما مجاني تماماً من ناحية الأثر على
العميل.

## القرار: proxy موثَّق، لا signed URLs
صلاحية الوصول لمحتوى `requests/` تتغيّر مع الوقت (فني جديد يقدّم عرضاً، فني
يتأكَّد، فني ينسحب) — `canAccessRequestChat()` (`utils/helpers.js`) مبني
أصلاً على هذه الفكرة. رابط موقَّع (signed URL) يجمّد الصلاحية لحظة الإصدار:
إما صلاحية قصيرة (refresh متكرر معقّد بالعميل) أو صلاحية طويلة (يرجع لنفس
مشكلة التسريب اللي إحنا عم نحلها). **Proxy موثَّق** (`auth` + نفس فحص
الصلاحية الحي المستخدَم أصلاً بكل مكان بهذا المشروع) بيفحص الصلاحية "الآن"
مش "وقت الإصدار" — أوفق مع نموذج الصلاحيات المتغيّر أصلاً بهذا التطبيق.

## التنفيذ — الخطوة الأولى فقط (avatars/ + payments/)
`routes/protected-uploads.routes.js` — راوتر جديد، يعترض `/uploads/avatars/:filename`
و`/uploads/payments/:filename` بمصادقة حقيقية، **قبل** `express.static('/uploads', ...)`
العام بـ`app.js` بترتيب التسجيل (Express بيوقف عند أول middleware بيرد على
الطلب — `express.static` ما بينادي `next()` لو لقى الملف). `app.js`'s
`createApp(deps)` أصبحت تاخد `deps` اختياري (بدونه: بالضبط نفس السلوك القديم
— `express.static` العام فقط، بلا أي لمسة لقاعدة البيانات، حفاظاً على قابلية
استيراد `app.js` وحدها لاختبار خفيف بلا DB حقيقية، كما يوثّق تعليق الملف
نفسه). `server.js` يمرّر الآن `{ db, middleware: { auth } }` — مجموعة مصغَّرة،
لا نفس `deps` الكامل المستخدَم لاحقاً بـ`/api`.

**الصلاحيات — كلاهما يعيد استخدام قاعدة موجودة أصلاً، لا قاعدة جديدة:**
- `avatars/:filename`: `auth` فقط (أي مستخدم مسجَّل دخوله). يطابق انفتاح
  `GET /technicians/:id/profile` (`routes/technicians.routes.js`) الموجود
  أصلاً، الذي يعرض `avatar_url` لأي مستخدم مصادَق بلا أي علاقة/ملكية.
- `payments/:filename`: نفس قاعدة `GET /topups` بالضبط — الأدمن يشوف كل
  شيء، الفني يشوف إيصالاته هو فقط (`topups.technician_id === req.user.id`).

**تفصيل تقني اكتُشف أثناء الاختبار:** `res.sendFile()` يرفض مساراً نسبياً
(يرمي "path must be absolute or specify root") — بعكس `express.static` الذي
يتحمّل `env.UPLOAD_DIR` النسبي (بيئة الاختبار: `DATA_DIR=./data-test`) بلا
مشكلة داخلياً. `path.resolve()` بدل `path.join()` بملف الراوتر الجديد يحل
هذا لكل الحالات.

**اختبارات جديدة:** `tests/protected-uploads.spec.js` — 9 حالات (رفض بلا
توكن، فتح avatar من أي مستخدم، فتح/منع إيصال حسب الملكية، الأدمن يشوف كل
الإيصالات، 404 لملف غير موجود بلا تسريب وجوده، رفض 400 لاسم ملف يحاول
اجتياز مسار). الاختبار كامل التطبيق (265 حالة) يبقى أخضر.

## نطاق متروك عمداً — الخطوات المتبقية بنفس الإصلاح
`express.static('/uploads', ...)` العام **بيضل شغّال** لـ`requests/`
و`audios/` حتى تكتمل بقية هذا الإصلاح — قرار مقصود: لو انكسر إشي بمنتصف
التنفيذ، يضل التطبيق شغّال. الترتيب المتبقي:
1. استخراج `_authHeadersFor` من `chat_bubble.dart` لمكان مشترك، وتطبيق
   الـproxy على صور الشات — الآلية جاهزة أصلاً بالعميل، فقط نقل مكانها.
2. `problem_image_url` بالشاشتين (`customer_request_details_screen.dart`،
   `technician_request_details_screen.dart`) باستخدام نفس الدالة المستخرَجة.
3. الصوت — **آخر خطوة، وتحتاج تحقيقاً تقنياً قبل الالتزام باتجاه**: هل
   `UrlSource` بـ`audioplayers: ^6.1.0` بيدعم `headers` أصلاً؟ لو لأ، البديل
   تحميل البايتات عبر `api_client.dart` (المُصادَق أصلاً) ثم تشغيلها من ملف
   مؤقت محلي — تعديل أكبر من الصور، يستاهل تقرير منفصل قبل التنفيذ.
4. حذف `express.static('/uploads', ...)` العام نهائياً — **فقط بعد** اكتمال
   الخطوات 1-3، وبعد حسم اتجاه الصوت تحديداً.

## تحديث — الخطوة الثانية اكتملت (صور الشات ضمن requests/)
`routes/protected-uploads.routes.js` أصبح يعترض `GET /uploads/requests/:filename`
أيضاً — لكن **فقط** لو الاسم يطابق `body` رسالة شات `[image]...` موجودة فعلياً
بجدول `messages` (نفس قاعدة `canAccessRequestChat` بالضبط، لا فحص جديد). أي
اسم ملف لا يطابق رسالة شات (وأهمها اليوم: `problem_image_url`) يكمل فوراً
(`next()`) لـ`express.static` كما كان قبل هذا التعديل تماماً — لا تغيير سلوكي
على `problem_image_url` بهذه الخطوة، مثبَّت باختبار مخصَّص
(`tests/protected-uploads.spec.js`).

**جانب عميل (Flutter):** `_authHeadersFor`/`_isFirstPartyUrl` استُخرجتا من
`chat_bubble.dart` لملف مشترك (`lib/core/utils/authenticated_media.dart`)،
وأُعيد استخدامهما أيضاً بمحدث `TokenStorage` بدل نسخة يدوية ثانية من مفتاح
التخزين. **تفصيل مهم اكتُشف أثناء التنفيذ:** الصورة المصغَّرة داخل فقاعة
الشات (`_ImageMessage`) لم تكن ترسل هيدر Authorization إطلاقاً أصلاً — فقط
معاينة الصورة كاملة الحجم (`_FullImageView`) كانت ترسله. هذا كان آمناً وقتها
لأن الخادم لم يكن يتحقق من الهيدر أصلاً ([FIX-CHATIMG-03] وثّق سبب إزالة
FutureBuilder من المصغَّرة تحديداً: كانت تُعيد تحميل الصورة من الصفر مع كل
إعادة بناء، تُلغي ImageCache). بعد هذا الإصلاح، هذا لم يعد صحيحاً — أُعيد بناء
`_ImageMessage` كـStatefulWidget يجلب الهيدرز **مرة واحدة فقط** بـ`initState`
ويخزّنها بالـstate، يحترم درس [FIX-CHATIMG-03] (لا Future جديد بكل rebuild)
مع توفير الهيدر المطلوب الآن.

**اختبار موجود كُسر وأُصلح:** `tests/chat.spec.js` كان فيه اختبار يجلب رابط
صورة شات مرفوعة حديثاً **بلا أي توكن** ("الفحص الحقيقي: الرابط نفسه يرجع
الصورة") — هذا بالضبط ما تغيّر بهذا الإصلاح. عُدِّل ليرسل توكن العميل، وأُضيفت
تغطية صريحة لحدود الصلاحية (403/401) بـ`tests/protected-uploads.spec.js`.

## نقطة تحتاج قراراً قبل الخطوة الثالثة (problem_image_url)
الخطة الأصلية افترضت أن `problem_image_url` يستخدم نفس `canAccessRequestChat`.
**هذا غير صحيح فعلياً** — تحقّق مباشر من `GET /requests` (routes/requests.routes.js)
يُثبت أن الفني الذي **لم يقدّم عرضاً بعد** (يتصفح فقط، تطابق خدمة/مدينة) يرى
`problem_image_url` ضمن `SELECT r.*` قبل أي عرض — تماماً كما يرى `lat/lng`
(نفس فئة سؤال "[SEC-FIX-CHATSCOPE-01] + [SEC-FIX-OFFERSCOPE-01]" المؤجَّل
بالأعلى). تطبيق `canAccessRequestChat` حرفياً على `problem_image_url` سيقيّد
الوصول لصاحب الطلب/الفني المؤكَّد فقط — **تغيير سلوك حقيقي**: يمنع فنياً
يتصفح فعلياً من رؤية صورة المشكلة قبل تقديم عرضه، لا مجرد "تأمين نفس الشيء
الموجود". هذا قرار منتج/صلاحية يستاهل نفس المعاملة اللي أخذها سؤال lat/lng —
لم يُنفَّذ بعد، بانتظار قرار صريح.

## تحديث — الخطوة الثالثة اكتملت (problem_image_url)
القرار الفعلي (منك مباشرة): الفني المتصفّح (لسا ما قدّم عرضاً) **لازم** يشوف
صورة المشكلة أصلاً ليقدر يقدّر سعر عرضه بشكل واقعي — نفس الصلاحية الحالية
تماماً، لا تضييق. التنفيذ (`routes/protected-uploads.routes.js`): فحص صف
واحد بجافاسكربت (`canBrowseRequest`) يطابق حرفياً شرط WHERE بفرع الفني بـ
GET /requests (routes/requests.routes.js) — الحالة ضمن 'بانتظار العروض'/
'وصلت عروض'، الخدمة مطابقة، والمدينة أو المنطقة متقاطعة. مقارنة بجافاسكربت
لا SQL جديد (تفادياً لخطر انحراف الصياغتين بصمت)؛ لا خطر أداء PERF-01 لأنه
فحص صف واحد لا قائمة كاملة. صاحب الطلب/الفني المؤكَّد/الأدمن عبر
`canAccessRequestChat` كالمعتاد، والفني المتصفّح إضافياً عبر `canBrowseRequest`.

**نتيجة جانبية صحيحة، غير مقصودة صراحة لكنها متّسقة تماماً:** بمجرد قبول
عرض على الطلب، حالته تخرج من 'بانتظار العروض'/'وصلت عروض' — فأي فني آخر لم
يعد يقدر يتصفّح الطلب (ولا صورته) حتى لو طابقت خدمته ومدينته، تماماً كما
يختفي الطلب من قائمته بـGET /requests لنفس السبب بالضبط. مثبَّت باختبار.

**جانب عميل (Flutter):** widget جديد مشترك `AuthenticatedNetworkImage`
(`lib/core/widgets/authenticated_network_image.dart`) يغلّف `Image.network`
+ الهيدرز المُخزَّنة مرة واحدة بـinitState (نفس نمط `_ImageMessage` بـ
chat_bubble.dart تماماً) — طُبِّق على شاشتَي تفاصيل الطلب (عميل وفني)، مكان
الاستدعاء المباشر لـImage.network بلا هيدرز الذي كان موجوداً هناك.

## الخطوة الرابعة (الصوت) — لسا معلَّقة
تحقّق مباشر (توثيق `audioplayers`، كل الإصدارات لحد الأحدث): `UrlSource` لا
يدعم `headers` إطلاقاً. البديل المُقترَح: `BytesSource(Uint8List bytes)` —
تشغيل من بايتات بالذاكرة مباشرة (بلا ملف مؤقت ولا تنظيف)، بعد تحميلها عبر
`api_client.dart`'s `dio` (مُصادَق أصلاً تلقائياً) بـ`responseType: ResponseType.bytes`.
بانتظار موافقة قبل التنفيذ. بعد إنجازها، وبعدها فقط، تُحذف
`express.static('/uploads', ...)` العامة نهائياً بـ`app.js` — آخر خطوة بهذا الإصلاح بالكامل.

---
# [DR-FIX-01] النسخ الاحتياطي كان على نفس القرص الذي يحميه — استعادة خارجية + مُختبَرة فعلياً

## الاكتشاف (من تدقيق جاهزية الإنتاج)
`config/db.js` كان (ولا يزال) يُنتج نسخاً احتياطية حقيقية وصحيحة كل 6 ساعات
عبر SQLite's Online Backup API (راجع التعليق الأصلي أعلى الملف) — لكن لمجلد
`DATA_DIR/backups/`، أي **نفس القرص الذي تعيش عليه القاعدة الحيّة نفسها**.
لو تعطّل أو فُقد قرص Render، تُفقَد القاعدة وكل نسخها الاحتياطية بنفس اللحظة —
هذا يحمي من كتابة خاطئة أو تلف منطقي، لا من فقدان القرص، وهو بالضبط السيناريو
الذي يُفترض أن النسخ الاحتياطي يحمي منه أصلاً. **والأخطر: ما كانت هناك أي أداة
استعادة فعلية إطلاقاً** — الاختبارات القائمة (`tests/db-integrity.spec.js`،
`tests/admin.spec.js`) تفتح ملف النسخة مباشرة وتتحقق من صحته، لكن لا شيء كان
يثبت أن الاستعادة الفعلية (استبدال ملف حيّ بنسخة، إعادة تشغيل، عمل النظام) تعمل.

## الحل — طبقتان منفصلتان تماماً

**١) نقل خارجي (`services/offsite-backup.js`)**: بعد نجاح كل نسخة احتياطية
محلية، تُرفَع نسخة إضافية إلى GitHub Releases على مستودع مخصَّص (اختياري —
`BACKUP_GITHUB_TOKEN`/`OWNER`/`REPO`؛ بدونها يُتخطّى الرفع بصمت مع تحذير واحد،
النسخ المحلي يستمر بلا أي تأثير). لماذا GitHub لا S3/R2/B2: صفر تبعية جديدة
(`fetch` مدمجة بـNode 20)، صفر حساب/فوترة جديدة — إعادة استخدام حساب موجود
أصلاً. كل نسخة = إصدار (release) بوسم زمني، الملف مرفق كـasset، نافذة احتفاظ
مطابقة للمحلية (7 أيام)، رفع بلا `await` حتى لا يُبطئ استجابة
`POST /admin/backup` ولا يُفشل `createDbBackup()` نفسها مهما حدث (لا ترمي
`uploadBackupOffsite` أبداً).

**٢) أداة استعادة حقيقية (`scripts/restore-db.js`)**: أداة سطر أوامر يُشغّلها
مُشغِّل بشري فعلياً أثناء كارثة — تدعم مصدراً محلياً أو وسم نسخة خارجية،
تفحص `PRAGMA integrity_check` قبل أي تثبيت (ترفض نسخة تالفة)، تطبع عدد صفوف
الجداول الجوهرية للفحص البشري، تنسخ احتياطياً أي ملف موجود بالمسار الهدف قبل
استبداله (لا حذف صامت أبداً)، وتتطلّب `--force` صراحة لو كان الهدف هو مسار
القاعدة الحيّة تحديداً.

## الإثبات — `tests/backup-restore-drill.spec.js`
هذا الاختبار هو الفرق الحقيقي عن الوضع السابق: يُنشئ بيانات حقيقية فريدة
(طلب بوصف عشوائي)، يُشغّل `POST /admin/backup` الحقيقي، **يُشغّل
`scripts/restore-db.js` كعملية منفصلة فعلياً** (تماماً كما سيفعل مُشغِّل
حقيقي، لا محاكاة) لمسار هدف جديد بالكامل، ثم يفتح **الملف المُستعاد نفسه**
(لا الأصلي) ويتحقق أن ذلك الطلب بعينه موجود فعلاً بعد الاستعادة. مغطّى أيضاً:
رفض ملف تالف بوضوح بلا لمس المسار الهدف، ورفض الكتابة فوق القاعدة الحيّة بلا
`--force` صريح. 3/3 اجتازت، والمجموعة الكاملة (281 اختباراً) تبقى خضراء.

## نطاق متروك عمداً
الرفع الخارجي نفسه (الشبكة الحقيقية لـGitHub Releases) لم يُختبَر فعلياً بهذه
الجلسة — يحتاج `BACKUP_GITHUB_TOKEN` حقيقياً لا يجوز لأي وكيل توليده نيابةً
عن صاحب الحساب (نفس فلسفة عدم توليد keystore التوقيع بمشروع العميل Flutter).
منطق الاستعادة الفعلي (الجزء الأهم — "هل يعمل الاستعادة فعلياً؟") مُثبَت
بالكامل ومستقل عن هذه الخطوة، لأنه يعمل على أي ملف نسخة صالح بغض النظر عن
مصدره (محلي أو مُحمَّل من الخارج).

# [MON-FIX-01] لا مراقبة ولا تنبيه إطلاقاً — لا أحد يعرف أن الخدمة معطّلة إلا من شكوى مستخدم

## المشكلة
لا شيء كان يُخبر أحداً أن الخدمة توقفت أو أن خطأً غير متوقع يحدث. أخطر من
ذلك: `middleware/security.js`'s `apiErrorHandler` كان يحوّل **أي** استثناء
غير متوقَّع (خارج حالتي التحقق المعروفتين: حجم/نوع ملف) إلى استجابة 400 عامة
**بلا أي سطر واحد بالسجلّات** — خطأ حقيقي بمسار API كان يختفي كلياً، حتى من
عيني مطوّر يقرأ سجلّات الإنتاج مباشرة، لا فقط من أي نظام تنبيه.

## الحل — طبقتان، كلاهما بلا أي تبعية أو حساب جديد

**١) فحص التشغيل (uptime)**: `GET /health` (موجود أصلاً، `server.js`) يفحص
قاعدة البيانات فعلياً (`SELECT 1`) لا مجرد "200 دائماً"، بلا مصادقة وبلا حد
طلبات — جاهز فعلياً لأي خدمة مراقبة خارجية. لم يُضَف أي كود؛ فقط رُبِط
بـUptimeRobot (مجاني) من لوحة التحكم — راجع تعليمات المحادثة.

**٢) تنبيه الأخطاء (`services/error-alert.js`)**: يعيد استخدام Resend
(`RESEND_API_KEY`/`RESEND_FROM` الموجودين أصلاً لبريد OTP) — صفر تبعية جديدة،
صفر مفتاح/حساب جديد. يُرسِل لـ`ALERT_EMAIL` (أو `ADMIN_EMAIL` كبديل، كلاهما
اختياري). كبح تكرار بالذاكرة: نفس مصدر الخطأ + نفس الرسالة لا يُنبِّه ثانية
خلال 15 دقيقة — خطأ يتكرر بكل طلب لن يُغرق البريد.

مربوط بثلاث نقاط: `middleware/security.js`'s `apiErrorHandler` (بعد إضافة
`console.error` المفقود أصلاً)، و`server.js`'s `unhandledRejection`/
`uncaughtException`. الأخيرة وحدها تنتظر (`await`) اكتمال محاولة التنبيه قبل
`gracefulShutdown` — تلك تنتهي بخروج العملية خلال أجزاء من الثانية أحياناً،
وfire-and-forget هناك تحديداً قد يعني عدم اكتمال طلب الشبكة لـResend إطلاقاً
قبل خروج العملية.

## الإثبات
`tests/error-alert.spec.js`: يثبت أن `alertError()` بلا `RESEND_API_KEY`/
`ALERT_EMAIL` (حالة أي بيئة لم تُعِدّ التنبيهات بعد) لا يرمي استثناءً أبداً
ويرجع `false` بوضوح — نفس الخاصية الجوهرية المُثبَتة يدوياً بـ[DR-FIX-01]
لـ`uploadBackupOffsite`. المجموعة الكاملة: 283/283 (كانت 281، +2 هنا) —
والسجلّ الفعلي لتشغيلة الاختبارات أظهر `[API-ERROR]` يُسجَّل فعلاً عند خطأ
حقيقي (PayloadTooLargeError) لأول مرة، مؤكداً أن الربط يعمل من طرف لطرف.

## نطاق متروك عمداً
لا Sentry ولا لوحة تحكم مخصَّصة ولا تجميع/تصنيف أخطاء — بريد بسيط بكبح تكرار
بدائي يكفي لما طُلِب ("يخبرني إن كانت الخدمة معطّلة أو بها خطأ")، وأي شيء
أكثر تعقيداً كان سيكون مبالغة غير مطلوبة لخدمة بحجمها الحالي. مسار الإرسال
الفعلي عبر الشبكة (Resend حقيقياً) لم يُختبَر بهذه الجلسة تحديداً — لكن
`services/email.js` (نفس المزوّد بالضبط) يعمل فعلياً بالإنتاج أصلاً لبريد
OTP، فهذا ليس مساراً جديداً غير مُثبَت، بل إعادة استخدام لمسار مُثبَت فعلياً.

# [SEC-FIX-REQSPAM-01] POST /requests كان بلا أي حد طلبات — الوحيد بين كل نقاط الإنشاء الحقيقية

## المشكلة
`POST /requests` (routes/requests.routes.js:38) لم يحمل أي rate limiter
إطلاقاً — بعكس `/auth/*`، `/requests/:id/messages`، و`/requests/:id/offer`
اللواتي جميعها حصلت على حدود بعد حوادث إغراق موثَّقة ([SEC-FIX-17]،
[SEC-FIX-18]). كل نداء ناجح لهذا المسار تحديداً أثقل من أي منها: يُدرج صفاً
بقاعدة البيانات **ويُنشئ إشعاراً دائماً لكل فني على المنصة** (`getTechnicianIds`/
`notifyBatch`، حتى سقف 5000) داخل معاملة متزامنة واحدة — عملية تتناسب
تكلفتها مع عدد الفنيين على المنصة كلها، لا مع العميل نفسه. عميل واحد بلا أي
حد يقدر يُغرق قاعدة البيانات وإشعارات كل فني على المنصة بنداءات متكررة.

## الحل
`requestLimiter` (`middleware/security.js`) — 10 طلبات/ساعة بالإنتاج (نفس
نمط `IS_PROD ? X : 1000` المستخدَم بكل حدود هذا الملف، فلا تتأثر اختبارات
Playwright الآلية). أضيق بكثير من `offerLimiter` عمداً: عميل حقيقي نادراً
ما ينشئ أكثر من طلب أو طلبين بالساعة، بينما 10 كافية جداً لأي استخدام طبيعي
وتمنع تماماً نمط الإغراق. مربوط قبل `upload.single('problem_image')` عمداً
(فحص رخيص يوقف الطلب قبل إنفاق أي جهد بتحليل جسم multipart)، بنفس ترتيب
باقي الحدود بهذا المشروع.

## الإثبات
- حارس التراجع الموجود أصلاً بـ`security-hardening.spec.js` (يمنع إضعاف أي
  حد إنتاج بصمت) وُسِّع ليشمل `requestLimiter`.
- اختبار وجود `POST /api/requests` الناجح (`tests/requests.spec.js`) الآن
  يتحقق أيضاً من رأس `ratelimit-limit` بالاستجابة — إثبات أن المحدِّد مربوط
  فعلياً على المسار، لا فقط أن ثابته صحيح بالمصدر (نفس النمط المستخدَم أصلاً
  لـ`messageLimiter` بـ`tests/chat.spec.js`).
- المجموعة الكاملة: 286/286، صفر تراجع.

# [SEC-FIX-STATUSFLOW-01] العميل كان يقدر يُكمل طلبه بلا أي عرض فني مقبول — يُخصَم رصيد فني لم يوافق على شيء، ويصير قابلاً لتقييم سلبي دائم

## الاكتشاف
راجعة إنتاجية مستقلة كاملة (`POST /requests/:id/status`) وجدت أن الفحص
الوحيد قبل الانتقال لأي حالة كان "الطلب ليس مغلقاً أصلاً (مكتمل/ملغي)" —
لا يوجد أي تحقق من أن **الحالة الحالية** تسمح فعلياً بالانتقال للحالة
المطلوبة. سلسلة استغلال كاملة مُتحقَّق منها بقراءة الكود من طرف لطرف:

١. `POST /requests` يقبل `technician_id` من العميل مباشرة (`routes/requests
.routes.js:42،52-57`) — حقل حقيقي بالـAPI، **لكن تطبيق العميل الفعلي
(`RequestsApi.createRequest()`, `lib/features/requests/data/requests_api
.dart`) لا يرسله إطلاقاً ولا توجد أي شاشة "اطلب من هذا الفني تحديداً"**.
التحقق الوحيد: الفني موجود، نشط. لا تحقق من موافقته على أي شيء.
٢. العميل يُنشئ طلباً مستهدِفاً فنياً بعينه (`technician_id` مضبوط، لكن
`status='بانتظار العروض'` كما هو دائماً — التعيين المباشر لا يُقفز أي حالة).
٣. العميل يستدعي فوراً `POST /requests/:id/status {status:'مكتمل'}`. يمر كل
فحص موجود (يملك الطلب، ليس مغلقاً، هو العميل). عمود `technician_id` غير
فارغ فيُفعِّل فرع خصم العمولة (`requests.routes.js:241-260`) — رصيد حقيقي
يُخصَم أو حصة "طلب مجاني" تُستهلَك، لفنيٍّ لم يُقدِّم عرضاً ولا وافق على شيء
إطلاقاً.
٤. `POST /requests/:id/rate` (`:347-349`) يتحقق فقط من `status='مكتمل'`
و`technician_id` غير فارغ — العميل يقدر يترك تقييماً سلبياً دائماً ضد فني
لم يتفاعل مع الطلب إطلاقاً.

## التحقق الجنائي من الإنتاج — قبل أي إصلاح
بطلب صريح من صاحب المنتج، استعلام جنائي حقيقي (وليس تخميناً) نُفِّذ مباشرة
على قاعدة بيانات الإنتاج الحيّة (عبر Render Shell، لا يملكه الوكيل نفسه):
كل طلب `status='مكتمل'` بفني معيَّن بلا أي صف `offers` مطابق بحالة
`accepted` لنفس الزوج (request_id, technician_id) — التوقيع الجنائي الدقيق
لهذا الاستغلال، بغض النظر عن طريقة تعيين `technician_id`. **النتيجة: صفر
حالة مشبوهة، صفر عمولة مُحصَّلة بهذه الطريقة، صفر طلب "مكتمل" بلا فني
إطلاقاً.** الثغرة كانت مفتوحة لكن لم تُستغَل فعلياً — لا حاجة لأي تعويض أو
إشعار مستخدمين.

## الحل — خريطة انتقالات صريحة، لا افتراض
`LEGAL_PREDECESSORS` (`routes/requests.routes.js`، داخل معالج `/status`):
انتقال أي طلب لأي من `قيد التنفيذ`/`بانتظار تأكيد الدفع`/`مكتمل` يتطلّب أن
تكون حالته الحالية بالفعل ضمن `{تم اختيار عرض، قيد التنفيذ، بانتظار تأكيد
الدفع}` — وهذه الثلاث لا تُكتَب أبداً إلا عبر `routes/offers.routes.js:158`
(معاملة قبول عرض حقيقية). خريطة الحالات الشرعية اشتُقَّت من سلوك تطبيق
العميل الفعلي نفسه، لا افتراضاً: `technician_request_details_screen.dart`
يُظهر زرَي "بدء العمل" و"بانتظار تأكيد الدفع" معاً بمجرد تعيين الفني (تم
اختيار عرض فما فوق)، وملف `db-integrity.spec.js` القائم أصلاً يُثبت انتقالاً
مباشراً من "تم اختيار عرض" إلى "مكتمل" (بلا "قيد التنفيذ") كسلوك مقصود —
لذا الحالات الثلاث جميعها متكافئة كـ"فني وافق فعلاً"، لا تسلسل صارم بينها.
`ملغي` مستثناة عمداً — فحوصها الخاصة الموجودة أصلاً كانت صحيحة ولم تتغيّر.

## الإثبات
`tests/status-transition-security.spec.js` (3 حالات جديدة، كلها تحاكي
سلسلة الاستغلال الفعلية المُكتشَفة): طلب بلا أي عرض إطلاقاً يُرفض إكماله
بـ409 `STATUS_TRANSITION_INVALID`، طلب موجَّه مباشرة لفني عبر `technician_id`
لم يقدِّم عرضاً يُرفض أيضاً (ورصيد الفني يبقى بلا أي تغيير، تحقُّق مباشر من
قاعدة البيانات)، والقفز المباشر لـ"قيد التنفيذ"/"بانتظار تأكيد الدفع" بلا
عرض مقبول يُرفض كذلك. المجموعة الكاملة: **286/286** (كانت 283، +3 هنا) —
صفر تراجع، بما فيها كل الاختبارات التي تعتمد على المسار المباشر "تم اختيار
عرض ← مكتمل" (`db-integrity.spec.js`، `offers.spec.js`، `requests.spec.js`)
التي بقيت خضراء تماماً كما هي.

## نطاق متروك عمداً
حقل `technician_id` عند الإنشاء نفسه لم يُمس — يبقى موجوداً بالـAPI (يُقيِّد
الآن فقط من يحق له تقديم عرض على الطلب، `offers.routes.js:16`، دون أي تجاوز
لموافقة الفني بعد هذا الإصلاح) رغم عدم استخدام تطبيق العميل له إطلاقاً.
قرار الإبقاء عليه كميزة مستقبلية غير مُفعَّلة، أو إزالته كسطح هجوم بلا فائدة
حالية، مطروح على صاحب المنتج ولم يُبتّ فيه بهذه الجلسة.

# [DEFERRED-AUDIT-03] قائمة مؤجَّلة — نتائج جولة التدقيق الثالثة غير المُصلَحة عمداً

هذه الجولة الثالثة (بعد إغلاق ثغرة انتقال حالة الطلب الحرجة، `SEC-FIX-STATUSFLOW-01`
أعلاه) طلبت تقييماً مستقلاً كاملاً من الصفر. من بين ما وُجد، بندان فقط
استحقّا إصلاحاً فورياً بقرار صاحب المنتج — تقرير الأعطال وتصفّح دفتر الحساب/
سجل العمليات (كلاهما بمستودع `sallehly_app`، راجع `DECISIONS.md` هناك:
`[FIX-CRASHREPORT-01]`، `[FIX-ADMINPAGINATION-01]`). **كل ما تبقى هنا مؤجَّل
عمداً بقرار صريح — لم يُصلَح، ولن يُصلَح بهذه الجلسة.** كل بند مثبَّت بموقعه
بالكود الفعلي، لا افتراضاً. الأولوية عند العودة لهذه القائمة: بندا "متوسط"
أولاً (تسريب مالي/صلاحيات فعلي وإن كان نطاقه ضيقاً)، ثم البقية حسب الأثر.

## متوسط — رصيد شحن يعلق على حساب محذوف بلا رجعة
`routes/topups.routes.js:59` (`POST /admin/topups/:id/review`) يُصادق على
طلب شحن معلَّق ويُضيف المبلغ لرصيد `t.technician_id` مباشرة، بلا أي فحص أن
هذا الحساب لا يزال حقيقياً (`is_active`/`deleted_at`). بالمقابل، حذف حساب
(`routes/admin.routes.js:312` و`routes/auth.routes.js:366`) يفحص فقط طلباً
نشطاً ورصيداً حالياً أكبر من صفر — **لا يفحص وجود طلب شحن `pending` بانتظار
مراجعة الأدمن**. لو فني قدَّم طلب شحن ثم حُذف حسابه (بنفسه أو من أدمن) قبل أن
يراجعه أحد، وبعدها وافق الأدمن على الطلب المعلَّق أصلاً (لا يعرف أن الحساب
صار "مستخدم محذوف")، الرصيد يُضاف لحساب `is_active=0` بلا `fcm_token` (صُفِّر
بـ`anonymizeUser`) — دخول مالي حقيقي (الفني دفع فعلياً وصعد إيصاله) يعلق بلا
أي طريقة لاسترجاعه غير تدخّل يدوي مباشر بقاعدة البيانات. **الحل المقترح لاحقاً:**
فحص `pending` topups بنفس شرط حظر الحذف الحالي، أو رفض المراجعة إن كان
الحساب المستهدف محذوفاً.

## متوسط — اتصال Socket.IO الحي لا يُقطَع عند حذف حساب من الأدمن
`routes/admin.routes.js:312` (`DELETE /admin/users/:id`) يستدعي `anonymizeUser`
لكن لا يستدعي `io.in(user-${id}).disconnectSockets(true)` إطلاقاً — بعكس
كلٍّ من `POST /admin/users/:id/toggle` (سطر ~113، `SEC-FIX-10`) و
`POST /admin/users/:id/role` (سطر ~299، `SEC-FIX-09`) اللذين يقطعان الاتصال
الحي صراحة بنفس النمط. أي مستخدم متصل فعلياً وقت حذف حسابه يبقى بجلسة
Socket.IO حية (يستقبل/يُرسل أحداث حتى إعادة اتصال طبيعية أو انقطاع شبكة)
رغم أن `is_active=0` و`token_version` زاد (REST محظور فوراً، لكن القناة
الحية لا). **الحل المقترح لاحقاً:** سطر واحد إضافي بنفس نمط الحذف، مطابق
لما هو موجود أصلاً بمسارين آخرين بنفس الملف.

## متوسط — أدمن عادي يقدر يوقف/يحذف حساب أدمن آخر
`POST /admin/users/:id/toggle` (سطر 98) يفحص فقط `id === req.user.id`
(منع إيقاف الحساب الذاتي) — لا يفحص دور الحساب المستهدف إطلاقاً. بالمقابل
`POST /admin/users/:id/role` (سطر 259) يحمل فحصاً صريحاً
(`if (u.role === 'admin') return 400`) يمنع تحويل دور حساب أدمن آخر. نفس
الفجوة بالضبط بـ`DELETE /admin/users/:id` (سطر 312) — لا فحص دور الهدف
إطلاقاً، فقط منع حذف الحساب الذاتي. عملياً: أي أدمن عادي (`requireRole('admin')`
فقط، لا `requireSuperAdmin`) يقدر يوقف أو حتى يحذف/يُصفِّي حساب أدمن آخر —
بما فيه (لم يُتحقَّق صراحة، يحتاج فحصاً إضافياً) حساب super admin نفسه.
مخاطرة داخلية (يتطلّب حساب أدمن مُخترَق أو أدمن خبيث فعلياً)، لا مخاطرة
خارجية. **الحل المقترح لاحقاً:** نفس فحص `role === 'admin'` المستخدَم أصلاً
بمسار تغيير الدور، يُضاف لكل من `toggle` و`delete`.

## منخفض — سحب عرض (`DELETE /offers/:id`) بلا `db.transaction()`
`routes/offers.routes.js` (نهاية الملف، `DELETE /offers/:id`): `DELETE FROM
offers` ثم `UPDATE requests SET status=...` استعلامان منفصلان بلا تغليف
بمعاملة واحدة (بعكس `[DATA-INTEGRITY-01]` أعلاه الذي غطّى إرسال/قبول
العروض تحديداً لا السحب). عطل بينهما (نادر جداً، لا يوجد I/O خارجي بينهما)
يترك العرض محذوفاً لكن حالة الطلب القديمة — عادة تُصحَّح تلقائياً بأول قراءة
لاحقة تعتمد فعلياً على وجود/عدم وجود صفوف `offers`، لا على حقل الحالة
المخزَّن نفسه، فالأثر العملي محدود جداً.

## منخفض/متوسط — `audios/` بلا مصادقة (موثَّق مسبقاً كفجوة معروفة)
`routes/protected-uploads.routes.js` يحمي `avatars/`، `payments/`،
`requests/` (`SEC-FIX-UPLOADS-01`) لكن تعليق الملف نفسه (سطر 3) يقر صراحة:
"audios/ لسا غير مُغطّاة إطلاقاً". الرسائل الصوتية بالشات لا تزال تُخدَّم عبر
`express.static` العام — الحماية الوحيدة اسم ملف عشوائي
(`crypto.randomBytes`، `utils/helpers.js:safeUploadName`) يصعب تخمينه، لا
تحقق صلاحية حقيقي. أي رابط مسرَّب (سجل متصفح، أداة مراقبة شبكة) يكشف رسالة
صوتية خاصة بشكل دائم. **الحل المقترح لاحقاً:** توسعة نفس نمط
`protected-uploads.routes.js` الموجود أصلاً لتغطية `audios/`.

## منخفض — `GET /admin/stats` بلا فهارس على أعمدة الفلترة
`routes/admin.routes.js:18` يُنفِّذ نحو 10 استعلامات منفصلة بكل طلب، منها
`WHERE status='ملغي'`، `GROUP BY service`، وثلاث نسخ (يومي/أسبوعي/شهري) من
`WHERE created_at >= datetime(...)` على كلٍّ من `requests` و`users`،
و`WHERE is_active=0`، و`WHERE verification_status='pending'`. الفهارس
الموجودة فعلياً (`config/migrate.js`) تغطي `requests.technician_id`،
`requests.customer_id`، `users.role`، `ledger.user_id`، `topups.technician_id`/
`package_id` — لا فهرس واحد على `requests.status`، `requests.created_at`،
`requests.service`، `users.created_at`، `users.is_active`، أو
`users.verification_status`. أداء فقط، لا صحة بيانات — كل استعلام يعمل
بشكل صحيح اليوم (مسح جدول كامل لكل استعلام)، يتدهور تدريجياً مع نمو الجدولين
لا فجأة. **الحل المقترح لاحقاً:** فهارس مركّبة/مفردة على الأعمدة أعلاه حسب
نمط الاستخدام الفعلي وقت المعالجة.

## منخفض — `DELETE /me` بلا تغليف معاملة بين الفحص والتنفيذ
فحوصات `activeRequest`/`balance > 0` (`routes/auth.routes.js:366`) واستدعاء
`anonymizeUser(id)` لاحقاً كلها استعلامات منفصلة بلا `db.transaction()`
واحدة تلفّها جميعاً — نافذة نظرية (Time-Of-Check-To-Time-Of-Use) لو وصل طلب
آخر متزامن يُنشئ طلباً نشطاً جديداً أو يُضيف رصيداً بين لحظة الفحص ولحظة
`anonymizeUser`. يتطلّب توقيتاً دقيقاً جداً لطلبين متزامنين على نفس الحساب،
وقيمة الهدف منخفضة لمهاجم فعلي (لا يفيد إلا صاحب الحساب نفسه بمهاجمة نفسه).

## منخفض — `Map` غير محدود العمر بـ`services/error-alert.js`
`lastSentAt` (سطر 20) خريطة بالذاكرة تكبح تكرار تنبيهات البريد لنفس الخطأ
(`MON-FIX-01`) — تُضاف لها إدخالات (`shouldSend()`، سطر 22) لكن **لا يوجد
أي حذف/تنظيف إطلاقاً** لأي مفتاح، بأي شرط. مفتاح كل إدخال هو
`${context}:${message}` (حتى 300 حرف) — أي خطأ تختلف رسالته قليلاً بين
حدوث وآخر (مثلاً رسالة تتضمن ID أو قيمة مُدخلة من المستخدم) يُنشئ مفتاحاً
جديداً دائم البقاء. تسرّب ذاكرة بطيء جداً يتراكم طوال عمر العملية (لا يُعاد
تشغيلها إلا بإعادة نشر)، ليس خطراً عملياً بالحجم الحالي لكنه غير محدود
بالتصميم.

## منخفض — لا حد استخدام (rate limit) على شكاوى/رسائل الدعم/بلاغات الرسائل
لا `POST /support`، ولا `POST /support/:id/messages`، ولا `POST /complaints`
(`routes/support.routes.js`)، ولا `POST /requests/:id/report-message`
(`routes/chat.routes.js:270`) يحمل أي تحديد لمعدل الاستخدام — بعكس
`SEC-FIX-REQSPAM-01` أعلاه الذي غطّى `POST /requests` تحديداً. `/support`
نفسها محمية جزئياً ببندٍ منطقي منفصل (تذكرة مفتوحة واحدة فقط لكل مستخدم،
`FIX-06`) لا بحد معدل حقيقي. مستخدم مسجَّل دخوله (كل هذه المسارات تتطلب
`auth`) يقدر نظرياً يُغرق لوحة الأدمن برسائل/بلاغات متكررة — لا يوجد أيضاً
أي فحص تكرار (dedup) بـ`report-message` تحديداً (سطر 289): نفس المستخدم
يقدر يُبلّغ نفس الرسالة عشرات المرات، كل بلاغ يُنشئ صفاً جديداً ويُطلق حدث
`new-message-report` منفصلاً لغرفة الأدمن. إزعاج تشغيلي، لا ثغرة أمنية —
يتطلّب حساباً مسجَّلاً فعلياً، لا وصولاً مجهولاً.

## منخفض — رفع إيصال الشحن (`receipt`) يعتمد الامتداد + `mimetype` المُصرَّح فقط، لا فحص محتوى فعلي
`middleware/upload.js` (سطر 36-53): فلترة الملفات تتحقق من `file.mimetype`
(قيمة يُقرّرها المتصفح/العميل، قابلة للتلاعب بسهولة بطلب مُعدَّل يدوياً) +
امتداد آمن (`hasSafeExt`) — لا "magic bytes"/فحص محتوى ثنائي فعلي يثبت أن
الملف صورة فعلاً. بما أن `SEC-FIX-UPLOADS-01` أعلاه أغلق أصلاً الوصول
المباشر غير المُصادَق عليه لملفات `payments/`، الأثر العملي لملف مصمَّم
بعناية (امتداد+mimetype صحيحان، محتوى غير ذلك) محدود — لا تنفيذ كود عن بعد
(الملفات تُخدَّم كبيانات ساكنة لا تُنفَّذ)، أقصى أثر واقعي محتوى مضلِّل يراه
أدمن يراجع طلب شحن يدوياً. **الحل المقترح لاحقاً:** مكتبة فحص "magic bytes"
(مثل `file-type`) كطبقة تحقق إضافية قبل القبول النهائي.

# [SEC-FIX-CANCELREVIVE-01] طلب مُلغى يقدر "يحيا" بصمت بقبول عرض بقي معلَّقاً عليه

## الاكتشاف
جولة تدقيق رابعة (طُلبت صراحة، بحث عن فئة أخطاء لم تغطِّها الجولات الثلاث
السابقة) طلبت فحص صحة قواعد العمل تحت تسلسلات أحداث شرعية لكن غير اعتيادية.
`POST /requests/:id/status` بالانتقال لحالة `ملغي` (`routes/requests.routes.js`)
لم يكن يلمس جدول `offers` إطلاقاً — بعكس المسارين الآخرين لإلغاء طلب
(`DELETE /requests/:id` و`POST /admin/requests/:id/cancel`)، وكلاهما يرفضان
أي عرض `pending` على الطلب فور الإلغاء. عميل يقدر يُلغي طلبه عبر هذا المسار
تحديداً بينما لا يزال عرض فني قائماً `pending` عليه (مسموح طالما لم يُقبَل أي
عرض بعد — الحالة `بانتظار العروض`/`وصلت عروض`).

بالتوازي، `POST /offers/:id/decision` (`routes/offers.routes.js`) كان يجلب
`r.status AS request_status` بالاستعلام نفسه — **لكن لا يقرأه إطلاقاً بأي
مكان بالمعالج** — فيتحقق فقط من أن العرض نفسه لا يزال `pending`. النتيجة:
تسلسل شرعي بالكامل من نفس العميل — إنشاء طلب ← عرض فني يصل ← العميل يُلغي
الطلب عبر `/status` (العرض يبقى `pending`، الثغرة) ← العميل يقبل نفس العرض
عبر `/offers/:id/decision` — كان يُعيد الطلب لحالة "تم اختيار عرض" ويكمل
مساره الطبيعي حتى الاكتمال واحتساب العمولة، **وكأنه لم يُلغَ إطلاقاً**، بلا
أي أثر يوضح أنه مرّ بحالة إلغاء أصلاً (هذا المسار أيضاً لا يسجّل
`cancelled_by`/`cancelled_at`، بعكس المسارين الآخرين).

هذا يقوّض بالضبط نفس مبدأ "الحالات المغلقة نهائية" الذي أُنشئ عمداً بـ
`[SEC-FIX-STATUSFLOW-01]` (الجولة الثالثة) — لكن عبر باب مختلف تماماً (قبول
عرض قديم، لا القفز المباشر بمسار `/status` الذي أُغلق هناك). الفحص المُضاف
هناك (`LEGAL_PREDECESSORS`) لا يغطي هذه الحالة لأن `ملغي` مُستثناة منه عمداً
وبحق — المشكلة هنا ليست بانتقال *إلى* `ملغي`، بل بالخروج *منه* لاحقاً عبر
مسار مختلف كلياً لا يعرف أن الطلب أُلغي.

## الحل — طبقتان دفاع مستقلتان، لا طبقة واحدة
1. **منع الحالة الشاذة من الحدوث أصلاً**: فرع `ملغي` بـ`POST /requests/:id/status`
   صار الآن يرفض فوراً أي عرض `pending` على الطلب (نفس عبارة SQL الحرفية
   المستخدَمة بالمسارين الآخرين)، ويسجّل `cancelled_by`/`cancelled_at` (بلا
   `cancel_reason` — هذا المسار لا يطلب سبباً، مطابقاً لـ`DELETE /requests/:id`
   لا للمسار الإداري المخصَّص الذي يطلب سبباً إلزامياً).
2. **طبقة دفاع مستقلة لو فات عرضٌ ما الطبقة الأولى** (مساراً مستقبلياً ينسى
   نفس الفحص، أو أي حالة لم تُتوقَّع): `POST /offers/:id/decision` صار يتحقق
   الآن صراحة أن `request_status` (كان يُجلَب فعلاً ولا يُقرأ) لا يزال ضمن
   `{بانتظار العروض، وصلت عروض}` قبل أي قرار (قبول أو رفض)، ويرفض بـ409
   `REQUEST_NO_LONGER_OPEN` غير ذلك.

كلا الطبقتين مُختبَرتان بشكل مستقل بالفعل (راجع الإثبات أدناه) — لا اعتماد
كل طبقة على أن الأخرى صحيحة.

## سؤال متابعة صريح — هل نفس النمط ("يُجلَب ولا يُقرأ") موجود بمكان آخر؟
بحث شامل بكل `routes/*.js` عن أي استعلام يجلب عمود حالة/دور عبر `JOIN` بلقب
صريح (نفس شكل `r.status request_status`) كشف مرشحاً واحداً إضافياً فقط:
`DELETE /offers/:id` (سحب عرض) يجلب نفس `r.status request_status` بالضبط —
لكنه **يقرأه بشكل صحيح فعلاً** (يرفض السحب لو لم تعد الحالة ضمن الحالتين
المفتوحتين). هذا تحديداً ما جعل الثغرة سهلة الإغفال: النمط الصحيح موجود
بدالة مجاورة مباشرة بنفس الملف، لا غائب عن الكود كليّاً. لا مثيل آخر لنفس
النمط (حقل يُجلَب لغرض تحقق ثم لا يُستخدَم) وُجد بأي ملف آخر.

## الإثبات
`tests/status-transition-security.spec.js` — اختباران جديدان أُضيفا لنفس
الوصف (`SEC-FIX-STATUSFLOW-01`) الذي يوثّق مبدأ "الحالات المغلقة نهائية"،
لأن هذا استمرار مباشر لنفس المبدأ عبر باب مختلف:
- الأول يثبت الطبقة الأولى: إلغاء طلب عليه عرض `pending` يرفضه فوراً
  (يتحقّق مباشرة من قاعدة البيانات بعد الإلغاء)، ثم يثبت أن محاولة قبول ذلك
  العرض بعدها تُرفض أصلاً بفحص "العرض لسا pending" (`OFFER_DECISION_ALREADY_MADE`)
  لأنه لم يعد كذلك.
- الثاني يعزل الطبقة الثانية تحديداً: يُلغي الطلب **مباشرة بقاعدة البيانات**
  (متجاوزاً مسار `/status` عمداً، محاكياً أي مسار مستقبلي قد لا يطبّق نفس
  إصلاح الطبقة الأولى) تاركاً العرض `pending` قصداً، ثم يثبت أن
  `/offers/:id/decision` وحده يرفض القبول بـ409 `REQUEST_NO_LONGER_OPEN`.
- **تحقَّق يدوياً أن الاختبار الثاني يفشل فعلاً بدون إصلاح الطبقة الثانية**
  (`git stash` مؤقت لتعديل `routes/offers.routes.js` فقط، إعادة تشغيل — فشل
  بـ`Expected: 409, Received: 200` كما هو متوقَّع تماماً — ثم استعادة
  الإصلاح) — إثبات أن الاختبار يكتشف الثغرة فعلاً لا يمر صدفة.

المجموعة الكاملة: **288/288** (كانت 286 بعد الجولة الثالثة، +2 هنا) — صفر
تراجع بأي اختبار آخر، بما فيها كل اختبارات الإلغاء/العروض/الحالة القائمة.

## نطاق متروك عمداً
الطبقة الثانية بـ`/offers/:id/decision` تمنع القبول والرفض معاً على طلب غير
مفتوح (لا القبول فقط) — قرار متعمَّد: رفض عرض على طلب مُلغى أصلاً لا قيمة
فعلية له، وتوحيد الفحص لحالة واحدة أبسط وأكثر أماناً من فحص مختلف لكل قرار.

# [SEC-FIX-PKGFINITE-01] Infinity/NaN بحقول الباقة المالية — رصيد فني قابل للتلف الدائم بخطأ إدخال أدمن واحد

## الاكتشاف
نفس جولة التدقيق الرابعة، محور صحة القيم الرقمية. `POST /admin/packages` و
`PUT /admin/packages/:id` (`routes/admin.routes.js`) يتحقّقان من
`amount`/`bonus`/`commission_per_order` بأنماط مثل `!amount || amount<=0`
و`bonusVal<0`/`commission<0` بلا أي استخدام لـ`Number.isFinite` — بعكس
المسار الشقيق `POST /admin/users/:id/balance` (سطر ~205) الذي يستخدمه
بشكل صحيح أصلاً. أُثبت بالتنفيذ المباشر (لا افتراضاً): `!Infinity` يُقيَّم
`false` و`Infinity<=0`/`Infinity<0` كلاهما أيضاً `false`، فلا فحص من
الثلاثة يرفض `Infinity` — تُخزَّن حرفياً بعمود `REAL` (`better-sqlite3` لا
يرفضها ولا يحوّلها). وبالمثل، أي قيمة نصية غير رقمية (`"abc"`) تتحوّل
بـ`Number()` إلى `NaN`، وكل فحوص المقارنة (`< 0`) تُقيَّم `false` لـ`NaN`
أيضاً — فتمر الفحوص، ويُخزَّن `NaN` كـ`NULL` صامت بقاعدة البيانات بلا أي
خطأ يظهر للأدمن.

**السلسلة الفعلية القابلة للحدوث بخطوتين إداريتين فقط:** أدمن يُنشئ أو يعدّل
باقة بقيمة `amount: "Infinity"` (خطأ إدخال، أو نتيجة تلاعب بطلب مُعدَّل
يدوياً) ← فني يشحن رصيداً على تلك الباقة ← أدمن (قد يكون شخصاً آخر، بلا أي
مؤشر يوضح له أن الباقة غير سليمة) يوافق على طلب الشحن
(`POST /admin/topups/:id/review`) ← `tech.balance + Infinity` يُحسَب،
`.toFixed(2)` على `Infinity` يُعيد النص `"Infinity"`، و`Number("Infinity")`
يُعيد `Infinity` مجدداً — فيُكتَب `Infinity` نهائياً بعمود `users.balance`
لذلك الفني، بلا أي طريقة لعكسه غير تدخّل يدوي مباشر بقاعدة البيانات.

## الحل
`Number.isFinite(...)` أُضيف كجزء أول بكل فحص من الثلاثة (`amount`،
`bonusVal`، `commission`) بكلا المسارين (إنشاء وتعديل) — نفس النمط الحرفي
المستخدَم أصلاً بـ`/admin/users/:id/balance`، لا نمط جديد. يرفض الآن
`Infinity`/`-Infinity`/`NaN` معاً بفحص واحد، بالإضافة للقيم السالبة/الصفرية
التي كانت مرفوضة أصلاً.

## الإثبات
`tests/admin.spec.js` — اختبار جديد يثبت الحالتين معاً (`Infinity` بـ`amount`
و`commission_per_order`، نص غير رقمي بـ`bonus`) مرفوضة بـ400 على كل من
`POST`/`PUT`، ثم يتحقّق مباشرة من قاعدة البيانات أن باقة سليمة موجودة أصلاً
لم تتلوّث بأي قيمة `Infinity`/`NULL` بعد محاولة تعديل فاشلة عليها.
**تحقَّق يدوياً أن الاختبار يفشل فعلاً بدون الإصلاح** (نفس أسلوب
`git stash` المؤقت المستخدَم بـ`SEC-FIX-CANCELREVIVE-01` أعلاه — فشل
بـ`Expected: 400, Received: 200` كما هو متوقَّع، ثم استعادة الإصلاح).
المجموعة الكاملة: **289/289** (كانت 288 بعد الإصلاح السابق، +1 هنا) — صفر
تراجع.

## نطاق متروك عمداً
لم يُضَف سقف أعلى صريح لـ`amount`/`bonus`/`commission_per_order` (بعكس سعر
العرض بـ`routes/offers.routes.js` المحدود بـ99,999) — `Number.isFinite`
يمنع `Infinity` تحديداً (الخطر الفعلي المُثبَت هنا)، لكن رقماً كبيراً جداً
لكنه منتهٍ (مثال: مليون) يبقى مقبولاً تقنياً. إضافة سقف أعلى منطقي (كأقصى
سعر باقة معقول) قرار منتج منفصل، لم يُطلَب هنا ولم يُطبَّق.

# [SEC-FIX-SUSPENDACTIVE-01] إيقاف حساب له طلب نشط يترك الطرف الآخر عالقاً للأبد

## الاكتشاف
نفس جولة التدقيق الرابعة، محور صحة قواعد العمل تحت تسلسلات أحداث غير
اعتيادية. `POST /admin/users/:id/toggle` (إيقاف/تفعيل حساب) كان يفحص فقط
منع إيقاف الحساب الذاتي — بلا أي فحص لوجود طلب نشط، بعكس
`DELETE /admin/users/:id` (سطر ~318) الذي يحمل بالضبط هذا الفحص أصلاً منذ
فترة. `middleware/auth.js` يرفض REST فوراً لأي حساب `is_active=0` بكل طلب
لاحق — فلو أُوقِف فني له طلب بحالة "تم اختيار عرض"/"قيد التنفيذ"/"بانتظار
تأكيد الدفع"، يُقفَل فوراً عن كل مسارات إكمال أو حتى رؤية ذلك الطلب، بلا أي
طريقة للفني نفسه لحله. العميل يبقى يرى طلبه "قيد التنفيذ" بفني لن يتحرك فيه
أبداً، حتى يتدخّل أدمن آخر يدوياً (إلغاء الطلب أو إعادة تفعيل الفني). نفس
المنطق يصيب عميلاً له طلب نشط لو أُوقِف حسابه هو (الفني يبقى معلَّقاً
بانتظار عميل لا يقدر يتواصل أو يؤكد شيئاً).

## الحل
نفس استعلام الفحص الحرفي من `DELETE /admin/users/:id` — `(customer_id=?
OR technician_id=?) AND status IN (...الحالات النشطة الخمس...)` — أُضيف
لفرع الإيقاف فقط (`newStatus === 0`) بـ`POST /admin/users/:id/toggle`، قبل
أي كتابة لقاعدة البيانات. إعادة التفعيل (`newStatus === 1`) لا تحتاج هذا
الفحص إطلاقاً — لا خطر بإعادة تفعيل حساب له طلب نشط، العكس تماماً هو
المطلوب أصلاً.

## الإثبات
`tests/admin.spec.js` — اختبار جديد: فني وعميل جديدان، طلب يصل لحالة "تم
اختيار عرض" فعلياً (عرض حقيقي مقبول)، ثم محاولة إيقاف كلٍّ من الفني
والعميل منفصلين — كلاهما يُرفض بـ409، ويبقى `is_active=1` بقاعدة البيانات
للطرفين. **تحقَّق يدوياً أن الاختبار يفشل فعلاً بدون الإصلاح** (نفس أسلوب
`git stash` المؤقت المستخدَم بالإصلاحين السابقين — فشل بـ`Expected: 409,
Received: 200`، ثم استعادة الإصلاح). المجموعة الكاملة: **290/290** (كانت
289، +1 هنا) — صفر تراجع.

## نطاق متروك عمداً
لم يُلمَس أي فحص مماثل لأي مسار آخر قد يُخرج حساباً من الخدمة (لا يوجد
مسار آخر حالياً يفعل ذلك غير `toggle`/`DELETE`، تحقَّق بالبحث الشامل). لم
تُضَف رسالة توضيحية مختلفة للعميل مقابل الفني — نفس نص الخطأ يُستخدَم لكلا
الحالتين (`لا يمكن إيقاف هذا الحساب — عنده طلب نشط...`)، مطابقاً لأسلوب
`DELETE /admin/users/:id` الذي يستخدم نفس النمط أيضاً بلا تمييز الدور.

# [FIX-DEADFIELD-01] حقل مجلوب بلا استخدام بـDELETE /offers/:id — تنظيف، لا ثغرة

## الاكتشاف
سؤال متابعة صريح على `[SEC-FIX-CANCELREVIVE-01]` أعلاه: هل نفس نمط "حقل
يُجلَب لغرض فحص ثم لا يُقرأ إطلاقاً" موجود بمكان آخر؟ مسح شامل ومتعمَّد (لا
تكرار للمسح السطحي السابق) لكل `db.prepare('SELECT ...')` بمجلد `routes/`
يحمل عموداً بلقب صريح، مقارَناً باستخدامه الفعلي بجسم المعالج نفسه (سواء
بفحص JS مباشر أو ضمن جسم الرد المُرسَل للعميل): 18 مرشحاً أوَّلياً، 17 منها
حقول عرض حقيقية تُرسَل كاملة للعميل ضمن `res.json` (مثل `user_name`،
`technician_name`) — استخدام فعلي، لا خطأ. واحد فقط حقيقي:
`DELETE /offers/:id` (`routes/offers.routes.js`، نفس المعالج المذكور
كمثال صحيح بـ`SEC-FIX-CANCELREVIVE-01` لاستخدامه `request_status` بشكل
سليم) كان يجلب أيضاً `r.technician_id` بلقب `request_tech` — **بلا أي
استخدام له إطلاقاً**، لا بفحص ولا برد (هذا المعالج يرجع فقط `{ok, message}`،
لا صف الطلب نفسه).

## لماذا هذا تنظيف لا ثغرة
الفحص الذي يبدو أن `request_tech` كان مقصوداً له ("لا تسمح بسحب العرض بعد
تعيين فني على الطلب") مغطّى بالكامل أصلاً بفحص `request_status` الموجود
بنفس المعالج (سطر يليه مباشرة): أي تعيين فني ينقل الطلب فوراً خارج
الحالتين المفتوحتين (`بانتظار العروض`/`وصلت عروض`)، وفحص `request_status`
يرفض ذلك بالفعل. لا سيناريو يمر من فحص `request_status` بينما `request_tech`
كان سيرفضه — الحقل فائض تماماً، لا فجوة تغطية حقيقية.

## الحل
حذف `r.technician_id request_tech` من الاستعلام مباشرة، مع تعليق قصير
يوضّح أن الفحص الذي يبدو أنه كان يخدمه مغطّى فعلاً بـ`request_status` —
حقل يُجلَب ولا يُستخدَم يدعو لاحقاً لافتراض خاطئ أنه يقوم بشيء.

## الإثبات
`grep -rn "request_tech"` بكامل المشروع (بما فيه الاختبارات) بعد الحذف:
لا نتيجة سوى تعليق الشرح نفسه — لم يكن أي اختبار أو كود آخر يعتمد عليه.
المجموعة الكاملة: **290/290** — صفر تراجع.

## نطاق متروك عمداً
هذا آخر إصلاح بجولة التدقيق الرابعة — لا جولة خامسة بقرار صريح من صاحب
المنتج.

# [SEC-FIX-SOCKETCRASH-01] أي مستخدم مسجَّل دخول يقدر يُسقط السيرفر بأكمله برسالة سوكت واحدة مشوَّهة

## الاكتشاف
جولة تدقيق خامسة (طُلبت صراحة بعد الرابعة — الفرضية أن الخطورة تنخفض مع كل
جولة **غير دقيقة**؛ الاكتشاف يعتمد على أين تبحث، لا كم مرة بحثت). محور جديد
كلياً: مسارات الفشل تحت ضغط غير اعتيادي (كتابة قاعدة بيانات تفشل، خدمة
خارجية معطَّلة، استدعاء ثانٍ لنفس الإجراء).

`services/socket.js`، معالج `join-request`، كان يمرر `requestId` (قادم
مباشرة من حمولة حدث Socket.IO التي يتحكم بها العميل بالكامل) خاماً إلى
`db.prepare('SELECT * FROM requests WHERE id=?').get(requestId)` بلا أي
تحقق من نوعه. **أُثبت بالتنفيذ المباشر**: `better-sqlite3` يرمي `RangeError`
متزامناً لو الوسيط المُمرَّر كائناً أو مصفوفة بدل رقم/نص بسيط. **أُثبت أيضاً
بقراءة كود مكتبة socket.io 4.8.3 نفسها** (`node_modules/socket.io/dist/socket.js`):
تستدعي مستمعي الأحداث مباشرة (`emitUntyped.apply`) داخل `process.nextTick`
بلا أي `try/catch` يحيط بها بالمكتبة نفسها. استثناء غير مُلتقَط هنا يصل
مباشرة لـ`process.on('uncaughtException')` بـ`server.js`، الذي **لا يكتفي
بالتسجيل** — يُنفِّذ `gracefulShutdown()` كاملاً: يوقف استقبال اتصالات
جديدة، وينتظر الطلبات الجارية، ثم `process.exit(0)` (تعيد منصة النشر
تشغيل العملية بعدها).

**الأثر الحقيقي:** أي مستخدم مسجَّل دخول (عميل أو فني عادي، بلا أي صلاحية
خاصة — هذا المعالج يعمل بعد فحص المصادقة العادي فقط) يقدر يفتح اتصال
WebSocket ويُرسل يدوياً `socket.emit('join-request', {أي كائن})` (تافه
التنفيذ من أدوات المطوّر بالمتصفح أو عميل Socket.IO خام — لا يحتاج تطبيق
العميل نفسه أن يفعل هذا، فقط ألا يمنعه شيء من إرساله) — فيوقف **السيرفر
بأكمله لكل المستخدمين المتصلين في تلك اللحظة**، لا فقط طلبه هو. هذا أخطر
من أي اكتشاف بالجولات الأربع السابقة من ناحية النطاق: تسريب بيانات أو
ثغرة احتيال مالي تصيب سجلات محدَّدة، بينما هذا يوقف الخدمة بالكامل لكل
مستخدم، ويتكرر بسهولة (سكربت واحد يعيد إرسالها كل ثانية).

## بحث شامل — هل نفس النمط بأي معالج سوكت آخر؟
`services/socket.js` بأكمله (86 سطراً) قُرئ كاملاً: معالج `leave-request`
الوحيد الآخر يستخدم `String(requestId)` — لا يرمي أبداً لأي مُدخَل (حتى
كائن/مصفوفة، `String({})` = "[object Object]"، دائماً نص صالح). `io.use(...)`
(فحص الاتصال) مُغلَّف بـ`try/catch` صراحة أصلاً. **`join-request` وحده** كان
يحمل هذا النمط بكل الملف.

## الحل — طبقتان معاً، لا طبقة واحدة
1. تحويل صريح لنوع آمن (`parseInt` + فحص `isNaN`) قبل أي استخدام لـ`requestId`
   — نفس نمط `parseInt`/`isNaN` المُستخدَم بكل مسارات REST المشابهة.
2. `try/catch` يُحيط بجسم المعالج بأكمله، دفاعاً إضافياً بعمق — حتى لو ظهر
   مستقبلاً أي طريق آخر لاستثناء غير متوقَّع بنفس المعالج، لا يصل مطلقاً
   لـ`uncaughtException`.

**نفس فئة العلّة بالضبط وُجدت بموقعين REST إضافيين** (رُصدا بنفس جولة
التدقيق، طلب صاحب المنتج إصلاحهما بنفس الدفعة): `POST /topups`
(`routes/topups.routes.js:12`) و`POST /complaints`
(`routes/support.routes.js:109`) — كلاهما يُمرِّر `package_id`/`request_id`
خاماً من `req.body` لاستعلام `.get()`. بما أن `multer` (المُطبَّق على
`/topups`) يتجاوز معالجة `req.body` بصمت لو الطلب `Content-Type:
application/json` وليس `multipart` فعلياً، وبما أن `/complaints` مسار JSON
عادي بلا `multer` إطلاقاً — كلا المسارين قابلان للوصول بمصفوفة/كائن حقيقي
كقيمة. **لا يُسقطان العملية** (معالجات REST متزامنة تُلتقَط تلقائياً بواسطة
Express ثم `apiErrorHandler`، بعكس مسار السوكت) لكنهما ينتجان خطأ 400 غير
نظيف بدل رسالة التحقق المقصودة (`PACKAGE_NOT_FOUND`/تجاهل هادئ). أُصلحا
بنفس المبدأ: **فحص `typeof` صريح قبل `parseInt`، لا الاعتماد على `parseInt`
وحدها** — اكتُشف أثناء كتابة الاختبار أن `parseInt([1,2], 10)` **لا يُرجع
`NaN`** كما قد يُفترَض: يحوّل المصفوفة لنص أولاً (`String([1,2])` = "1,2")
ثم يقرأ الرقم البادئ منها (`1`)، فيقبل بصمت معرّفاً قد لا علاقة له بالمقصود
إطلاقاً بدل رفضه بوضوح.

## الإثبات
- `tests/socket-crash-resilience.spec.js` (ملف جديد كلياً): يُشغّل **نسخة
  سيرفر معزولة تماماً** بمنفذ/`DATA_DIR` مستقلَّين تماماً عن سيرفر الاختبارات
  المشترك (المنفذ 4001 الذي تعتمد عليه كل ملفات الاختبار الأخرى) — عمداً،
  لأن الثغرة المُختبَرة **تُسقط العملية بأكملها**؛ تشغيلها ضد السيرفر
  المشترك كان سيُفشل كل اختبار آخر بالمجموعة بصمت بدل إثبات هذه الثغرة
  تحديداً. يُسجّل عميلاً حقيقياً، يتصل بسوكت حقيقي، يُرسل حمولة `join-request`
  مشوَّهة، ثم يتحقق أن السيرفر لا يزال حياً (طلب HTTP عادي لاحق ينجح، لا
  "connection refused"). **تحقَّق يدوياً بتشغيل الاختبار قبل أي إصلاح**:
  فشل فعلاً بـ`ECONNREFUSED` (العملية ماتت فعلاً)، مُثبتاً الثغرة قبل إصلاحها
  لا مفترَضاً وجودها.
- `tests/topups.spec.js`/`tests/complaints-support.spec.js`: اختباران
  جديدان، كل منهما يُرسل مصفوفة كـ`package_id`/`request_id` بجسم JSON
  صريح، ويتحقق من استجابة نظيفة (404/200 حسب المسار) بدل خطأ غامض.
  **تحقَّق يدوياً أن كليهما يفشلان فعلاً بدون الإصلاح** (`git stash` مؤقت
  للملفين، فشل بـ`Expected: 404/200، Received: 400` كما هو متوقَّع، ثم
  استعادة الإصلاح). أثناء هذا التحقق تحديداً اكتُشفت مشكلة `parseInt`
  على مصفوفة الموصوفة أعلاه — الاختبار الأول (قبل تشديد الفحص بـ`typeof`)
  فشل بسبب خطأ بمنطق الإصلاح نفسه لا بسبب غياب الإصلاح، فصُحِّح الإصلاح
  أولاً قبل اعتماد الاختبار.

المجموعة الكاملة: **293/293** (كانت 290، +3 هنا) — صفر تراجع.

## نطاق متروك عمداً
لم يُدقَّق كل موقع آخر بالمشروع يُمرِّر قيمة من `req.body`/حمولة سوكت
مباشرة لاستعلام `db.prepare(...).get()/.all()` بلا تحويل نوع صريح —
الموقعان المُصلَحان هنا وُجدا تحديداً أثناء تدقيق مخصَّص لهذا النمط
(جولة التدقيق الخامسة، محور التحقق من المُدخلات)، لا بمسح شامل مُعلَن لكل
استعلام بالمشروع. أي موقع مشابه يُكتشَف لاحقاً يستحق نفس المعالجة.

# [FIX-PENDINGOFFER-01] حاجز "الحساب النشط" الذي أضافته الجولة الرابعة يفوّت العروض المعلَّقة

## الاكتشاف
نفس جولة التدقيق الخامسة، محور تداخل عمليتين شرعيتين بترتيب لم يُصمَّم له.
`[SEC-FIX-SUSPENDACTIVE-01]` (الجولة الرابعة) أضاف فحص "طلب نشط" لمنع إيقاف
حساب فني له طلب بحالة "تم اختيار عرض" فما بعدها — لكن هذا الفحص يعتمد كلياً
على `requests.technician_id`، وهذا العمود **يبقى `NULL`** طالما لم يُقبَل
أي عرض على الطلب بعد. فني قدَّم عرضاً `pending` (لم يُحسَم بعد — لا قبول ولا
رفض) على طلب لم يُعيَّن له فني بعد كان **يفوت هذا الفحص بالكامل**، رغم أن
نفس المشكلة التي بُني الفحص أصلاً لمنعها لا تزال قابلة للحدوث بالضبط: فني
يُوقَف أو يُحذَف (من الأدمن، أو حذفاً ذاتياً)، ثم العميل يقبل عرضه المعلَّق
لاحقاً (لا شيء يمنعه — العرض لا يعرف أن مُقدِّمه توقف)، فيرتبط الطلب بحساب
لن يستطيع تسجيل الدخول إليه أحد بعد الآن — نفس نتيجة "الوظيفة العالقة" التي
كان الإصلاح الرابع مصمَّماً لمنعها بالضبط، من الباب الوحيد الذي لم يفحصه.

**ملاحظة مهمة تستحق ذكرها صراحة:** هذا ليس اكتشاف ثغرة بكود شخص آخر —
هذا فحص فوّته أنا شخصياً بالجولة الرابعة على نفس هذه الجلسة. الفحص الصحيح
(`SELECT id FROM offers WHERE technician_id=? AND status='pending'`) كان
موجوداً أصلاً بمكان آخر بنفس الملف (`POST /admin/users/:id/role`، تحويل
فني→عميل) — نفس فئة الدرس المُستفاد بـ`SEC-FIX-CANCELREVIVE-01`: النمط
الصحيح كان بمتناول اليد، لم يُطبَّق بعمق كافٍ على كل الحالات المشابهة.

## الحل
نفس استعلام `offers.status='pending'` المُستخدَم أصلاً بمسار تحويل الدور،
أُضيف كفحص مستقل (بجانب فحص `activeRequest` الموجود، لا بديلاً عنه) لكل
المسارات الثلاثة التي تُخرج حساباً عن الخدمة:
- `POST /admin/users/:id/toggle` (فرع الإيقاف فقط، مطابقاً لنمط الفحص
  الأول).
- `DELETE /admin/users/:id`.
- `DELETE /me` (رمز خطأ جديد `DELETE_ACCOUNT_PENDING_OFFER`، مطابقاً لشكل
  `{error, code, params}` المستخدَم أصلاً بباقي أخطاء هذا المسار).

## الإثبات
اختباران جديدان بـ`tests/admin.spec.js` (يغطي `toggle` و`DELETE
/admin/users/:id` معاً بنفس السيناريو) و`tests/account-deletion.spec.js`
(`DELETE /me`، ملف/describe مستقل تماماً — حساب وطلب وعرض خاصان به، لا
اعتماد على الحالة المشتركة بباقي اختبارات الملف). كل سيناريو: فني يقدّم
عرضاً، العميل **لا يتخذ أي قرار عليه بعد** (يبقى `pending` صراحة)، ثم محاولة
إيقاف/حذف الفني تُرفض بـ409. **تحقَّق يدوياً أن الاختبارين يفشلان فعلاً بدون
الإصلاح** (`git stash` مؤقت للملفين، فشل بـ`Expected: 409, Received: 200`
كما هو متوقَّع تماماً لكلا الاختبارين، ثم استعادة الإصلاح).

المجموعة الكاملة: **295/295** (كانت 293، +2 هنا) — صفر تراجع.

## نطاق متروك عمداً
لم تُضَف ترجمة عميل (client-side) لرمز الخطأ الجديد `DELETE_ACCOUNT_PENDING_OFFER`
بمستودع `sallehly_app` — النص العربي الخام (`error`) يبقى متاحاً كـfallback
دائماً (نفس اتفاقية بقية الأكواد غير المُترجَمة بهذا المسار)، فلا خطأ وظيفي
بغيابها، فقط تجربة أقل تخصيصاً حتى تُضاف لاحقاً إن استُهدِف هذا المسار
تحديداً بجولة ترجمة.

# [SEC-FIX-BLOCKSCOPE-01] DELETE /block وGET /block-status بلا فحص نطاق المحادثة — كشف customer_id لأي طلب عشوائي

## الاكتشاف
نفس جولة التدقيق الخامسة، محور صلاحيات كل مسار على حدة (مصفوفة كاملة).
`routes/chat.routes.js` يحمل ستة مسارات على الأقل تخص محادثة طلب بعينه —
كلها تتحقق من `canAccessRequestChat(req.user, r)` أولاً (المُوحَّدة بـ
`SEC-FIX-CHATSCOPE-03`)، **إلا اثنين**: `DELETE /requests/:id/block`
(إلغاء الحظر) و`GET /requests/:id/block-status` (حالة الحظر). حتى
`POST /requests/:id/block` (إنشاء الحظر) المجاور مباشرة يحمل الفحص بشكل
صحيح — الاثنان الناقصان فقط شذّا عن نمط الملف بأكمله.

بدون الفحص، `getOtherPartyId(r, req.user.id)` (`الطرف الآخر بمحادثة طلب
معيّن`) تُرجِع دائماً `r.customer_id` لأي مستخدم لا علاقة له بالطلب إطلاقاً
(الفرع الوحيد بالشرط الثلاثي عندما `userId !== customer_id`). **الأثر
الحقيقي:** أي مستخدم مسجَّل دخول (عميل أو فني، بلا أي صلاحية خاصة) يقدر
يستهدف `request_id` عشوائياً (تسلسلي، سهل التخمين) عبر
`GET /requests/:id/block-status` ويحصل على `customer_id` الحقيقي لصاحب ذلك
الطلب (عبر حقل `otherUserId` بالرد) بالإضافة لمعرفة وجود حظر قائم من عدمه —
ثغرة حصاد معرّفات (نفس فئة `SEC-FIX-OFFERSCOPE-01`/`SEC-FIX-CHATSCOPE-01`
بجولات سابقة، لم يكن يجمعها كلها فحص واحد بعد). `DELETE /block` كذلك يسمح
لأي مستخدم بمحاولة حذف علاقة حظر لا علاقة له بها (يقتصر عملياً على حذف حظر
هو نفسه `blocker_id`، فلا يقدر يُلغي حظراً وضعه طرف آخر ضده، لكنه لا يزال
يمر على طلب لا صلاحية له عليه).

## الحل
نفس فحص `canAccessRequestChat` المُستخدَم أصلاً بكل مسار آخر بالملف، أُضيف
لكلا المسارين، بنفس شكل رفض `403 AUTH_FORBIDDEN` المطابق تماماً لبقية
الملف. `canAccessRequestChat` لا تفحص حالة الطلب (فقط الهوية: أدمن، أو
`customer_id`، أو `technician_id` المؤكَّد) — فطرف شرعي سابق بالطلب يبقى
قادراً على الوصول لهذين المسارين حتى بعد اكتمال/إلغاء الطلب، مطابقاً تماماً
لسلوك `POST /block` وبقية مسارات المحادثة.

## الإثبات
`tests/chat.spec.js` — اختبار جديد: فني لا علاقة له بالطلب إطلاقاً (لم
يقدّم عرضاً ولا هو الفني المؤكَّد) يحاول `GET /block-status` و
`DELETE /block` على طلب عميل آخر — كلاهما يُرفض الآن بـ403، بينما العميل
نفسه (طرف حقيقي) يصل `block-status` بنجاح (200). **تحقَّق يدوياً أن
الاختبار يفشل فعلاً بدون الإصلاح** (`git stash` مؤقت للملف، فشل بـ
`Expected: 403, Received: 200` كما هو متوقَّع تماماً، ثم استعادة الإصلاح).

المجموعة الكاملة: **296/296** (كانت 295، +1 هنا) — صفر تراجع.

## نطاق متروك عمداً
لم يُكتشَف هذا الموقع تحديداً بأي من الجولات الثلاث السابقة التي دقّقت
مسارات الدردشة (`SEC-FIX-CHATSCOPE-01/02/03`) — الجولة الخامسة كانت أول
جولة تبني مصفوفة صلاحيات كاملة (كل مسار × كل دور) بدل تدقيق مسار بعينه رُصد
فيه انحراف بالفعل. لم يُدقَّق بنفس الصرامة أي ملف route آخر غير
`chat.routes.js` بحثاً عن مسارات مشابهة تفوت فحص النطاق رغم وجود مسارات
شقيقة تطبّقه — هذا الملف تحديداً فقط كان محور الاكتشاف الفعلي، لا نتيجة مسح
شامل مُعلَن لكل ملف.

# [SEC-FIX-INVISIBLECHARS-01] تجاوز فلتر مشاركة أرقام الهاتف/المنصات بحروف Unicode غير مرئية

## الاكتشاف
`chatViolationReason(body)` بـ`routes/chat.routes.js` يفحص "مقطع أرقام
متصل" (regex `/\+?[0-9](?:[0-9\s-]*[0-9])?/g`) لاصطياد أرقام الهاتف، وفحص
substring/حدود-كلمة لاصطياد أسماء منصات التواصل ("واتساب"/"whatsapp"
وغيرها). كلا الفحصين يعمل على النص كما هو، بلا أي إزالة لحروف Unicode غير
المرئية أولاً. `normalizeChatText()` يزيل التشكيل والمسافات والترقيم
المرئي فقط — لا يلمس الحروف عديمة العرض إطلاقاً.

النتيجة: إدراج حرف مثل U+200B (ZERO WIDTH SPACE) بين كل رقمين من رقم هاتف
حقيقي (`0‌7‌9‌1‌2‌3‌4‌5‌6‌7`) يفكّك "المقطع المتصل" إلى عشرة مقاطع منفصلة
بطول 1 كل واحد — لا يصل أي منها أبداً لعتبة 10 خانات المطلوبة، فيمر الفحص
بـ200 نظيفاً. النص كما يظهر للطرف المستقبِل (بعد أن يعرضه Flutter) مطابق
تماماً لرقم هاتف عادي بلا أي أثر مرئي للحرف المُدرَج — لا مسافة، لا رمز،
لا شيء. نفس الحيلة بالضبط تفكّك substring اسم المنصة (`wh‌atsapp` بحرف
ZERO WIDTH NON-JOINER U+200C وسطها) وتتجاوز فحص الكلمات المفتاحية بنفس
الطريقة.

هذا يضرب نموذج العمولة مباشرة: الفلتر بأكمله موجود لمنع طرفي طلب من
الاتفاق على التواصل خارج التطبيق لتفادي عمولة المنصة — تجاوزه بحرف غير
مرئي بسيط يُسقط الحماية بالكامل بلا أي جهد يُذكر من مستخدم عادي (نسخ URL
مولّد لحرف zero-width من أي أداة على الإنترنت، أو حتى SMS يحوي حرفاً كهذا
عرضاً، كافٍ).

اكتُشف بالجولة الخامسة (تدقيق مخصص لمسارات "الخادم يثق بقيمة العميل بلا
تحقق" شمل نمط ترميز/تطبيع المدخلات، لا فقط قيمتها).

## الحل
دالة `chatViolationReason` تُزيل الآن كل حروف Unicode غير المرئية المعروفة
من النص أولاً (`INVISIBLE_CHARS_RE`)، قبل أي فحص آخر — فحص الأرقام، فحص
substring/حدود الكلمة، وفحص الإيميل كلها تُشتق الآن من `original` بعد
الإزالة (`lower`/`compact`/`digitNormalized` جميعها تُبنى من `original`
المُنظَّف، لا من `rawBody` الخام كما كان الحال سابقاً لبعضها).

النطاقات المُزالة، بالاسم الكامل، عبر `new RegExp('[...]', 'g')` بصيغة
`\uXXXX` صريحة (لا حروف حرفية غير مرئية مضمَّنة بالكود المصدري نفسه —
تعمّدنا هذا تحديداً كي يبقى الكود قابلاً للمراجعة البصرية؛ حرف غير مرئي
حقيقي داخل range حرفي بالكود كان سيجعل التحقق من صحة الـregex نفسه مستحيلاً
بصرياً):
- U+00AD SOFT HYPHEN
- U+034F COMBINING GRAPHEME JOINER
- U+061C ARABIC LETTER MARK (وثيقة الصلة بتطبيق عربي تحديداً)
- U+180E MONGOLIAN VOWEL SEPARATOR
- U+200B–U+200F: ZERO WIDTH SPACE، ZERO WIDTH NON-JOINER، ZERO WIDTH
  JOINER، LEFT-TO-RIGHT MARK، RIGHT-TO-LEFT MARK
- U+202A–U+202E: علامات bidi embedding/override (LRE/RLE/PDF/LRO/RLO)
- U+2060–U+2064: WORD JOINER وما شابهها (INVISIBLE TIMES/PLUS/SEPARATOR..)
- U+2066–U+2069: علامات bidi isolate (LRI/RLI/FSI/PDI)
- U+FEFF: ZERO WIDTH NO-BREAK SPACE / BOM

النص المرئي الفعلي للمستخدم لا يتأثر إطلاقاً — هذه الحروف بلا أي تمثيل
مرئي بأي حال، إزالتها لا تُغيّر شكل الرسالة كما يراها أي طرف.

## الإثبات
`tests/chat.spec.js` — describe جديد مستقل `[SEC-FIX-INVISIBLECHARS-01]`
بأربعة اختبارات:
1. رقم هاتف حقيقي بحروف U+200B بين كل رقمين → 400 مع رسالة تحوي "هاتف"
2. `whatsapp` بحرف U+200C وسطها → 400
3. BOM (U+FEFF) ببداية النص + Arabic Letter Mark (U+061C) وسط "واتساب" →
   400 (يثبت تعدد الأنواع، لا فقط U+200B)
4. رسالة عربية عادية تماماً بلا أي حرف غير مرئي → تبقى تمر 200 (يثبت أن
   الإصلاح لم يكسر المسار العادي)

**تحقَّق يدوياً أن الاختبارات الثلاثة الأولى تفشل فعلاً بدون الإصلاح**
(`git stash` مؤقت لـ`routes/chat.routes.js` فقط، فأعاد `chatViolationReason`
لحالتها قبل الإصلاح): النتيجة `Expected: 400, Received: 200` بدقة على
الثلاثة، بينما الاختبار الرابع (الرسالة العادية) استمر بالنجاح كما هو
متوقَّع لأنه لا يعتمد على الإصلاح أصلاً. استُعيد الإصلاح بعدها
(`git stash pop`) وأعيد التشغيل: **4/4 نجاح**.

المجموعة الكاملة: **300/300** (كانت 296، +4 هنا) — صفر تراجع.

قبل كتابة الاختبار الفعلي، تحقَّق مباشرة عبر `node -e` أن الـregex الجديد
(بصيغة `new RegExp` مع `\uXXXX` صريحة) يُزيل كل الأمثلة المستهدفة بنجاح
(رقم هاتف مفصول بـU+200B، `whatsapp` بـU+200C وسطها، BOM ببداية النص،
Arabic Letter Mark وسط كلمة) قبل الاعتماد عليه بأي اختبار — تفادياً
لمشكلة "regex غير مُتحقَّق منه" التي كانت النسخة الأولى من هذا الإصلاح
(مبنية بحروف حرفية غير مرئية مباشرة داخل character class بالكود المصدري)
عرضة لها فعلياً.

## نطاق متروك عمداً
القائمة أعلاه تغطي النطاقات الشائعة الاستخدام فعلياً لهذا النوع من
التحايل (zero-width spaces/joiners، bidi controls، BOM، soft hyphen) —
وليست كل نقطة رمز Unicode "غير مرئية" نظرياً بيونيكود (مثل بعض علامات
التحكم النادرة بمخطط Cc، أو variation selectors كـU+FE00–U+FE0F، أو بعض
تركيبات emoji ZWJ المعقّدة). لم يُدقَّق نطاق `normalizeChatText()` نفسه —
الدالة المستخدَمة أصلاً بمسارات أخرى غير هذا الفلتر — بنفس هذا الفحص، رغم
احتمال وجود نفس فئة الثغرة بأي موضع آخر يعتمد نصاً خاماً من المستخدم بلا
تطبيع مشابه؛ هذا خارج نطاق الإصلاح الحالي المخصَّص لـ`chatViolationReason`
فقط كما طلب المستخدم صراحةً.

# [FIX-COMMISSIONSNAPSHOT-01] عمولة الباقة تُقرأ حيّة وقت المراجعة بدل أن تُلتقَط وقت التقديم

## الاكتشاف
`POST /admin/topups/:id/review` (عند الموافقة) كان يقرأ
`packages.commission_per_order` **حيّاً** لحظة المراجعة نفسها
(`db.prepare('SELECT commission_per_order FROM packages WHERE id=?').get(t.package_id)`)
ويُطبّقها فوراً على `users.active_commission` للفني. بينما `POST /topups`
(التقديم) كان يُلقِط `amount`/`bonus` بجدول `topups` وقت التقديم لكن **لا**
يلقط `commission_per_order` معهما إطلاقاً.

الفجوة الزمنية بين التقديم والمراجعة حقيقية وليست نظرية: الفني يرفع إثبات
دفع وينتظر مراجعة يدوية من الأدمن — قد تستغرق ساعات أو أياماً حسب توفر فريق
الإدارة. لو عدّل الأدمن عمولة الباقة (`PUT /admin/packages/:id`، مسار
إداري عادي تماماً، لا علاقة له مباشرة بطلبات الشحن المعلَّقة) خلال هذه
الفترة — لأي سبب تسعيري — فكل طلب شحن معلَّق حالياً على تلك الباقة سيُطبَّق
عليه معدّل العمولة **الجديد** عند موافقته، لا المعدّل الذي كان سارياً فعلياً
لحظة دفع الفني وتقديمه للطلب. هذا يضرب نموذج العمولة مباشرة (يحدّد حصة
المنصة من كل طلب ينفّذه الفني لاحقاً) بلا أي علم أو موافقة من الفني على
المعدّل الفعلي المُطبَّق عليه.

اكتُشف بالجولة الخامسة (تدقيق مخصص لـ"عمليتين شرعيتين تتداخلان بترتيب لم
يصمَّم له أحد" — هنا: تعديل باقة إداري وموافقة على طلب شحن قديم لنفس
الباقة، بترتيب زمني تعسفي).

## الحل
عمود جديد `topups.commission_per_order` (نوع `REAL`، بلا افتراضي — يبقى
`NULL` للصفوف القديمة عبر `ALTER TABLE ADD COLUMN` القياسي، ويُضاف مباشرة
بتعريف `CREATE TABLE` لقواعد البيانات الجديدة). `POST /topups` يلقطها الآن
من `pkg.commission_per_order` بنفس سطر الإدراج الذي يلقط `amount`/`bonus`
أصلاً — تماماً نفس المبدأ، نفس اللحظة.

`POST /admin/topups/:id/review` يستخدم الآن `t.commission_per_order`
(القيمة المُلتقَطة وقت التقديم) كأولوية أولى، مع سلسلة fallback صريحة
لطلبات الشحن القديمة (قُدِّمت قبل هذا الإصلاح، فالعمود `NULL` لها):
القيمة الحيّة الحالية بـ`packages` كخيار ثانٍ، ثم `active_commission`
الحالي للفني كخيار أخير — نفس ترتيب الـfallback المنطقي المُستخدَم أصلاً،
فقط بعد إعطاء الأولوية للقطة الحقيقية حين تكون متوفرة.

## الإثبات
`tests/topups.spec.js` — `describe` جديد مستقل
`[FIX-COMMISSIONSNAPSHOT-01]`: ينشئ باقة اختبار بعمولة 5، يقدّم فني طلب
شحن عليها، **ثم** يعدّل الأدمن عمولة نفس الباقة لتصير 9 (محاكاة فرق زمني
واقعي)، ثم يوافق الأدمن على طلب الشحن — ويتحقّق أن `active_commission`
النهائي للفني **5** (قيمة وقت التقديم)، لا 9 (القيمة الحيّة وقت المراجعة).

**تحقَّق يدوياً أن الاختبار يفشل فعلاً بدون الإصلاح** (`git stash` مؤقت
لـ`routes/topups.routes.js` و`config/migrate.js` معاً): فشل بـ
`Expected: 5, Received: 9` بالضبط كما هو متوقَّع، ثم استعادة الإصلاح
(`git stash pop`) وإعادة التشغيل: نجاح.

المجموعة الكاملة: **301/301** (كانت 300، +1 هنا) — صفر تراجع.

## نطاق متروك عمداً
لم تُلقَط `amount`/`bonus` بنفس منطق fallback الجديد (كانتا مُلتقَطتين
أصلاً منذ البداية، هذا الإصلاح لم يمسّهما). لم يُدقَّق أي مسار آخر بالتطبيق
لنفس فئة الثغرة (قيمة تسعير/عمولة تُقرأ حيّة وقت تنفيذ عملية لاحقة بدل وقت
الالتزام الأصلي) — الجولة الخامسة رصدت هذا الموضع تحديداً، لا نتيجة مسح
شامل مُعلَن لكل موضع تسعير بالكود.

# [SEC-FIX-COORDZERO-01] إحداثية 0 الشرعية تُسقَط كأنها غير مُرسَلة

## الاكتشاف
`POST /requests`: `const lat = req.body.lat ? Number(req.body.lat) : null;`
(ونفس الشيء لـ`lng`). خط الاستواء (`lat=0`) وخط غرينتش (`lng=0`) إحداثيات
شرعية تماماً، لكن `0` بجافاسكربت falsy بالضبط كـ`undefined`/`null` — فطلب
JSON حقيقي بـ`{lat: 0, lng: 0}` (موقع فعلي على تلك الخطوط، أو أي جهاز GPS
يُرجع بدقة صفراً بأحد المحورين قرب تقاطعهما) يفقد إحداثيته بصمت، تُخزَّن
NULL بدل القيمة الحقيقية. هذا يضرب أي ميزة تعتمد الموقع الدقيق للطلب
(عرض على الخريطة، حساب مسافة الفني) لهذه الفئة الضيقة لكن الحقيقية من
المواقع.

## الحل
فحص وجود صريح (`!== undefined && !== null && !== ''`) بدل الاعتماد على
truthiness — `0` الآن قيمة "مُرسَلة" بنفس معاملة أي رقم آخر، تمر عبر فحص
النطاق (`-90..90`/`-180..180`) الموجود أصلاً بلا أي تغيير عليه.

## الإثبات
`tests/requests.spec.js` — `describe` جديد `[SEC-FIX-COORDZERO-01]`:
جسم JSON صريح (لا multipart، حتى يصل `lat`/`lng` كرقم `0` حقيقي لا كنص)
بـ`lat:0, lng:0` → يتحقّق أنهما `0` بالرد، لا `null`. اختبار ثانٍ يثبت عدم
التراجع: طلب بلا `lat`/`lng` إطلاقاً يبقى `null` كسابقاً. **تحقَّق يدوياً
أن الاختبار الأول يفشل فعلاً بدون الإصلاح** (`git stash` مؤقت لـ
`routes/requests.routes.js`): `Expected: 0, Received: null`، ثم استعادة
الإصلاح.

## نطاق متروك عمداً
لم يُدقَّق أي حقل رقمي آخر بالتطبيق لنفس نمط `x ? Number(x) : null` —
`technician_id` بنفس الراوت كان له فحص منفصل (`SEC-FIX-NANTECHID-01`
أدناه، فئة عطل مختلفة قليلاً رغم التشابه بالسطر).

# [SEC-FIX-NANTECHID-01] technician_id غير رقمي يتحوّل NULL بصمت بدل أن يُرفَض

## الاكتشاف
`POST /requests`: `const requestedTechId = req.body.technician_id ? Number(req.body.technician_id) : null;`
ثم `if (requestedTechId) { فحص وجود الفني... }`. نص غير رقمي (مثلاً "abc")
truthy فيمر الشرط الأول، لكن `Number("abc")` يساوي `NaN`. `NaN` بدوره
falsy، فـ`if (requestedTechId)` أسفل يتخطى فحص وجود الفني بالكامل. عند
الإدراج، better-sqlite3 يحوّل معامل `NaN` إلى `NULL` بصمت بلا أي خطأ —
النتيجة: طلب موجَّه فعلياً (بنيّة العميل) لمعرّف فني معيّن، ولو كان خاطئاً
أو تالفاً بالنقل، يتحوّل بهدوء لطلب عام بلا أي تنبيه أن الفني المطلوب لم
يُعالَج أصلاً كما قصد العميل.

## الحل
فحص وجود صريح (بنفس نمط `SEC-FIX-COORDZERO-01` أعلاه) لتمييز "لم يُرسَل"
عن "أُرسل بقيمة"، ثم فحص `isNaN` صريح على النتيجة يرفض بـ400
(`REQUEST_INVALID_TECHNICIAN_ID`) بدل الانزلاق لـ`if (requestedTechId)`
الذي يعامل NaN وnull بنفس المعاملة تماماً.

## الإثبات
`tests/requests.spec.js` — `describe` جديد `[SEC-FIX-NANTECHID-01]`:
`technician_id: 'abc'` → 400 بكود `REQUEST_INVALID_TECHNICIAN_ID` (كان
200 بصمت). اختبار ثانٍ يثبت عدم التراجع: بلا `technician_id` إطلاقاً يبقى
`null` كسابقاً. **تحقَّق يدوياً أن الاختبار الأول يفشل فعلاً بدون الإصلاح**
(`git stash` مؤقت لنفس الملف): `Expected: 400, Received: 200`، ثم استعادة
الإصلاح.

المجموعة الكاملة لكلا الإصلاحين أعلاه: **305/305** (كانت 301، +4) — صفر تراجع.

## نطاق متروك عمداً
لم يُفحَص ماذا يحدث لو `technician_id=0` تحديداً (نظرياً falsy فتُعامَل
كـ"غير مُرسَل") — بما أن `AUTOINCREMENT` بـSQLite يبدأ من 1 دائماً، معرّف
0 حقيقي مستحيل عملياً بهذا التطبيق، فهذه الحالة الحدّية بلا أي أثر واقعي.

# [SEC-FIX-AMOUNTBOUND-01] لا سقف أعلى لمبالغ الأدمن المالية المنتهية (رصيد يدوي، قيمة/بونص الباقة)

## الاكتشاف
`Number.isFinite(amount)` بـ`POST /admin/users/:id/balance` و
`POST`/`PUT /admin/packages` (المُضافة أصلاً بـ`SEC-FIX-PKGFINITE-01`)
ترفض فقط `NaN`/`Infinity` — رقم منتهٍ فعلياً كـ`99999999999` (خطأ كتابة
بسيط: رقم إضافي بالغلط، أو تلاعب متعمَّد) يمر الفحص بلا أي تحذير، ويُخزَّن
حرفياً برصيد المستخدم أو بقيمة/بونص الباقة. لا يوجد أي سقف واقعي بأي من
المسارين الثلاثة، رغم أن أكبر باقة فعلية حالية بالمنصة 100 د.أ فقط.

## الحل
ثابت دفاعي بحت `MAX_FINANCIAL_AMOUNT = 1000000` (مليون د.أ — أعلى بكثير
من أي استخدام فعلي معقول، لا قاعدة عمل) مُضاف أعلى `admin.routes.js`،
يُفحَص بعد `Number.isFinite` مباشرة بكل من المسارات الثلاثة: `amount`
(بالقيمة المطلقة، لأن تعديل الرصيد يقبل سالباً للخصم) بمسار الرصيد،
و`amount`/`bonus` بكلا مساري إنشاء/تعديل الباقة.

## الإثبات
`tests/admin.spec.js` — اختبار جديد يغطي المسارات الثلاثة بمبلغ
`99999999999` (منتهٍ فعلياً، لكن غير واقعي بوضوح): رصيد يدوي، إنشاء باقة
(amount وbonus كلاهما بشكل منفصل)، وتعديل باقة موجودة — كلها 400. يتحقّق
أيضاً أن الرصيد/قيمة الباقة الفعليين لم يتغيّرا إطلاقاً (لا نصف نجاح).
**تحقَّق يدوياً أن الاختبار يفشل فعلاً بدون الإصلاح** (`git stash` مؤقت
لـ`routes/admin.routes.js`): `Expected: 400, Received: 200`، ثم استعادة
الإصلاح.

المجموعة الكاملة: **306/306** (كانت 305، +1) — صفر تراجع.

## نطاق متروك عمداً
لم يُطبَّق السقف على `commission_per_order` (لم يُطلَب صراحة، ورقم عمولة
ضخم غير واقعي أثره محدود مقارنة بمبلغ/رصيد ضخم — يُترَك لطلب لاحق إن
أُريد). القيمة `1000000` اختيار عشوائي مقصود كسقف دفاعي بعيد جداً، لا
قاعدة عمل حقيقية — قابلة للتعديل بسهولة من مكان واحد (`MAX_FINANCIAL_AMOUNT`)
إن رأى المستخدم رقماً أنسب.

# [SEC-FIX-EMPTYSERVICES-01] services غير إلزامية عند تسجيل فني، وتُتجاهَل بصمت عند مسحها بتعديل البروفايل

## الاكتشاف
موقعان منفصلان بنفس فئة العطل:

1. **`POST /auth/register`**: لا يوجد أي فحص يُلزم `services` بغير فارغة
   للفني — بعكس `avatar`/`national_number` الإلزاميين صراحة له. فني يسجّل
   بلا أي خدمة مسجَّلة يدخل بحساب لا يمكنه أبداً تلقي أي طلب متطابق (كل
   مطابقة بالمنصة، وكل فحص عند تقديم عرض بـ`offers.routes.js`، تعتمد على
   تقاطع `services`)، بلا أي تنبيه له وقت التسجيل بالسبب.
2. **`POST /me/profile`**: `const services = req.body.services ? ... : null;`
   ثم `if (role === 'technician' && services !== null) UPDATE...`. نص فارغ
   `""` مُرسَل صراحة (نية الفني الواضحة: "امسح كل خدماتي") falsy بالضبط
   كـ`undefined` — فـ`services` تصير `null`، والـ`UPDATE` يتخطى عمود
   `services` بالكامل بصمت. لا رفض، لا تطبيق — الفني قد يظن أنه صفّر
   خدماته بينما القديمة لا تزال فعّالة تماماً بالخلفية.

## الحل
1. تسجيل: فحص `if (role === 'technician' && !services)` جديد يرفض بـ400
   (`REGISTER_TECH_SERVICES_REQUIRED`)، بنفس مكان/نمط فحص `avatar`.
2. تعديل البروفايل: فحص وجود صريح (`!== undefined && !== null`) يميّز
   "الحقل لم يُرسَل" (`null`، السلوك القديم: تخطّي العمود، محفوظ كما هو)
   عن "أُرسل فارغاً" (`''`، الآن حالة مختلفة صراحة). فني بالحالة الثانية
   يُرفَض بـ400 (`PROFILE_SERVICES_REQUIRED`) بدل التجاهل الصامت أو
   السماح بحساب بلا خدمات.

## الإثبات
`tests/misc.spec.js` — `describe` جديد `[SEC-FIX-EMPTYSERVICES-01]`،
أربعة اختبارات: تسجيل فني بلا `services` إطلاقاً → 400؛ تسجيل بـ`services=''`
صراحة → نفس الكود (يثبت أن التسجيل يرفض كلتا الحالتين بنفس المعاملة)؛
تعديل بروفايل بـ`services=''` صراحة (جسم **JSON**، لا multipart — عميل
Playwright نفسه أسقط حقل نص فارغ ضمن `multipart:` بصمت أثناء تطوير هذا
الاختبار؛ فحص مباشر بـ`fetch`/`FormData` خام أثبت أن الخادم يتصرّف بشكل
صحيح تماماً بكلا الحالتين — العطل بأداة الاختبار لا بالخادم، فتحوّل
الاختبار لجسم JSON الذي multer يتجاوزه بصمت أصلاً لطلب غير multipart) →
400، مع تحقّق أن الخدمات القديمة بقيت محفوظة (لم تُمسَح رغم الرفض)؛ وتعديل
بلا `services` إطلاقاً → يبقى بلا تغيير (لا تراجع). **تحقَّق يدوياً أن
الاختبارات الثلاثة الأولى تفشل فعلاً بدون الإصلاح** (`git stash` مؤقت
لـ`routes/auth.routes.js`): `Expected: 400, Received: 200` بدقة على
الثلاثة، ثم استعادة الإصلاح.

المجموعة الكاملة: **310/310** (كانت 306، +4) — صفر تراجع (تأكَّد خصوصاً
أن كل تسجيل فني بملفات الاختبار الأخرى يُرسل `services` فعلياً، فلا شيء
انكسر بالمتطلَّب الجديد).

## نطاق متروك عمداً
لم يُدقَّق مسار "الأدمن يعدّل خدمات فني نيابة عنه" (لا يوجد endpoint كهذا
حالياً بالكود، فلا موقع لتطبيق نفس الفحص عليه). لا حد أدنى على *عدد*
الخدمات غير الفراغ ذاته (فني بخدمة واحدة فقط سلوك طبيعي مقبول تماماً، لا
عطل).

# [SEC-FIX-EMAILTIMEOUT-01] إرسال إيميل OTP بلا أي مهلة زمنية — انقطاع Resend يعلّق الطلب للأبد

## الاكتشاف
`services/email.js`: `await resend.emails.send({...})` بلا أي حد زمني.
حزمة `resend` تعتمد داخلياً على `fetch` المدمَج بـNode (undici)، الذي بلا
أي مهلة افتراضية إطلاقاً. انقطاع أو تعليق حقيقي بطرف Resend (لا رد ولا
إغلاق اتصال — سيناريو شبكي واقعي تماماً، لا افتراضي) كان يعلّق
`await sendOtpEmail(...)` للأبد، وبالتالي `/auth/forgot-password` (خطوة
1 من نسيان كلمة السر — إليها تحديداً يشير الاسم الشائع "reset password"
رغم أن نقطة النهاية الفعلية `/auth/reset-password` منفصلة ولا ترسل إيميلاً
أصلاً) و`/auth/register` بأكملهما (كلاهما ينتظر `sendOtpEmail` قبل أي رد)
— العميل يبقى بانتظار رد لن يصل إطلاقاً، بلا حتى 500 واضح يقدر إعادة
المحاولة بعده. هذا "الشيء الذي يتصرّف بشكل مختلف بلا سبب ظاهر" فعلياً: نفس
الطلب بالضبط قد ينجح بسرعة عادةً، وقد يعلّق التطبيق بأكمله لدى ذلك المستخدم
تحديداً حين تتعثّر خدمة خارجية لا تحكّم للتطبيق بها.

## الحل
دالة `withTimeout(promise, ms, message)` جديدة (مصدَّرة من `services/email.js`)
تُطبِّق `Promise.race` بين الوعد الفعلي ومهلة `setTimeout` (15 ثانية)،
وترفض بخطأ واضح لو انقضت المهلة أولاً. `resend.emails.send(...)` أُحيط بها
الآن — انقطاع Resend يتحوّل الآن لـ500 واضح (`EMAIL_SEND_FAILED`) خلال 15
ثانية بحد أقصى، بدل تعليق أبدي.

## الإثبات
`tests/email-prod-safety.spec.js` — اختبار وحدة مباشر (لا خادم Resend حيّ
معلَّق حقيقياً — محاكاته تتطلّب خادم HTTP وهمي، خارج نطاق هذا الإصلاح) يثبت
الآلية الفعلية نفسها: وعد لا يُحل أبداً (يحاكي طلب شبكة معلَّق حرفياً) يُمرَّر
لـ`withTimeout` بمهلة قصيرة (300ms) — يُرفَض خلال أقل من ثانيتين، لا أن
يعلّق منتظِره للأبد. اختبار ثانٍ يثبت المسار السعيد: وعد ناجح سريع يُرجع
نتيجته الفعلية بلا أي رفض. **تحقَّق يدوياً أن كلا الاختبارين يفشلان فعلاً
بدون الإصلاح** (`git stash` مؤقت لـ`services/email.js`): الأول فشل عملياً
بتعليق حتى مهلة Playwright الافتراضية للاختبار نفسها (بلا `withTimeout`
مصدَّرة، الوعد اللانهائي يبقى معلَّقاً فعلياً — نفس العطل الحقيقي يتكرر
داخل الاختبار نفسه، أقوى دليل ممكن)، والثاني بخطأ ترجمة صريح
(`withTimeout is not a function`) — دليل قاطع أن الآلية لم تكن موجودة
إطلاقاً. استُعيد الإصلاح بعدها، نجاح كلا الاختبارين.

المجموعة الكاملة: **312/312** (كانت 310، +2) — صفر تراجع.

## نطاق متروك عمداً
لا إلغاء فعلي لطلب `fetch` الأصلي المعلَّق داخل `resend` نفسها عند انتهاء
المهلة (`Promise.race` يحدّ فقط زمن انتظار المستدعي؛ الطلب المهجور يبقى
يعمل بالخلفية حتى يفشل أو ينتهي من تلقاء نفسه) — إلغاء حقيقي يتطلّب
`AbortController` مُمرَّر عبر واجهة حزمة `resend` نفسها، تغيير أعمق خارج
نطاق هذا الإصلاح المخصَّص لحل "الطلب يعلّق العميل للأبد" تحديداً، لا
"تبديد موارد خادم بخلفية معلَّقة" (أثر أصغر بكثير، ولا يمنع أي طلب آخر من
النجاح). 15 ثانية اختيار عملي متوسط (كافٍ لبطء شبكي عادي، غير مُطوَّل
بلا داعٍ) لا رقماً موثَّقاً بمعيار خارجي.

# [SEC-FIX-ADMINTARGET-01] أدمن عادي يقدر يوقف/يحذف حساب أدمن آخر

## الخطورة
متوسط — مخاطرة داخلية بحتة (يتطلّب حساب أدمن مُخترَق أو أدمن خبيث فعلياً)،
لا مخاطرة خارجية؛ لا يصل لأي عميل/فني عادي إطلاقاً. أول بند من قائمة
[DEFERRED-AUDIT-03] يُصلَح ضمن قاعدة جديدة: لا تأجيل بعد الآن.

## الاكتشاف
`POST /admin/users/:id/toggle` و`DELETE /admin/users/:id` كانا يفحصان فقط
`id === req.user.id` (منع الإجراء الذاتي) — بلا أي فحص على دور الحساب
المستهدَف. بالمقابل `POST /admin/users/:id/role` يحمل فحصاً صريحاً
(`if (u.role === 'admin') return 400`) يمنع استهداف حساب إدارة آخر تماماً.
عملياً: أي أدمن عادي (`requireRole('admin')` فقط، لا `requireSuperAdmin`)
كان يقدر يوقف أو حتى يحذف/يُصفِّي حساب أدمن آخر — بما فيه super admin نفسه.

## الحل
نفس فحص `role === 'admin'` المستخدَم أصلاً بمسار تغيير الدور، أُضيف لكلا
المسارين مباشرة بعد جلب المستخدم المستهدَف — قبل أي منطق آخر (فحص الطلب
النشط، العرض المعلَّق، إلخ). رمز خطأ جديد موحَّد لكليهما:
`ADMIN_CANNOT_TARGET_ADMIN`.

## الإثبات
`tests/admin.spec.js` — `describe` جديد `[SEC-FIX-ADMINTARGET-01]`: يُنشئ
حسابَي أدمن مباشرة بقاعدة البيانات (لا endpoint عام لإنشاء أدمن ثانٍ —
`POST /auth/register` يرفض `role='admin'` صراحة)، يسجّل دخول الأول
(`actorToken`)، ويتحقّق أن `POST .../toggle` و`DELETE ...` كلاهما يُرفَضان
بـ400/`ADMIN_CANNOT_TARGET_ADMIN` ضد الثاني، وأن الهدف يبقى `is_active=1`
بلا أي تغيير فعلي رغم المحاولتين. **تحقَّق يدوياً أن الاختبار يفشل فعلاً
بدون الإصلاح** (`git stash` مؤقت لـ`routes/admin.routes.js`):
`Expected: 400, Received: 200`، ثم استعادة الإصلاح.

المجموعة الكاملة: **313/313** (كانت 312، +1) — صفر تراجع.

## نطاق متروك عمداً
لا شيء — هذا الإصلاح ينفّذ بالضبط ما اقترحه [DEFERRED-AUDIT-03] الأصلي
بلا أي تبسيط أو تأجيل جزء منه.

# [SEC-FIX-SOCKETDISCONNECT-01] اتصال Socket.IO الحي لا يُقطَع عند حذف حساب من الأدمن

## الخطورة
متوسط — مستخدم مُحذَّف يبقى بجلسة لحظية حية (يستقبل/يُرسل أحداث) رغم أن
REST محظور فوراً؛ نافذة تعرّض ضيقة (حتى انقطاع طبيعي أو إعادة اتصال) لا
اختراقاً مباشراً، لكنها تناقض ضمانة "الحذف يقطع فوراً" المُطبَّقة أصلاً
بمسارين شقيقين بنفس الملف.

## الاكتشاف
`DELETE /admin/users/:id` يستدعي `anonymizeUser` لكن لا يستدعي
`io.in(user-${id}).disconnectSockets(true)` إطلاقاً — بعكس كلٍّ من
`POST /admin/users/:id/toggle` (`SEC-FIX-10`) و`POST /admin/users/:id/role`
(`SEC-FIX-09`) بنفس الملف، اللذين يقطعان الاتصال الحي صراحة بنفس النمط.
أي مستخدم متصل فعلياً وقت حذف حسابه يبقى بجلسة Socket.IO حية حتى انقطاع
طبيعي أو إعادة اتصال لاحقة.

## الحل
سطر واحد إضافي بنفس نمط `toggle`/`role` بالضبط — `io.in(user-${id}).disconnectSockets(true)`
بعد نجاح `anonymizeUser`، ملفوف بـ`try/catch` صامت مطابقاً للنمط الموجود
أصلاً بالمسارين الآخرين.

## الإثبات
`tests/socket-realtime.spec.js` — `describe` جديد
`[SEC-FIX-SOCKETDISCONNECT-01]`: عميل يتصل فعلياً بالسوكت، الأدمن يحذف
حسابه عبر `DELETE /admin/users/:id`، ويتحقّق الاختبار من وصول حدث
`disconnect` فعلي خلال 5 ثوانٍ (لا محاكاة). **تحقَّق يدوياً أن الاختبار
يفشل فعلاً بدون الإصلاح** (`git stash` مؤقت لـ`routes/admin.routes.js`):
`Received: null` (لم يصل أي `disconnect` إطلاقاً خلال المهلة)، ثم استعادة
الإصلاح.

المجموعة الكاملة: **314/314** (كانت 313، +1) — صفر تراجع.

## نطاق متروك عمداً
لا شيء إضافي — نفس النمط المُطبَّق حرفياً على المسارين الشقيقين.

# [SEC-FIX-PENDINGTOPUP-01] رصيد شحن يعلق على حساب محذوف بلا رجعة

## الخطورة
متوسط — دخل مالي حقيقي (فني دفع فعلياً وصعد إيصاله) قد يعلق بلا أي طريقة
استرجاع غير تدخّل مباشر بقاعدة البيانات؛ يتطلّب توقيتاً محدَّداً (حذف
الحساب قبل مراجعة الأدمن) لا استغلالاً مباشراً، لكن الأثر عند وقوعه حقيقي
ومالي.

## الاكتشاف
`POST /admin/topups/:id/review` يُصادق على طلب شحن معلَّق ويُضيف المبلغ
لرصيد `t.technician_id` مباشرة، بلا أي فحص أن هذا الحساب لا يزال حقيقياً.
بالمقابل، حذف حساب (`DELETE /admin/users/:id`، `DELETE /me`) كان يفحص
فقط طلباً نشطاً وعرضاً معلَّقاً ورصيداً حالياً — **لا يفحص وجود طلب شحن
`pending` بانتظار مراجعة الأدمن**. لو فني قدَّم طلب شحن ثم حُذف حسابه قبل
أن يراجعه أحد، وبعدها وافق الأدمن على الطلب المعلَّق أصلاً (لا يعرف أن
الحساب صار "مستخدم محذوف")، الرصيد يُضاف لحساب `is_active=0` مُصفّى
بـ`anonymizeUser` بلا أي طريقة لاسترجاعه.

## الحل
نفس نمط فحص `pendingOffer` المُستخدَم أصلاً بكلا مساري الحذف — فحص إضافي
`pendingTopup` (`SELECT id FROM topups WHERE technician_id=? AND status='pending'`)
يرفض الحذف بـ409/`DELETE_ACCOUNT_PENDING_TOPUP` لو وُجد طلب شحن معلَّق،
بدل السماح بالحذف ثم فشل المراجعة لاحقاً بلا أي مسار تعافٍ واضح. لم
يُطبَّق على `toggle` (الإيقاف) عمداً — الإيقاف عكسي (تفعيل لاحق يُعيد
الحساب فعّالاً بلا أي فقدان)، بعكس الحذف/التصفية التي لا رجعة فيها؛ نفس
منطق `SEC-FIX-PENDINGTOPUP-01` لا ينطبق على حساب سيُعاد تفعيله لاحقاً.

## الإثبات
`tests/account-deletion.spec.js` — `describe` جديد `[SEC-FIX-PENDINGTOPUP-01]`،
اختباران: فني له طلب شحن `pending` يحاول حذف حسابه ذاتياً (`DELETE /me`)
→ 409/`DELETE_ACCOUNT_PENDING_TOPUP`؛ ونفس السيناريو عبر الأدمن
(`DELETE /admin/users/:id`) → نفس الرفض. كلاهما يتحقق أن الحساب يبقى
موجوداً وقادراً على تسجيل الدخول بعد الرفض. **تحقَّق يدوياً أن كلا
الاختبارين يفشلان فعلاً بدون الإصلاح** (`git stash` مؤقت لـ
`routes/admin.routes.js` و`routes/auth.routes.js` معاً):
`Expected: 409, Received: 200` على كليهما، ثم استعادة الإصلاح.

المجموعة الكاملة: **316/316** (كانت 314، +2) — صفر تراجع.

## نطاق متروك عمداً
لا فحص مماثل على `toggle` (إيقاف) — قرار مقصود موضَّح أعلاه (الإيقاف
عكسي، لا خطر فقدان دائم). لا تنبيه تلقائي للأدمن أن طلب شحن معلَّق أصبح
"عالقاً" فعلياً بسبب حالة أخرى (لا يوجد سيناريو كهذا الآن بعد هذا
الإصلاح تحديداً، لكن قد يستحق مراقبة تشغيلية مستقبلاً).

# [SEC-FIX-INVISIBLECHARS-02] توسعة نطاق الحروف غير المرئية المُزالة — variation selectors وحروف تحكم C0/C1 نادرة

## الخطورة
منخفض — نسخة أضيق من ثغرة `SEC-FIX-INVISIBLECHARS-01` نفسها (خطير عند
إصلاحها الأصلي)؛ النطاقات الشائعة الاستخدام فعلياً بهذا النوع من التحايل
(zero-width، bidi، BOM) كانت مغطّاة أصلاً — هذا يغلق فئات أضيق وأقل
شيوعاً استُخدامها فعلياً، لا ثغرة جديدة مكتشَفة بالاستغلال الفعلي.

## الاكتشاف
نطاق "متروك عمداً" صريح بإصلاح `SEC-FIX-INVISIBLECHARS-01` الأصلي: القائمة
كانت تغطي الأنواع الشائعة فقط، لا "كل نقطة رمز Unicode غير مرئية نظرياً"
— اثنان مذكوران بالاسم: variation selectors (`U+FE00`–`U+FE0F`، تُستخدَم
عادة لتحديد نمط عرض إيموجي معيّن، لكنها بلا أي تمثيل مرئي مستقل بذاتها لو
أُدرجت وسط نص عادي)، وعلامات تحكم C0/C1 النادرة (`U+0000`–`U+001F`،
`U+007F`–`U+009F`، باستثناء `\t`/`\n`/`\r` المُعالَجة أصلاً كمسافة عادية).
كلاهما بلا أي تمثيل مرئي بأي حال — نفس فئة الهجوم بالضبط (تفكيك مقطع
الأرقام المتصل، أو substring اسم المنصة) عبر نقاط رمز مختلفة لم تكن
بالقائمة الأصلية.

## الحل
`INVISIBLE_CHARS_RE` رُفعت من داخل `chatViolationReason` لمستوى الملف
(`stripInvisibleChars`)، مع إضافة النطاقين أعلاه بالإضافة لـ
`U+2028`/`U+2029` (LINE/PARAGRAPH SEPARATOR) و`U+FFF9`–`U+FFFB`
(INTERLINEAR ANNOTATION). **تفصيل دقيق اكتُشف أثناء التحقق المباشر
بـ`node`:** أول محاولة لنطاق C0 (`U+000B`–`U+001F` كتلة واحدة) كانت تُزيل
`\r` (CARRIAGE RETURN، `U+000D`) أيضاً بالخطأ رغم أنه مقصود الاستثناء —
صُحِّح بتقسيم النطاق لـ`U+000B`–`U+000C` و`U+000E`–`U+001F` (يتخطّى
`U+000D` تحديداً)، وتحقَّق مباشرة أن `\t`/`\n`/`\r` الثلاثة تبقى محفوظة
بعد الإصلاح.

`normalizeChatText` نفسها أصبحت تستدعي `stripInvisibleChars` بأول خطوة
داخلها مباشرة (بدل الاعتماد على أن `chatViolationReason` تُنظّف المُدخَل
قبل تمريره) — لو استُخدمت هذه الدالة مستقبلاً بموضع آخر غير
`chatViolationReason` (لا يوجد استخدام آخر حالياً، تحقَّق بالبحث الشامل)،
تبقى الحماية سارية تلقائياً بدل الاعتماد على انضباط كل مستدعٍ مستقبلي.

## الإثبات
`tests/chat.spec.js` — اختباران جديدان بنفس describe الأصلي
`[SEC-FIX-INVISIBLECHARS-01]`: اسم منصة مفصول بـVARIATION SELECTOR
(`U+FE0F`) وسط "whatsapp" → 400؛ ورقم هاتف مفصول بحرف تحكم C0 نادر
(`U+0001`) → 400. **تحقَّق يدوياً أن كلا الاختبارين يفشلان فعلاً بدون
الإصلاح** (`git stash` مؤقت لـ`routes/chat.routes.js`):
`Expected: 400, Received: 200` على كليهما، ثم استعادة الإصلاح. المجموعة
الكاملة لملف `chat.spec.js` (48 اختباراً، بما فيها الـ4 الأصلية لـ
[SEC-FIX-INVISIBLECHARS-01]) بقيت خضراء بالكامل — لا تراجع من رفع النطاق
لمستوى الملف أو من ربط `normalizeChatText` بها.

المجموعة الكاملة: **318/318** (كانت 316، +2) — صفر تراجع.

## نطاق متروك عمداً
لم تُضَف نقاط رمز الـTag characters الفائقة (`U+E0000`–`U+E007F`، مستوى
Unicode فائق يتطلّب صيغة surrogate pair بجافاسكربت — تُستخدَم أحياناً
بتقنيات إخفاء نص متقدمة، لكن لا دليل استخدام فعلي بهذا السياق) ولا كل
نقطة تحكم Unicode نظرية أخرى — نفس فلسفة الإصلاح الأصلي: تغطية شاملة
عملياً، لا شاملة نظرياً بحثاً عن اكتمال مطلق لا نهاية له.

# [SEC-FIX-SUPPORTSPAM-01] لا حد طلبات على الشكاوى/الدعم/بلاغات الرسائل، ولا منع لتكرار بلاغ نفس الرسالة

## الخطورة
متوسط — بلا تكلفة مباشرة (لا بريد خارجي مدفوع، لا عملية ثقيلة)، لكن ثلاث
نقاط إنشاء حقيقية (شكوى، تذكرة دعم، رسالة على تذكرة، بلاغ رسالة) كانت
الاستثناء الوحيد بلا أي حد طلبات مقارنة بكل نقاط الإنشاء المشابهة الأخرى
بالمشروع (طلبات، عروض، رسائل دردشة) — إغراق أو مضايقة متكررة بلا أي عائق،
وبلاغ الرسالة تحديداً بلا أي منع لتكرار نفس المستخدم لنفس البلاغ.

## الاكتشاف
`routes/support.routes.js`: `POST /support`، `POST /complaints`، و
`POST /support/:id/messages` كانت الثلاثة بلا أي middleware لحد الطلبات —
بعكس `messageLimiter`/`offerLimiter`/`requestLimiter` الموجودة أصلاً
لنظائرها بـ`routes/chat.routes.js` و`routes/requests.routes.js`.
`routes/chat.routes.js`: `POST /requests/:id/report-message` نفس الغياب،
بالإضافة لغياب أي فحص تكرار — نفس المستخدم يقدر يُبلّغ نفس الرسالة (أو
نفس المحادثة بلا `messageId` محدَّد) عدد غير محدود من المرات، كل بلاغ
يُنشئ صفاً منفصلاً بـ`message_reports` تراجعه الإدارة يدوياً.

## الحل
حد جديد `supportLimiter` بـ`middleware/security.js` (15 دقيقة نافذة،
`IS_PROD ? 15 : 1000` — نفس نمط `IS_PROD` بكل الحدود الأخرى بالملف)،
مُمرَّر عبر `deps.limiters` بـ`server.js`، ومُطبَّق على الأربع نقاط:
`POST /support`، `POST /complaints`، `POST /support/:id/messages`
(`routes/support.routes.js`)، و`POST /requests/:id/report-message`
(`routes/chat.routes.js`). بالإضافة لذلك، فحص تكرار على `report-message`:
`SELECT id FROM message_reports WHERE request_id=? AND reporter_id=? AND
message_id IS ?` (باستخدام `IS ?` لا `=` — يطابق `NULL` بأمان لبلاغ بلا
`messageId` محدَّد، تحقَّق مباشرة بسكربت `better-sqlite3` مستقل قبل
الاعتماد عليه) — بلاغ ثانٍ لنفس المستخدم على نفس الطلب (ونفس `messageId`
أو غيابه) يُرفض بـ409/`REPORT_ALREADY_SUBMITTED` بدل صف مكرر جديد.

## الإثبات
اختباران جديدان بـ`tests/chat.spec.js` (describe جديد
`[SEC-FIX-SUPPORTSPAM-01]`): ترويسة `ratelimit-limit` موجودة فعلياً على
`report-message`؛ وبلاغ ثانٍ من نفس المستخدم بلا `messageId` يُرفض
بـ409. **تحقَّق يدوياً أن كليهما يفشلان فعلاً بدون الإصلاح** (`git stash`
مؤقت لـ`middleware/security.js`، `server.js`، `routes/support.routes.js`،
`routes/chat.routes.js`): الأول `Received: undefined` على الترويسة،
الثاني `Expected: 409, Received: 200`، ثم استعادة الإصلاح وتأكيد النجاح.
أُضيف أيضاً `expect(res.headers()['ratelimit-limit']).toBeTruthy()` لثلاث
اختبارات موجودة بـ`tests/complaints-support.spec.js` (شكوى، تذكرة دعم،
رسالة على تذكرة) للتأكد أن الحد مربوط فعلياً على المسارات الثلاثة
الأخرى — فشلت الثلاثة كما هو متوقَّع بالفحص المؤقَّت (`Received:
undefined`)، ونجحت بعد الاستعادة. وأُضيف `supportLimiter: { max: 15,
windowMs: 15*60*1000 }` لاختبار الحراسة الموجود بـ
`tests/security-hardening.spec.js` الذي يقارن قيم الإنتاج الموثَّقة بكل
حد فعلياً بالكود — يمنع تغييراً صامتاً مستقبلياً لهذه القيمة تحديداً.

المجموعة الكاملة: **320/320** — صفر تراجع.

## نطاق متروك عمداً
لا حد منفصل لعدد التذاكر/الشكاوى المفتوحة بنفس الوقت لنفس المستخدم (يوجد
أصلاً فحص "تذكرة دعم مفتوحة واحدة فقط" بمنطق العمل، غير متأثر بهذا
الإصلاح). لا حد على `GET` بنفس الملفات — نفس فلسفة كل الحدود الأخرى
بالمشروع (تُطبَّق فقط على نقاط الإنشاء/التعديل الفعلية).

# [DATA-INTEGRITY-02] ثمانية جداول تشير لـusers/requests/messages بلا أي FOREIGN KEY مُصرَّح — أُضيفت الآن

## الخطورة
منخفض — لا أثر عملي اليوم (لا يوجد أي `DELETE` حقيقي على `users`/`requests`
حالياً، `anonymizeUser` لا تحذف صفاً أبداً — راجع الملاحظة التوثيقية أعلى
هذا الملف)؛ هذا يغلق فجوة حماية غير مُفعَّلة الأثر بعد، لا ثغرة مُستغَلة.

## الاكتشاف
موثَّق مسبقاً بالتفصيل بالملاحظة التوثيقية أعلى هذا الملف
(`## [DATA-INTEGRITY-02] توضيح`): `messages`، `chat_violations`،
`chat_reads`، `message_reports`، `user_blocks`، `ratings`، `ledger`،
و`complaints` تشير جميعاً لـ`users`/`requests`/`messages` بمعرّف رقمي، بلا
أي `FOREIGN KEY` مُصرَّح بتعريف الجدول — بعكس `topups`/`requests`/`offers`/
`support_tickets`/`support_messages`/`notifications` التي تحمل القيد فعلياً.
لو أُضيف مستقبلاً أي مسار `DELETE` حقيقي على `requests`/`users`، هذه
الجداول الثمانية كانت ستبقى يتيمة بصمت بلا أي خطأ يمنع ذلك.

## الحل
SQLite لا يدعم `ALTER TABLE ADD CONSTRAINT` — الطريقة الوحيدة لإضافة قيد FK
لجدول قائم فعلياً هي إعادة بناء كامل. `config/migrate.js`:
1. تعريفات `CREATE TABLE IF NOT EXISTS` للجداول الثمانية أعلى الملف أصبحت
   تحمل `FOREIGN KEY` مباشرة — يخدم فقط تنصيباً جديداً بالكامل.
2. دالة جديدة `migrateTableAddForeignKeys(db, table, createSql, columns)` —
   لقاعدة بيانات موجودة فعلياً: تفحص `PRAGMA foreign_key_list(table)` أولاً
   (idempotent، تتخطّى فوراً لو كان القيد موجوداً)، ثم تُعيد البناء داخل
   `db.transaction()` واحدة: تسمية الجدول القديم مؤقتاً → إنشاء الجديد
   (نفس الأعمدة + FK) → `INSERT ... SELECT` بأسماء أعمدة صريحة (لا `SELECT *`
   الذي يعتمد على ترتيب ضمني) → حذف القديم. تُستدعى بعد كل `ALTER TABLE ADD
   COLUMN` المذكورة أعلاه بالملف (تحديداً بعد `chat_violations.status`) حتى
   تطابق قائمة الأعمدة الجدول القديم فعلياً كما هو، وقبل أي `CREATE INDEX`
   يخص هذه الجداول (فهارسها تُعاد إنشاؤها فوراً بعدها بفضل `IF NOT EXISTS`
   الموجودة أصلاً بكل واحدة).

**أمان الفشل — مُثبَت مباشرة (سكربت `node` مستقل، قبل لمس الكود الحقيقي):**
بما أن `foreign_keys=ON` افتراضياً بـ`better-sqlite3` (تحقَّق مباشرة)،
`INSERT ... SELECT` بمعاملة واحدة يفحص كل صف تلقائياً أثناء النسخ — أي صف
يتيم فعلي (غير متوقَّع اليوم، لكن نظرياً ممكن) يُفشل `INSERT` بأكمله
فيتراجع كل شيء تلقائياً (`rollback`، بما فيها تسمية الجدول)، لا يترك الجدول
بحالة منتصفة أبداً. مُغلَّف بـ`try/catch` إضافي: فشل جدول واحد يُسجَّل
بوضوح (`console.error`) ولا يُسقط بقية الترحيل أو إقلاع الخادم.

## الإثبات
`tests/db-integrity.spec.js` — describe جديد `[DATA-INTEGRITY-02]`، اختباران:
1. يحاكي قاعدة إنتاج قائمة فعلياً: ترحيل طبيعي أول (ينشئ `users`/`requests`
   الحقيقيين)، إدراج بيانات حقيقية (رسالة شات)، ثم إعادة كتابة الجداول
   الثمانية يدوياً بشكلها القديم (بلا FK) مع نقل نفس البيانات — يحاكي بالضبط
   حالة الإنتاج الحقيقية قبل هذا الإصلاح. ترحيل ثانٍ (`migrate()` مرة أخرى)
   يُثبت: كل الثمانية تحمل FK الآن، الصف المُدرَج قبل الترحيل لا يزال موجوداً
   بنفس قيمه بالضبط، `sqlite_sequence` استمر بشكل صحيح (صف جديد يأخذ id أعلى
   من أي موجود، لا تصادم)، صف يتيم جديد يُرفَض الآن فعلياً بخطأ FK صريح،
   وترحيل ثالث لا يعيد البناء مرة أخرى (idempotent).
2. صف يتيم فعلي موجود مسبقاً بجدول قديم: الترحيل يتراجع بأمان (الجدول يبقى
   بلا FK، الصف اليتيم نفسه لا يزال موجوداً بلا أي فقدان)، و`migrate()`
   الكامل لا يرمي استثناءً غير مُلتقَط رغم ذلك.

**تحقَّق يدوياً أن الاختبار الأول يفشل فعلاً بدون الإصلاح** (`git stash`
مؤقت لـ`config/migrate.js`): `Expected: > 0, Received: 0` على فحص FK بعد
الترحيل الثاني — لأن الكود القديم لا يحمل `migrateTableAddForeignKeys`
إطلاقاً، فالجداول الثمانية تبقى كما أُعيد كتابتها يدوياً (بلا FK) للأبد.
ثم استعادة الإصلاح وتأكيد النجاح.

المجموعة الكاملة: **322/322** (كانت 320، +2) — صفر تراجع.

## نطاق متروك عمداً
لا فحص استباقي لبيانات يتيمة موجودة فعلياً بقاعدة الإنتاج الحقيقية قبل هذا
النشر — الآلية أعلاه تتعامل مع ذلك تلقائياً وبأمان (تراجع + تسجيل، لا إسقاط)
لو وُجدت، لكن لم يُشغَّل أي فحص مباشر على قاعدة الإنتاج نفسها بهذه الجلسة
(لا وصول لها). لا تُضاف قيود `ON DELETE CASCADE`/`ON DELETE SET NULL` صراحة
— القيم الافتراضية (`NO ACTION`) تكفي الهدف الحالي (منع صف يتيم صامت لدى أي
كتابة مستقبلية)، وبما أنه لا يوجد أي `DELETE` حقيقي على `users`/`requests`
اليوم، اختيار سلوك cascade محدَّد قرار منتج منفصل يُترَك ليوم إضافة حذف فعلي.

# [DATA-INTEGRITY-03] سحب عرض (`DELETE /offers/:id`) بلا `db.transaction()`

## الخطورة
منخفض — عطل بين الاستعلامين نادر جداً (لا I/O خارجي بينهما)، والأثر
العملي محدود أصلاً (موثَّق مسبقاً بالملاحظة أعلى هذا الملف): حالة الطلب
تُصحَّح عادة تلقائياً بأول قراءة لاحقة تعتمد على وجود/عدم وجود صفوف
`offers`، لا على حقل الحالة المخزَّن نفسه.

## الاكتشاف
`routes/offers.routes.js`، `DELETE /offers/:id`: `DELETE FROM offers` ثم
`UPDATE requests SET status=...` استعلامان منفصلان بلا `db.transaction()`
واحدة — بعكس `submitOffer`/`applyRejection`/`applyAcceptance` بنفس الملف
(`DATA-INTEGRITY-01`)، التي تغطي إرسال/قبول/رفض العرض لكن ليس سحبه.

## الحل
نفس النمط الحرفي المستخدَم أصلاً بالدوال الثلاث الأخرى بهذا الملف —
`db.transaction()` تلفّ `DELETE FROM offers` وإعادة حساب/تحديث حالة الطلب
معاً، فانهيار بينهما يتراجع بالكامل بدل ترك عرض محذوف وحالة طلب قديمة.

## الإثبات
لا يوجد سيناريو عمل حقيقي يفشل طبيعياً بين الاستعلامين (بعكس نقص الرصيد
بـ`doComplete`)، فأُثبتت الذرّية مباشرة عبر `tests/offers.spec.js` (describe
جديد `[DATA-INTEGRITY-03]`) بتقنية SQLite قياسية لمحاكاة فشل منتصف معاملة:
`CREATE TRIGGER ... BEFORE UPDATE ON requests WHEN NEW.id=? BEGIN SELECT
RAISE(ABORT, ...)` مؤقت (غير `TEMP` عمداً — يجب أن يكون مرئياً لاتصال
السيرفر الفعلي، لا فقط لاتصال الاختبار المنفصل) يُفشل تحديداً خطوة تحديث
حالة الطلب. الاختبار يثبت: العرض يبقى موجوداً (لم يُحذف رغم فشل الخطوة
التالية)، حالة الطلب لم تتغيّر، ثم بعد حذف الـtrigger السحب الطبيعي ينجح
فعلياً (يثبت أن الفشل كان بسبب الـtrigger المؤقت فقط لا عطل حقيقي بالمسار).
**تحقَّق يدوياً أن الاختبار يفشل فعلاً بدون الإصلاح** (`git stash` مؤقت
لـ`routes/offers.routes.js`): `العرض حُذف رغم فشل الخطوة التالية — لا
ذرّية` (`Received: undefined` بدل صف موجود) — يثبت أن الكود القديم كان
يحذف العرض فعلياً حتى مع فشل التحديث التالي له. ثم استعادة الإصلاح.

المجموعة الكاملة: **323/323** (كانت 322، +1) — صفر تراجع.

# [PERF-HARDEN-05] لا فهارس على أعمدة الفلترة المستخدَمة فعلياً بـ`GET /admin/stats`

## الخطورة
منخفض — أداء فقط، لا صحة بيانات. كل استعلام يعمل بشكل صحيح اليوم (مسح جدول
كامل)، يتدهور تدريجياً مع نمو `requests`/`users`، لا عطلاً مفاجئاً.

## الاكتشاف
`GET /admin/stats` (`routes/admin.routes.js`) ينفّذ نحو 10 استعلامات منفصلة
بكل طلب — أبرزها `WHERE status='ملغي'`/`'مكتمل'`، `GROUP BY service` على
`requests`، ثلاث نسخ (يومي/أسبوعي/شهري) من `WHERE created_at >=
datetime(...)` على كلٍّ من `requests` و`users`، و`WHERE is_active=0`،
و`WHERE role='technician' AND verification_status='pending'`. لا فهرس واحد
كان موجوداً على `requests.status`، `requests.created_at`،
`requests.service`، `users.created_at`، `users.is_active`، أو
`users.verification_status` — بعكس `requests.technician_id`/`customer_id`
و`users.role` المفهرسة أصلاً.

## الحل
ستة فهارس جديدة idempotent (`CREATE INDEX IF NOT EXISTS`، نفس نمط كل فهرس
آخر بالملف) — نفس الأعمدة الستة المذكورة أعلاه بالضبط، فهرس مفرد لكل واحد.

## الإثبات
`tests/admin.spec.js` — describe جديد `[PERF-HARDEN-05]`، نفس أسلوب
`idx_users_role` (`PERF-HARDEN-02`) بالضبط: ستة اختبارات تثبت وجود كل فهرس
فعلياً بقاعدة الاختبار بعد `migrate()` (`PRAGMA index_list`/`index_info`)،
وستة إضافية تُشغِّل نص الاستعلام الحقيقي المستخدَم فعلياً بـ`GET
/admin/stats` عبر `EXPLAIN QUERY PLAN` وتتحقق أن الخطة تستخدم الفهرس
(`SEARCH ... USING INDEX` بدل `SCAN` كامل) — بما فيها استعلام `GROUP BY
service` (يستخدم `idx_requests_service` فعلياً لتفادي فرز مؤقت منفصل).
**تحقَّق يدوياً أن كل الـ12 اختباراً يفشلون فعلاً بدون الإصلاح** (`git
stash` مؤقت لـ`config/migrate.js`): الستة الأولى بغياب الفهرس من
`PRAGMA index_list`، والستة الثانية بخطة فعلية `SCAN` كامل (أو استخدام فهرس
آخر غير مناسب، مثال: استعلام `verification_status` استخدم `idx_users_role`
فقط لشرط `role=?` وتجاهل `verification_status` تماماً بالخطة). ثم استعادة
الإصلاح وتأكيد النجاح.

المجموعة الكاملة: **335/335** (كانت 323، +12) — صفر تراجع.

## نطاق متروك عمداً
لا فهارس مركّبة (مثال: `(role, verification_status)`) رغم أن بعض
الاستعلامات تفلتر على عمودين معاً — نفس فلسفة `idx_offers_request`/
`idx_offers_technician` وغيرهما بالملف: فهارس مفردة بسيطة تخدم كل استعلام
موثَّق اليوم فعلياً (أُثبت مباشرة أن SQLite يستخدم الفهرس المفرد المناسب
بكل حالة)، لا تحسين استباقي لاستعلامات مستقبلية افتراضية.

# [SEC-FIX-DELETETOCTOU-01] `DELETE /me` يستخدم رصيداً قُرئ قبل `await bcrypt.compare()` بفحص الرصيد بعده

## الخطورة
منخفض — يتطلّب توقيتاً دقيقاً (رصيد يُضاف بالضبط أثناء نافذة `bcrypt.compare`
القصيرة)، وقيمة الهدف منخفضة لمهاجم فعلي (لا يفيد إلا صاحب الحساب نفسه
بمهاجمة نفسه، أو تصادفاً حقيقياً نادراً بين مراجعة أدمن وحذف ذاتي متزامنين).

## الاكتشاف
`routes/auth.routes.js`، `DELETE /me`: `u.balance` (المستخدَم بفحص "لا يمكن
حذف حساب برصيد متبقٍّ") يُقرأ **قبل** `await bcrypt.compare(...)` —
النقطة الوحيدة بهذا المعالج التي يُعلَّق فيها تنفيذه فعلياً (كل ما بعدها
متزامن بالكامل حتى `anonymizeUser`، فلا نافذة تزامن حقيقية أخرى بالمعالج
كله رغم أن التوثيق السابق وصفها بشكل أعمّ). أي رصيد يُضاف لهذا الحساب أثناء
تلك النافذة (تعديل رصيد يدوي من الأدمن، موافقة على طلب شحن) لا ينعكس على
الفحص لاحقاً لأنه يعتمد على `u.balance` القديم المحفوظ بالمتغيّر، لا قراءة
حيّة عند لحظة القرار الفعلية.

## الحل
الفحص يقرأ الرصيد مباشرة من قاعدة البيانات في تلك اللحظة (`freshBalance`)
بدل الاعتماد على `u.balance` المحفوظ قبل الـ`await` — بنفس نمط
`activeRequest`/`pendingOffer`/`pendingTopup` المجاورة (تُقرأ جميعها فعلياً
عند لحظة الفحص، لا قبله).

## الإثبات
`tests/account-deletion.spec.js` — describe جديد `[SEC-FIX-DELETETOCTOU-01]`:
اختبار تزامن حقيقي (لا محاكاة) بطلبَي HTTP فعليَّين متزامنين — `DELETE /me`
يُطلَق أولاً بلا انتظار، ثم بعد 10ms (لضمان دخول `bcrypt.compare` فعلاً
لنافذة التعليق) يُطلَق تعديل رصيد يدوي من الأدمن (`POST
/admin/users/:id/balance`) ويُنتظَر اكتماله بالكامل — bcrypt الأصلي بتكلفة
12 يستخدم thread pool حقيقياً (لا يحجز حلقة الحدث)، فيمنح وقتاً كافياً
لاكتمال طلب الأدمن الأسرع بكثير قبل أن يستأنف `DELETE /me`. يثبت الاختبار:
الحذف يُرفض بـ409 `DELETE_ACCOUNT_BALANCE_REMAINING` بالرصيد الصحيح
المُضاف حديثاً (50)، الحساب يبقى فعّالاً وقابلاً لتسجيل الدخول، والرصيد
المُضاف لم يضع. **تحقَّق يدوياً أن الاختبار يفشل فعلاً بدون الإصلاح** (`git
stash` مؤقت لـ`routes/auth.routes.js`): `Expected: 409, Received: 200` —
الحساب كان يُحذَف فعلياً رغم الرصيد المُضاف أثناء السباق. ثم استعادة
الإصلاح وتأكيد النجاح (شُغِّل الاختبار ثلاث مرات متتالية للتأكد من عدم
تذبذب التوقيت — نجح بثبات كل مرة).

المجموعة الكاملة: **336/336** (كانت 335، +1) — صفر تراجع.

## نطاق متروك عمداً
`activeRequest`/`pendingOffer`/`pendingTopup` لم تُلمَس — كانت أصلاً تُقرأ
حيّة عند لحظة الفحص (بعد الـ`await` مباشرة، بلا أي نافذة تعليق أخرى بينها
وبين `anonymizeUser`)، فلا ثغرة TOCTOU فعلية بها رغم أن التوثيق السابق أشار
إليها ضمن نفس الوصف العام. لم تُضَف `db.transaction()` تلفّ الفحوصات مع
`anonymizeUser` — لا فائدة إضافية هنا تحديداً بما أنها جميعاً قراءات، لا
كتابات متعددة تحتاج ذرّية (بعكس `DATA-INTEGRITY-01`/`02`/`03`).

# [SEC-FIX-ERRORALERTMAP-01] `Map` غير محدود العمر بـ`services/error-alert.js`

## الخطورة
منخفض — تسرّب ذاكرة بطيء جداً يتراكم طوال عمر العملية (لا يُعاد تشغيلها إلا
بإعادة نشر)، ليس خطراً عملياً بالحجم الحالي لكنه غير محدود بالتصميم.

## الاكتشاف
`lastSentAt` (خريطة بالذاكرة تكبح تكرار تنبيهات البريد لنفس الخطأ خلال 15
دقيقة، `MON-FIX-01`) تُضاف لها إدخالات (`shouldSend()`) لكن لا يوجد أي
حذف/تنظيف إطلاقاً لأي مفتاح. مفتاح كل إدخال هو `${context}:${message}`
(حتى 300 حرف) — أي خطأ تختلف رسالته قليلاً بين حدوث وآخر (مثلاً تتضمن ID
أو قيمة مُدخلة من المستخدم) ينشئ مفتاحاً جديداً يبقى للأبد.

## الحل
تنظيف انتهازي (`pruneExpired`) يُنفَّذ كأول خطوة بكل استدعاء لـ`shouldSend`
— يحذف أي مفتاح تجاوز مهلة الكبح (15 دقيقة) قبل معالجة المفتاح الحالي.
لا مؤقّت (`setInterval`) منفصل: حجم الخريطة أصلاً محدود بعدد أنواع الأخطاء
المميّزة لا عدد الطلبات، فالتكلفة الإضافية بكل استدعاء مهملة، ولا حاجة
لآلية زمنية مستقلة عن نفس نقطة الاستدعاء الموجودة أصلاً.

## الإثبات
`shouldSend`/`lastSentAt` صُدِّرتا من `services/error-alert.js` خصيصاً
للاختبار المباشر (نفس فلسفة تصدير `withTimeout` من `services/email.js`
لـ`SEC-FIX-EMAILTIMEOUT-01`) — بيئة الاختبار بلا `RESEND_API_KEY`، فـ
`alertError()` نفسها ترجع مبكراً بلا لمس الخريطة إطلاقاً، فلا طريقة لاختبار
هذا إلا عبر استدعاء `shouldSend` مباشرة. `tests/error-alert.spec.js` —
describe جديد `[SEC-FIX-ERRORALERTMAP-01]`: مفتاح بطابع زمني قديم جداً
(24 ساعة، يحاكي خطأ لن يتكرر) يُدرَج يدوياً بالخريطة، ثم استدعاء `shouldSend`
بمفتاح **مختلف تماماً** يجب أن يحذف المفتاح القديم كأثر جانبي — يثبت أن
التنظيف يطال كل المفاتيح منتهية الصلاحية، لا فقط المفتاح المُستدعى به.
اختبار ثانٍ يثبت أن مفتاحاً ضمن مهلة الكبح الحالية لا يُحذف ويستمر بمنع
التكرار (لا كسر للسلوك الأصلي). **تحقَّق يدوياً أن كلا الاختبارين يفشلان
فعلاً بدون الإصلاح** (`git stash` مؤقت لـ`services/error-alert.js`):
`TypeError` صريح على كلا الاختبارين (`Cannot read properties of undefined
(reading 'set')` والثاني `shouldSend is not a function`) — يثبت أن
التصدير والتنظيف كليهما غير موجودين إطلاقاً بالكود القديم. ثم استعادة
الإصلاح وتأكيد النجاح.

المجموعة الكاملة: **338/338** (كانت 336، +2) — صفر تراجع.

# [SEC-FIX-UPLOADMAGIC-01] رفع الصور (إيصال شحن، صورة مشكلة، صورة شات، صورة بروفايل) يعتمد الامتداد + mimetype المُصرَّح فقط، لا محتوى الملف الفعلي

## الخطورة
منخفض — بما أن `SEC-FIX-UPLOADS-01` أغلق أصلاً الوصول المباشر غير
المُصادَق عليه لملفات `payments/` (الأخطر عملياً)، لا تنفيذ كود عن بعد ممكن
(الملفات تُخدَّم كبيانات ساكنة لا تُنفَّذ) — أقصى أثر واقعي محتوى مضلِّل
يراه أدمن يراجع طلب شحن، أو ملف غير صورة فعلياً محفوظ بمجلد صور.

## الاكتشاف
`middleware/upload.js`'s `fileFilter` (يخدم `receipt`، `problem_image`،
`image` بالشات، و`avatar` — نفس `upload` المشترك) يتحقق فقط من
`file.mimetype` (يُقرّره المتصفح/العميل، قابل للتلاعب بسهولة بطلب مُعدَّل
يدوياً) وامتداد الاسم — لا أي فحص لمحتوى الملف الثنائي الفعلي يثبت أنه صورة
حقيقية من النوع المُدَّعى.

## الحل
`verifyImageMagicBytes` — middleware إضافية جديدة تُشغَّل **بعد**
`upload.single(...)` مباشرة بكل نقطة الأربع (`POST /auth/register`،
`POST /me/profile`، `POST /requests`، `POST /requests/:id/images`، `POST
/topups`) — الملف مكتوب فعلياً على القرص بهذه المرحلة (لا يتوفر أصلاً وقت
`fileFilter` نفسه، قبل اكتمال الكتابة). تقرأ أول 12 بايت فقط (لا الملف
كاملاً — تكلفة I/O مهملة) وتتحقق أن "التوقيع السحري" (magic bytes) يطابق
فعلياً نوع MIME الذي ادّعاه الطلب (JPEG: `FF D8 FF`، PNG: 8 بايتات التوقيع
الكاملة، WebP: `RIFF`+`WEBP`) — لا مجرد تطابق الامتداد/الترويسة. عدم تطابق
(أو ملف غير قابل للقراءة) يحذف الملف فوراً (لا يُترَك يتيماً بالقرص) ويرفض
برسالة تحمل عبارة "نوع الملف" التي يتعرّف عليها `apiErrorHandler` أصلاً
(`middleware/security.js`) فتُترجَم تلقائياً لـ`FILE_TYPE_NOT_ALLOWED` بلا
أي تعديل هناك. لا تبعية جديدة (لا مكتبة `file-type` خارجية) — فحص توقيع
يدوي بسيط بجافاسكربت خام، يطابق فلسفة المشروع (صفر تبعية جديدة عند الإمكان).

## أثر جانبي على بيانات اختبار موجودة — اكتُشف أثناء التنفيذ
بيانات تجهيز الصور بـ40 موقعاً عبر 15 ملف اختبار كانت تستخدم توقيع PNG
**مبتور** (`[0x89, 0x50, 0x4e, 0x47]`، 4 بايتات فقط لا 8) كصورة وهمية —
كافٍ للفحص القديم (امتداد+mimetype فقط) لكن غير كافٍ للتحقق الجديد. لم تكن
أي من هذه المواقع تختبر فعلياً "هل مضمون مزيَّف يُرفض" — مجرد بيانات تجهيز
عرضية لميزات أخرى (تسجيل، شات، إيصالات...)، فاستُبدلت جميعها بالتوقيع
الكامل الصحيح (`[0x89, 0x50, 0x4e, 0x47, 0x0d, 0x0a, 0x1a, 0x0a]`) — لا
يغيّر ما تختبره أي منها. **تفصيل تقني اكتُشف أثناء الاختبار:** أول تنفيذ
للفحص استخدم `buf.length` (طول الـ`Buffer.alloc(12)` المُخصَّص مسبقاً،
ثابت دائماً على 12 بغض النظر عن حجم الملف الفعلي بسبب التصفير التلقائي)
بدل `bytesRead` الفعلي المُعاد من `fs.readSync` — يعني هذا أن الفحص كان
سيعتبر أي ملف بحجم أقل من 12 بايت "طويلاً بما يكفي" خطأً، فيمرر بايتات
صفرية زائفة على أنها جزء من محتوى الملف. اكتُشف مباشرة عبر سكربت تحقّق
مستقل (`node`) قبل لمس أي اختبار حقيقي، وصُحِّح باستخدام `buf.subarray(0,
bytesRead)`.

## الإثبات
`tests/topups.spec.js` — اختبار جديد: إيصال باسم/امتداد/mimetype صورة
(`.png`، `image/png`) لكن محتواه نص عادي خام (لا بايتات PNG حقيقية) يُرفض
بـ400/`FILE_TYPE_NOT_ALLOWED`. **تحقَّق يدوياً أن الاختبار يفشل فعلاً بدون
الإصلاح** (`git stash` مؤقت لـ`middleware/upload.js` وملفات الراوتات
الخمسة و`server.js`): `Expected: 400, Received: 200` — المحتوى المزيَّف
كان يُقبَل بالكامل. ثم استعادة الإصلاح وتأكيد النجاح.
`tests/crash-safety.spec.js` (بنية اختبار منفصلة تماماً تبني تطبيق Express
مصغَّراً بـ`deps` يدوية، لا خادم الاختبار المشترك) احتاجت إضافة
`verifyImageMagicBytes` المزيَّفة لـ`deps.middleware` — بدونها، تسجيل
الراوت نفسه كان يرمي `Route.post() requires a callback function but got a
[object Undefined]` عند الإقلاع (خطأ بنيوي بتسجيل الراوت، لا وقت التشغيل)،
مُسقِطاً كل الاختبارات الخمسة بذلك الملف دفعة واحدة — لا علاقة له بالإصلاح
الأمني نفسه، مجرد تحديث لازم لبنية اختبار مصغَّرة يدوية التركيب.

المجموعة الكاملة: **339/339** (كانت 338، +1) — صفر تراجع.

## نطاق متروك عمداً
لا فحص magic bytes على الملفات الصوتية (`uploadAudio`) — النطاق الأصلي
للفجوة الموثَّقة خصّ الصور صراحة (`middleware/upload.js` الجزء الخاص
بـ`upload`، لا `uploadAudio`)، ولم يُطلَب توسيعه. لا فحص أعمق من التوقيع
الأول (مثلاً تحقق بنيوي كامل لصلاحية ملف PNG/JPEG، أو فحص محتوى صورة
خبيثة مُضمَّنة داخل صورة صالحة بنيوياً — polyglot files) — هذا يتجاوز هدف
الإصلاح المحدَّد أصلاً ("magic bytes كطبقة تحقق إضافية")، ويتطلّب مكتبة
تحليل صور فعلية حقيقية لا تبرير تكلفتها بحجم المخاطرة الحالية (لا تنفيذ
كود ممكن أصلاً، الملفات تُخدَّم كبيانات ساكنة).

# [SEC-FIX-AMOUNTBOUND-02] `commission_per_order` بلا سقف أعلى (استكمال SEC-FIX-AMOUNTBOUND-01)

## الخطورة
منخفض — رقم عمولة ضخم غير واقعي أثره محدود مقارنة بمبلغ/رصيد ضخم مباشر
(العمولة تُخصَم تدريجياً طلباً بطلب، لا دفعة واحدة)، ويتطلّب خطأ إدخال أو
تلاعب إداري لإحداث أثر فعلي.

## الاكتشاف
موثَّق مسبقاً كنطاق متروك عمداً بـ`SEC-FIX-AMOUNTBOUND-01` أعلاه:
`Number.isFinite(commission)` بـ`POST`/`PUT /admin/packages` ترفض فقط
`NaN`/`Infinity` — رقم منتهٍ فعلياً كـ`99999999999` يمر الفحص بلا أي سقف،
بعكس `amount`/`bonus` بنفس المسارين اللذين يحملان `MAX_FINANCIAL_AMOUNT`
فعلياً منذ الإصلاح الأصلي.

## الحل
`commission > MAX_FINANCIAL_AMOUNT` أُضيفت لنفس شرط الفحص الموجود أصلاً
(`amount > MAX_FINANCIAL_AMOUNT || bonusVal > MAX_FINANCIAL_AMOUNT`) بكلا
المسارين — نفس الثابت الدفاعي الموجود أصلاً، لا رقم جديد.

## الإثبات
`tests/admin.spec.js` — اختبار جديد يغطي المسارين (إنشاء وتعديل باقة)
بعمولة `99999999999`: كلاهما 400، ويتحقّق أن عمولة الباقة الفعلية بقاعدة
البيانات لم تتغيّر (لا نصف نجاح). **تحقَّق يدوياً أن الاختبار يفشل فعلاً
بدون الإصلاح** (`git stash` مؤقت لـ`routes/admin.routes.js`): `Expected:
400, Received: 200`. ثم استعادة الإصلاح.

المجموعة الكاملة: **340/340** (كانت 339، +1) — صفر تراجع.

# [SEC-FIX-EMAILCANCEL-01] مهلة إرسال الإيميل لا تُلغي طلب fetch الأصلي فعلياً (استكمال SEC-FIX-EMAILTIMEOUT-01)

## الخطورة
منخفض — `Promise.race` تحمي المستدعي (العميل يستلم 500 خلال 15 ثانية بدل
تعليق أبدي، وهذا كان الخطر الحقيقي المُصلَح أصلاً بـ`SEC-FIX-EMAILTIMEOUT-01`)؛
هذا يغلق فقط تبديد موارد الخادم بطلب خلفي مهجور، أثر أصغر بكثير ولا يمنع
أي طلب آخر من النجاح.

## الاكتشاف
موثَّق مسبقاً كنطاق متروك عمداً: `Promise.race` بـ`withTimeout` تحدّ فقط
زمن انتظار المستدعي — الطلب المهجور (`resend.emails.send()`) يبقى يعمل
بالخلفية فعلياً حتى يفشل أو ينجح من تلقاء نفسه، بلا أي إلغاء حقيقي.

## التحقيق قبل التنفيذ
تحقَّق مباشرة (قراءة مصدر حزمة `resend@6.14.0`، لا افتراضاً) أن
`resend.emails.send(payload, options)` يمرّر `options` بالكامل لـ`fetch()`
الداخلية (`{method, body, ...options, headers}`) بلا أي تصفية — فرغم أن
تعريف النوع (TypeScript) الرسمي لـ`options` لا يذكر `signal` صراحة، تمريره
فعلياً وقت التشغيل يصل لـ`fetch()` الحقيقية ويعمل. أُثبت هذا بسكربت `node`
مستقل (طلب حقيقي لـ`api.resend.com` بمفتاح وهمي): طلب أُلغي بـ
`controller.abort()` قبل الإرسال رجع خلال 1ms (بلا أي اتصال شبكي فعلي)،
مقارنة بطلب طبيعي أخذ 63ms (اتصال شبكي حقيقي حصل فعلاً) — دليل قاطع أن
الإلغاء يعمل فعلياً على مستوى الشبكة، لا مجرد نظرياً.

## الحل
`withTimeout` (`services/email.js`) أصبحت تقبل وسيطاً رابعاً اختيارياً
`onTimeout` — يُستدعى بالضبط لحظة انتهاء المهلة، قبل الرفض. كل الاستدعاءات
الحالية بثلاثة وسطاء (بما فيها الاختبارات الموجودة أصلاً) تبقى بلا أي
تغيير سلوكي. `sendOtpEmail` تُنشئ الآن `AbortController` حقيقياً، تمرّر
`signal`ه لـ`resend.emails.send()`، وتمرّر `() => controller.abort()`
كـ`onTimeout` — عند انتهاء الـ15 ثانية، الطلب الأصلي يُلغى فعلياً بدل أن
يُهجَر بالخلفية.

## الإثبات
`tests/email-prod-safety.spec.js` — describe جديد `[SEC-FIX-EMAILCANCEL-01]`:
خادم HTTP حقيقي محلي (لا يرد أبداً — يحاكي بالضبط "خادم Resend حيّ معلَّق
فعلياً" الذي وصفه `SEC-FIX-EMAILTIMEOUT-01` كخارج نطاقه وقتها، متاح الآن
عبر `Resend`'s `baseUrl` القابل للتخصيص) يتتبّع حدث `'close'` على الطلب
الوارد. `withTimeout` بمهلة قصيرة (200ms) و`onTimeout` حقيقي يُشغَّل، ثم
يُتحقَّق أن **الخادم نفسه** رأى الاتصال يُغلَق من طرف العميل — إثبات إلغاء
حقيقي على مستوى الشبكة، لا مجرد تجاهل الوعد بجافاسكربت بينما الاتصال
الحقيقي يبقى مفتوحاً بالخلفية. **تحقَّق يدوياً أن الاختبار يفشل فعلاً بدون
الإصلاح** (`git stash` مؤقت لـ`services/email.js`): `الخادم لم يرَ الاتصال
يُغلَق` (`Received: false`) — الطلب القديم يبقى معلَّقاً بالخلفية فعلياً
رغم انتهاء المهلة من طرف المستدعي. ثم استعادة الإصلاح وتأكيد النجاح.

المجموعة الكاملة: **341/341** (كانت 340، +1) — صفر تراجع.

## نطاق متروك عمداً
لا تعديل على `services/error-alert.js` (يستخدم `resend.emails.send()`
مباشرة بلا `withTimeout` أصلاً — خارج نطاق هذا الإصلاح المخصَّص لمسار OTP
تحديداً، وله فلسفة مختلفة أصلاً: تنبيه أخطاء، لا يحجب استجابة أي طلب
مستخدم). لا فحص أن `AbortController` مدعوم فعلياً بكل بيئة تشغيل محتملة —
مدمج بـNode منذ الإصدار 15، والمشروع يتطلّب Node 20+ أصلاً (راجع
`package.json`)، فلا حاجة لأي فحص توافق إضافي.

# [FIX-DEADFIELD-02] إزالة الحقل الخامل `technician_id` عند إنشاء الطلب — قرار منتج

## الخطورة
منخفض — لا يستخدمه تطبيق العميل الحقيقي إطلاقاً (تحقَّق سابقاً بقراءة
الكود مباشرة)، فلا أثر عملي على أي مستخدم حالي؛ هذا تقليص سطح هجوم غير
مُفعَّل، لا إغلاق ثغرة مُستغَلة فعلياً.

## الاكتشاف
موثَّق مسبقاً كنطاق متروك عمداً بجولة تدقيق سابقة: `POST /requests` كان
يقبل `technician_id` اختيارياً بجسم الطلب — طلب "موجَّه مباشرة" لفني معيّن.
لا شاشة بتطبيق فلاتر ترسل هذا الحقل إطلاقاً؛ بقاؤه بلا استخدام حقيقي كان
يفتح سطح هجوم بلا فائدة (أي طرف يقدر يرسل الحقل خاماً بنداء API مباشر)
ويترك فرصة لمن يبني عليه مستقبلاً بافتراض أنه ميزة فعلية مدعومة.

## القرار
**قرار منتج مباشر (لا فحص أمني وحده): الحقل يُزال بالكامل** بدل تركه أو
بناء واجهة له — لو احتاجه المنتج مستقبلاً كميزة حقيقية (حجز مباشر لفني
معيّن)، يُبنى من الصفر بواجهة مستخدم فعلية وموافقة صريحة من الفني المستهدَف
(لا افتراض قبول ضمني بمجرد استهدافه بمعرّفه)، لا إحياء لهذا الحقل الخامل.

## الحل
`routes/requests.routes.js`، `POST /requests`: حُذف تحليل/تحقق
`technician_id` بالكامل (`rawTechId`/`requestedTechId`، فحص `NaN`، فحص
وجود الفني وتفعيله) — الإدراج الآن يمرّر `NULL` صراحة لعمود `technician_id`
دائماً عند الإنشاء، بغض النظر عمّا يصل بجسم الطلب (يُتجاهَل بصمت مثل أي حقل
غير معروف آخر، لا يُرفَض ولا يُعالَج). `routes/offers.routes.js`،
`POST /requests/:id/offer`: حُذف فرع `REQUEST_DIRECT_TO_OTHER_TECHNICIAN`
الذي كان يعتمد على `r.technician_id` المضبوط عند الإنشاء — بما أن الميزة
نفسها أُزيلت، هذا الفرع لم يعد قابلاً للتحقق إطلاقاً (الحالة محصورة أصلاً
بـ`'بانتظار العروض'`/`'وصلت عروض'` بالسطر السابق مباشرة، وتلك الحالتان
تستبعدان أي `technician_id` مضبوط بالتصميم بعد هذا الإصلاح).

## الإثبات
`tests/requests.spec.js` — describe جديد `[FIX-DEADFIELD-02]` (يحل محل
`[SEC-FIX-NANTECHID-01]` المُزال بالكامل): يثبت أن `technician_id` (رقمي
صحيح يشير لفني حقيقي، أو نص عشوائي غير صالح، أو غائب تماماً) لا يُنتج أي
فرق سلوكي — النتيجة دائماً `technician_id: null` بالطلب المُنشَأ، بلا أي
خطأ 400 على أي حالة. اختبار رابع يثبت الأثر العملي المباشر لحذف فرع
`REQUEST_DIRECT_TO_OTHER_TECHNICIAN`: أي فني مؤهَّل يقدر يقدّم عرضاً بلا أي
قيد "توجيه" متبقٍّ على طلب عادي.

**تحقَّق يدوياً أن اختبارين من الأربعة يفشلان فعلاً بدون الإصلاح** (`git
stash` مؤقت لـ`routes/requests.routes.js`و`routes/offers.routes.js`):
الأول (`technician_id` صحيح) فشل بـ`Expected: null, Received: 8` (معرّف
الفني الحقيقي)، والثاني (`technician_id` غير صالح) فشل بـ`Expected: 200,
Received: 400` (الفحص القديم كان يرفضه) — يثبتان أن الميزة كانت لا تزال
فعّالة فعلاً قبل الإزالة. ثم استعادة الإصلاح وتأكيد نجاح الأربعة.

**اختبار قائم انكسر واحتاج تحديثاً** (لا تراجع حقيقي — تكيّف مع إزالة
مسار واحد فقط من طريقتين لإثبات نفس الضمانة): `tests/status-transition-security.spec.js`'s
اختبار "طلب موجَّه مباشرة لفني... لم يقدّم عرضاً" كان يُنشئ الطلب بـ
`technician_id` عبر الـAPI مباشرة (المسار المُزال الآن) ليثبت أن
`SEC-FIX-STATUSFLOW-01` (لا إكمال بلا عرض مقبول حقيقي) تبقى سارية حتى مع
`technician_id` مضبوطاً. عُدِّل ليضبط `technician_id` مباشرة بقاعدة
البيانات (محاكياً أي مصدر مستقبلي محتمل للحقل، لا الـAPI المُزال تحديداً)
— **نفس الضمانة الأمنية المُختبَرة بالضبط**، فقط تغيّرت طريقة تجهيز
الحالة الأولية لتعكس أن الـAPI لم يعد مصدرها الوحيد المحتمل.

المجموعة الكاملة: **343/343** (كانت 341، +2 هنا [FIX-DEADFIELD-02]، صفر
اختبارات جديدة بـ`status-transition-security.spec.js` — نفس العدد، تعديل
فقط) — صفر تراجع.

## نطاق متروك عمداً
لا حذف لعمود `technician_id` نفسه من جدول `requests` — العمود لا يزال
جوهرياً ومُستخدَماً بكثافة (يُضبَط عند قبول عرض، يُستخدَم بكل استعلامات
"طلبات الفني الحالية"، التقييم، الإشعارات...). المُزال فقط قدرة تعيينه
**عند الإنشاء تحديداً** عبر جسم الطلب — لا العمود ولا آلية التعيين الشرعية
الوحيدة المتبقية (قبول عرض). لم تُحذَف رموز الخطأ `REQUEST_INVALID_TECHNICIAN_ID`/
`REQUEST_TECHNICIAN_UNAVAILABLE`/`REQUEST_DIRECT_TO_OTHER_TECHNICIAN` من
مورّد رموز الخطأ بالعميل (`sallehly_app`) — نُظِّفت هناك بفرع منفصل بنفس
الجولة (راجع `DECISIONS.md` بمستودع `sallehly_app`).

# [SEC-FIX-COORDMASK-01] إخفاء الإحداثيات الدقيقة عن الفني قبل قبول عرضه — قرار منتج

## الخطورة
متوسط — تسريب موقع دقيق لبيت الزبون (خصوصية حقيقية) لأي فني قدّم عرضاً أو
حتى يتصفح فقط، لا فني مؤكَّد فقط كما كان يُفترَض — نطاق أوسع من مستخدم واحد
بحساب مُخترَق، لكن يتطلّب حساب فني فعلي (لا وصولاً مجهولاً بالكامل).

## القرار (من صاحب المنتج)
فني قدّم عرضاً لكن لم يُقبَل بعد (أو يتصفح فقط بلا أي عرض) يرى **المدينة
والمنطقة فقط**، لا `lat`/`lng` الدقيقة لبيت الزبون. الإحداثيات الكاملة تظهر
**فقط بعد** أن يصبح الفني مؤكَّداً فعلياً (عرضه مقبول). الفني يقدر يقرّر
تقديم عرضه بناءً على المنطقة وحدها، بلا حاجة لمعرفة بيت الزبون بالضبط قبل
الالتزام.

## الاكتشاف — تدقيق شامل لكل نقطة تُعيد بيانات طلب لفني (لا نقطة واحدة فقط)
موثَّق سابقاً كسؤال مؤجَّل بجولة تدقيق سابقة (`SEC-FIX-CHATSCOPE-01`/
`SEC-FIX-OFFERSCOPE-01`) بلا حسم. طُلب صراحة تدقيق **كل** نقطة، لا فقط
الحالة الأصلية المذكورة — بحث شامل (`grep` لكل `SELECT r.*`/`SELECT *
FROM requests` بكل ملفات `routes/`+`services/`) كشف أربع نقاط تسريب فعلية،
لا نقطة واحدة:

1. **`GET /requests` (فرع الفني)** — استعلام واحد بـ`WHERE` يجمع فرعين
   مختلفين تماماً بنفس النتيجة: `r.technician_id = معرّفه` (مؤكَّد، يستحق
   الإحداثيات) **أو** `r.status` ضمن الحالتين المفتوحتين بتطابق خدمة/مدينة
   (تصفّح/عرض معلَّق، لا يستحقها) — كلاهما كان يُرجع `r.*` كاملة بلا تمييز.
2. **استجابة `POST /requests/:id/offer` نفسها** — فور تقديم العرض (لا يزال
   `pending`)، الرد المباشر للفني المُقدِّم يحمل `request` كاملة بإحداثياتها
   — تسريب مباشر بلا حتى انتظار قراءة لاحقة.
3. **`GET /requests/:id/offers`** — شرط `allowed` يسمح لأي فني له `hasOffer`
   (أي عرض، `pending` أو مرفوض، لا مقبولاً بالضرورة) — كان يُرجع `request`
   كاملة لأي منهم، لا للمؤكَّد فقط.
4. **بث Socket.IO لـ`technicians-room`** (`POST /offers/:id/decision`) —
   عند أي قرار قبول/رفض، يبث `requests-updated` لـ**كل** الفنيين المتصلين
   على المنصة (لا فني واحد مستهدَف)، حاملاً `request` كاملة — فني غريب
   بلا أي علاقة بالطلب يستقبل إحداثياته الدقيقة بمجرد كونه متصلاً بالسوكت.

**نقاط تحقَّق منها ولم تكن مصدر تسريب**: `safeEmit(request.id, ...)` (يبث
لغرفة الطلب نفسها، التي يقتصر الانضمام إليها أصلاً على الفني المؤكَّد/
العميل/الأدمن — `canAccessRequestChat`، `SEC-FIX-CHATSCOPE-03`)، حدث
`new-request-created` (يستثني `lat`/`lng` أصلاً بتصميمه الحالي)، و
`routes/chat.routes.js` (لا يُعيد كائن الطلب بأي استجابة إطلاقاً).

## الحل
دالة صرفة واحدة جديدة `maskCoordsUnlessConfirmedTechnician(request,
technicianId)` بـ`utils/helpers.js` — تُعيد نسخة سطحية بـ`lat`/`lng` كـ
`null` إلا لو `request.technician_id` يطابق `technicianId` (الفني المؤكَّد
فعلاً)، وإلا تُعيد الكائن كما هو بلا نسخ (نفس فلسفة `canAccessRequestChat`
المجاورة تماماً — دالة واحدة تُستدعى من كل نقطة، لا نسخ يدوية متكررة قد
تنحرف مستقبلاً، نفس درس `SEC-FIX-CHATSCOPE-03`). تُستدعى الآن من الأربع
نقاط أعلاه بالضبط:
- `GET /requests`: كل صف بفرع الفني يمر عبرها قبل الإرجاع.
- `POST /requests/:id/offer`: تُطبَّق على نسخة الاستجابة فقط (`res.json`) —
  الكائن الأصلي `request` يبقى كاملاً للبث لغرفة الطلب/العميل/الأدمن الذين
  يستحقون الإحداثيات الكاملة بنفس اللحظة.
- `GET /requests/:id/offers`: تُطبَّق فقط لو كان طالب الطلب فنياً (لا أثر
  على العميل/الأدمن، الدالة أصلاً لا تُستدعى لهما).
- بث `technicians-room`: تُستدعى بـ`technicianId=null` عمداً (بث جماعي غير
  موجَّه لفني بعينه، `Number(null) === 0` لا يطابق أي `technician_id` حقيقي
  أبداً — `AUTOINCREMENT` يبدأ من 1 دائماً) — يُقنَّع دوماً بلا استثناء؛ الفني
  المؤكَّد يستقبل نسخته الكاملة بالفعل عبر بث مستقل موجَّه له تحديداً
  (`user-${technician_id}`، السطر المجاور مباشرة، غير مُلمَّس).

## الإثبات
`tests/requests.spec.js` — describe جديد `[SEC-FIX-COORDMASK-01]`
(`test.describe.serial`، يبني حالة تراكمية عبر خمسة اختبارات متتالية بنفس
نمط `[FIX-OFFERQUOTA-01]` الموجود أصلاً بـ`tests/offers.spec.js`): يثبت أن
`lat`/`lng` تبقيان `null` بثلاث نقاط (تصفّح، استجابة تقديم العرض، `GET
/requests/:id/offers`) طالما العرض `pending`، ثم تظهران بقيمتيهما الحقيقيتين
بكل الثلاث فور قبول العميل للعرض — والعميل صاحب الطلب يراهما كاملتين طوال
الوقت (فرعه بالكود منفصل تماماً، لم يُلمَس). `tests/socket-realtime.spec.js`
— اختبار مستقل جديد: فني غريب متصل بالسوكت فقط (بلا `join-request`، الانضمام
لـ`technicians-room` تلقائي لأي فني) يستقبل `requests-updated` عند قبول عرض
فني آخر تماماً — يتحقّق أن `lat`/`lng` بالحدث المُستقبَل `null`، بينما باقي
الحقول (المدينة، الحالة) تبقى ظاهرة (التقنيع مقصور على الإحداثيات فقط).

**تحقَّق يدوياً أن كل الاختبارات تفشل فعلاً بدون الإصلاح** (`git stash` مؤقت
لـ`utils/helpers.js`، `routes/requests.routes.js`، `routes/offers.routes.js`):
أول اختبار بـ`[SEC-FIX-COORDMASK-01]` (تصفّح) فشل بـ`Expected: null,
Received: 31.9539` (الإحداثية الحقيقية)، والباقي بنفس الوصف تراجعت
تلقائياً (`test.describe.serial` cascade-skip — سلوك متوقَّع، لا عطل إضافي)؛
اختبار البث فشل بنفس النمط. ثم استعادة الإصلاح وتأكيد نجاح الجميع.

**تفصيل اكتُشف أثناء كتابة الاختبار:** أول محاولة كتبت `[SEC-FIX-COORDMASK-01]`
بـ`tests/requests.spec.js` بـ`test.describe` عادية (لا `.serial`) — فشلت
اختبارات لاحقة بـ403 غير متوقَّع عند تشغيلها منفردة أو بترتيب مختلف، لأن
حالة "الفني قدّم عرضاً" (من اختبار سابق) لم تعد مضمونة الحدوث أولاً. صُحِّح
بـ`test.describe.serial` (نفس نمط `[FIX-OFFERQUOTA-01]` الموجود أصلاً بهذا
المشروع لضبط تسلسل تراكمي مشابه) — يضمن ترتيب التنفيذ.

المجموعة الكاملة: **350/350** (كانت 343، +7 هنا) — صفر تراجع.

## نطاق متروك عمداً
لم يُدقَّق ما إذا كان تطبيق العميل (Flutter) يتعامل بأمان مع `lat`/`lng`
`null` بشاشات الفني (خريطة/تفاصيل الطلب) — هذا جزء منفصل من نفس المهمة،
يُنفَّذ ويُوثَّق بمستودع `sallehly_app` (`DECISIONS.md` هناك). لا تغيير على
`problem_image_url`/الوصف — القرار الحالي (`SEC-FIX-UPLOADS-01`'s
تحديث الخطوة الثالثة) يسمح للفني المتصفّح برؤيتهما كما هما، ولم يُطلَب
تغيير ذلك بهذا القرار تحديداً (مقصور على الإحداثيات فقط).

# [SEC-FIX-AUDIOAUTH-01] رسائل الشات الصوتية (`/uploads/audios`) كانت آخر خطوة مفتوحة من `SEC-FIX-UPLOADS-01`

## الخطورة
عالٍ — نفس فئة `SEC-FIX-UPLOADS-01` (avatars/payments/requests أُغلقت
هناك)، لكن `audios/` تُركت عمداً حينها لأن إصلاحها الحقيقي يتطلب أيضاً
تعديل عميل Flutter (`audioplayers`'s `UrlSource` لا يدعم هيدرز إطلاقاً —
راجع `DECISIONS.md` بمستودع `sallehly_app`، `SEC-FIX-AUDIOAUTH-01` هناك).
حتى إتمامه، كل رسالة صوتية بأي محادثة كانت قابلة للاستماع من أي طرف يعرف/
يخمّن مسارها — لا جلسة، لا علاقة بالمحادثة مطلوبة.

## الحل
راوت جديد `GET /uploads/audios/:filename` بـ`routes/protected-uploads.routes.js`،
بنفس نمط `/requests/:filename` (فرع صور الشات) بالضبط: يبحث عن رسالة
`body` تبدأ بـ`'[audio]' + الرابط` (مطابقة **بادئة صريحة عبر `substr`،
لا `LIKE`** — لتفادي أي معنى خاص لحرف `_` بمحتوى اسم الملف القادم من
الرابط نفسه، `isSafeFilename` يسمح به فعلياً وهو حرف بديل بـ`LIKE`؛
البادئة ضرورية أصلاً لأن `body` قد يحمل لاحقة `|<duration>` بعد الرابط،
راجع `routes/chat.routes.js`)، ثم يطبّق `canAccessRequestChat` نفسها —
لا قاعدة صلاحية جديدة، ولا فرع "تصفّح فني" (لا يوجد ملف صوتي غير منتمٍ
لمحادثة أصلاً، بعكس `problem_image_url`).

`express.static('/uploads', UPLOAD_DIR)` (كان fallback عام بـ`app.js` يخدم
audios/ ريثما يكتمل هذا الإصلاح) حُذف نهائياً — آخر خطوة من `SEC-FIX-UPLOADS-01`
الأصلي.

## الإثبات
`tests/protected-uploads.spec.js` — استُبدلت الفقرة السابقة ("لسا بلا
مصادقة") بـ`test.describe.serial` كامل (7 اختبارات): رفض بلا توكن، العميل
صاحب الطلب، الفني المؤكَّد، فني آخر (403)، الأدمن، ملف غير مرتبط برسالة
(404 لا 403 — بلا تسريب وجوده من عدمه)، ومحاولة اجتياز مسار (400).
`tests/chat.spec.js` — اختبار موجود مسبقاً (`POST /requests/:id/audio —
المدة المُرسَلة...`) كان يفترض أن الرابط قابل للجلب **بلا** توكن (كان هذا
صحيحاً فقط بسبب الثغرة نفسها) — عُدِّل ليمرّر توكن العميل، مطابقاً السلوك
الصحيح الجديد؛ تغطية الرفض بلا توكن انتقلت بالكامل لملف الاختبار الجديد.

**تحقَّق يدوياً أن الاختبارات تفشل فعلاً بدون الإصلاح** (`git stash` مؤقت
لـ`routes/protected-uploads.routes.js` فقط، مع الإبقاء على إصلاح ترتيب
`app.js` أدناه): أول اختبار بالمجموعة الجديدة فشل بـ`Expected: 401,
Received: 200` (الطلب يمرّ فعلياً لـcatch-all الـSPA بـ`server.js` بدل أي
رفض — يثبت أن الراوت غير موجود أصلاً بدون الإصلاح، بغضّ النظر عن مصدر الـ200
الدقيق). استُعيد الإصلاح بعدها، **357/357** فعلياً بالمجموعة الكاملة
(كانت 350، +7 هنا).

## اكتشاف جانبي أثناء هذا الإصلاح — راجع `SEC-FIX-UPLOADDIR-ORDER-01` أدناه
أثناء التحقق من ترتيب middleware بـ`app.js` لإتمام هذا الإصلاح، تبيَّن وجود
علة ترتيب أعمق وأخطر — موثَّقة بقرار منفصل أدناه لأنها تمس `SEC-FIX-UPLOADS-01`
الأصلي بأكمله (avatars/payments/requests أيضاً)، لا `audios/` فقط.

# [SEC-FIX-UPLOADDIR-ORDER-01] ترتيب `express.static('public')` قبل راوت `/uploads` المحمي كان يُبطل `SEC-FIX-UPLOADS-01` بأكمله بلا `DATA_DIR`

## الخطورة
حرج — يُبطل صمتاً كل حماية `SEC-FIX-UPLOADS-01`/`SEC-FIX-AUDIOAUTH-01`
(avatars، payments، صور الشات، صور المشكلة، الرسائل الصوتية) دفعة واحدة،
بشرط واحد فقط: غياب `DATA_DIR` من بيئة التشغيل.

## الاكتشاف
`config/env.js`: بدون `DATA_DIR` مضبوطاً، `UPLOAD_DIR = path.join(BASE,
'public', 'uploads')` — **داخل** مجلد `public` نفسه حرفياً (حالة موثَّقة
ومقبولة صراحة بالكود نفسه: تحذير `console.warn` عند الإنتاج بلا قرص دائم،
لا رفض إقلاع — راجع تعليق `[FIX-CHATIMG-02]` بنفس الملف). `app.js` كان
يسجّل `app.use(express.static(path.join(env.BASE, 'public')))` **قبل**
`app.use('/uploads', protected-uploads.routes)`. ترتيب Express: أول
middleware يطابق الطلب يفوز — بما أن الملف المرفوع يقع فعلياً تحت `public/`
بهذه الحالة، `express.static('public')` يجده موجوداً بالقرص ويُرجعه مباشرة
بحالة 200، **قبل** أن يصل الطلب لراوت `/uploads` المحمي (`auth` middleware)
إطلاقاً — يتجاوزه بالكامل بصمت.

هذه العلة موجودة منذ `SEC-FIX-UPLOADS-01` الأصلي (لم تُحدَث بهذا الإصلاح)،
ولم تُكتشَف من قبل لأن **كل** بيئات الاختبار الحالية (`playwright.config.js`،
وكل ملف اختبار معزول بالمشروع كـ`socket-crash-resilience.spec.js`) تضبط
`DATA_DIR` دائماً (`./data-test` أو مسار مؤقّت مشابه)، فيقع `UPLOAD_DIR`
خارج `public/` دائماً بالاختبارات — يتفادى المسار المُعطوب تماماً بلا قصد.
اكتُشفت فقط أثناء مراجعة `app.js` كاملاً لإتمام `SEC-FIX-AUDIOAUTH-01` أعلاه.

## الحل
إعادة ترتيب سطرين فقط بـ`app.js`: راوت `/uploads` المحمي (`if (deps) {...}`)
الآن يُسجَّل **قبل** `express.static('public')`، لا بعده. لا تغيير على أي
منطق صلاحية بأي راوت — فقط ترتيب التسجيل.

## الإثبات
`tests/upload-dir-order.spec.js` — ملف جديد، يُشغّل نسخة سيرفر معزولة تماماً
**بلا `DATA_DIR` مضبوطاً إطلاقاً** (بعكس كل ملف اختبار آخر بالمشروع — هذا
هو الشرط الذي يُفعّل الثغرة تحديداً)، يكتب ملفاً تجريبياً مباشرة بالمسار
الحقيقي الذي يحسبه `config/env.js` بهذه الحالة (`public/uploads/audios/`)،
ثم يطلبه بلا أي هيدر `Authorization` ويتحقق أن المحتوى **لا** يُرجَع (لا
`200`، لا يحتوي الرد نص الملف السرّي). الملف التجريبي يُحذَف فوراً بعد كل
اختبار (`finally`)، بلا أي أثر متروك بالمستودع.

**تحقَّق يدوياً أن الاختبار يفشل فعلاً بدون الإصلاح** (`git stash` مؤقت
لـ`app.js` فقط): فشل فعلي بـ`Expected: not 200, Received: 200` — الملف
السرّي رجع فعلاً بلا أي مصادقة، يُثبت الثغرة حرفياً. استُعيد الإصلاح بعدها،
نجاح تام.

المجموعة الكاملة: **357/357** (نفس رقم `SEC-FIX-AUDIOAUTH-01` أعلاه — كلا
الإصلاحين بنفس الجولة، اختبار واحد إضافي هنا فقط لأن باقي التغطية مشتركة
أصلاً مع `tests/protected-uploads.spec.js`).

## نطاق مرتبط — تنبيه صريح للمالك
هذا الاكتشاف يعني أن **أي نشرة إنتاج فعلية لهذا التطبيق تم تشغيلها
تاريخياً بلا `DATA_DIR` مضبوطاً** كانت (قبل هذا الإصلاح) تُقدّم كل ملفات
`/uploads` (صور شخصية، إيصالات دفع، صور شات، رسائل صوتية) بلا أي مصادقة
إطلاقاً، بغضّ النظر عن أي إصلاح صلاحية سابق (`SEC-FIX-UPLOADS-01` وما تلاه) —
لم يكن أي منها فعّالاً فعلياً بهذا التكوين. لا معرفة لهذه الجلسة بما إذا
كانت بيئة الإنتاج الفعلية (Render أو غيرها) تضبط `DATA_DIR` فعلاً أو لا —
هذا يتطلب تأكيداً مباشراً من صاحب الحساب على إعدادات النشر الفعلية، لا
افتراضاً من الكود وحده.

## تحديث — مؤكَّد من صاحب الحساب مباشرة من لوحة Render
`DATA_DIR=/var/data`، وقرص دائم فعلي (1GB، خطة Starter) مُركَّب على نفس
المسار بالضبط. أي `UPLOAD_DIR` محسوب فعلياً يقع خارج `public/` تماماً —
شرط التفعيل الذي يحتاجه `SEC-FIX-UPLOADDIR-ORDER-01` (غياب `DATA_DIR`)
**لم يتحقق فعلياً بالنشرة الحالية**. هذا لا "يُلغي" الاكتشاف بأثر رجعي —
العلّة كانت حقيقية بالكود (تحقَّقت تجريبياً بالإصلاح نفسه، ليست افتراضية)،
لكنه يُصحّح تقييم الأثر: **لم تكن ثغرة نشطة فعلياً بالإنتاج الحالي**، بل
فجوة تصميم كامنة كانت ستتفعّل فقط بخطأ إعداد مستقبلي (حذف/نسيان `DATA_DIR`
عند إعادة إعداد الخدمة، ترحيل لمنصة نشر أخرى، إلخ). الإصلاح نفسه (ترتيب
middleware الصحيح) يبقى — دفاع إضافي لا يعتمد على ضبط بيئة صحيح بمكان آخر،
بلا أي تكلفة أو مخاطرة من إبقائه. لا حاجة لأي إجراء إضافي على بيئة
الإنتاج نفسها.

# [SEC-FIX-CHATSCOPE-04] `GET /chats` كان يُسرّب آخر رسالة فعلية واسم العميل لفني عرضه مرفوض

## الخطورة
عالٍ — نفس فئة `SEC-FIX-CHATSCOPE-01` (فني رُفض عرضه يبقى طرفاً بالمحادثة)،
مكتشَفة بجولة تدقيق سادسة على نقطة لم يشملها الإصلاح الأصلي.

## الاكتشاف
فرع الفني بـ`GET /chats` (`routes/chat.routes.js`) كان:
```sql
WHERE r.technician_id=? OR EXISTS(SELECT 1 FROM offers o WHERE o.request_id=r.id AND o.technician_id=?)
```
بلا فلتر `status` على `EXISTS` — يطابق أي عرض قدّمه الفني على هذا الطلب،
`pending` أو `rejected` على حدٍّ سواء، لا الفني المؤكَّد فقط
(`r.technician_id`). كل الصف المُعاد يتضمن `last_body` (**نص آخر رسالة
فعلي بالمحادثة**) و`other_name` (اسم العميل الحقيقي) — بينما كل نقاط
الشات الأخرى (`POST/GET /requests/:id/messages`، الصور، الصوت، الحظر)
تستخدم `canAccessRequestChat` بالفعل وترفض هذا الفني بـ403 لو حاول
الوصول المباشر. `GET /chats` وحدها لم تُحدَّث لتطابق نفس القاعدة.

**السيناريو:** فنيان يقدّمان عرضاً على نفس الطلب، العميل يقبل أحدهما
فيُرفَض الآخر تلقائياً (`UPDATE`، لا `DELETE` — الصف يبقى للأبد). الطرفان
المؤكَّدان يتبادلان رسائل لاحقة (عنوان، تفاوض سعر). الفني المرفوض يستدعي
`GET /api/chats` بتوكنه هو فقط — يستلم `last_body` = آخر رسالة حقيقية
بينهما، بلا أي حد زمني.

## الحل
شرط الفني اقتُصر على `r.technician_id=?` فقط — الفني المؤكَّد حصراً، نفس
قاعدة `canAccessRequestChat` بالضبط. لا فرع "تصفّح" هنا أصلاً (بعكس
`problem_image_url`) — لا معنى لظهور محادثة لفني لا يملك صلاحية قراءتها.

## الإثبات
`tests/chat.spec.js` — اختبار جديد: فنيّان يقدّمان عرضاً على طلب منفصل
(حسابان جديدان خصّيصاً — الحسابان المشتركان بالوصف `technician`/`outsider`
لديهما بالفعل طلب نشط من `beforeAll` فيُرفَض أي عرض ثانٍ منهما بـ409)،
قبول أحدهما يرفض الآخر تلقائياً، رسالة تُرسَل بعدها، ثم يُتحقَّق أن
`GET /chats` بتوكن الفني المرفوض **لا يحتوي** هذا الطلب إطلاقاً — بينما
الفني المؤكَّد يراه بـ`last_body` الصحيح.

**تحقَّق يدوياً أن الاختبار يفشل فعلاً بدون الإصلاح** (`git stash` مؤقت
لـ`routes/chat.routes.js`): فشل فعلي — الصف يعود فعلاً ومعه `last_body`
النص السرّي الكامل. استُعيد الإصلاح بعدها، نجاح تام. المجموعة الكاملة:
**362/362** (رقم نهائي يشمل كل إصلاحات هذه الجولة الثلاثة أدناه أيضاً).

# [SEC-FIX-BALANCETOGHOST-01] `POST /admin/users/:id/balance` يقدر يُضيف رصيداً لحساب محذوف بلا استرجاع

## الخطورة
متوسط — يتطلب فعل أدمن (استهداف معرّف قديم عمداً أو بالخطأ)، لكن النتيجة
مالية حقيقية وغير قابلة للاسترجاع إلا بتدخّل مباشر بقاعدة البيانات — نفس
فئة الضرر التي عالجتها `SEC-FIX-PENDINGTOPUP-01` عند نقاط الحذف نفسها.

## الاكتشاف
`anonymizeUser` (`utils/db-helpers.js`, يُشغَّل بـ`DELETE /admin/users/:id`)
يضبط `deleted_at`، `is_active=0`، بريد/هاتف/كلمة سر عشوائيين — لكنه **لا
يلمس `balance` إطلاقاً**. `POST /admin/users/:id/balance` كان يتحقق فقط
من وجود المستخدم (`SELECT * FROM users WHERE id=?`)، بلا أي فحص
`deleted_at`. أدمن يستهدف معرّفاً قديماً (سجل تدقيق، تذكرة دعم، أو مجرّد
خطأ كتابة) يقدر يُضيف رصيداً فعلياً لحساب لم يعد أحد يقدر يسجّل دخوله
إليه أبداً — الرصيد يبقى عالقاً للأبد.

## الحل
فحص صريح `if (u.deleted_at) return res.status(400)...` قبل أي كتابة.
عمداً `deleted_at` لا `is_active` — التعليق (`toggle`) يضبط `is_active=0`
أيضاً لكنه قابل للتراجع (تفعيل لاحق)، وحساب معلَّق قد يستحق تعديل رصيد
فعلاً أثناء التحقيق قبل قرار نهائي؛ الحذف وحده (`deleted_at`) هو الحالة
غير القابلة للتراجع فعلاً.

## الإثبات
`tests/admin.spec.js` — اختبار جديد: عميل جديد يُحذَف عبر
`DELETE /admin/users/:id`، ثم محاولة `POST /admin/users/:id/balance`
على نفس المعرّف تُرفَض بـ400/`USER_DELETED`، ويبقى `balance=0` بجلب
البروفايل مجدداً.

**تحقَّق يدوياً أن الاختبار يفشل فعلاً بدون الإصلاح** (`git stash` مؤقت
لـ`routes/admin.routes.js`): فشل فعلي — الطلب ينجح بـ200 ويُضاف الرصيد
فعلياً لحساب محذوف. استُعيد الإصلاح بعدها، نجاح تام.

# [DATA-INTEGRITY-04] ثلاثة مواقع إلغاء طلب إضافية بلا `db.transaction()` (استكمال DATA-INTEGRITY-01/03)

## الخطورة
منخفض — نفس تقييم `DATA-INTEGRITY-03`: لا I/O بين الاستعلامين (كتابتا
SQLite متزامنتان)، فنافذة الانهيار الفعلية شبه معدومة عملياً. يُصلَح رغم
ذلك، ليس مؤجَّلاً — نفس المبدأ المطبَّق بكل هذه الجولة.

## الاكتشاف
ثلاثة مواقع أخرى تُنفّذ بالضبط نفس نمط "رفض العروض المعلَّقة ثم تحديث
حالة الطلب" كاستعلامين منفصلين بلا معاملة واحدة تلفّهما — نفس الفئة التي
عالجتها `DATA-INTEGRITY-01` (قبول/رفض عرض) و`DATA-INTEGRITY-03` (سحب
عرض)، لم تُكتشَف بالجولات السابقة:
- `DELETE /requests/:id` (`routes/requests.routes.js`)
- `POST /requests/:id/status` بفرع `status==='ملغي'` (نفس الملف)
- `POST /admin/requests/:id/cancel` (`routes/admin.routes.js`)

انهيار العملية (أو فشل ديسك متزامن) بين الاستعلامين يترك إما عرضاً
مرفوضاً بلا تحديث حالة الطلب (يبدو الطلب لا يزال مفتوحاً)، أو العكس —
نفس الحالة الشاذة التي أثبت `DATA-INTEGRITY-03` إمكانيتها.

## الحل
كل موقع لُفّ بـ`db.transaction()` واحدة، نفس النمط الحرفي المستخدَم أصلاً
بـ`applyRejection`/`applyAcceptance` (`offers.routes.js`).

## الإثبات
ثلاثة اختبارات جديدة (`tests/requests.spec.js`، `tests/status-transition-security.spec.js`،
`tests/admin.spec.js`) — نفس تقنية `DATA-INTEGRITY-03` بالضبط: SQLite
trigger مؤقت (`RAISE(ABORT,...)`) يُفشل تحديداً `UPDATE requests` للطلب
المستهدَف، يُثبت أن فشل الخطوة الثانية لا يترك الأولى مطبَّقة جزئياً، ثم
يُزال الـtrigger ويُعاد المحاولة للتأكد أن المسار الطبيعي يعمل بعدها.

**تحقَّق يدوياً أن الثلاثة تفشل فعلاً بدون إصلاحاتها** (`git stash` مؤقت
لكل ملف على حدة): فشل فعلي بكل حالة — العرض يُرفَض رغم فشل تحديث حالة
الطلب (لا ذرّية). استُعيدت الإصلاحات، نجاح تام. المجموعة الكاملة النهائية
لهذه الجولة: **362/362**.

# [SEC-FIX-CHATSCOPE-CONSOLIDATION-01] تدقيق شامل — تحقُّق أن كل مسار يُعيد محتوى/بيانات الشات يمر عبر فحص واحد موحَّد

## الخطورة
لا ينطبق — هذا مُدخَل تحقُّق (verification)، لا إصلاح. لا كود تغيّر بهذا المُدخَل.

## السبب
ثلاث جولات تدقيق متتالية (`SEC-FIX-CHATSCOPE-01`، `SEC-FIX-CHATSCOPE-02/03`
عبر `SEC-FIX-BLOCKSCOPE-01`، ثم `SEC-FIX-CHATSCOPE-04` بالجولة السادسة) وجدت
كل مرة **تسريباً مختلفاً** لبيانات محادثة لفني غير مؤكَّد (رُفض عرضه أو لم
يُقدَّم أصلاً)، من مسار لم يستخدم `canAccessRequestChat` (`utils/helpers.js`)
رغم وجودها. طُلب صراحة (بدل ملاحقة تسريب خامس بجولة ثامنة): تعداد **كل**
مسار بالكود يقدر يُعيد محتوى رسالة أو بيانات محادثة (لا بيانات وسائط عبر
`protected-uploads.routes.js` وحدها — أي شيء)، والتحقق الفعلي (لا افتراضي)
من أن كل واحد منها يمر عبر `canAccessRequestChat` أو معادل صارم مُراجَع سابقاً.

## نتيجة التدقيق (تحقُّق مباشر بقراءة شرط WHERE/الفحص الفعلي بكل موقع، لا مطابقة اسم)

| # | المسار | يُعيد محتوى/بيانات محادثة؟ | الحارس | التحقُّق |
|---|---|---|---|---|
| 1 | `POST /requests/:id/messages` | نعم (`getMessages` كاملة) | `canAccessRequestChat` | ✅ |
| 2 | `POST /requests/:id/audio` | نعم | `canAccessRequestChat` | ✅ |
| 3 | `POST /requests/:id/images` | نعم | `canAccessRequestChat` | ✅ |
| 4 | `GET /requests/:id/messages` | نعم | `canAccessRequestChat` | ✅ |
| 5 | `POST /requests/:id/report-message` | يقرأ نص رسالة داخلياً (لا يُعاد للمستدعي) | `canAccessRequestChat` | ✅ |
| 6 | `POST /requests/:id/block` | لا محتوى، لكن يكشف `otherPartyId` | `canAccessRequestChat` | ✅ |
| 7 | `DELETE /requests/:id/block` | يكشف `otherPartyId` ضمنياً | `canAccessRequestChat` | ✅ (`SEC-FIX-BLOCKSCOPE-01`) |
| 8 | `GET /requests/:id/block-status` | يكشف `otherUserId` | `canAccessRequestChat` | ✅ (`SEC-FIX-BLOCKSCOPE-01`) |
| 9 | `GET /chats` — فرع العميل | نعم (`last_body`، `other_name`، `unread_count`) | `WHERE r.customer_id=?` — العميل يملك الصف دائماً، لا احتمال تسريب | ✅ |
| 10 | `GET /chats` — فرع الفني | نعم | `WHERE r.technician_id=?` فقط (بلا أي `EXISTS(...offers...)` بديل) | ✅ **أُصلح بالمُدخَل مباشرة قبل هذا (`SEC-FIX-CHATSCOPE-04`)** — أُعيد التحقق من شرط WHERE الحيّ فعلياً هنا |
| 11 | `socket.on('join-request', ...)` | يمنح عضوية غرفة → بث `messages-updated` لاحق | `canAccessRequestChat` | ✅ |
| 12 | `GET /uploads/requests/:filename` (صور شات) | يخدم بايتات صورة مُشار إليها برسالة | `canAccessRequestChat` | ✅ |
| 13 | `GET /uploads/audios/:filename` | يخدم بايتات صوت مُشار إليه برسالة | `canAccessRequestChat` | ✅ |
| 14 | `utils/db-helpers.js: getMessages()` | استعلام خام، بلا حارس خاص بها | كل الاستدعاءات الأربعة (`grep`) مسبوقة بفحص `canAccessRequestChat` بنفس المعالج | ✅ |
| 15 | `GET /chat-violations`، `GET /message-reports` (إشراف الأدمن) | نعم | `requireRole('admin')` | ✅ (الأدمن مسموح له بالتصميم) |
| 16 | `routes/support.routes.js` (جدول `support_messages` — منفصل تماماً، محادثة 1:1 تذكرة↔أدمن، ليس جدول `messages` متعدد الأطراف) | نعم | فحص IDOR صريح داخل الراوت (`ticket.user_id !== req.user.id && role!=='admin'`) | ✅ نطاق مستقل بالتصميم، لا يحتاج `canAccessRequestChat` |

لا يوجد أي استعلام/راوت آخر يلمس جدول `messages` (`grep -rn "FROM messages"` عبر
`routes/`، `services/`، `utils/` لا يُرجع أي موقع إضافي غير المذكورة أعلاه).

## الخلاصة
`canAccessRequestChat` هي الحارس **الوحيد والحصري** الآن لكل راوت REST،
معالج Socket.IO الوحيد، وفرعَي الوسائط المحمية التي تلمس محتوى الشات.
`SEC-FIX-CHATSCOPE-04` (المُدخَل السابق مباشرة) أغلق آخر ثغرة معروفة
(فرع الفني بـ`GET /chats`). **لم يُعثَر على تسريب خامس.** هذا لا يضمن عدم
ظهور فئة جديدة تماماً مستقبلاً (مثلاً حقل جديد يُضاف لاحقاً لراوت موجود بلا
مراجعة)، لكنه يُثبت أن كل ما هو موجود اليوم مُغطّى فعلياً، بتحقُّق مباشر لا افتراض.

# [SEC-FIX-PENDINGTOPUP-02] تحويل فني→عميل بلا فحص طلب شحن معلَّق — استكمال SEC-FIX-PENDINGTOPUP-01

## الخطورة
متوسط — يتطلب توقيتاً محدداً (طلب شحن معلَّق وقت التحويل تحديداً) وفعل
super admin، لكن النتيجة مالية حقيقية عالقة بلا استرجاع إلا بتدخّل مباشر
بقاعدة البيانات — نفس فئة الضرر بالضبط.

## الاكتشاف
جولة تدقيق سابعة. `SEC-FIX-PENDINGTOPUP-01` أضاف فحص طلب شحن معلَّق لثلاث
نقاط تعطيل حساب (`DELETE /admin/users/:id`، `DELETE /me`، تعليق الحساب)
لكنه عدَّد تلك الثلاث صراحة — لم يشمل `POST /admin/users/:id/role` (تحويل
فني→عميل)، وهو المسار الوحيد الذي يُخرج حساباً من كونه "فنياً" بينما يبقى
`topups.technician_id` (عمود بلا `FOREIGN KEY`، راجع `DATA-INTEGRITY-02`)
يشير إليه. `POST /admin/topups/:id/review` يبحث عن الحساب بـ`technician_id`
فقط، بلا أي فحص `role` — موافقة أدمن لاحقة على طلب شحن قُدِّم قبل التحويل
تُضيف `balance`/`active_commission` فعلياً على حساب `role='customer'` الآن.
لا مسار كتابة يصرف `balance` عميل إطلاقاً (كل منطق العروض/الشحن يفترض
`role='technician'`) — المبلغ يبقى عالقاً للأبد.

## الحل
نفس فحص `SEC-FIX-PENDINGTOPUP-01` بالضبط (`SELECT id FROM topups WHERE
technician_id=? AND status='pending'`) أُضيف لفرع فني→عميل بـ`POST
/admin/users/:id/role`، برمز `ROLECHANGE_PENDING_TOPUP`.

## الإثبات
`tests/admin-superadmin.spec.js` — اختبار جديد: فني يقدّم طلب شحن (يبقى
`pending`)، محاولة تحويله لعميل تُرفَض بـ409/`ROLECHANGE_PENDING_TOPUP`،
ثم بعد رفض طلب الشحن (لا موافقة) التحويل ينجح بشكل طبيعي.

**تحقَّق يدوياً أن الاختبار يفشل فعلاً بدون الإصلاح** (`git stash` مؤقت
لـ`routes/admin.routes.js`): فشل فعلي — التحويل ينجح بـ200 رغم الطلب
المعلَّق. استُعيد الإصلاح بعدها، نجاح تام.

# [SEC-FIX-BACKUPTIMEOUT-01] طلبات GitHub API للنسخ الاحتياطي الخارجي بلا مهلة زمنية

## الخطورة
منخفض — مهمة خلفية best-effort مُصمَّمة أصلاً لتفشل بصمت (`uploadBackupOffsite`/
`pruneOldOffsiteBackups` كلتاهما لا ترميان استثناءً للمستدعي)، لا تأثير على
دورة طلب/رد المستخدم، ولا على النسخ الاحتياطي المحلي (المصدر الموثوق فعلياً).

## الاكتشاف
`services/offsite-backup.js`'s `githubRequest()` (نقطة الاستدعاء المشتركة
لكل نداءات GitHub API الخمسة بالملف) كانت تستدعي `fetch()` المدمَجة بـNode
بلا أي `AbortSignal`/مهلة — نفس فئة `SEC-FIX-EMAILTIMEOUT-01` (`services/email.js`)
بالضبط، لم تُطبَّق هنا. انقطاع أو تعليق بطرف GitHub API كان يعلّق الاستدعاء
للأبد بالخلفية (بلا أي أثر خارجي فوري، لكن بلا أي مسار خروج أيضاً).

## الحل
`AbortSignal.timeout(60000)` (60 ثانية — سخية عمداً، رفع ملف `.sqlite`
فعلي كـasset لا استدعاء API خفيف) يُمرَّر افتراضياً لكل استدعاء عبر
`githubRequest()` ما لم يمرِّر المستدعي `signal` خاصاً به (لا مستدعٍ حالي
يفعل). أبسط من حل `SEC-FIX-EMAILTIMEOUT-01` (`withTimeout` + `Promise.race`)
لأن هذا استدعاء `fetch()` خام مباشر، لا مُغلَّف بمكتبة SDK خارجية.

## الإثبات
`tests/offsite-backup-timeout.spec.js` — ملف جديد، اختبار وحدة مباشر (لا
يمر بالسيرفر الحقيقي؛ `BACKUP_GITHUB_TOKEN`/`OWNER`/`REPO` تُضبَط بعملية
الاختبار نفسها قبل أي `require`). `fetch` و`AbortSignal.timeout` مموَّهتان:
الأولى تُحاكي طلباً معلَّقاً حقيقياً (لا يُحسَم إلا بحدث `abort` على
`signal`)، والثانية تُستبدَل بإصدار نتحكم بلحظة "انتهاء المهلة" يدوياً بدل
انتظار الـ60 ثانية الحقيقية بكل تشغيل. يثبت: (1) `signal` يُمرَّر فعلياً
لـ`fetch` الآن (لم يكن يُمرَّر إطلاقاً قبل الإصلاح)، (2) إطلاق المهلة يُلغي
الطلب المعلَّق فعلياً (رفض عبر `AbortError`، لا تعليق أبدي)، (3) المسار
السليم (رد ناجح) لا يتأثر.

**تحقَّق يدوياً أن الاختبارين يفشلان فعلاً بدون الإصلاح** (`git stash`
مؤقت لـ`services/offsite-backup.js`): فشل فعلي بكليهما —
`options.signal` غير معرَّف إطلاقاً (`Cannot read properties of undefined
(reading 'addEventListener')`)، بالضبط العلّة الموصوفة. استُعيد الإصلاح
بعدها، نجاح تام. المجموعة الكاملة: **365/365**.

# [SEC-FIX-RESETTOKENVER-01] `POST /auth/reset-password` كان لا يُبطل التوكن القديم إطلاقاً — نسخة مسروقة تبقى صالحة بعد استرجاع الحساب

## الخطورة
خطير — تحايل كامل على حماية أمنية قائمة فعلياً بالمشروع (إبطال الجلسة عند
تغيير بيانات الاعتماد، `SEC-FIX-09`) عبر بالضبط المسار المصمَّم أصلاً
للتعامل مع حساب قد يكون تعرَّض لاختراق فعلي (نسيت كلمة السر ← إعادة تعيين)؛
لا يتطلَّب أي شرط خاص من جهة الخادم — فقط أن يكون هجوم سرقة توكن قد وقع
فعلاً أصلاً، وهو بالضبط السيناريو الذي وُجد هذا التدفّق ليحلّه.

## الاكتشاف
جولة تدقيق ثامنة، محور تناسق مسارات "تغيير بيانات اعتماد حسّاسة" عبر كل
الملف مقارنة ببعضها البعض. `routes/auth.routes.js`'s `POST /me/password`
(تغيير كلمة السر الذاتي، مصادَق) يزيد `token_version+1` صراحة — موثَّق أصلاً
بـ`SEC-FIX-09`: "توكن أُبطل بتغيير كلمة سر لا يجوز أن يبقى صالحاً، تماماً
كنسخة مسروقة كانت لا تزال صالحة حتى الآن". `POST /auth/reset-password`
(نسيت كلمة السر — **غير** مصادَق، يعتمد فقط على OTP بريد إلكتروني) كان
يعدّل `password_hash` فقط، بلا أي لمس لـ`token_version` إطلاقاً — الفحص
المباشر أثبت أن كل الاستعلامات بهذا المعالج (`UPDATE users SET
password_hash=? WHERE id=?`) لا تحمل `token_version` على الإطلاق، ولا أي
استدعاء لقطع اتصال Socket.IO حي (بعكس `toggle`/`role`/`DELETE
/admin/users/:id`/`DELETE /me` بنفس الملف، جميعها تستدعي
`io.in(...).disconnectSockets(true)` صراحة عند تغيير حسّاس).

**الأثر الحقيقي:** تدفّق "نسيت كلمة السر" هو بالضبط الأداة التي يستخدمها
صاحب حساب لاسترجاع التحكّم به حين يشتبه أن كلمة سره (أو توكن جلسته)
مُخترَقة. لكن أي JWT صادر **قبل** إعادة التعيين — بما فيها نسخة مسروقة كانت
سبب اللجوء لهذا التدفّق أصلاً — يبقى صالحاً بالكامل حتى انتهاء صلاحيته
الطبيعية (7 أيام)، لأن `middleware/auth.js` يقارن `decoded.tokenVersion`
بـ`token_version` الحيّ بقاعدة البيانات فقط، ولم يتغيّر أي منهما. مهاجم
يملك توكناً مسروقاً يستمر بالوصول الكامل لحساب الضحية (REST كاملاً، بما
فيه كل مسار حسّاس آخر) رغم أن الضحية غيَّر كلمة سره بنجاح معتقداً أنه
استرجع حسابه بالكامل.

## الحل
`db.prepare('UPDATE users SET password_hash=?, token_version=token_version+1
WHERE id=?')` — نفس النمط الحرفي المستخدَم أصلاً بـ`/me/password` (`SEC-FIX-09`).
بعكس `/me/password`، لا حاجة لإصدار توكن جديد هنا (هذا المسار غير مصادَق
أصلاً — لا "جهاز حالي" يجب الحفاظ على تسجيل دخوله، رسالة النجاح تقول صراحة
"يمكنك الدخول الآن"). بالإضافة لذلك، `io.in(user-${d.userId}).disconnectSockets(true)`
مُضافة أيضاً (ملفوفة بـ`try/catch` صامت، نفس نمط `toggle`/`role`/`DELETE`
المجاورة) — `token_version+1` وحدها لا تقطع أي اتصال Socket.IO حي فوراً
(يُعاد التحقق فقط عند اتصال جديد، راجع نطاق متروك عمداً بـ`SEC-FIX-CHATSCOPE-03`)،
وهذا التدفّق تحديداً (استرجاع حساب من اختراق مُشتبَه) يستحق نفس القطع
الفوري الصريح المطبَّق أصلاً بمسارات "تغيير حسّاس" الأخرى.

## الإثبات
`tests/auth-session.spec.js` — اختباران جديدان بنفس `describe` الموجود أصلاً
لإبطال الجلسات:
1. عميل يسجّل، توكنه (يُحاكي نسخة مسروقة) يعمل فعلاً، ثم `forgot-password`
   ← `reset-password` بكلمة سر جديدة — نفس التوكن بالضبط يُرفَض الآن بـ401
   على `GET /me`، والحساب يبقى قابلاً لتسجيل الدخول طبيعياً بكلمة السر
   الجديدة.
2. عميل متصل فعلياً بسوكت حيّ، `reset-password` يُنفَّذ — يتحقّق وصول حدث
   `disconnect` فعلي (`io server disconnect`) خلال مهلة معقولة، نفس نمط
   اختبار `SEC-FIX-10`/`toggle` الموجود أصلاً بنفس الملف.

**تحقَّق يدوياً أن كلا الاختبارين يفشلان فعلاً بدون الإصلاح** (`git stash`
مؤقت لـ`routes/auth.routes.js` فقط، مع الإبقاء على ملفَي الاختبار الجديدين):
الأول فشل بـ`Expected: 401, Received: 200` بالضبط — التوكن المسروق استمر
بالعمل بعد إعادة التعيين. الثاني (منفرداً بعد إعادة الأول لحالته الطبيعية،
لتفادي غموض cascade-skip بالوصف المتسلسل) فشل بـ`Expected: "io server
disconnect", Received: null` — لم يصل أي `disconnect` إطلاقاً خلال المهلة،
لأن `disconnectSockets` لم تكن تُستدعى إطلاقاً بالكود القديم. استُعيد
الإصلاح بعدها (`git stash pop`)، نجاح كامل لكلا الاختبارين.

المجموعة الكاملة: **368/368** (كانت 365، +3 هنا [SEC-FIX-RESETTOKENVER-01]
+ [SEC-FIX-ERRORALERTTIMEOUT-01] أدناه معاً) — صفر تراجع.

## نطاق متروك عمداً
لم يُدقَّق أي مسار آخر "غير مصادَق" مشابه بحثاً عن نفس النمط — هذا الموقع
تحديداً (المسار الوحيد بالمشروع الذي يعدّل `password_hash` دون أن يكون
`/me/*` مصادَقاً) اكتُشف بمقارنة مباشرة مع `/me/password` المجاور، لا بمسح
شامل مُعلَن لكل مسار بالملف.

# [SEC-FIX-ERRORALERTTIMEOUT-01] تنبيه الأخطاء (`services/error-alert.js`) بلا مهلة زمنية — قد يؤخّر `gracefulShutdown()` بعد عطل حقيقي بلا أي حد

## الخطورة
متوسط — يتطلَّب تزامن ظرفين معاً (عطل `uncaughtException` حقيقي + انقطاع/تعليق
متزامن بطرف Resend بالضبط تلك اللحظة) ليظهر أثره الفعلي، ولا يوقف حلقة الحدث
نفسها (طلبات أخرى تستمر بالمعالجة أثناء الانتظار) — لكن الأثر عند وقوعه حقيقي:
تأخير غير محدود لإعادة تشغيل عملية بحالة قد تكون فاسدة أصلاً، بالضبط باللحظة
التي يهم فيها التعافي السريع أكثر من أي وقت آخر.

## الاكتشاف
جولة تدقيق ثامنة، محور مراجعة كل موقع `resend.emails.send()` بالمشروع
للتأكّد من التزام كل واحد منها بنفس درس `SEC-FIX-EMAILTIMEOUT-01` (مهلة
زمنية إلزامية على أي طلب شبكة خارجي، بعد أن أثبتت جولتان سابقتان أن هذا
النمط يتكرر فعلياً — `services/email.js` ثم `services/offsite-backup.js`
بـ`SEC-FIX-BACKUPTIMEOUT-01`). `services/error-alert.js`'s `alertError()`
كانت لا تزال تستدعي `resend.emails.send()` خاماً بلا `withTimeout` — موثَّق
هذا صراحة كـ"نطاق متروك عمداً" بـ`SEC-FIX-EMAILCANCEL-01`: "لا تعديل على
`services/error-alert.js`... خارج نطاق هذا الإصلاح المخصَّص لمسار OTP
تحديداً، وله فلسفة مختلفة أصلاً: تنبيه أخطاء، **لا يحجب استجابة أي طلب
مستخدم**".

**هذا التبرير غير دقيق لأحد مستدعيَين اثنين فعلياً بـ`server.js`.**
`middleware/security.js`'s `apiErrorHandler` يستدعي `alertError(...).catch(()
=> {})` بالفعل fire-and-forget (التبرير صحيح هناك). لكن `process.on('uncaughtException')`
بـ`server.js` يفعل العكس تماماً — يوثّق صراحة سبب انتظاره (`await`):
"هذا المسار ينتهي بخروج العملية خلال أجزاء من الثانية أحياناً — fire-and-forget
هون قد يعني عدم اكتمال طلب الشبكة لـResend قبل خروج العملية إطلاقاً"، وينفّذ
فعلياً `alertError('uncaughtException', err).catch(() => {}).finally(() =>
gracefulShutdown('uncaughtException'))`. بلا أي مهلة على `alertError()` نفسها،
انقطاع/تعليق حقيقي بطرف Resend بالضبط تلك اللحظة (لا رد، لا إغلاق اتصال —
سيناريو شبكي واقعي، خصوصاً أثناء عطل إنتاج حقيقي قد يصاحبه اضطراب شبكي عام)
كان يعلّق `alertError()` للأبد — وبالتالي `gracefulShutdown()` نفسها لا
تُستدعى أبداً، فالعملية (بحالة `uncaughtException` غير موثوقة أصلاً، هذا هو
بالضبط سبب وجود `gracefulShutdown` عند هذا الحدث دون غيره) تبقى قائمة بلا
حد زمني، بدل إعادة تشغيل نظيفة سريعة من منصة النشر.

## الحل
`withTimeout` (مُصدَّرة أصلاً من `services/email.js`) تُستدعى الآن هنا أيضاً
— نفس النمط الحرفي المستخدَم بـ`sendOtpEmail` (`SEC-FIX-EMAILCANCEL-01`):
`AbortController` حقيقي، `signal`ه يُمرَّر لـ`resend.emails.send()`، ومهلة
(`ALERT_SEND_TIMEOUT_MS`، افتراضياً 15 ثانية — نفس مهلة OTP، رسالة تنبيه
خفيفة لا سبب لمهلة مختلفة) تُلغي الطلب المعلَّق فعلياً عند انتهائها بدل
تركه معلَّقاً بالخلفية. المهلة قابلة للتجاوز عبر `ERROR_ALERT_TIMEOUT_MS`
(نفس نمط `PERF_LOG_SLOW_MS` بـ`middleware/perf-monitor.js`) **فقط** لتمكين
اختبار السلوك الفعلي بمهلة قصيرة بلا انتظار 15 ثانية حقيقية بكل تشغيلة
اختبارات — الإنتاج لا يضبط هذا المتغيّر إطلاقاً فيبقى 15000 دائماً. لا
تغيير على فلسفة "لا يرمي استثناءً أبداً للمستدعي" — `alertError()` تبقى
تُعيد `false` بهدوء عند أي فشل (بما فيه انتهاء المهلة الآن)، تماماً كسابقاً.

## الإثبات
`tests/error-alert.spec.js` — `describe` جديد `[SEC-FIX-ERRORALERTTIMEOUT-01]`:
خادم HTTP حقيقي محلي لا يرد أبداً (نفس أسلوب `SEC-FIX-EMAILCANCEL-01`
بـ`tests/email-prod-safety.spec.js`)، مع `RESEND_BASE_URL` (مدعوم من حزمة
`resend` نفسها عبر متغيّر بيئة، تحقَّق مباشرة من مصدر الحزمة قبل الاعتماد
عليه — لا حاجة لتعديل `services/error-alert.js` لإتاحة `baseUrl` مخصَّص)
موجَّه إليه — يحاكي بالضبط "خادم Resend حيّ معلَّق فعلياً". يثبت الاختبار:
`alertError()` تُرجع `false` خلال أقل من 3 ثوانٍ (لا تعليق أبدي)، والخادم
نفسه يرى الاتصال يُغلَق فعلاً من طرف العميل (إلغاء حقيقي على مستوى الشبكة،
لا مجرد تجاهل الوعد بجافاسكربت).

**تحقَّق يدوياً أن الاختبار يفشل فعلاً بدون الإصلاح** (`git stash` مؤقت
لـ`services/error-alert.js` فقط): فشل بتجاوز مهلة الاختبار بأكملها
(`Test timeout of 30000ms exceeded`) — `alertError()` عَلِقَت فعلياً للأبد
بانتظار خادم لن يرد أبداً، مثبتاً الثغرة حرفياً لا افتراضاً. استُعيد الإصلاح
بعدها (`git stash pop`)، نجاح خلال ~1 ثانية.

المجموعة الكاملة: **368/368** (راجع `SEC-FIX-RESETTOKENVER-01` أعلاه لنفس
الرقم النهائي — كلا الإصلاحين بنفس الجولة) — صفر تراجع.

## نطاق متروك عمداً
لا تعديل على `services/email.js` نفسها (كانت مُصلَحة أصلاً بالكامل —
`SEC-FIX-EMAILTIMEOUT-01`/`SEC-FIX-EMAILCANCEL-01`). لم يُدقَّق ما إذا كان
هناك مستدعٍ ثالث محتمل مستقبلاً لـ`alertError()` قد يعتمد فعلياً على مدة
انتظار أطول من 15 ثانية — لا مستدعٍ كهذا موجود اليوم (اثنان فقط، كلاهما
مذكوران أعلاه).

# [SEC-FIX-CHATACCESS-CHOKEPOINT-01] ضمان بنيوي (لا تدقيق لحظي) لصلاحية قراءة محتوى الشات — نقطة وصول واحدة مفروضة داخلياً + حارس CI

## الخطورة
خطير — لا يُصلح ثغرة نشطة محدَّدة (تحقَّق `SEC-FIX-CHATSCOPE-CONSOLIDATION-01`
أن كل مسار حالي محروس فعلياً اليوم)، لكنه يغلق فئة ثغرة أثبتت نفسها **أربع
مرات متتالية** بخطورة عالية (`SEC-FIX-CHATSCOPE-01/02/03/04`) عبر تحويل
الضمان من "تذكّر كل مطوّر مستقبلي أن يفحص `canAccessRequestChat` بكل موقع
استدعاء جديد" إلى ضمان لا يعتمد على تذكّر أحد.

## القرار — لماذا هذا العمل منفصل، لا جزءاً من جولة تدقيق
هذا ليس اكتشاف/إصلاح بقّة، بل تنفيذ قرار معماري صريح طلبه صاحب الحساب مباشرة
بعد أن لاحظ النمط بنفسه: "Chat authorization has leaked from four different
endpoints across four passes... enumerate every code path... tell me whether
they all go through one shared check". `SEC-FIX-CHATSCOPE-CONSOLIDATION-01`
أجاب على ذلك التدقيق اللحظي (نعم، اليوم). هذا الإدخال ينفّذ الخطوة التالية
المطلوبة صراحة: "A point-in-time audit that has to be redone every pass isn't
a fix... propose the approach before implementing".

## الخيارات المطروحة وسبب اختيار هذا التصميم
عُرضت ثلاثة خيارات على صاحب الحساب قبل البناء: (أ) نقطة وصول بيانات واحدة
مفروضة + حارس CI نصّي رخيص، (ب) قاعدة ESLint حقيقية (تحليل AST)، (ج) middleware
على مستوى الراوتر. اختار (أ) صراحة: يطابق شكل التسريبَين الفعليين تاريخياً
(استعلام نقطة واحدة عبر `getMessages`، واستعلام قائمة بفلترة لكل صف عبر
`GET /chats`)، بتكلفة أقل من (ب) (المشروع لا يملك ESLint إطلاقاً اليوم —
تبنّيه فقط لهذا الفحص استثمار غير متناسب)، وتغطية أوسع فعلياً من (ج) (middleware
على راوتر يلائم مسارات "طلب واحد" فقط، لا شكل `GET /chats` القائمة/الفلترة
لكل صف — بالضبط حيث ظهر آخر تسريب فعلي `SEC-FIX-CHATSCOPE-04`).

## الفحص الذي سبق التصميم
بحث مباشر عن كل استعلام SQL يلمس محتوى جدول `messages` بكامل المشروع (لا
افتراضاً) كشف ثلاثة أشكال فعلية:
1. **نقطة وصول واحدة قائمة أصلاً** (`utils/db-helpers.js`'s `getMessages(requestId)`)
   — الدالة الوحيدة التي تُعيد رسائل طلب بعينه كاملة، لكنها كانت تثق
   بالمُستدعي تماماً لفحص `canAccessRequestChat` *قبل* استدعائها — بلا أي
   فرض داخلي.
2. **استعلام قائمة مستقل تماماً** (`routes/chat.routes.js`'s `GET /chats`) —
   منطق SQL مكتوب مرتين (عميل/فني) بجملة `WHERE` منفصلة عن
   `canAccessRequestChat` كلياً، بلا أي طبقة تحقق ثانية — بالضبط الموقع الذي
   ظهر به `SEC-FIX-CHATSCOPE-04` سابقاً (جملة `WHERE` كانت خاطئة، ولا شيء
   آخر كان سيمنع النتيجة الخاطئة من الوصول للعميل).
3. **قراءة رسالة مفردة لأجل بلاغ** (`report-message`'s `SELECT * FROM
   messages WHERE id=? AND request_id=?`) — مقيَّدة أصلاً بفحص الراوت الأب،
   لا خطر تسرّب مستقل، لكنها استعلام SQL ثالث خارج الملف الأول.

## الحل
**١. `utils/errors.js` (جديد):** صنف `ForbiddenError` بسيط (`status=403`،
`code='AUTH_FORBIDDEN'` افتراضياً) — يفرّق "قرار صلاحية متوقَّع" عن "فشل غير
متوقَّع"، حتى تقدر `apiErrorHandler` تترجمه صح.

**٢. `middleware/security.js`'s `apiErrorHandler`:** فرع جديد أول شيء داخل
معالجة الخطأ — `err instanceof ForbiddenError` → `res.status(err.status).json({error: err.message, code: err.code})`
مباشرة، **قبل** مسار التسجيل/التنبيه (`console.error`+`alertError`) العام —
نفس معاملة أخطاء التحقق الأخرى (حجم/نوع ملف) بهذا الملف بالضبط. هذا هو
الجزء الذي يضمن أن أي حارس يرمي `ForbiddenError` مستقبلاً (بأي موقع بالمشروع،
لا فقط الشات) يترجَم لاستجابة 403 نظيفة تلقائياً، لا 500/400 عامة.

**٣. `utils/db-helpers.js`'s `getMessages(requestId)` → `getMessages(user, requestId)`:**
تجلب الطلب داخلياً، تستدعي `canAccessRequestChat`، ترمي `ForbiddenError` عند
الرفض — **قبل** أي استعلام على `messages` نفسها. مواقع الاستدعاء الأربعة
الحالية (`routes/chat.routes.js`) أبقت فحصها الصريح المسبق كما هو (يحمي أيضاً
عمليات جانبية سابقة — إدراج رسالة، بث Socket.IO — لا بديل عنه)؛ هذا الفحص
الداخلي طبقة إضافية مستقلة لا تعتمد على انضباط أي مُستدعٍ، حالي أو مستقبلي.

**٤. `getChatsList(user)` (جديدة، `utils/db-helpers.js`):** نُقل منطق
`GET /chats` بالكامل هنا (نفس استعلامَي SQL بلا تغيير)، مع **طبقة دفاع ثانية
مستقلة**: `filterChatRowsForUser(rows, user)` تُعيد استخدام `canAccessRequestChat`
الحقيقية (لا نسخة SQL موازية) لفلترة كل صف بعد الاستعلام. حتى لو انحرفت
جملة `WHERE` مستقبلاً (بالضبط ما حدث فعلياً بـ`SEC-FIX-CHATSCOPE-04`)، هذا
الفلتر يمنع تسرّب أي صف لا يستحقه المستخدم دون الاعتماد على تذكّر مطوّر
مستقبلي. `customer_id`/`technician_id` أُضيفا للـ`SELECT` فقط لتمكين الفلتر،
ويُحذفان قبل الإرجاع (لا تغيير بشكل استجابة العميل).

**٥. `getMessageForReport(requestId, messageId)` (جديدة):** نقل بحت لاستعلام
`report-message` — بلا فحص داخلي إضافي (مُبرَّر صراحة بتعليق بالكود: مقيَّد
أصلاً بفحص الراوت الأب + `request_id` بجملة `WHERE` نفسها). الهدف: نقطة
وصول واحدة معروفة لكل قراءة من `messages`، لا استثناء بلا تفسير.

**٦. `scripts/check-chat-content-chokepoint.js` (جديد) + خطوة CI جديدة
(`backend-tests.yml`):** سكربت نصّي (لا ESLint، لا تبعية جديدة) يفشل CI لو
ظهر `FROM messages` بأي ملف `.js` خارج `utils/db-helpers.js` أو قائمة
`ALLOWLIST` صريحة بالسكربت نفسه (ثلاثة استثناءات موثَّقة: `config/db.js` —
مهمة صيانة دورية بلا سياق طلب مستخدم؛ `scripts/loadtest/run.js`/`scripts/restore-db.js`
— أدوات تشغيلية خارج مسار الإنتاج؛ `routes/protected-uploads.routes.js` —
يجلب `request_id` فقط، لا `body`، لا تسرّب محتوى). ليس تحليل AST حقيقياً —
مجرد grep نصّي يمكن خداعه بإعادة صياغة SQL — لكنه يمنع الحالة الشائعة
الفعلية (مطوّر يكتب استعلاماً مباشراً براوت جديد) بكلفة سطرين بملف `package.json`
وخطوة CI واحدة، لا تبنّي أداة جديدة كاملة.

## الإثبات
**`tests/chat-access-chokepoint.spec.js` (ملف جديد)** — اختبارات وحدة مباشرة
(تستدعي الدوال الحقيقية مباشرة، لا عبر HTTP) عمداً: الهدف إثبات أن الحارس
نفسه يعمل حتى لو حُذف كل فحص `canAccessRequestChat` من كل مسار HTTP اليوم —
شيء لا يمكن إثباته باختبار HTTP وحده طالما الفحوصات الموضعية الحالية لا تزال
قائمة (وتبقى قائمة عمداً، دفاع بعمق):
1. طرف خارجي تماماً عن الطلب: `getMessages(outsider.user, r.id)` (بلا أي
   فحص `canAccessRequestChat` مسبق من طرف الاختبار) ترمي `ForbiddenError`
   فعلياً — `status=403`، `code='AUTH_FORBIDDEN'`.
2. صاحب الطلب الحقيقي: نفس الاستدعاء المباشر ينجح، يُرجع الرسائل.
3. راوت Express حقيقي (مُصغَّر، بـ`apiErrorHandler` الفعلي مُركَّبة) يرمي
   `ForbiddenError` — طلب HTTP فعلي يصل بـ`403` و`{error, code}` صريحين،
   لا استجابة خطأ عامة (400/500).
4. `filterChatRowsForUser` مع صفوف مُصطنَعة (أحدها لا يستحقه المستخدم) —
   الفلتر يستبعده بمعزل تام عن أي استعلام SQL حقيقي.

**تحقَّق يدوياً أن الأربعة يفشلون فعلاً بدون الإصلاح** (`git stash` مؤقت
لـ`middleware/security.js`/`routes/chat.routes.js`/`utils/db-helpers.js`
فقط، مع إبقاء `utils/errors.js` وملف الاختبار الجديدين): فشل حقيقي بكل
الأربعة — الاستدعاء بالتوقيع القديم (`getMessages(requestId)`) يتجاهل معامل
`user` الجديد بصمت (JS لا يرفض معاملات زائدة)، فيمرَّر كائن `user` بالخطأ
كـ`requestId` لاستعلام SQL فيرمي خطأ ربط معامل حقيقي من `better-sqlite3` —
إثبات ملموس أن التوقيع القديم لم يكن يفرض شيئاً؛ اختبار `apiErrorHandler`
فشل بـ`Expected: 403, Received: 400` بالضبط؛ `filterChatRowsForUser` فشل بـ
`TypeError: ... is not a function` (لم تكن موجودة). استُعيد الإصلاح بعدها،
نجاح تام لكل الأربعة.

**سكربت CI** (`scripts/check-chat-content-chokepoint.js`): تحقَّق مباشرة —
إدراج سطر `FROM messages` مؤقت بملف خارج القائمة المسموحة يُفشل السكربت
(`exit 1`) برسالة واضحة تشير للملف/السطر بالضبط؛ إزالته يعيده لـ`exit 0`.

**المجموعة الكاملة**: **372/372** (كانت 368، +4 اختبارات جديدة) — صفر تراجع.
تحقَّق أيضاً أن كل اختبارات `tests/chat.spec.js` القائمة (55 اختباراً، تشمل
`SEC-FIX-CHATSCOPE-04`/`SEC-FIX-CHATSCOPE-01`/`SEC-FIX-BLOCKSCOPE-01`/
`SEC-FIX-SUPPORTSPAM-01` regression tests) لا تزال تمر بلا أي تعديل عليها —
إثبات أن إعادة الهيكلة (`getChatsList`/`getMessageForReport`) حافظت على نفس
السلوك الخارجي تماماً، لا مجرد "لم تُكسَر الاختبارات الجديدة".

## نطاق متروك عمداً
لا middleware جديدة على مستوى الراوتر (الخيار (ج) المرفوض أعلاه). لا قاعدة
ESLint حقيقية (الخيار (ب) المرفوض). السكربت النصّي (`check-chat-content-chokepoint.js`)
حماية heuristic لا تحليل AST — قابل للخداع نظرياً بإعادة صياغة SQL غريبة
(مثال: بناء نص الاستعلام من أجزاء مُلحَقة). لم يُطبَّق نفس النمط (نقطة وصول
واحدة + `ForbiddenError`) على أي جدول آخر بالمشروع (مثل `support_messages`،
مُدقَّق أصلاً كمسار مستقل بـ`SEC-FIX-CHATSCOPE-CONSOLIDATION-01`) — هذا
العمل مقصور على `messages` تحديداً، الجدول الوحيد الذي أثبت فعلياً تكرار
هذه الفئة من الأخطاء أربع مرات.

# [SEC-FIX-TOGGLEGHOST-01] `POST /admin/users/:id/toggle` يقدر "يُحيي" حساباً محذوفاً نهائياً — يظهر من جديد كـ"شبح" بقوائم عامة

## الخطورة
متوسط — يتطلّب فعل أدمن (استهداف معرّف محذوف، عمداً أو بالخطأ)، لا يمنح أي
وصول فعلي للحساب نفسه (كلمة السر عشوائية، لا يعرفها أحد)، لكن الأثر
حقيقي وموجَّه لمستخدمين حقيقيين: أي عميل يتصفّح قائمة الفنيين العامة بعد ذلك
يرى حساباً "شبحاً" (اسمه حرفياً "مستخدم محذوف"، بلا صورة ولا خدمات) وكأنه
فني حقيقي متاح — تسريب حالة داخلية (حساب محذوف) لواجهة عامة، لا مجرد ارتباك
إداري داخلي.

## الاكتشاف
جولة تدقيق تاسعة، محور "أي مسار آخر يقدر يعيد ضبط `is_active=1` غير
`config/migrate.js` وقت الإقلاع؟". `utils/db-helpers.js`'s `anonymizeUser`
(تُشغَّل بـ`DELETE /admin/users/:id`/`DELETE /me`) تضبط `is_active=0` و
`deleted_at=CURRENT_TIMESTAMP` وتُصفِّي كل حقل يعرّف صاحب الحساب (`name`
تصير حرفياً `'مستخدم محذوف'`، `email`/`phone`/`password_hash` عشوائية،
`avatar_url`/`city`/`areas`/`services` تصير `NULL`) — لكنها **لا تلمس `role`
إطلاقاً**. `POST /admin/users/:id/toggle` (`routes/admin.routes.js`) يحسب
`newStatus = u.is_active ? 0 : 1` بلا أي فحص لـ`deleted_at` على فرع إعادة
التفعيل (`newStatus===1`) — كل فحوصات هذا المسار (`activeRequest`،
`pendingOffer`) مقصورة عمداً على فرع الإيقاف (`newStatus===0`) فقط، بنفس
منطق `SEC-FIX-PENDINGTOPUP-01` ("الإيقاف عكسي، إعادة التفعيل لا تحتاج نفس
الحراسة") — لكن هذا المنطق لا ينطبق على حساب **محذوف نهائياً**، لا موقوف
مؤقتاً، والفرق بين الحالتين (`deleted_at`) لم يكن يُفحَص هنا إطلاقاً.

**الأثر الفعلي المُتحقَّق منه:** أدمن يضغط `/toggle` على معرّف فني محذوف
مسبقاً (بالخطأ، أو من سجل تدقيق قديم) يُعيد `is_active=1` بلا أن تتغيّر أي
من `role`/`name`/`avatar_url`/`services` المُصفَّاة أصلاً. بما أن
`GET /technicians` (بحث الفنيين العام، `routes/technicians.routes.js`)
يفلتر فقط بـ`WHERE role='technician' AND is_active=1` (بلا فلتر خدمة/مدينة
إلزامي — يظهر بأي تصفّح عام بلا معايير)، وبما أن
`GET /technicians/:id/profile` كان يرفض 404 صراحة لحساب `is_active=0`
(`[FIX-DELETE-01]`) لكنه يعود يعرضه فور `is_active=1`، هذا الحساب "الشبح"
يعود يظهر لأي عميل حقيقي يتصفّح الفنيين — بروفايل باسم "مستخدم محذوف"، بلا
صورة، بلا خدمات مسجَّلة، لا يمكن التواصل معه فعلياً (لا أحد يعرف كلمة سره).
نفس الأثر يطال `topTechs` بـ`GET /admin/stats` (يفلتر `is_active=1` أيضاً).

## الحل
فحص صريح `if (newStatus === 1 && u.deleted_at) return 400
ADMIN_CANNOT_REACTIVATE_DELETED_ACCOUNT` مباشرة بعد حساب `newStatus`، قبل
أي فحص آخر — نفس فحص `deleted_at` المستخدَم أصلاً بـ
`/admin/users/:id/balance` (`SEC-FIX-BALANCETOGHOST-01`)، مُطبَّق هنا على
فرع إعادة التفعيل تحديداً. لا حاجة لفحصه على فرع الإيقاف (`newStatus===0`)
— حساب محذوف يبدأ أصلاً بـ`is_active=0`، فلا يصل هذا الفرع إطلاقاً بحساب
محذوف بالفعل.

## الإثبات
`tests/admin.spec.js` — اختبار جديد: فني جديد يُحذف عبر
`DELETE /admin/users/:id`، ثم محاولة `POST .../toggle` عليه تُرفض بـ400/
`ADMIN_CANNOT_REACTIVATE_DELETED_ACCOUNT`، ويتحقّق مباشرة من قاعدة البيانات
أن `is_active` بقي `0` و`deleted_at` لا يزال موجوداً — وأن الحساب لا يظهر
بـ`GET /technicians` (بحث عام بلا فلتر خدمة، بنفس مدينة الحساب المحذوف).

**تحقَّق يدوياً أن الاختبار يفشل فعلاً بدون الإصلاح** (`git stash` مؤقت
لـ`routes/admin.routes.js` فقط، مع إبقاء ملف الاختبار الجديد): فشل فعلي
بـ`Expected: 400, Received: 200` — الحساب المحذوف كان يُعاد تفعيله بنجاح
فعلياً. استُعيد الإصلاح بعدها (`git stash pop`)، نجاح كامل.

المجموعة الكاملة (راجع تذييل الجولة أدناه): صفر تراجع على أي اختبار `toggle`
آخر قائم (إيقاف/تفعيل حساب عادي، منع إيقاف حساب أدمن آخر، منع إيقاف حساب له
طلب/عرض نشط — كلها تبقى تعمل بلا أي تغيير سلوكي).

## نطاق متروك عمداً
لم يُطبَّق فحص `deleted_at` مماثل على `POST /admin/users/:id/verify` أو
`POST /admin/users/:id/profile` (تعديل اسم/مدينة) — كلاهما يبقى بلا أي أثر
عملي على حساب محذوف طالما `is_active=0` (لا يظهر بأي قائمة عامة تفلتر عليه)،
وهذا الإصلاح تحديداً هو الحارس الوحيد الذي يمنع الحالة الشاذة (`is_active=1`
لحساب محذوف) من الحدوث أصلاً؛ لا حاجة لتكرار نفس الفحص بمسارات لا تقدر على
إحداث هذا الأثر بمفردها. لم يُدقَّق ما إذا كان تطبيق العميل (Flutter) يتعامل
بأمان مع بروفايل فني بحقول `null`/فارغة (avatar/services) — هذا لم يعد
قابلاً للوصول إطلاقاً بعد هذا الإصلاح، فلا حاجة فعلية لتغطيته هناك.

# [SEC-FIX-LIKEESCAPE-01] `escapeLike()` بلا `ESCAPE '\'` بجملة SQL — الإفلات من `%`/`_` كان بلا أثر فعلياً

## الخطورة
منخفض — لا يعيد فتح ثغرة الوايلدكارد غير المقصود التي عالجها `SEC-FIX-05`
أصلاً (أُثبت مباشرة أدناه: النتيجة صفر نتائج، لا مطابقة زائدة)، فقط عطل
اتجاه معاكس (نتائج بحث ناقصة/خاطئة) ضيق النطاق (يتطلّب اسم/مدينة/منطقة تحوي
حرفياً `%` أو `_`، نادر بأسماء ومدن حقيقية).

## الاكتشاف
جولة تدقيق تاسعة. `escapeLike()` (`utils/helpers.js`, `SEC-FIX-05`) تُدرج
`\` قبل كل `%`/`_`/`\` بالمُدخَل — لكن كل استدعاء لها بجملة `LIKE ?` عبر
المشروع (`routes/technicians.routes.js` بحث الفنيين، و`routes/admin.routes.js`'s
`GET /admin/audit-logs` بحث السجل) لم يكن يحمل `ESCAPE '\'' صريحة بجملة SQL
نفسها. **أُثبت مباشرة بسكربت `better-sqlite3` مستقل قبل أي افتراض:** SQLite
لا يُعطي `\` أي معنى خاص بمحارف `LIKE` افتراضياً (بعكس محركات أخرى) — بلا
`ESCAPE`، النمط `%50\%off%` يتطلّب حرف `\` **حرفياً** موجوداً بالبيانات
الفعلية (`%` تبقى وايلدكارد كما هي رغم الـ`\` قبلها)؛ لا صف حقيقي يحمل هذا
الحرف عادةً، فبحث عن أي قيمة تحوي `%`/`_` حرفياً (مثال: اسم فني "خصم 50%")
كان يعيد **صفر نتائج دائماً** — لا الوايلدكارد غير المرغوب (الخطر الأصلي
الذي عالجه `SEC-FIX-05`)، ولا المطابقة الحرفية الصحيحة المقصودة، بل فشل صامت
تام لتلك الفئة الضيقة من عمليات البحث.

## الحل
`ESCAPE '\'' أُضيفت صراحة لكل استدعاء `LIKE ?` مرتبط بـ`escapeLike()` أو
بنفس نمط الإفلات اليدوي (`replace(/[%_\\]/g, ...)`): ثلاثة مواقع
بـ`routes/technicians.routes.js` (بحث بالاسم/الخدمة، المدينة، المنطقة)،
وأربعة بـ`routes/admin.routes.js`'s `GET /admin/audit-logs`
(`actor_name`/`action`/`target_type`/`details`). لا تغيير على منطق
`escapeLike()` نفسها ولا على أي بحث بلا `%`/`_` بالمدخل — يبقى يعمل تماماً
كما كان.

## الإثبات
`tests/misc.spec.js` — `describe` جديد `[SEC-FIX-LIKEESCAPE-01]`: فني بالاسم
"فني % مميز خصم50%" (حرف `%` حرفي وسط الاسم)، بحث `GET /technicians?q=خصم50%`
يجب أن يجده الآن، واختبار ثانٍ يثبت عدم التراجع (بحث نصي عادي بلا `%` يستمر
بالعمل بلا أي تغيير).

**تحقَّق يدوياً أن الاختبار الأول يفشل فعلاً بدون الإصلاح** (`git stash`
مؤقت لـ`routes/technicians.routes.js`، مع إبقاء ملف الاختبار): فشل فعلي
`Expected: truthy, Received: undefined` — الفني بالاسم المحتوي على `%` لم
يكن يظهر بنتائج البحث إطلاقاً رغم مطابقته الفعلية. استُعيد الإصلاح بعدها،
نجاح كامل لكلا الاختبارين.

## نطاق متروك عمداً
لم يُدقَّق أي استدعاء `LIKE` آخر بالمشروع خارج الموقعين المذكورين — بحث شامل
(`grep -rn "LIKE"` بكل `routes/`/`utils/`/`config/`/`services/`) أثبت أن
هذين الموقعين هما الوحيدان اللذان يستخدمان قيمة مستخدِم مباشرة عبر
`escapeLike`/إفلات يدوي مشابه؛ الاستخدامات الأخرى لـ`LIKE` (`config/db.js`
تنظيف الملفات اليتيمة) تستخدم بادئات نصية ثابتة بالكود (`'[image]%'`) لا
مُدخَل مستخدِم، فلا حاجة لـ`ESCAPE` هناك أصلاً.

# مسح مخصَّص — "كود يبدو حامياً لكنه ليس كذلك" — لا نتائج جديدة خارج SEC-FIX-LIKEESCAPE-01

## الخطورة
لا ينطبق — لا كود تغيّر، توثيق فقط.

## السبب
بعد `SEC-FIX-LIKEESCAPE-01` (إفلات `LIKE` بلا `ESCAPE` فعلية)، طلب صاحب
الحساب مسحاً مخصَّصاً — لا جولة تدقيق عامة جديدة — يستهدف فئة الخطأ نفسها
تحديداً بكل المشروع: كود دفاعي (إفلات، تحقق مُدخلات، فحوصات صلاحية، حدود
معدّل، حراس idempotency/تكرار، حدود transaction، مهلات زمنية، تحقق توقيع)
يُتحقَّق أن أثره الفعلي مطابق لما يدّعيه، لا فقط أن الكود موجود ويبدو صحيحاً.

## الفحص
تدقيق مباشر (لا قراءة سطحية) لكل فئة: كل استدعاء `LIKE` بالمشروع (راجع أعلاه)،
كل محدِّدات المعدّل السبعة بـ`middleware/security.js` (كل واحد مربوط فعلياً
براوته الصحيح، بلا `keyGenerator` مخصَّص يُشارك دلواً واحداً بالخطأ)، كل قيد
idempotency/تكرار حقيقي بقاعدة البيانات (`ratings.request_id UNIQUE`،
`offers UNIQUE(request_id, technician_id)`، فحص `message_id IS ?` الآمن مع
`NULL` بـ`message_reports` — مُثبَت بسكربت مستقل)، دوال تنظيف المُدخلات
(`escapeLike`، `clean`، `verifyImageMagicBytes` — تتحقق من `bytesRead`
الفعلي لا المخزن المؤقت الكامل المبطَّن)، سلسلة فلترة محتوى الشات
(`stripInvisibleChars`→`normalizeChatText`→`chatViolationReason`)، كل
`try/catch` يلمس عملية أمنية (لا شيء يفشل بصمت لصالح السماح)، ونقطة الوصول
الوحيدة لصلاحية الشات (`SEC-FIX-CHATACCESS-CHOKEPOINT-01`) — أُعيد التحقق
منها هنا أيضاً، لا تجاوزاً.

## النتيجة
لا موقع إضافي من نفس الفئة عُثر عليه — `SEC-FIX-LIKEESCAPE-01` يبقى الحالة
الوحيدة المكتشَفة بهذا المشروع لهذه الفئة تحديداً، وهي مُصلَحة ومُتحقَّق منها
بالفعل (سكربت `better-sqlite3` مستقل يبني السيناريو العدائي الحقيقي). المجموعة
الكاملة: 375/375، فحص `npm run check:chat-chokepoint`: نظيف. لا التزام كود.

## نطاق متروك عمداً
هذا يوثِّق نتيجة مسح **بتاريخ هذا الإدخال** — لا ضمانة دائمة. أي كود دفاعي
جديد يُضاف لاحقاً يستحق نفس السؤال ("هل يفعل فعلاً ما يدّعيه، لا فقط هل هو
موجود؟") وقت كتابته، لا الاعتماد على أن هذا المسح يغطيه للأبد.

# [SEC-FIX-DEPAUDIT-01] `socket.io-parser` (استنزاف ذاكرة، بلا تسجيل دخول) و`fast-xml-parser` — تصحيح افتراض سابق: لا قفل يمنع الإصلاح

## الخطورة
خطير — `socket.io-parser` تحديداً: ثغرة استنزاف ذاكرة (CVSS 7.5،
GHSA-2m8v-j782-fhvr) قابلة للوصول من **مهاجم بلا أي تسجيل دخول إطلاقاً**،
لأن تفكيك حزمة Socket.IO الخام يحدث بطبقة البروتوكول (`socket.io-parser`)
**قبل** أن تصل الحزمة أصلاً لـ`io.use()` (فحص JWT بـ`services/socket.js`) —
أي عميل يقدر يفتح اتصال WebSocket لنقطة النهاية العامة يقدر يرسل حزمة
مشوَّهة تصل للـparser مباشرة، بصرف النظر تماماً عن أي حارس مصادقة لاحق.

## السبب — سؤال مباشر بعد الجولة التاسعة
بعد `SEC-FIX-LIKEESCAPE-01` وسؤال مباشر عن سطح الاعتماديات (لم يُلمَس إطلاقاً
عبر تسع جولات)، طُلب تدقيق `npm audit` صريح، مع تمييز "قابل للاستغلال فعلياً"
عن "موجود بشجرة الحزم فقط"، وتحديد أيها مقفول بترقية رئيسية (semver-major)
لدى حزمة أعلى وأيها بمب عادي فقط.

## الاكتشاف
`npm audit` أظهر 13 تحذيراً (٢ خطير، ١٠ متوسط، ١ منخفض) — تركيز هذا الإدخال
على الاثنين الخطيرين:

**`socket.io-parser` 4.2.6 (النطاق الضعيف 4.0.0-4.2.6)**: الجولة الرابعة
سجّلت هذا الاكتشاف مع ملاحظة أنه "مُقفَل بترقية `socket.io` نفسها" — هذا
**غير دقيق اليوم** (وربما لم يكن دقيقاً أصلاً حتى وقتها؛ لم يُتحقَّق حينها
بشكل مباشر). فحص مباشر لنطاق `socket.io@4.8.3` (أحدث إصدار 4.x، وهو
بالضبط ما كان مثبَّتاً) يُظهر `"socket.io-parser": "~4.2.4"` — نطاق semver
تلدة يشمل `>=4.2.4 <4.3.0`، وهذا **يشمل فعلاً** الإصدار المُصلَح `4.2.7`.
لا حاجة لأي ترقية لـ`socket.io` نفسها إطلاقاً — `npm audit fix --dry-run`
أكّد هذا مباشرة: تغيير `socket.io-parser` فقط، بلا لمس `socket.io`.

**`fast-xml-parser` 5.9.3 (النطاق الضعيف 5.9.3-5.10.0، DOCTYPE متكرر يُصفّر
حدود توسّع الكيانات)**: تبعية غير مباشرة عبر `firebase-admin` →
`@google-cloud/storage`. **غير قابلة للوصول فعلياً بهذا المشروع**: بحث مباشر
(`grep` لكل استدعاء `.storage()`/`.firestore()`) أثبت أن `services/push.js`
هو المستخدِم الوحيد لـ`firebase-admin`، ويستدعي `admin.messaging()` حصراً —
`firebase-admin` يحمّل كل الحزم الفرعية لكل خدمات Google Cloud بصرف النظر
عمّا يُستخدَم فعلياً، فمسار تفكيك XML موجود بـ`node_modules` لكنه لا يُنفَّذ
أبداً بهذا التطبيق. مع ذلك، أُصلحت أيضاً (بلا كلفة إضافية — بمب عادي ضمن
نفس نطاق `@google-cloud/storage`'s `"fast-xml-parser": "^5.3.4"` المُعلَن،
لا يتطلَّب ترقية `@google-cloud/storage`/`firebase-admin`).

**اكتشاف إضافي أثناء التدقيق**: `body-parser` (منخفض، CVSS 3.7،
GHSA-v422-hmwv-36x6 — قيمة `limit` غير صحيحة قد تُعطِّل حد الحجم بصمت)،
تبعية غير مباشرة عبر `express` — أُصلحت بنفس العملية.

## الحل
`npm audit fix` (بلا `--force`) — يغيّر `package-lock.json` فقط
(`package.json` بلا أي تغيير، تأكيد أن كل الإصلاحات ضمن النطاقات المُعلَنة
أصلاً): `socket.io-parser` 4.2.6→4.2.7، `fast-xml-parser` 5.9.3→5.11.1،
`body-parser` 1.20.5→1.20.6، بالإضافة لتبعياتهم الفرعية
(`@google-cloud/storage` 7.21.0→7.22.0 وحزم داخلية لـ`fast-xml-parser`
الجديدة). صفر ترقيات رئيسية (semver-major) بأي مكان بالسلسلة.

## الإثبات
`npm audit` بعد الإصلاح: **صفر خطير، صفر منخفض، ١٠ متوسط فقط** (راجع "نطاق
متروك عمداً" أدناه). المجموعة الكاملة: **375/375**، صفر تراجع.

**تحقُّق إضافي مُتعمَّد** (طُلب صراحة — الإصلاح بطبقة بروتوكول Socket.IO،
نجاح مجموعة اختبارات عامة وحدها غير كافٍ): `tests/socket-realtime.spec.js`
و`tests/socket-crash-resilience.spec.js` بمعزل، **8/8** — تشمل تحديداً
اتصال حقيقي، انقطاع وإعادة اتصال، `join-request` لغرفة طلب، إرسال/استقبال
رسائل دردشة حقيقية عبر الحدث اللحظي (بترتيب صحيح، بلا تكرار)، **وحزمة
Socket.IO مشوَّهة عمداً لا تُسقط عملية Node** (`SEC-FIX-SOCKETCRASH-01`،
موجودة أصلاً — أعادت تأكيد نفسها هنا بالضبط، وهي أقرب اختبار موجود لسيناريو
CVE نفسه: حزمة بروتوكول مشوَّهة). لا استخدام حالي لمرفقات Socket.IO الثنائية
الحقيقية بهذا المشروع (صور/تسجيلات صوتية تمر عبر REST multipart، لا عبر
Socket.IO) — الجزء الأكثر تحديداً بالثغرة الأصلية (binary attachment
count صفر) غير مُستخدَم أصلاً بحمولات هذا التطبيق، طبقة حماية إضافية
واقعية فوق الإصلاح نفسه.

## نطاق متروك عمداً
الـ١٠ تحذيرات المتوسطة المتبقية (`uuid`/`hyperid`/`autocannon`،
`gaxios`/`google-gax`/`@google-cloud/firestore`/`retry-request`/
`teeny-request`/`@google-cloud/storage` [تحذيرات متوسطة منفصلة عن الخطيرة
أعلاه]) **تُركَت عمداً**، بسببين مختلفين حسب السلسلة:
- **سلسلة `autocannon`**: `devDependency` فقط (`scripts/loadtest/run.js`،
  أداة اختبار حمل محلية، لا تُنشَر للإنتاج إطلاقاً) — صفر خطر إنتاجي.
- **سلسلة `firebase-admin`/Firestore**: غير قابلة للوصول بنفس منطق
  `fast-xml-parser` أعلاه بالضبط — هذا المشروع لا يستخدم Firestore إطلاقاً
  (`.messaging()` فقط). الإصلاح يتطلَّب ترقية `firebase-admin` 13→14
  (`isSemVerMajor: true` صراحة بمخرجات `npm audit`) — ترقية رئيسية حقيقية
  لحزمة مصادقة/دفع إشعارات جوهرية بالتطبيق، تستحق مراجعة اختبار مخصَّصة
  (نطاق تغييرات API، سلوك `admin.messaging().send()` تحديداً) قبل أي تنفيذ،
  لا مجرد `npm audit fix --force` — **لم تُنفَّذ هذه المراجعة بعد، هذا القرار
  مؤجَّل بوعي لا مُغفَل**. سجِّل هذا هنا حتى لا تُعاد نفس الجولة من الصفر لاحقاً:
  الخطورة متوسطة لا خطيرة، وغير قابلة للوصول اليوم — أولوية أقل من أي
  اكتشاف قابل للاستغلال فعلياً.

# [SEC-FIX-ALERTKEYDOS-01] مفتاح كبح تنبيهات الأخطاء مبني من نص خطأ يتحكم به مهاجم غير مصادَق — استنزاف بريد/حصة Resend المشتركة مع OTP

## الخطورة
حرج — قابل للاستغلال بلا أي تسجيل دخول، بلا أي حد معدّل (فشل تحليل JSON
يحدث **قبل** وصول الطلب لأي راوت أو rate limiter)، وأثره يمتد لأبعد من
إزعاج بريدي: حصة Resend نفسها مُستخدَمة لإرسال أكواد OTP بالتسجيل واستعادة
كلمة السر — استنزافها يعطّل دخول مستخدمين حقيقيين جدد، لا مجرد يُغرق صندوق
الأدمن.

## الاكتشاف
جولة تدقيق عاشرة (فحص طازج، لا إعادة قراءة مسارات) شخّصت الثغرة، وتحقَّق
منها مباشرة (`node -e`) قبل أي إصلاح: `JSON.parse` بـNode يُضمِّن مقتطفاً
حرفياً من محتوى المُدخَل الفاشل برسالة `SyntaxError` نفسها — مثال حقيقي:
`JSON.parse('{"a":AAAA_MARKER}')` يرمي `"Unexpected token 'A', "{"a":AAAA_MARKE"... is not valid JSON"`.
`middleware/security.js`'s `apiErrorHandler` يستدعي `alertError('API error',
err)` لأي خطأ غير مصنَّف صراحة (حجم/نوع ملف) — يشمل هذا بالضبط أخطاء
`express.json()` الفاشلة على جسم مشوَّه، والتي تحدث **قبل** أي middleware
مصادقة أو rate limiter (فشل تحليل الجسم بمستوى التطبيق نفسه، لا الراوت).

`services/error-alert.js`'s `alertError()` كانت تبني مفتاح الكبح
(`shouldSend`) من `` `${context}:${message}` `` مباشرة — بما أن `message`
هي `err.message` الحاملة مقتطف المحتوى المُتحكَّم به بالكامل من طرف
المُرسِل، مهاجم يغيّر بايتات الجسم بكل طلب (بلا أي جهد — أي تشويه مختلف
لجسم JSON) يحصل على مفتاح جديد كل مرة، فيتجاوز كبح الـ15 دقيقة كلياً رغم
وجود الآلية. تحقُّق مباشر عبر تطبيق Express حقيقي مصغَّر (لا Error مصطنَع
يدوياً) أثبت الرسالتين مختلفتين تماماً لحمولتين مختلفتين، وبالتالي مفتاحين
مختلفين قبل الإصلاح.

## الحل
مفتاح الكبح الآن (`computeDedupKey`، دالة جديدة مصدَّرة للاختبار) يعتمد
على `err.constructor.name` (نوع الخطأ — لا يتأثر بمحتوى الطلب) + أعلى ٣
أسطر من `err.stack` **بعد** تخطي السطر الأول (رسالة الخطأ نفسها، الحاملة
المحتوى). أسطر stack اللاحقة تحدَّد بمسار تنفيذ الكود (أي دالة استدعت أي
دالة) لا بمحتوى المُدخَل — نفس نقطة فشل حقيقية بكودنا/مكتباتنا تُنتج نفس
الأسطر دائماً بصرف النظر عن قيمة المُدخَل الذي أدّى إليها (مُثبَت تجريبياً:
حمولتان مختلفتان تماماً تُنتجان نفس أول ٣ أسطر stack من `body-parser`).
هذا يحافظ على الهدف الأصلي للكبح (أخطاء حقيقية مختلفة، بمسارات كود مختلفة،
تُنبَّه بشكل مستقل — لا خطأ واحد يُسكِت كل الأخطاء الأخرى لـ15 دقيقة) بينما
يمنع مهاجماً من توليد مفاتيح غير محدودة بتغيير نص لا علاقة له بمسار الفشل
الفعلي. `message`/`stack` الكاملان يبقيان بمحتوى الإيميل نفسه (لفائدة
تشخيصية للمشغِّل) — التغيير مقصور على المفتاح فقط.

## الإثبات
`tests/error-alert.spec.js` — مجموعة جديدة، ٣ اختبارات:
1. **الإثبات الجوهري**: تطبيق Express حقيقي مصغَّر (`express.json()`
   فعلية) يستقبل حمولتين JSON مشوَّهتين مختلفتين تماماً (بايتات مختلفة)،
   يلتقط خطأي `SyntaxError` الحقيقيين الناتجين (لا مصطنَعين)، يتحقق أنهما
   فعلاً برسالتين مختلفتين (`AAAA` مقابل `BBBB`) — ثم يثبت أن `computeDedupKey`
   تُنتج **نفس المفتاح** لكليهما، وأن `shouldSend` تكبح الاستدعاء الثاني
   فعلياً بنفس المفتاح.
2. سياقين مختلفين فعلياً (`'API error'` مقابل `'uncaughtException'`) بنفس
   الخطأ يبقيان يُنتجان مفاتيح مختلفة — التمييز الحقيقي بين مصادر مختلفة لم
   يُفقَد.
3. نوعا خطأ مختلفان (`TypeError`/`SyntaxError`) بنفس context يُنتجان
   مفاتيح مختلفة أيضاً.

**تحقَّق يدوياً أن الاختبارات الثلاثة تفشل فعلاً بدون الإصلاح** (`git
stash` مؤقت لـ`services/error-alert.js` فقط، مع إبقاء ملف الاختبار
الجديد): فشل حقيقي — `computeDedupKey is not a function` (الدالة لم تكن
مُصدَّرة أصلاً بالكود القديم، جزء من الإصلاح نفسه — تصديرها ضروري لإثبات
الثغرة والإصلاح معاً، لا زخرفة). استُعيد الإصلاح بعدها (`git stash pop`)،
نجاح تام لكل الاختبارات الثمانية بالملف (٥ قديمة + ٣ جديدة).

المجموعة الكاملة: **378/378** (كانت 375، +3 اختبارات جديدة) — صفر تراجع.

## نطاق متروك عمداً
لم يُدقَّق كل مكان آخر بالمشروع قد يُنشئ `Error` تحمل محتوى مُدخَل مستخدِم
برسالتها (بحث موسَّع لهذا النمط تحديداً لم يُجرَ) — التركيز هنا على المسار
المُثبَت فعلياً (`apiErrorHandler` ← `alertError`)، وهو المسار الوحيد
القابل للوصول من مستخدم غير مصادَق بلا أي حد معدّل أصلاً.

# [SEC-FIX-MEPWSOCKET-01] `POST /me/password` يزيد `token_version` لكن لا يقطع أي اتصال Socket.IO حي — الوحيد بين كل مسارات إبطال الجلسة الذي فاته هذا

## الخطورة
خطير — نفس فئة `SEC-FIX-RESETTOKENVER-01` بالضبط: تحايل جزئي على حماية
أمنية قائمة فعلياً بالمشروع، عبر المسار المصمَّم أصلاً للتعامل مع نسخة
توكن مسروقة (مستخدم يغيّر كلمة سره عادة لأنه يشك بجهاز/نسخة توكن أخرى
تستخدم حسابه). token_version+1 يُبطل REST فوراً، لكن سوكت حي بالتوكن
القديم — بالضبط سوكت الجهاز المسروق الذي دفع لتغيير كلمة السر أصلاً —
يبقى متصلاً ويستقبل كل التحديثات اللحظية (رسائل شات، تحديثات طلبات،
تحديثات رصيد) حتى ينقطع من تلقاء نفسه لاحقاً.

## الاكتشاف
جولة تدقيق عاشرة (مقارنة كل مسار يزيد `token_version` ببعضه — لا قراءة
مسار واحد بمعزل). أربعة مسارات أخرى تزيد `token_version`: `/auth/logout`
(يقطع السوكت ضمناً — الجهاز نفسه يفصل السوكت يدوياً بعد تسجيل الخروج)،
`/auth/reset-password` (`SEC-FIX-RESETTOKENVER-01`، يستدعي
`disconnectSockets(true)` صراحة)، وثلاثة مسارات أدمن
(`routes/admin.routes.js`: toggle/role/DELETE، كلها تستدعي
`disconnectSockets(true)` صراحة). `POST /me/password`
(`routes/auth.routes.js:362-384`) وحده كان يزيد `token_version` ويُصدر
توكناً جديداً (`sign(updated)`) بلا أي استدعاء لـ`disconnectSockets` —
لا تعليق يوثِّق هذا كقرار متعمَّد، لا اختبار يغطيه (اختبار
`tests/auth-session.spec.js` القائم يثبت فقط إبطال REST، لا السوكت).

## الحل
`try { io.in(\`user-${req.user.id}\`).disconnectSockets(true); } catch (e2) {}`
أُضيف بعد `res.cookie('token', ...)` مباشرة، بنفس نمط
`SEC-FIX-RESETTOKENVER-01`/`admin.routes.js` حرفياً (نفس اسم الغرفة، نفس
`disconnectSockets(true)` لا `()` بلا وسيط، نفس try/catch صامت — فشل قطع
السوكت لا يجوز أن يُسقط نجاح تغيير كلمة السر نفسه).

**ملاحظة مهمة لمن يقرأ هذا لاحقاً**: بعكس reset-password/admin toggle
(حيث الجلسة المقطوعة تخص جهازاً *آخر* غير الطالب، أو حساباً *غير*
المسؤول الذي نفّذ الإجراء)، هنا الجهاز الحالي نفسه (الذي غيّر كلمة سره
للتو ويستمر باستخدام التطبيق) يملك عادة سوكتاً حياً بنفس الحساب — فيُقطع
أيضاً. هذا مقصود ومطلوب أمنياً (المخزون الآمن الوحيد هو "اقطع الكل، أعد
الاتصال بالتوكن الجديد")، لكنه يعني أن الطرف العميل (`sallehly_app`)
يحتاج يعيد الاتصال بالسوكت بالتوكن الجديد الذي أعادته هذه الاستجابة —
عولج في نفس الجولة على `sallehly_app` (`SEC-FIX-MEPWSOCKET-CLIENT-01`،
راجع DECISIONS.md بذلك المستودع)، وإلا كان هذا الإصلاح نفسه سيُحدث فجوة
مؤقتة بالتحديثات اللحظية على الجهاز الحالي بعد كل تغيير كلمة سر ناجح.

## الإثبات
`tests/auth-session.spec.js` — اختبار جديد بنفس نمط اختبار سوكت
reset-password تماماً (سوكت حقيقي عبر `socket.io-client`، لا مُقلَّد):
يتصل بتوكن صالح، ينفّذ `POST /me/password` ناجحاً، يثبت أن السوكت يستقبل
حدث `disconnect` بسبب `'io server disconnect'` (القطع الصريح من الخادم،
لا انقطاع شبكة). **تحقَّق يدوياً fail-before/pass-after** (`git stash`
مؤقت لـ`routes/auth.routes.js` فقط): بدون الإصلاح، فشل حقيقي — `reason`
يبقى `null` (السوكت لا ينقطع إطلاقاً خلال مهلة الانتظار 8 ثوانٍ، تماماً
كالسلوك القديم). المجموعة الكاملة: **379 اختباراً ناجحاً** (كانت 378،
+1)، لا تراجع.

## نطاق متروك عمداً
لا اختبار لسيناريو "أكثر من جهاز متصل بنفس الحساب وقت تغيير كلمة السر" —
`disconnectSockets(true)` تقطع كل الأجهزة بالتصميم (نفس حجة
`SEC-FIX-RESETTOKENVER-01`)، والتحقق أعلاه يكفي لإثبات آلية القطع نفسها
تعمل؛ اختبار تعدد الأجهزة تحديداً غير مضاف هنا لأنه يكرر نفس الآلية
المُختبَرة فعلاً بلا إضافة تغطية جديدة.

# [SCHED-FIX-BOOTRUN-01] المهام الدورية الأربع (نسخ احتياطي، تنظيف ملفات/تسجيلات/إشعارات) لا تُنفَّذ إلا بعد أول فاصل زمني كامل — لا شيء عند الإقلاع نفسه

## الخطورة
حرج — على منصّة نشر تُعيد بناء الحاوية بمعدّل أسرع من أقصر فاصل زمني هنا
(ساعة واحدة لتنظيف التسجيلات المعلَّقة، ٦ ساعات للنسخ الاحتياطي)، وهو
الوضع الفعلي مع كل push/redeploy، فإن أياً من هذه المهام — وعلى رأسها
**النسخ الاحتياطي** — قد لا يُنفَّذ **إطلاقاً** طوال عمر أي نسخة معيَّنة
من العملية. النتيجة: صفر نسخ احتياطية فعلية رغم أن الآلية بأكملها
(`createDbBackup`، تدوير النسخ القديمة، الرفع الخارجي) صحيحة ومُختبَرة
بمعزل — تعطّل صامت تام، بلا أي خطأ ظاهر بالسجلات (لأن `setInterval` نفسه
يُسجَّل بنجاح، فقط لا يُتاح له وقت كافٍ ليُطلَق أبداً قبل إعادة التشغيل
التالية).

## الاكتشاف
جولة تدقيق عاشرة — محور "ما يظهر فقط مع حجم بيانات حقيقي أو بعد أشهر من
التشغيل" وتفاعل نمط النشر (Render، إعادة بناء الحاوية بكل push) مع
افتراضات الكود. الأربعة تسجيلات بـ`config/db.js`
(`if (IS_PROD) setInterval(fn, N).unref();` عند الأسطر 82/162/171/189)
كلها بلا أي استدعاء أول فوري — `setInterval` بتصميمه ينتظر أول فاصل زمني
كامل (N ملي‌ثانية) قبل أول تنفيذ. عملية تعيش أقل من N (وهذا متوقَّع جداً
بدورة نشر نشطة) لا تُنفِّذ الدالة **ولا مرة واحدة** طوال حياتها.

## الحل
كتلة جديدة `if (IS_PROD) { ... }` بعد `migrate(db);` مباشرة (الجداول يجب
أن توجد أولاً — تشغيل هذه الدوال قبل `migrate()` كان سيفشل على قاعدة
جديدة تماماً) تستدعي كل دالة مرة واحدة فوراً، **بالإضافة** للفاصل الزمني
المتكرر — لا بديلاً عنه. الدالتان غير المتزامنتان (`createDbBackup`/
`cleanupOrphanUploads`) بلا `await` هنا عمداً، بنفس فلسفة استدعائهما داخل
`setInterval` أصلاً (لا داعي لتأخير باقي إقلاع الوحدة). كل الدوال الأربع
تمتص أخطاءها داخلياً بالفعل (`try/catch`)، فتشغيلها هنا لا يخاطر بإسقاط
إقلاع السيرفر.

## الإثبات
`tests/scheduled-jobs-boot.spec.js` (جديد) — اختبار وحدة مباشر (لا يستخدم
سيرفر الاختبارات المشترك، الذي يعمل دائماً بـ`NODE_ENV=test` فلا تُنفَّذ
أي مهمة دورية فيه أصلاً). يستورد `config/db.js` طازجاً مرتين على قاعدة
بيانات مؤقتة معزولة: مرة بـ`NODE_ENV=test` لتجهيز المخطّط وزرع تسجيل
معلَّق منتهي الصلاحية، ثم مرة ثانية بـ`NODE_ENV=production` (تحاكي إعادة
تشغيل حقيقية) — يثبت أن التسجيل المعلَّق يُحذَف فوراً (`cleanupExpiredPendingUsers`
متزامنة) وأن ملف نسخة احتياطية يظهر خلال ثوانٍ قليلة (`createDbBackup`
غير متزامنة، يُنتظَر ظهورها بـpolling قصير بدل افتراض فوريتها). **تحقَّق
يدوياً fail-before/pass-after** (`git stash` مؤقت لـ`config/db.js` فقط):
بدون الإصلاح، فشل حقيقي — التسجيل المنتهي يبقى موجوداً (`Expected: 0,
Received: 1`)، بالضبط الفجوة الموصوفة. المجموعة الكاملة: **380 اختباراً
ناجحاً** (كانت 379، +1)، لا تراجع.

## نطاق متروك عمداً
لا اختبار مباشر لـ`cleanupOrphanUploads()` عند الإقلاع (الاختبار الحالي
يغطي الدالتين الأخطر تأثيراً — النسخ الاحتياطي وتنظيف التسجيلات — بما
يكفي لإثبات أن آلية "تشغيل فوري بالإضافة للفاصل الزمني" تعمل لكلا النمطين
(متزامن/غير متزامن) اللذين تشترك بهما كل الدوال الأربع؛ اختبار منفصل
لـ`cleanupOrphanUploads`/`cleanupOldNotifications` تحديداً كان سيكرر نفس
الآلية المُثبَتة فعلاً بلا إضافة تغطية جوهرية جديدة).

# [DEFERRED-AUDIT-10] قائمة مؤجَّلة — نتائج جولة التدقيق العاشرة غير المُصلَحة عمداً

الجولة العاشرة (تقنية جديدة عمداً — لا قراءة راوتات/مزوّدات متسلسلة كالجولات
التسع السابقة، بل محور: تفاعل تبعيات مُحدَّثة، سلوك بحجم بيانات حقيقي/بعد
أشهر، انحراف الإعداد بين البيئات، فجوة بين ما تثبته الاختبارات وما تدّعيه،
تفاعل مكوِّنين كل منهما صحيح بمعزل، وما قد يكسره مهندس جديد بأسبوعه الأول)
قيّمت المستودع **7/10**. من بين ما وُجد، ثلاثة بنود حرجة/عالية استحقّت
إصلاحاً فورياً بقرار صاحب المنتج فوراً بهذه الجلسة — راجع `[SEC-FIX-ALERTKEYDOS-01]`،
`[SEC-FIX-MEPWSOCKET-01]`، `[SCHED-FIX-BOOTRUN-01]` أعلاه. **كل ما تبقى هنا
مؤجَّل عمداً بقرار صريح من صاحب المنتج ("everything else goes on the deferred
list with severity") — لم يُصلَح، ولن يُصلَح بهذه الجلسة.**

كل بند إما مثبَت مباشرة بقراءة الكود (مُعلَّم "مؤكَّد")، أو منقول كما ورده
وكيل التقييم بمنهجية تنفيذ فعلي (لا تخمين) لكن بلا تحقق مستقل ثانٍ مني
(مُعلَّم "غير مُتحقَّق منه مستقلاً"). القرار الصريح بعد هذه الجولة: تقنية
"قراءة الكود المتسلسلة" اعتُبرت مستنفَدة لهذا المستودع — الجولة التالية
المتفَق عليها هي تيليمتري إنتاج حقيقي (أحجام جداول فعلية، مساحة قرص فعلية،
اختبار curl حقيقي على origin الحي لسؤال ثقة البروكسي أدناه) لا مزيد من
قراءة المصدر.

## خطير (آلية مؤكَّدة، قابلية استغلال فعلية غير مُتحقَّق منها)
### `app.js:24` — `trust proxy = 2` قد يجعل كل حدود المعدَّل قابلة للتزوير
التعليق المرافق يوثِّق أن هذه القيمة ضُبطت من سجلات إنتاج حقيقية (Cloudflare
+ Render = هوبان بالضبط). لكن `sallehly.onrender.com` نفسه مُدرَج ضمن
`ALLOWED_ORIGINS` (`config/env.js`) — إن كان أصل Render قابلاً للوصول
مباشرة متجاوزاً Cloudflare (محتمل، غير مُستبعَد)، فكل طلب عبر هذا المسار
يحمل هوباً واحداً فقط لا اثنين، فيصبح `req.ip` (الذي يعتمد عليه كل
`express-rate-limit` بالمشروع) قابلاً للتزوير بالكامل عبر ترويسة
`X-Forwarded-For` مصطنعة — أي حد معدّل (تسجيل دخول، OTP، إلخ) يصبح بلا
قيمة فعلية لمهاجم يتجنَّب Cloudflare عمداً. **الفحص المقترح للتحقق الفعلي:**
قصف `/api/auth/login` بـXFF مزوَّرة من مصدر خارج Cloudflare مباشرة على
عنوان Render، ومراقبة هل يظهر `429` إطلاقاً.

**مُحدَّث — مُغلَق:** راجع `[SEC-FIX-TRUSTPROXY-CLOSED-01]` أدناه. تحقُّق
ميداني مباشر من صاحب المنتج يثبت أن `sallehly.onrender.com` غير قابل
للوصول مباشرة أصلاً (يرجع "not found") — الاستغلال الموصوف هنا غير ممكن
بالوضع الحالي. يبقى الشرط مشروطاً باستمرار عدم إمكانية الوصول المباشر؛
راجع الملاحظتين المفتوحتين بالإصلاح المذكور.

## متوسط
### `config/migrate.js:621` — بيانات اعتماد الأدمن تُعاد ضبطها من متغيّرات البيئة بكل إقلاع
`ADMIN_EMAIL`/`ADMIN_PASSWORD` تُعاد كتابتهما على حساب الأدمن بكل تشغيلة
للسيرفر (مقصود جزئياً لعلم `is_super_admin` حسب تعليق موجود مسبقاً بالملف،
لكن أثر إعادة ضبط كلمة السر نفسها يبدو غير مقصود). أدمن يغيّر كلمة سره من
داخل التطبيق، ثم يُعاد نشر السيرفر (شائع جداً بدورة نشر نشطة) — كلمة السر
تعود بصمت للقيمة القديمة بمتغيّرات البيئة، بلا أي تنبيه.

### لا حصة تخزين (quota) على أي مستخدم — عميل واحد موثَّق يقدر يملأ قرص Render (غير مُتحقَّق منه مستقلاً)
وفق تقدير وكيل التقييم: رفع وسائط شات مستمر ضد نفسه (لا حاجة لأي طرف آخر)
يقدر يملأ القرص المحدود (1GB على Render) خلال ~7 دقائق تقريباً. لا فحص حصة
لكل مستخدم بأي مسار رفع بالمشروع حالياً.

### `scripts/check-chat-content-chokepoint.js` — regex الفحص يمكن تجاوزه بصياغة SQL شائعة، لا استثنائية (غير مُتحقَّق منه مستقلاً)
`JOIN messages` أو استعلام SQL متعدد الأسطر — كلاهما صياغة عادية جداً، لا
حيلة تحايل متعمَّدة — يتجاوزان الفحص وفق تقدير الوكيل. لا اختبار يغطي أداة
الفحص نفسها (chokepoint checker بلا اختبار للـchecker).

### لا تنظيف دوري لأي جدول عدا `notifications`/`pending_users` — `messages`/`audit_logs`/`chat_violations` تنمو للأبد (غير مُتحقَّق منه مستقلاً)
وفق تقدير الوكيل، مسحان كاملان للجدول (full-table scan) يعملان أصلاً كل 6
ساعات ضد جدول `messages` غير المحدود — يتدهور أداؤهما تدريجياً مع الوقت لا
فجأة.

### مهمتا النسخ الاحتياطي وتنظيف الرفوعات مجدولتان لتنطلقا معاً، للأبد
`config/db.js:82` و`config/db.js:162` — كلتاهما بفاصل 6 ساعات مضبوط بنفس
اللحظة تقريباً (وقت إقلاع العملية، والآن أيضاً وقت تشغيل `[SCHED-FIX-BOOTRUN-01]`
الفوري) — تنافس مستمر على I/O القرص نفسه بلا أي تباعد متعمَّد بينهما.

### `nixpacks.toml` ينشر بـ`npm install` لا `npm ci` — تثبيت `package-lock.json` غير مضمون النجاة من أي نشر (غير مُتحقَّق منه مستقلاً)
تفريغ الأصفار الدقيقة لعمل تدقيق التبعيات الأخير (`SEC-FIX-DEPAUDIT-01`)
غير مضمون أن يبقى كما هو بأي نشر لاحق ما دام سكربت البناء يستخدم
`npm install` (يسمح بتحديث `package-lock.json` نفسه) بدل `npm ci` (يُثبَّت
حرفياً أو يفشل).

## منخفض/تافه
- `GET /api/*` لمسار غير معروف يُرجع `200` وصفحة HTML بدل `404` JSON —
  استجابة مضلِّلة لأي عميل يتوقع JSON.
- حدود حجم الجسم غير متماثلة عبر المسارات المختلفة (لم تُفحَص كفجوة أمنية
  فعلية، فقط عدم اتساق ملحوظ).
- `X-Frame-Options: DENY` (Helmet) مُضعَّفة فعلياً بصمت بواسطة
  `frame-ancestors 'self'` بسياسة CSP — الأخيرة هي التي تُطبَّق فعلياً
  بالمتصفحات الحديثة (CSP يتفوّق على X-Frame-Options عند تعارضهما)، فالنية
  المعلَنة بـ`DENY` غير محقَّقة فعلياً (`self` تسمح بتضمين الموقع داخل نفسه).

## تفاعل التبعيات الحديثة — فُحص، سليم بلا أي فعل مطلوب
`socket.io-parser` 4.2.6→4.2.7: الوكيل قارن الفرق بمصدر الحزمة مباشرة —
التغيير الوحيد الآخر يمسّ حزم binary/attachment فقط، والمشروع لا يرسل أي
بيانات ثنائية عبر Socket.IO إطلاقاً (تحقَّق: لا أعمدة BLOB، لا `Buffer` بأي
حمولة `emit`). `fast-xml-parser`: مسار الاستخدام الوحيد (`admin.messaging()`)
مؤكَّد أنه غير قابل للوصول فعلياً. لا فعل مطلوب هنا — ذُكر فقط لإثبات أن
سؤال الجولة العاشرة الأول (تفاعل الترقيات الأخيرة مع محيطها) طُرح وأُجيب
عليه، لا أُغفل.

# [SEC-FIX-TRUSTPROXY-CLOSED-01] `trust proxy = 2` تحقَّق ميدانياً أنه آمن — وإزالة `sallehly.onrender.com` كأصل مسموح ميت بقوائم CSRF/CORS

## الحالة
مُغلَق — كان `خطير (آلية مؤكَّدة، قابلية استغلال فعلية غير مُتحقَّق منها)`
بـ`DEFERRED-AUDIT-10` أعلاه. التحقق الميداني المباشر يثبت أن الاستغلال
الموصوف غير ممكن بالوضع الحالي.

## التحقق الميداني
صاحب المنتج فحص مباشرة: `https://sallehly.onrender.com` يرجع "not found" —
أصل Render الافتراضي غير قابل للوصول مباشرة (متجاوزاً Cloudflare)، الوصول
الفعلي الوحيد عبر النطاقين المخصَّصين (`sallehly.com`/`www.sallehly.com`)
خلف Cloudflare. بهذا، كل طلب فعلي يمر بهوبين بالضبط (Cloudflare edge +
بروكسي Render الداخلي) كما وثَّق `trust proxy = 2` أصلاً — لا مسار حالي
يسمح لمهاجم بتمرير هوب واحد فقط، فتزوير `X-Forwarded-For` لتجاوز
`express-rate-limit` غير ممكن بالوضع الحالي.

لم أتحقق من هذا مباشرة بنفسي بهذه الجلسة — محاولة `curl` مباشرة من بيئة
التنفيذ هذه لم تصل أصلاً (لا استجابة عبر بروكسي الشبكة المُهيَّأ، `HTTP_STATUS:000`)،
فلا يمكنني تأكيد أو نفي النتيجة بشكل مستقل من هنا. هذا تحقق ميداني من صاحب
المنتج نفسه، مُسجَّل هنا كما ورد — بالضبط نوع التحقق (تيليمتري/فحص إنتاج
حقيقي) الذي اتُّفق عليه كخطوة تالية بعد `DEFERRED-AUDIT-10` بدل مزيد من
قراءة الكود.

## ملاحظتان مهمتان تبقيان مفتوحتين — لا تُغلقان بهذا التحقق
1. **هذا يصمد فقط طالما بقي أصل Render غير قابل للوصول مباشرة.** أي تغيير
   مستقبلي بإعداد Render (تفعيل الوصول المباشر لأي سبب — تصحيح أخطاء،
   اختبار مؤقت...) يعيد فتح هذه الثغرة فوراً وبصمت، بلا أي تنبيه بالكود
   نفسه يذكّر بهذا الشرط. لا آلية مراقبة/تنبيه آلي تكتشف تغيّر هذا
   الافتراض — يعتمد كلياً على تذكّر بشري (وعلى وجود هذا القيد الموثَّق هنا).
2. وجود `sallehly.onrender.com` بقوائم `ALLOWED_ORIGINS`/`IO_CORS_ORIGINS`
   (`config/env.js`) أصبح بلا أي غرض فعلي — لا مسار وصول مباشر له، ولا أي
   عميل حقيقي يستخدمه (تطبيق Flutter يضرب `sallehly.com` افتراضياً،
   `AppConfig.baseUrl` بمستودع `sallehly_app` — لا أي إشارة لـ`onrender.com`
   بذلك المستودع إطلاقاً)، ولا أي إشارة أخرى له بأي مكان بالمستودعين
   (بحث شامل: الظهور الوحيد كان هاتين القائمتين بالضبط). أصل مسموح مُدرَج
   صراحة لكنه ميت فعلياً يوحي بصلاحية مسار لا يعمل، ويُوسِّع سطح الثقة
   بلا فائدة — أُزيل أدناه.

## الحل
`sallehly.onrender.com` أُزيل من مصفوفتَي `ALLOWED_ORIGINS` (فحص CSRF لـ
Origin/Referer، `middleware/security.js`) و`IO_CORS_ORIGINS` (سماحية CORS
لـSocket.IO، `services/socket.js`) بـ`config/env.js` — كلا القائمتين الآن
تقتصران على النطاقين الحقيقيين المخصَّصين فقط. تعليق جديد بمكانهما يوثِّق
أن إعادة إضافة أي أصل `onrender.com` لاحقاً يجب أن تكون قراراً صريحاً عند
تلك اللحظة (لو أُعيد فتح الوصول المباشر لسبب ما)، لا استمراراً "احتياطياً"
بلا مبرِّر حالي.

## الإثبات
`tests/allowed-origins-config.spec.js` (جديد) — اختبار وحدة مباشر (بنفس
أسلوب `tests/email-prod-safety.spec.js`، يستورد `config/env.js` طازجاً
بـ`NODE_ENV=production` بمعزل عن سيرفر الاختبارات المشترك الذي يعمل دائماً
بـ`NODE_ENV=test`، حيث القائمتان أصلاً تحملان نطاقات محلية لا نطاقات
الإنتاج فلا تُختبَران هناك إطلاقاً). يثبت أن كلا `ALLOWED_ORIGINS`/
`IO_CORS_ORIGINS` بالإنتاج لا يحملان `sallehly.onrender.com` بعد الآن،
ويحملان النطاقين الحقيقيين فقط (بالضبط اثنين، لا أكثر). **تحقَّق يدوياً
fail-before/pass-after** (`git stash` مؤقت لـ`config/env.js` فقط): بدون
الإصلاح، فشل حقيقي — كلا الاختبارين يفشلان (`toContain` يثبت وجود الأصل
الميت فعلياً بكلا المصفوفتين). المجموعة الكاملة: **382 اختباراً ناجحاً**
(كانت 380، +2)، لا تراجع.

## نطاق متروك عمداً
لا آلية آلية (health-check دوري، تنبيه) تراقب استمرار عدم إمكانية الوصول
المباشر لأصل Render — الملاحظة الأولى أعلاه تبقى اعتماداً بشرياً بحتاً،
خارج نطاق هذا التغيير.

**مُحدَّث:** الملاحظة الأولى أعلاه مغطّاة الآن آلياً — راجع
`[SEC-FIX-TRUSTPROXY-WATCHDOG-01]` أدناه.

# [SEC-FIX-TRUSTPROXY-WATCHDOG-01] فحص آلي جديد بـpost-deploy-smoke-test.yml يفشل CI لو صار sallehly.onrender.com قابلاً للوصول مباشرة

## الخطورة
منخفض (كإصلاح بحد ذاته — لا ثغرة، مراقبة وقائية) لكنه يغلق فجوة حقيقية:
`[SEC-FIX-TRUSTPROXY-CLOSED-01]` أغلق بند `trust proxy = 2` بناءً على تحقق
ميداني بلحظة واحدة، ووثَّق صراحة أن هذا يصمد فقط طالما بقي أصل Render غير
قابل للوصول مباشرة — بلا أي آلية تراقب استمرار هذا الشرط. أي انحراف إعداد
لاحق بـRender (تفعيل الوصول المباشر لأي سبب) كان سيعيد فتح ثغرة تزوير
`X-Forwarded-For` بصمت تام، بلا أي تنبيه لأحد.

## الحل
خطوة جديدة بـ`.github/workflows/post-deploy-smoke-test.yml` (يعمل أصلاً
على كل push لـ`master` + تشغيل يدوي) — تفحص `https://sallehly.onrender.com/api/requests`
بنفس منطق الفحص الموجود أصلاً لـ`sallehly.com/api/requests` بالخطوة الأولى:
إن رجعت `401` (يعني طلب فعلي وصل لـmiddleware المصادقة الحقيقي بتطبيقنا —
أي الأصل قابل للوصول مباشرة ويخدم التطبيق)، يفشل الـCI فوراً برسالة توضّح
السبب الدقيق وتُحيل لهذا التوثيق. أي رد آخر (صفحة "not found" من Render
نفسها، فشل اتصال، مهلة) يعني الافتراض لا يزال صحيحاً، فيمر الفحص بنجاح.

## الإثبات
لا اختبار Playwright ممكن هنا (يعتمد على بنية تحتية خارجية حقيقية — نفس
قيد الخطوة الأولى الموجودة أصلاً بهذا الملف، التي لا تحمل أي اختبار وحدة
مستقل أيضاً لنفس السبب). التحقق تم بطريقتين مباشرتين بدلاً من ذلك:
1. صحة صيغة YAML (`python3 -c "import yaml; yaml.safe_load(...)"`) — سليمة.
2. صحة صياغة Bash (`bash -n`) على السكربت المُستخرَج فعلياً من الملف —
   سليمة.
3. محاكاة منطق الفحص الثلاث حالات المحتملة (`curl` مُزيَّفة تُرجع القيمة
   المطلوبة): `401` (الأصل قابل للوصول) → يفشل صراحة كما هو متوقَّع؛ `404`
   (رد Render نفسه) → ينجح؛ `000` (فشل اتصال/DNS) → ينجح. الثلاثة سلوكها
   مطابق للمتوقَّع تماماً.

## نطاق متروك عمداً
هذا الفحص يعمل فقط بعد push فعلي لـ`master` (أو تشغيل يدوي) — لا يعمل
دورياً بمعزل عن أي نشر (مثال: cron يومي بصرف النظر عن حدوث push). لو مضت
فترة طويلة بلا أي push لـ`master`، انحراف بإعداد Render خلال تلك الفترة
لن يُكتشَف حتى أول push تالٍ. مقبول بحكم أن هذا بالأصل post-*deploy* smoke
test مرتبط بحدث نشر، لا مراقبة مستمرة عامة — إضافة جدولة cron مستقلة كانت
لتوسّع نطاق هذا التغيير بلا طلب صريح لذلك.

# [FEAT-ADMINREQUESTDETAIL-01] `GET /admin/requests/:id` — أول endpoint يجمع صورة كاملة لطلب واحد (الطلب + العروض + المحادثة) للحكم بنزاع

## الخطورة
لا ينطبق (ميزة منتج جديدة، لا إصلاح ثغرة) — لكن الفجوة التي تسدّها كانت
موثَّقة صراحة بتقرير استقصاء منفصل (راجع محادثة هذا التغيير): أدمن يحاول
الحكم بنزاع بين عميل وفني كان مضطراً يجمّع الصورة يدوياً من ثلاث شاشات
منفصلة، وبلا القدرة على فتح المحادثة إطلاقاً رغم أن الصلاحية موجودة أصلاً
بالباك إند. أي قرار كان تخميناً جزئياً.

## الاكتشاف
جولة استقصاء مخصَّصة لتوفّر أدوات حل النزاعات لدى الأدمن (لا قراءة كود
تسلسلية — تتبّع مسار "ماذا يحدث فعلياً عند نزاع؟" عبر كل الراوتات ذات
الصلة). ثلاث نقاط تحديداً:
1. لا `GET /admin/requests/:id` إطلاقاً — القائمة الوحيدة (`GET /requests`
   بفرع `role==='admin'`) صف مسطَّح واحد لكل طلب (id/service/status/أسماء)،
   بلا عروض، بلا محادثة، بلا `cancel_reason`/`cancelled_by`/`cancelled_at`.
2. `cancel_reason`/`cancelled_by`/`cancelled_at` (`config/migrate.js:267-269`)
   تُسجَّل فعلياً منذ `POST /admin/requests/:id/cancel` — لا أي استجابة تُعيدها.
3. `canAccessRequestChat` (`utils/helpers.js:38-43`) تسمح لـ`role==='admin'`
   صراحةً بقراءة أي محادثة — لا أي راوت أدمن يستدعيها.

## الحل
`GET /admin/requests/:id` (`requireRole('admin')`) يُعيد ثلاثة أجزاء بطلب
واحد:
- **الطلب كاملاً**: `SELECT r.*` مع أسماء/هواتف العميل والفني (`LEFT JOIN`
  للفني — قد يكون `NULL` قبل قبول عرض) و**اسم من ألغى الطلب** (`LEFT JOIN`
  إضافي على `cancelled_by`) — `cancel_reason`/`cancelled_by`/`cancelled_at`
  موجودة أصلاً ضمن `r.*`، لم تكن تحتاج أي عمود جديد، فقط عرضها.
- **كل العروض** المُقدَّمة على الطلب (لا المقبول فقط) مع اسم كل فني مُقدِّم —
  تاريخ التفاوض الكامل يهمّ عند الحكم بنزاع (هل كان هناك عرض أرخص رُفض؟
  هل تأخّر الفني بالرد؟).
- **المحادثة الكاملة** عبر `getMessages(req.user, id)` — الدالة المشتركة
  الحقيقية نفسها المستخدَمة بـ`GET /requests/:id/messages`، **لا نسخة SQL
  موازية** (نفس درس `SEC-FIX-CHATACCESS-CHOKEPOINT-01` بالضبط: نسخة مكرَّرة
  يدوياً من فحص الصلاحية قد تنحرف عن الأصل مستقبلاً بصمت). عمداً **بلا**
  استدعاء `markChatRead` — عرض الأدمن للمحادثة لا يجوز أن يغيّر مؤشر "تمت
  المشاهدة" الخاص بالطرفين الفعليين (مُثبَت باختبار مباشر على جدول
  `chat_reads` نفسه، لا افتراضاً).

**قرار نطاق متعمَّد**: لا محاولة لربط قيود دفتر الأستاذ (`ledger`) بالطلب.
الجدول لا يحمل عمود `request_id` إطلاقاً؛ الرابط الوحيد المتاح نص حر بحقل
`note` (مثال: `"خصم عمولة الطلب رقم 42"`) — مطابقة نصية عليه كانت ستكون
هشة وعرضة للانحراف الصامت، بالضبط فئة `SEC-FIX-LIKEESCAPE-01` ("يبدو
فلترة صحيحة لكنه ليس كذلك دائماً"). `requests.commission_charged` (عمود
موجود أصلاً، ضمن `r.*`) هو المصدر الموثوق فعلاً لمعرفة "هل خُصمت عمولة
على هذا الطلب تحديداً وكم" — لا حاجة لأي `JOIN` هش إضافي.

## الإثبات
`tests/admin-request-detail.spec.js` (جديد، 6 اختبارات، سيناريوهات حقيقية
كاملة — تسجيل عميل/فني، إنشاء طلب، تقديم عرض، قبوله، تبادل رسالتين
حقيقيتين، لا بيانات مصطنعة يدوياً):
1. طلب مكتمل الخطوات: الرد يحمل اسم العميل/الفني، العرض بحالته الصحيحة
   (`accepted`)، والرسالتين بترتيبهما الصحيح مع `sender_name` الصحيح لكل منهما.
2. بعد `POST /admin/requests/:id/cancel`: `cancel_reason`/`cancelled_by`/
   `cancelled_at`/`cancelled_by_name` تظهر كلها بالضبط كما أُدخلت.
3. طلب بلا فني مُعيَّن بعد: `technician_name` فارغة، `offers`/`messages`
   فارغتان — بلا أي خطأ (لا افتراض ضمني بوجود فني).
4. معرّف غير موجود: `404`.
5. عميل أو فني (لا أدمن): `403` — نفس حماية `requireRole` المستخدَمة بكل
   مسارات الأدمن الأخرى.
6. **تحقّق مباشر من جدول `chat_reads` نفسه** (لا من شكل الرد): عدد الصفوف
   لهذا الطلب قبل وبعد استدعاء الأدمن للمسار متطابق تماماً — إثبات ملموس
   أن عرض الأدمن لا يُسجَّل كـ"مشاهدة" لأي طرف.

**تحقَّق يدوياً fail-before/pass-after** (`git stash` مؤقت لـ`routes/admin.routes.js`
فقط، ملف الاختبار الجديد يبقى بالشجرة): بدون الإصلاح، فشل حقيقي — 5 من
أصل 6 اختبارات فشلت فعلياً (المسار غير موجود أصلاً، `404`/`403` المتوقَّعان
لم يصلا لأن الطلب نفسه لم يُعالَج بأي مكان). المجموعة الكاملة: **388
اختباراً ناجحاً** (كانت 382، +6)، لا تراجع.

## نطاق متروك عمداً
لا واجهة عميل بعد تستهلك هذا المسار — بند منفصل تالٍ بنفس هذه الجلسة (راجع
محادثة هذا التغيير: "wire the existing chat-read capability into a
request-detail screen"). هذا الإصلاح خادم فقط.

# [FEAT-REFUND-01] استرداد إداري حقيقي لعمولة طلب — ledger.request_id + POST /admin/requests/:id/refund-commission

## الخطورة
متوسط-مرتفع — البند #3 من أدوات حل النزاعات المعتمدة، وأول جزء من هذه
الجولة يلمس بيانات مالية حقيقية بقاعدة إنتاج فعلية (تحقَّقنا مباشرة عبر
Render قبل البدء — راجع "الإثبات" أدناه). قبل هذا الإصلاح، الأداة الوحيدة
المتاحة لتعويض فني كانت `POST /admin/users/:id/balance` — تكتب دائماً
كـ'تعديل يدوي من الإدارة' بلا أي فرق مرئي بين استرداد نزاع فعلي وأي تعديل
رصيد عشوائي آخر، وبلا أي رابط بالطلب المسبِّب. وبشكل أعمّ: `ledger` لم يحمل
قط عمود يربطها بالطلب المسبِّب لها — أي قيد مالي كان مستحيلاً تتبّع سببه
الفعلي دون قراءة `note` كنص حر (نفس الفجوة التي منعت `FEAT-ADMINREQUESTDETAIL-01`
من ضمّ سجل مالي الطلب بردّه).

## الاكتشاف
البند #3 من قرار البناء المعتمد، بثلاثة شروط صريحة من صاحب المنتج:
(1) قيد الاسترداد يجب أن يكون مميَّزاً عن التعديل اليدوي العام، (2) عكس
`commission_charged` وإضافة الرصيد للفني معاملة واحدة — استرداد جزئي أسوأ
من عدمه، (3) عند بناء الاسترداد الحقيقي، أضِف `request_id` لـ`ledger` وارحّل
ما هو قابل للاسترجاع، واترك الباقي `NULL` صراحة بدل التخمين لو لم يكن
الاسترجاع آمناً.

قبل الكتابة، صاحب المنتج طلب تحققاً صريحاً من أن هذه الهجرة (تلمس قاعدة
إنتاج حقيقية) آمنة: (أ) نسخة احتياطية حديثة موجودة فعلاً — تحقَّقنا مباشرة
عبر Render logs/الاتصال بالخادم: نسخ كل 6 ساعات من 24 أغسطس حتى اليوم، وأن
النسخة الأحدث تطابق القاعدة الحيّة صفاً بصف بجداول `users`/`requests`/
`ledger`/`messages` وبأحدث `created_at` لكليهما — النسخ الاحتياطي يعكس
الحالة الحيّة فعلياً، لا يتخلَّف عنها. (ب) حجم قاعدة الإنتاج الفعلي —
تبيَّن أنها بيانات اختبار (9 مستخدمين، 26 طلباً، 15 قيد `ledger`)، لا حجم
إنتاج حقيقي — الهجرة والترحيل بحجم تافه، لا مخاطرة أداء.

## الحل
**`ledger.request_id INTEGER`** (`config/migrate.js`) — عمود بسيط قابل لـ
`NULL` بلا `FOREIGN KEY` (لا إعادة بناء جدول؛ `ALTER TABLE ADD COLUMN`
لعمود بلا `DEFAULT` محسوب هو عملية meta-data فقط بـSQLite، بصرف النظر عن
حجم الجدول)، مُضاف بعد كل استدعاءات `migrateTableAddForeignKeys` (لا قبلها)
لضمان بقائه لو أعاد أحدها بناء الجدول لأي سبب مستقبلي. الأسطر الجديدة
(`INSERT`) بـ`routes/requests.routes.js` (نوعا `'طلب مجاني'` و`'خصم عمولة
طلب'`) تمرّر `request_id` مباشرة من الكود من الآن فصاعداً — لا نص حر يُقرأ
لاحقاً لاستنتاجه لأي قيد جديد.

**الترحيل التاريخي**: فقط نوع `'خصم عمولة طلب'` قابل للاسترجاع فعلياً —
ملاحظته مُولَّدة آلياً بالكامل (لا نص مستخدم مطلقاً) وتتبع قالباً ثابتاً
`خصم عمولة الطلب رقم ${r.id}` لم يتغيّر شكله إطلاقاً منذ إدخاله أول مرة
(تحقَّقنا عبر `git log -p` الكامل للملف — لا تخمين). نوع `'طلب مجاني'` يبقى
`NULL` عمداً لكل الصفوف القديمة — ملاحظته النصية عامة بلا أي رقم طلب
مضمَّن، ولا حقل آخر يفكّ الغموض (فني واحد قد يُنجز طلبات مجانية متتالية
بفارق ثوانٍ) — مطابقة بالتوقيت كانت ستكون تخميناً لا استرجاعاً، بالضبط ما
طُلب تجنّبه. نوعا `'شحن رصيد'` و`'تعديل يدوي من الإدارة'` يبقيان `NULL`
للأبد بتصميم متعمَّد — لا علاقة بطلب أصلاً (الأول مرتبط بطلب شحن، الثاني
عام). كل الترحيل داخل معاملة واحدة (all-or-nothing)، مُغلَّف بـ`try/catch`
خارجي لا يُسقط الإقلاع، ومحمي بشرط `WHERE request_id IS NULL` فيكون
idempotent فعلياً — تكرار الإقلاع لا يعيد المحاولة على صفوف رُحِّلت مسبقاً.

**`POST /admin/requests/:id/refund-commission`** (`routes/admin.routes.js`)
— استرداد كامل فقط، لا مبلغ جزئي يُدخله الأدمن؛ المبلغ = `commission_charged`
بالضبط، محسوب من الخادم لا من مدخل مستخدم. `commission_charged` نفسه لا
يُصفَّر أو يُعاد كتابته أبداً (سجل تاريخي لما خُصم فعلياً لحظة الإكمال) —
عمود جديد `requests.commission_refunded_at` وحده يميّز حالة الاسترداد
ويمنع استرداداً مزدوجاً لنفس الطلب. معاملة واحدة (`db.transaction`) تُحدِّث
رصيد الفني، تضبط `commission_refunded_at`، وتكتب قيد `ledger` معاً — لا
يمكن أن ينجح جزء ويفشل آخر. نوع القيد `'استرداد عمولة نزاع'` مختلف تماماً
عن `'تعديل يدوي من الإدارة'` (النوع المستخدَم بمسار تعديل الرصيد العام)،
ومرتبط بـ`request_id` — قابل للتمييز والتتبّع للأبد. فحوصات الحراسة: يرفض
طلباً بلا فني، بلا عمولة مخصومة أصلاً (`NULL` أو `0` — يشمل الفرصتين
المجانيتين)، طلباً استُرِدَّت عمولته مسبقاً، فنياً محذوفاً، وسبباً مفقوداً
أو أقصر من 3 أحرف — نفس نمط `adjustUserBalance` أعلاه بالضبط (`clean`،
حدود الطول، `logAudit`، `sendPush`/`notify` بنفس صيغة `FIX-NOTIF-GAP-01`).
`requireRole('admin')` لا `requireSuperAdmin` — يطابق صلاحية تعديل الرصيد
العام الحالية، لا إجراء أعلى حساسية منه.

## الإثبات
**تحقُّق الشرطين المسبقين على الإنتاج الفعلي** (عبر Render، قبل كتابة أي
كود): نسخ احتياطية فعلية كل 6 ساعات من 24 أغسطس حتى اليوم (`/var/data/backups/`)،
والنسخة الأحدث تطابق القاعدة الحيّة تماماً بعدد الصفوف وأحدث `created_at`
لأربعة جداول (`users`/`requests`/`ledger`/`messages`) — النسخ الاحتياطي
غير متخلِّف عن الواقع. حجم القاعدة الفعلي (9 مستخدمين، 26 طلباً، 15 قيد
`ledger`) بيانات اختبار، لا إنتاج حقيقي.

`tests/db-integrity.spec.js` (`test.describe` جديد، اختبار واحد شامل) —
يحاكي بيانات `ledger` قديمة (بلا `request_id`) بأنواعها الأربعة الحقيقية
كلها، ثم يُعيد تشغيل `migrate()`: يثبت أن `'خصم عمولة طلب'` بـ`note` صحيح
يُشير لطلب موجود يُربَط بمعرّفه الصحيح، وأن نفس النوع بطلب غير موجود أو
بـ`note` غير قابل للتحليل يبقى `NULL`، وأن `'طلب مجاني'` يبقى `NULL` رغم
وجود طلب حقيقي مرتبط منطقياً به (لأن `note` لا يحمل رقمه)، وأن `'شحن رصيد'`
يبقى `NULL` رغم أن `note` يحمل شكلاً مشابهاً ("رقم 5") — يثبت أن الفلترة
بـ`type` لا بمطابقة نصية عامة هي ما يمنع التقاطاً خاطئاً. تشغيل `migrate()`
ثانية يؤكّد idempotency (لا يعيد المعالجة، لا يغيّر ما تُرك `NULL`).

هذا الاختبار كشف عِلّة حقيقية بالاختبار القائم `[DATA-INTEGRITY-02]`
("قاعدة قائمة مسبقاً بلا FK ..."): حلقته العامة لإعادة كتابة الجداول
الثمانية بالشكل القديم كانت تنسخ قائمة الأعمدة *الحيّة* (تشمل `request_id`
الجديد الآن) إلى جدول `ledger` المُعاد إنشاؤه بالشكل القديم (الذي لا يحمله)
— فشل فوري (`no column named request_id`). أُصلحت الحلقة لتقاطع الأعمدة
الحيّة مع الأعمدة الفعلية بالجدول المُعاد إنشاؤه (`PRAGMA table_info`) بدل
افتراض تطابقهما — إصلاح ضروري كشفه هذا التغيير، لا تغيير سلوك مقصود بذاته.

`tests/refund-commission.spec.js` (جديد، 8 اختبارات، سيناريوهات حقيقية
كاملة — تسجيل، طلب، عرض، قبول، إكمال بعمولة حقيقية عبر إعداد مباشر لقاعدة
اختبار معزولة يفرض استهلاك الفرصتين المجانيتين، نفس نمط `db-integrity.spec.js`):
استرداد ناجح (رصيد الفني +2 بالضبط، `commission_refunded_at` يُضبَط،
`commission_charged` يبقى بلا تغيير، قيد `ledger` بنوع `'استرداد عمولة
نزاع'` مرتبط بـ`request_id` الصحيح، ولا أي قيد `'تعديل يدوي من الإدارة'`
ناتج)، استرداد ثانٍ يُرفَض (`REQUEST_ALREADY_REFUNDED`) بلا أي أثر مالي
إضافي، طلب بلا عمولة مخصومة (لم يُكمَل / ضمن الفرصتين المجانيتين) يُرفَض
(`REQUEST_NO_COMMISSION_CHARGED`)، طلب بلا فني يُرفَض (`REQUEST_NO_TECHNICIAN`)،
سبب مفقود أو قصير يُرفَض بلا أي تغيير على الرصيد، معرّف غير موجود `404`،
وعميل/فني (لا أدمن) `403`.

**تحقَّق يدوياً fail-before/pass-after** (`git stash` مؤقت للملفات
الإنتاجية الثلاثة — `config/migrate.js`، `routes/admin.routes.js`،
`routes/requests.routes.js` — كل ملفات الاختبار الجديدة/المعدَّلة تبقى
بالشجرة): بدون الإصلاح، فشل حقيقي — كل الاختبارات الثمانية الجديدة (اختبار
الترحيل السبعة + سبعة من ثمانية اختبارات `refund-commission.spec.js`) فشلت
فعلياً بـ`404` (المسار غير موجود) أو بعمود مفقود؛ السبعة اختبارات القديمة
غير المرتبطة بهذا التغيير استمرت بالنجاح دون أي تأثير. المجموعة الكاملة
بعد استعادة الإصلاح: **397 اختباراً ناجحاً** (كانت 388، +9 — 8 اختبارات
`refund-commission.spec.js` الجديدة بالكامل، زائد اختبار ترحيل واحد جديد
بـ`db-integrity.spec.js` يُحسَب ضمن نفس الملف)، لا تراجع.

## نطاق متروك عمداً
لا واجهة عميل بعد تستهلك هذا المسار — بند منفصل لاحق. `GET /admin/requests/:id`
(`FEAT-ADMINREQUESTDETAIL-01`) لا يزال عمداً بلا `JOIN` لقيود `ledger` رغم
توفر `request_id` الآن — خارج نطاق ما طُلب بهذه الجولة تحديداً، `commission_charged`/
`commission_refunded_at` (كلاهما ضمن `SELECT *` أصلاً) كافيان لعرض حالة
الاسترداد بشاشة تفاصيل الطلب بلا أي `JOIN` إضافي لو طُلب ذلك لاحقاً.

# [FEAT-COMPLAINTOUTCOME-01] إغلاق شكوى يتطلّب إجراءً موصوفاً صراحة — لا مجرد قلب status

## الخطورة
متوسط — البند #4 (الأخير) من أدوات حل النزاعات المعتمدة. قبل هذا الإصلاح،
`POST /complaints/:id/status` كان يسمح بنقل أي شكوى إلى `'resolved'` أو
`'rejected'` بجسم طلب يحمل `status` فقط — لا أي وصف لما حدث فعلياً. النتيجة:
شكوى مُعلَّمة "محلولة" لا تحمل أي دليل أن شيئاً حصل فعلاً — "سجل يكذب"
بتعبير صاحب المنتج الحرفي، وأخطر من ذلك تشغيلياً: بلا خيار شرعي لـ"رُوجعت
ولم يُتَّخذ إجراء"، موظف دعم يواجه شكوى غير مؤسَّسة أمام خيارين فقط — تركها
مفتوحة للأبد، أو اختلاق تعويض/استرداد غير مستحق فقط لإغلاق التذكرة. الأول
يُراكم تذاكر معلَّقة، الثاني خسارة مالية فعلية على شكاوى غير صحيحة.

## الاكتشاف
البند #4 من قرار البناء المعتمد، بشرط إضافي حاسم أُضيف صراحة بعد المسودة
الأولى: "an admin resolving a complaint should be able to attach 'no
action taken' as an explicit outcome, not just cancel/refund/compensate...
forcing a remedy would push admins toward giving undeserved refunds just
to close the ticket. So: an outcome is always required, but 'reviewed, no
action' counts as one, with the reason recorded." — أي: الإلزام ليس على
*نوع مُعيَّن* من الإجراء، بل على *وجود وصف صادق* لما حدث، ويجب أن يكون
"لا إجراء" وصفاً شرعياً بذاته لا استثناءً مُتحايَلاً عليه.

## الحل
عمودان جديدان قابلان لـ`NULL` بجدول `complaints` (`config/migrate.js`، نفس
نمط `ALTER TABLE ADD COLUMN` المُستخدَم بكل الملف): `outcome` (enum ثابت،
لا نص حر — `'no_action'`/`'refund'`/`'compensation'`/`'request_cancelled'`/
`'other'`، يطابق أدوات المنصة الفعلية اليوم: `no_action` = شكوى غير
مؤسَّسة، `refund` = عبر `POST /admin/requests/:id/refund-commission`
الجديد، `compensation` = عبر تعديل الرصيد اليدوي، `request_cancelled` =
عبر `POST /admin/requests/:id/cancel`، `other` = أي شيء آخر موصوف بالنص)
و`outcome_note` (نص وصفي إلزامي، 3-500 حرف، نفس حدود الطول المستخدَمة بكل
أسباب هذا المشروع). لا محاولة تحقّق تلقائي أن الإجراء المُختار حدث فعلياً
(مثلاً لا فحص وجود قيد `ledger` مطابق عند `outcome='refund'`) — هذا يتجاوز
ما طُلب (توثيق صادق، لا نظام تحقّق آلي معقّد)، ويبقى وصفاً يكتبه الأدمن،
كأي حقل `reason` آخر بهذا المشروع.

`POST /complaints/:id/status` (`routes/support.routes.js`) يفرض `outcome`+
`outcome_note` فقط عند الانتقال لحالة *مُغلِقة* (`resolved` أو `rejected`)
— `open`/`in_review` تبقيان بلا أي متطلَّب إضافي، بلا أي تغيير سلوكي.
إعادة فتح شكوى مُغلَقة مسبقاً (`status` يعود لـ`open`/`in_review`) تُصفّر
`outcome`/`outcome_note` القديمين عمداً بنفس الاستعلام — إجراء قديم لشكوى
أُعيد فتحها للتحقيق لم يعد صحيحاً، وإبقاؤه كان سيوحي زوراً بقرار نهائي.
كل إغلاق يُسجَّل بـ`audit_logs` عبر `logAudit` (لم يكن مُستخدَماً بهذا
الملف سابقاً — أُضيف لـ`deps.utils` بنفس نمط `routes/admin.routes.js`).

## الإثبات
`tests/complaints-support.spec.js` — الاختبار القائم `POST /complaints/:id/status
— الأدمن يحدّث الحالة إلى resolved` عُدِّل ليرسل `outcome`/`outcome_note`
(كان سيفشل الآن بدونهما — هذا هو fail-before الحقيقي لهذا التغيير، لا
اختبار مصطنع منفصل). `test.describe` جديد (6 اختبارات، كل واحد يبني شكواه
الخاصة لتجنّب تشابك حالة عبر سلسلة `serial`):
1. إغلاق بلا `outcome` إطلاقاً: `400 COMPLAINT_OUTCOME_REQUIRED`.
2. إغلاق بقيمة `outcome` غير معروفة (خارج الـenum): نفس الرفض.
3. إغلاق بـ`outcome` صحيح لكن `outcome_note` مفقود أو أقصر من 3 أحرف:
   `400 COMPLAINT_OUTCOME_NOTE_REQUIRED` لكلتا الحالتين.
4. **الهدف الصريح المطلوب**: رفض شكوى بـ`outcome='no_action'` ونص سبب
   حقيقي ينجح فعلياً (`200`)، والقيم تُخزَّن وتُرجَع كما هي — عبر رد
   `POST` نفسه وعبر `GET /complaints` أيضاً (لا فرق بين ما يُرجعانه).
5. نقل لحالة غير مُغلِقة (`in_review`): ينجح بلا أي `outcome` — لا تغيير
   سلوكي على المسار غير المُغلِق.
6. إغلاق ثم إعادة فتح (`resolved` بـ`outcome='refund'` ← `open`):
   `outcome`/`outcome_note` يعودان `null` بعد إعادة الفتح.

**تحقَّق يدوياً fail-before/pass-after** (`git stash` مؤقت لـ`config/migrate.js`
و`routes/support.routes.js`، كل ملفات الاختبار تبقى بالشجرة): بدون
الإصلاح، فشل حقيقي — 7 اختبارات فشلت فعلياً (الاختبار القائم المُعدَّل +
كل الستة الجديدة بـ`test.describe` الجديد)، 18 اختباراً آخر بنفس الملف
غير المرتبط بهذا التغيير استمر بالنجاح دون أي تأثير. المجموعة الكاملة بعد
استعادة الإصلاح: **403 اختباراً ناجحاً** (كانت 397، +6 — الاختبار السابع
تعديل لاختبار قائم، لا إضافة)، لا تراجع.

## نطاق متروك عمداً
لا تحقّق آلي أن `outcome` المُختار يطابق إجراءً فعلياً حدث بقاعدة البيانات
(مثلاً `outcome='refund'` بلا التأكد من وجود قيد `ledger` مطابق بـ`request_id`
نفسه) — هذا نظام تحقّق أكبر بكثير مما طُلب؛ الهدف هنا توثيق صادق يكتبه
الأدمن، لا فرض تسلسل عمليات إلزامي بين الإجراء الفعلي وتسجيل نتيجة الشكوى.
هذا يُنهي بناء أدوات حل النزاعات الأربعة المعتمدة (١-٤) بترتيبها المتفق
عليه؛ إعادة التعيين وحذف التقييم يبقيان مؤجَّلين عمداً باتفاق صريح سابق.

# [FEAT-PRIVACYPOLICY-02] `public/privacy.html` هو مصدر الحقيقة الوحيد لسياسة الخصوصية — تصحيح، لا استضافة جديدة

## الخطورة
متوسط-مرتفع — نفس خطورة `FEAT-PRIVACYPOLICY-01` بمستودع `sallehly_app`
(مستند قانوني تُعتمد عليه لنشر Google Play، أي مبالغة فيه بيان زائف)، لكن
هذه النسخة تحديداً **حيّة وفعلية** (`https://sallehly.com/privacy.html`،
`canonical` مضبوط، مربوطة بفوتر الموقع) — بعكس نسخة `sallehly_app` التي
لم تكن مستضافة بعد. أي مستخدم حقيقي اليوم يقرأ هذه النسخة تحديداً.

## الاكتشاف
أثناء تنفيذ خطة استضافة سياسة الخصوصية على نطاق `sallehly.com` (بدل GitHub
Pages)، تبيَّن وجود هذه الصفحة أصلاً — لم تكن معروفة وقت إصلاح `sallehly_app`.
مقارنتها بالنسختين الأخريين (`sallehly_app`/`docs/privacy-policy.html`
المحذوفة الآن، والشاشة داخل التطبيق) كشفت انحرافاً حقيقياً بينها بالفعل:
صف الرقم الوطني **غائب تماماً** من هذه النسخة رغم كونه بيانات حقيقية تُجمع
(`national_number`، إلزامي للفنيين، `routes/auth.routes.js`)، بينما تحمل
هذه النسخة صف "محتوى تذاكر الدعم" (بيانات حقيقية أيضاً، `POST /api/support`)
الذي لم تحمله النسختان الأخريان إطلاقاً — دليل مباشر على أن تعدّد نسخ نفس
المستند يقود للانحراف، بالضبط كما حذّر صاحب المنتج صراحة.

نفس الفجوات الثلاث المُكتشفة بـ`FEAT-PRIVACYPOLICY-01` موجودة هنا أيضاً
(لا عجب — كانت النسخ الثلاث متطابقة تقريباً وقت كتابتها أول مرة): لا ذكر
لصورة إيصال الدفع، لا ذكر لـFirebase Crashlytics بقسم الأطراف الثالثة،
و"تُحذف بياناتك الشخصية نهائياً" مبالغة لا تطابق `anonymizeUser()`.

**اكتشاف إضافي أثناء مراجعة `public/delete-account.html`** (المُشار إليها
من قسم الاحتفاظ بالبيانات): قراءة `DELETE /me` (`routes/auth.routes.js`)
كاملاً — لا الجزء الذي راجعناه سابقاً فقط — كشفت **شرطاً رابعاً حقيقياً
يمنع الحذف** لم يكن موثَّقاً بأي من النسخ الثلاث: رصيد متبقٍّ بالمحفظة
(`freshBalance > 0` → `DELETE_ACCOUNT_BALANCE_REMAINING`) — بالإضافة
للثلاثة الموثَّقة سابقاً (طلب نشط، عرض معلَّق، طلب شحن قيد المراجعة).
`delete-account.html` تحديداً كانت توثّق شرط الرصيد لكنها فاتها شرطا العرض
المعلَّق وطلب الشحن قيد المراجعة — كل نسخة كانت ناقصة بشكل مختلف.

كذلك، جدول "ما الذي يُحذف بالضبط؟" بـ`delete-account.html` كان يبالغ بشكل
أكثر تحديداً من `privacy.html`: يدّعي أن الاسم/البريد/الهاتف/كلمة السر
"تُحذف نهائياً" (فعلياً: تُستبدَل بقيم عشوائية، لا تُحذف)، وأن الصورة
الشخصية "تُحذف نهائياً من الخادم" (فعلياً: `avatar_url` يُصفَّر فوراً
بقاعدة البيانات، لكن الملف نفسه على القرص لا يُحذف مباشرة — يُحذف لاحقاً
عبر `cleanupOrphanUploads` خلال أقل من يوم، لا فوراً)، ولم تذكر التقييمات
أو سجل المحفظة أو صور الإيصالات إطلاقاً رغم بقائها جميعاً بعد الحذف.

## الحل
**`public/privacy.html` أصبح مصدر الحقيقة الوحيد للسياسة العامة** — لا
صفحة جديدة، لا رابط جديد؛ نفس الرابط الحيّ المفهرَس والمربوط بالموقع.
`sallehly_app`/`docs/privacy-policy.html` (النسخة التي لم تكن مستضافة
إطلاقاً) حُذفت نهائياً بدل الإبقاء عليها كنسخة ثانية عرضة للانحراف مجدداً.

تصحيحات `public/privacy.html`: استرجاع صف الرقم الوطني، إضافة صف صورة
إيصال الدفع، الإبقاء على صف تذاكر الدعم (تحقَّق أنه حقيقي — `POST /api/support`
يخزّن عنواناً ونصاً حقيقيين بجدول `support_tickets`)، إضافة Crashlytics
لقسم الأطراف الثالثة، وإعادة صياغة قسم الاحتفاظ بالبيانات ليصف الاستبدال
(anonymization) بدقة بدل "الحذف النهائي"، مع إحالة للتفاصيل الكاملة
بـ`delete-account.html`.

تصحيحات `public/delete-account.html` (أكثر تفصيلاً بطبيعتها، فاحتاجت
تصحيحاً أعمق): الفقرة التمهيدية ووصف meta لم تعودا تدّعيان "نهائياً" بلا
تفصيل؛ جدول "ما الذي يُحذف بالضبط؟" أُعيد كتابته بالكامل — تمييز صريح بين
ما **يُستبدَل** (الاسم/البريد/الهاتف/كلمة السر — قيم عشوائية غير قابلة
للربط، لا حذف) وما **يُحذف فعلياً فوراً** (الرقم الوطني، FCM token) وما
**يُحذف خلال مهلة** (ملف الصورة الشخصية، عبر تنظيف الملفات اليتيمة) وما
**يبقى محفوظاً غير مرتبط بالهوية** (رسائل الشات، التقييمات، سجل المحفظة،
صور الإيصالات — أُضيفت جميعاً كصفوف صريحة بدل تركها بلا ذكر). صندوق التحذير
أُكمل بالشرطين الناقصين (عرض معلَّق، طلب شحن قيد المراجعة). قسم "الاحتفاظ
ببعض السجلات" أُعيد صياغته من ادّعاء غامض ("رقم معاملة سابقة") لتفسير
صريح يطابق الجدول أعلاه.

## الإثبات
كل بند مُتحقَّق مباشرة من الكود المصدري: `routes/topups.routes.js` (الإيصال)،
`pubspec.yaml`+`main.dart` بمستودع `sallehly_app` (Crashlytics)، `utils/db-helpers.js:anonymizeUser`
(الحقول المُستبدَلة بالضبط — تُصفَّر `national_number`/`fcm_token`، تُستبدَل
لا تُصفَّر `name`/`email`/`phone`/`password_hash`/`avatar_url`)، `routes/auth.routes.js:DELETE /me`
كاملاً (الشروط الأربعة الحقيقية بالضبط بترتيب فحصها الفعلي)، `config/db.js:cleanupOrphanUploads`
(تنظيف الملفات اليتيمة كل 6 ساعات + فوراً عند الإقلاع، لا فوراً عند الحذف
نفسه)، و`routes/support.routes.js:POST /support` (تحقُّق أن تذاكر الدعم
بيانات حقيقية، لا ادّعاء زائف بالنسخة الحيّة).

لا اختبار آلي جديد — هذا تصحيح نص/محتوى قانوني على صفحات HTML ثابتة تُخدَم
مباشرة (`express.static('public')`)، لا سلوك برمجي قابل للاختبار.

## نطاق متروك عمداً
- لا مراجعة قانونية رسمية — نفس تحفّظ `FEAT-PRIVACYPOLICY-01`.
- قسم "شروط الاستخدام" الموجود بالنسخة المحذوفة (`sallehly_app`/`docs/privacy-policy.html`)
  لم يُنقَل لـ`public/privacy.html` — لا يوجد قسم مطابق بالنسخة الحيّة أصلاً،
  وإضافته قرار محتوى جديد (لا تصحيح دقّة) يستحق موافقة صريحة منفصلة، لم
  تُطلَب بهذه الجولة.
- **لا آلية آلية تربط `public/privacy.html` (هذا المستودع) بشاشة `PrivacyPolicyScreen`
  (مستودع `sallehly_app`)** — مستودعان منفصلان، بلا CI مشترك يتحقّق من
  التطابق. أي تعديل مستقبلي على أحدهما دون الآخر يعيد فتح نفس فئة الانحراف
  التي عولجت هنا. هذا اعتماد على انضباط يدوي، لا ضمانة هيكلية — يستحق أداة
  تحقّق آلية مستقبلاً (مثلاً: اختبار CI يقارن نص القسمين) إن استمر هذا
  التكرار.

---

# [FEAT-DEDUP-01] لوحة الفني تعرض عمولة ثابتة "2 د.أ" بدل القيمة الحقيقية — البند الأول من مسح تكرار المحتوى/المنطق الجذري

نفس المسح الموثَّق بمُدخَل `FEAT-DEDUP-01` بمستودع `sallehly_app` (طرف
العميل) — راجعه هناك للسياق الكامل عن الثمانية بنود وترتيب معالجتها. هذا
المُدخَل يوثّق التغيير المستقل بهذا المستودع لنفس البند الأول.

## الخطورة
خطير — صحيح بالصدفة فقط لأن كل الباقات الحالية تخصم نفس الرقم (2 دينار).
أول باقة بعمولة مختلفة تجعل لوحة الفني تعرض رقماً ثابتاً لا علاقة له بما
سيُخصَم فعلياً من رصيده — نزاع مالي مباشر بين ما يراه الفني وما يحدث فعلياً.

## الاكتشاف
`public/app.js`, دالة `techDash()` (لوحة الفني، تبويب "الرئيسية"): بطاقة
"ملخص سريع" تعرض `<div class="mini-list-row"><span>العمولة لكل طلب</span><b>2 د.أ</b></div>`
— نص ثابت حرفياً، بينما `GET /api/me` (الذي تستدعيه نفس الدالة أصلاً
لجلب `me`) يُرجع بالفعل `active_commission` الحقيقي غير مُفلتَر (تأكَّد
بقراءة `userPublic()`, `utils/helpers.js` — يُخفي فقط `password_hash`/
`token_version`، لا `active_commission`).

**ملاحظة إضافية اكتُشفت أثناء التحقق (خارج نطاق هذا البند تحديداً):**
نفس الرقم "2" مكرَّر أيضاً كـ`DEFAULT 2` على عمود `users.active_commission`
بالمخطط (`config/migrate.js`)، ويُعاد التأكيد عليه صراحة عند توثيق فني
(`admin.routes.js`)، وكـ`?? 2` احتياطي دفاعي بحسابات العمولة الفعلية
(`offers.routes.js`, `requests.routes.js`, `topups.routes.js`). هذه
الاحتياطات **متّسقة مع قيمة العمود الافتراضية فعلياً** (SQLite يملأ
`DEFAULT` تلقائياً عند أي إدراج صف بلا قيمة صريحة، فالعمود لا يكون `NULL`
عملياً بعد الهجرة) — ليست نفس عيب هذا البند (عرض رقم لا يطابق القيمة
الحقيقية المُخزَّنة)، بل احتياط دفاعي متكرر يعكس نفس افتراض المخطط. لم
تُعالَج هنا لأنها خارج نطاق البند 1 كما حدَّده صاحب المنتج (تكرار عرض، لا
تكرار افتراضي بمنطق حساب)، وتُذكَر فقط لتُدرَج بقرار صاحب المنتج إن رغب
بتوسيع النطاق لاحقاً.

## الحل
البطاقة تعرض الآن `${me.active_commission!=null?me.active_commission+' د.أ':'—'}`
— القيمة الحقيقية المُرجَعة من `GET /api/me`، أو "—" الصريحة إن كانت
فعلاً غائبة (لا تخمين برقم بديل). بطاقة لوحة الأدمن المنفصلة (نفس الملف،
شاشة الباقات، "٢ د.أ / افتراضي") **تُركت كما هي عمداً** — مُعلَّمة صراحة
"افتراضي"، لا تدّعي كونها قيمة حيّة لكل فني، فليست نفس عيب هذا البند.

## الإثبات
لا اختبار Playwright آلي لهذا الملف — تأكَّد أن `public/app.js` لا يحمل أي
تغطية اختبارية بهذا المستودع حالياً (لا اختبار واحد يشير إليه إطلاقاً)،
فهذه فجوة تغطية عامة سابقة على هذا التغيير، لا شيء يمكن إضافته لملف بلا
أي بنية اختبار قائمة له. التحقق يدوي: قراءة مباشرة لاستجابة `GET /api/me`
(تؤكد أن `active_commission` غير مُفلتَرة) ومطابقة نص القالب الجديد يدوياً.

## نطاق متروك عمداً
الاحتياطات الدفاعية (`?? 2`) بمنطق حساب العمولة الفعلي (`offers.routes.js`,
`requests.routes.js`, `topups.routes.js`) لم تُعدَّل — راجع ملاحظة
"الاكتشاف" أعلاه. البنود 2-8 من نفس المسح (طرف الخادم منها) لم تُعالَج
بعد بهذا المُدخَل.

**قرار صاحب المنتج، متابعة:** بعد عرض ملاحظة "الاكتشاف" أعلاه على صاحب
المنتج، القرار الصريح: إبقاء `?? 2` كما هي — الحجة مقبولة (`DEFAULT 2` على
العمود يجعل `NULL` غير قابل للحدوث عملياً)، لكن توثيقها هنا أفضل من حذفها
والاكتشاف لاحقاً أن مساراً ما يُدرج صفاً بلا قيمة صريحة لهذا العمود.

---

# [FEAT-DEDUP-01] البند 4: عدد endpoints قديم بـREADME يقرأ كتحقّق حالي — البند 5: لوحة الفني/العميل تجلب قائمة المحافظات من الخادم ثم تتجاهلها

الدفعة الثانية من نفس مسح `FEAT-DEDUP-01` (راجع المُدخَل أعلاه للسياق
الكامل). البند 5 مستقل بكل مستودع — راجع `sallehly_app`/DECISIONS.md
لتفاصيل طرف العميل (أربع شاشات + تصحيح انحراف تهجئة "عمّان"/"عمان").

## الخطورة
منخفض للبند 4 — توثيقي داخلي فقط، لا أثر إنتاجي. متوسط للبند 5 — أي
محافظة تُضاف مستقبلاً لـ`routes/meta.routes.js` تبقى غائبة عن قائمة
"المحافظة" بلوحة الفني/العميل رغم أن الصفحة *تجلب* `state.meta.cities`
فعلياً عند الإقلاع، فقط لتتجاهله لصالح كائن JS ثابت منفصل — عكس الهدف
الكامل من جلب القيمة أصلاً.

## الاكتشاف
`README-التقسيم.md` يذكر "تم التحقق آلياً إن كل الـ 49 endpoint... موجودين
بالضبط مرة وحدة" — صحيح وقت تقسيم `server.js` (2026-07-31، الكوميت الوحيد
الذي يلمس هذا الملف). العدد الفعلي الحالي (`grep -c` على `routes/*.routes.js`):
**79** — نمو يزيد عن 60% منذ تلك اللحظة بلا تحديث الرقم.

`state.meta = await api('/api/meta')` يُنفَّذ عند الإقلاع (`public/app.js`
سطر 411)، ويحوي `cities` الحقيقية من `routes/meta.routes.js`. لكن
`governorateOptions()` كانت تبني قائمة "المحافظة" حصراً من
`Object.keys(JORDAN_AREAS)` — كائن JS ثابت منفصل بنفس الملف، بلا أي علاقة
بـ`state.meta.cities` المجلوبة أصلاً لنفس الغرض تقريباً.

## الحل
**البند 4:** استُبدل بملاحظة تاريخية مؤرَّخة صراحة، تنص أن الرقم لا
يُحدَّث مع نمو الكود (مع ذكر 79 كمرجع إضافي بنفس التحذير).

**البند 5:** `governorateOptions()` تستخدم الآن `state.meta.cities` عند
توفرها وعدم فراغها، مع `Object.keys(JORDAN_AREAS)` كاحتياط فقط (تعطّل
`/meta` أو عدم اكتمال الجلب بعد بعد). `areaOptions()` (تقسيم محافظة→أحياء)
تُركت بلا تغيير عمداً — `/meta` لا يُرجع تفاصيل الأحياء إطلاقاً، فلا "قيمة
مجلوبة" لتجاهلها أصلاً بهذا الجزء تحديداً.

## الإثبات
البند 4: لا اختبار — تصحيح توثيقي على Markdown. البند 5: لا بنية اختبار
Playwright لـ`public/app.js` (نفس فجوة البند 1 المُسجَّلة سابقاً بهذا
الملف) — تحقّق يدوي عبر محاكاة Node مباشرة لمنطق `governorateOptions()`
الجديد بثلاث حالات (قائمة مجلوبة، `meta` بلا `cities`، `cities` فارغة):
النتائج الثلاث صحيحة (تستخدم القيمة المجلوبة عند توفرها، وتحتاط بمفاتيح
`JORDAN_AREAS` غير ذلك).

## نطاق متروك عمداً
`areaOptions()`/`JORDAN_AREAS` (تقسيم محافظة→أحياء) تبقى ثابتة محلياً
بالكامل — لا معادِل خادمي إطلاقاً حالياً؛ إضافة endpoint لهذا قرار نطاق
أوسع لم يُطلَب بهذه الجولة. البنود 6-8 لم تُعالَج بعد بهذا المُدخَل.

---

# [FEAT-DEDUP-01] البند 6: قائمة حالات الطلب "الحاجزة" مكرَّرة حرفياً بلا مصدر واحد

الدفعة الثالثة من نفس مسح `FEAT-DEDUP-01` (راجع المُدخَلات أعلاه). البند 6
مستقل بكل مستودع — راجع `sallehly_app`/DECISIONS.md لتفاصيل طرف العميل
(getters موجودة أصلاً بـ`RequestModel` كانت تُتجاوَز بمقارنات حرفية).

## الخطورة
متوسط — كل الحالات المكرَّرة هنا هي فحوصات حماية فعلية (حظر إيقاف/حذف/تحويل
دور حساب له طلب نشط) لا منطق عرض فقط. تكرارها بلا مصدر واحد يعني أن أي
حالة جديدة تُضاف لدورة حياة الطلب مستقبلاً (مثلاً حالة وسيطة جديدة) قد
تُحدَّث بموضع وتُنسى ببقية المواضع — فجوة حماية صامتة، لا خطأ ظاهر فوراً.

## الاكتشاف
قائمتان حرفيتان متكرّرتان مستقلتان، بلا أي ثابت مشترك:
- **القائمة الكاملة (5 حالات: 'بانتظار العروض'، 'وصلت عروض'، 'تم اختيار
  عرض'، 'قيد التنفيذ'، 'بانتظار تأكيد الدفع')** — "أي طلب لم يُغلَق بعد
  للعميل أو الفني" — مكرَّرة حرفياً 4 مرات: `admin.routes.js` (إيقاف حساب،
  حذف حساب، تحويل دور عميل→فني) و`auth.routes.js` (`DELETE /me`).
- **القائمة الفرعية (3 حالات: 'تم اختيار عرض'، 'قيد التنفيذ'، 'بانتظار
  تأكيد الدفع')** — "الفني ملتزم بعمل نشط بالفعل" (لا معنى لـ'بانتظار
  العروض'/'وصلت عروض' هنا: `technician_id` يبقى `NULL` طالما لم يُقبَل
  عرض) — مكرَّرة حرفياً 3 مرات: `admin.routes.js` (تحويل دور فني→عميل)،
  `offers.routes.js` ×2 (تقديم عرض جديد، قبول عرض للفني).

## الحل
ثابتان جديدان بـ`config/env.js`، مُمرَّران عبر `deps.constants`
(`server.js`) بنفس نمط `COOKIE_OPTS`/`BASE` الموجود أصلاً:
- `BLOCKING_REQUEST_STATUSES`/`_SQL` — القائمة الكاملة (5 حالات).
- `TECHNICIAN_ACTIVE_JOB_STATUSES`/`_SQL` — القائمة الفرعية (3 حالات).

النسخة الجاهزة لـSQL (`_SQL`) مُحسَبة مرة واحدة عند الإقلاع
(`.map(s => \`'${s}'\`).join(',')`) — الحالات أسماء ثابتة بالكود لا مدخلات
مستخدم، فلا خطر حقن باستخدامها مباشرة داخل نص `IN(...)`. السبع مواضع
(4+3) استُبدلت لاستخدام الثابت المناسب بدل النص الحرفي. `tests/crash-safety.spec.js`
(الملف الوحيد الذي يبني `deps` مصغَّرة يدوياً بدل السيرفر الحقيقي) حصل على
نفس القيمتين لإبقاء بنيته متزامنة، رغم أن لا مسار مُختبَر به يستخدمهما
فعلياً حالياً.

## الإثبات
حزمة Playwright الكاملة (403 اختباراً، تشمل `admin.spec.js`،
`admin-superadmin.spec.js`، `offers.spec.js`، `account-deletion.spec.js`
التي تختبر كل فحوصات الحماية السبعة المذكورة أعلاه مباشرة ضد خادم حقيقي
وقاعدة بيانات اختبار معزولة) تعمل بلا أي فشل جديد بعد التعديل — نفس
النتيجة قبل وبعد (لا سلوك جديد؛ استبدال مصدر النص الحرفي فقط، لا تغيير
بالمنطق).

## نطاق متروك عمداً
البنود 7-8 لم تُعالَجا بعد بهذا المُدخَل.

**إضافة لاحقة — 5 مواضع إضافية اكتُشفت أثناء العمل على البند 7.** المسح
الأصلي لهذا البند بحث فقط عن نمط SQL (`status IN (...)`) — فاتته 5 مواضع
بـ`routes/requests.routes.js` تستخدم **نفس القائمة الفرعية الثلاثية**
(`TECHNICIAN_ACTIVE_JOB_STATUSES`) لكن كمصفوفة JS مع `.includes()` بدل
SQL: فحص منع إلغاء الطلب (مرتين، أحدهما بـ`DELETE /requests/:id` والآخر
بـ`POST /requests/:id/status`) وجدول `LEGAL_PREDECESSORS` لتحقّق انتقال
الحالة (3 مدخلات). استُبدلت الخمسة بنفس الثابت `TECHNICIAN_ACTIVE_JOB_STATUSES`
(النسخة المصفوفة الخام، لا `_SQL`) — بالضبط النمط الذي حذّر منه صاحب
المنتج: ثابت جديد يُستخدَم ببعض المواضع ويُترَك حرفياً بمواضع أخرى بنفس
المفهوم. حزمة Playwright الكاملة (403 اختباراً) تعمل بلا أي فشل جديد بعد
هذا التصحيح الإضافي أيضاً.

---

# [FEAT-DEDUP-01] البند 7: عدد الفرص المجانية "2" — طرف الخادم (منطق العمل الفعلي)

الدفعة الرابعة من نفس مسح `FEAT-DEDUP-01` (راجع المُدخَلات أعلاه). البند 7
مستقل بكل مستودع — راجع `sallehly_app`/DECISIONS.md لتفاصيل طرف العميل
(نصوص عرض فقط، لا منطق فعلي هناك).

## الخطورة
متوسط — على عكس طرف العميل (نصوص فقط)، هذه مواضع منطق عمل فعلي تحدد
متى يبدأ خصم العمولة الحقيقية من رصيد الفني. الرقم "2" مكرَّر حرفياً
بثلاثة ملفات routes مستقلة (`auth.routes.js`، `offers.routes.js`،
`requests.routes.js`) عبر **عدّادين منفصلين تماماً**
(`free_offers_used`/`free_orders_used` — راجع `FIX-OFFERQUOTA-01`). أي
تعديل مستقبلي لسياسة "عدد الفرص المجانية" (قرار منتج شائع الحدوث نسبياً)
كان سيحتاج تذكّر تحديث 3 مواضع مستقلة، بالإضافة لعدة مواضع عرض بـ
`public/app.js`.

## الاكتشاف
`Number(2)`/`< 2`/`>= 2` مكرَّرة حرفياً بـ:
- `auth.routes.js` — حساب `free_offers_remaining` ضمن استجابة `GET /me`.
- `offers.routes.js` — بوابة `INSUFFICIENT_BALANCE` عند تقديم عرض جديد.
- `requests.routes.js` — بوابة احتساب الطلب ضمن الفرصتين المجانيتين عند
  الإكمال.

بالإضافة لـ**5 مواضع عرض** بـ`public/app.js` (كلها تستخدم صيغة `/2` أو
`<2` بنص القالب مباشرة): بطاقة "طلبات مجانية" بلوحة الفني (تظهر مرتين
بنفس الدالة `techDash()` الحيّة — تبويب "الرصيد" والتبويب الافتراضي)،
`balancePage()`، `techCard()` (مؤشر "متاح/بحاجة رصيد" بقائمة الفنيين
القريبين)، و`v22TechBalance()`.

**اكتشاف إضافي: نسخة ثانية كاملة من `techDash()` غير مُنفَّذة (dead code)
تحوي نفس النمط.** أثناء البحث، تبيَّن أن `public/app.js` يحمل تعريفين
كاملين لدالة `async function techDash(){...}` بنفس الاسم بنطاق عام واحد
(أسطر متباعدة ~700 سطر) — بجافاسكريبت، الإعلان الثاني يستبدل الأول كلياً
(لا خطأ، لا تحذير)، فالتعريف الأول **ميت تماماً، لا يُنفَّذ إطلاقاً مهما
حدث**. هذا خارج نطاق البند 7 (ليس تكرار "2" قابل للتصحيح — الكود كله ميت)
لكنه اكتشاف مستقل يستحق قراراً منفصلاً (حذف الكود الميت، أو دمج أي منطق
مفيد لو وُجد بالنسخة القديمة). لم يُعالَج هنا — يُترَك لقرار صاحب المنتج.

## الحل
ثابت جديد `FREE_TIER_QUOTA = 2` بـ`config/env.js`، مُمرَّر عبر
`deps.constants` بنفس نمط الثوابت الأخرى. المواضع الثلاثة بمنطق العمل
الفعلي استُبدلت لاستخدامه. رسالة الخطأ النصية بـ`offers.routes.js`
("فرصتين مجاناً") **تُركت كما هي عمداً** — صيغة مثنى عربي صحيحة فقط لأن
القيمة 2 فعلياً؛ عُلِّق عليها صراحةً بأنها مرتبطة بـ`FREE_TIER_QUOTA` ويجب
تحديثها يدوياً معه لو تغيّر (لا صيغة نحوية عربية واحدة تتبدّل تلقائياً
حسب الرقم). ثابت مطابق `const FREE_TIER_QUOTA = 2` أُضيف أيضاً بأعلى
`public/app.js` (ملف ثابت منفصل كلياً، بلا نظام وحدات مشترك مع الخادم)
واستُخدم بكل المواضع الخمسة الحيّة — النسخة الميتة من `techDash()`
تُركت بلا تعديل (تعديل كود لا يُنفَّذ إطلاقاً بلا فائدة).

## الإثبات
حزمة Playwright الكاملة (403 اختباراً، تشمل `offers.spec.js` الذي يختبر
بوابة الفرصتين المجانيتين مباشرة: `[FIX-OFFERQUOTA-01] عدّاد فرص العروض
المجانية الدائم`) تعمل بلا أي فشل جديد — نفس السلوك قبل وبعد (استبدال
مصدر الرقم فقط). لا اختبار Playwright لـ`public/app.js` (فجوة تغطية سابقة
معروفة، راجع البند 1) — تحقّق يدوي بمراجعة كل موضع بعد الاستبدال يؤكد
تطابق النص الناتج قبل وبعد لقيمة `FREE_TIER_QUOTA=2` الحالية.

## نطاق متروك عمداً
النسخة الميتة من `techDash()` لم تُحذَف — راجع "الاكتشاف" أعلاه، اكتشاف
مستقل يحتاج قراراً منفصلاً. رسالة `offers.routes.js` النصية لم تُحوَّل
لصيغة ديناميكية — راجع "الحل" أعلاه. البند 8 لم يُعالَج بعد بهذا المُدخَل.

---

# [FEAT-DEDUP-01] البند 8: الحد الأقصى لمحاولات OTP، وتوليد الكود، ونمط الهاتف — طرف الخادم

الدفعة الخامسة والأخيرة من مسح `FEAT-DEDUP-01` (راجع المُدخَلات أعلاه).
البند 8 مستقل بكل مستودع — راجع `sallehly_app`/DECISIONS.md لتفاصيل طرف
العميل (تحقّق تنسيق محلي فقط، لا منطق أمني).

## الخطورة
منخفض — التحقّق من التنسيق (الهاتف) توثيقي/تجربة مستخدم بحت، ومُعاد
تطبيقه بطرف الخادم بغض النظر (`middleware`/`validator` لا علاقة لها).
الحد الأقصى للمحاولات (5) ونمط توليد الكود (6 أرقام) أهم قليلاً — منطق
حماية فعلي ضد التخمين العشوائي (brute-force) لكود OTP — لكن تكراره لم
يكن قد انحرف فعلياً (كلا المسارين كانا متطابقين بالقيمة، مجرد نسخ يدوي).

## الاكتشاف
- **الحد الأقصى لمحاولات OTP الخاطئة (5):** مكرَّر حرفياً 4 مرات
  بـ`auth.routes.js` (فحص الحد الأقصى، وحساب "المحاولات المتبقية"، بمساري
  تسجيل العضوية وإعادة تعيين كلمة السر كلٌّ على حدة).
- **توليد كود OTP (`Math.floor(100000 + Math.random() * 900000)`،
  6 أرقام):** نفس الصيغة الحسابية بالضبط مكرَّرة مرتين (نفس المسارين
  أعلاه).
- **نمط رقم الهاتف (`/^07\d{8}$/`):** مكرَّر حرفياً مرتين (تسجيل، تعديل
  الملف الشخصي)، بنفس نص/رمز الخطأ بالضبط.

## الحل
- `OTP_MAX_ATTEMPTS = 5` و`FREE_TIER_QUOTA`... (راجع البند 7) بـ`config/env.js`،
  عبر `deps.constants` بنفس النمط المُتَّبع بقية البنود.
- `PHONE_REGEX = /^07\d{8}$/` ودالة `generateOtp()` جديدتان بـ`utils/helpers.js`
  (دوال/ثوابت صرفة، بلا اعتماد على `db` — تنضم تلقائياً لـ`deps.utils`
  عبر `{ ...utilsHelpers, ... }` الموجود أصلاً بـ`server.js`، بلا أي تعديل
  إضافي هناك).
- المواضع السبعة (4 محاولات + 2 توليد + 2 هاتف — إجمالاً 8 استبدالات؛
  الفرق بين 7 مواضع منطقية و8 استبدالات لأن أحد فحوص الهاتف يظهر بموضعين
  منفصلين فعلياً) استُبدلت بالثوابت/الدالة الجديدة.
- `tests/crash-safety.spec.js` (الملف الوحيد الذي يبني `deps` مصغَّرة
  يدوياً) حصل على `PHONE_REGEX`/`generateOtp`/`OTP_MAX_ATTEMPTS` أيضاً —
  ضرورية فعلياً هذه المرة (لا اختيارية كبقية الثوابت المُضافة سابقاً لنفس
  الملف): اختبار `POST /auth/register` بهذا الملف يصل فعلياً لفحص تنسيق
  الهاتف قبل أي نداء db، فبدون `PHONE_REGEX` بالـmock كان الاختبار
  سينجح لسبب خاطئ (خطأ نوع مبكر بدل رفض DB المقصود فعلاً بالسيناريو).

## الإثبات
حزمة Playwright الكاملة (403 اختباراً) تعمل بلا أي فشل جديد — نفس السلوك
قبل وبعد (استبدال مصدر الرقم/الصيغة فقط، لا تغيير منطقي). تحقّق يدوي
إضافي أن `tests/crash-safety.spec.js`'s register test لا يزال يصل لنفس
مسار الفشل المقصود (رفض db، لا خطأ نوع مبكر) بعد إضافة `PHONE_REGEX`
للـmock.

## نطاق متروك عمداً
لا شيء — هذا آخر بند بالمسح الأصلي الثمانية. النسخة الميتة من `techDash()`
(البند 7) لا تزال بانتظار قرار منفصل من صاحب المنتج.

---

# [FEAT-DEDUP-01] حذف النسخة الميتة من techDash() — قرار صاحب المنتج

صاحب المنتج راجع الاكتشاف بالبند 7 أعلاه وطلب الحذف، بشرط التحقّق أولاً
أن النسخة الحيّة لا تفتقد شيئاً كانت النسخة الميتة تعرضه.

## التحقّق قبل الحذف
قارنت الجسدين حرفياً بندًا ببند: النسخة الميتة (تبويب "dash" الافتراضي)
كانت تعرض 4 قيم فقط عبر `<div class="cards4">` بسيطة — الرصيد،
`free_orders_used/2`، التقييم، الأعمال المكتملة. النسخة الحيّة تعرض نفس
الأربعة بالضبط ضمن `dashboardHero(...)` (بما فيها النسخة المُصحَّحة
`free_quota_used ?? (free_orders_used||0)` بدل القديمة القابلة للتحيّز)،
**زائد** محتوى إضافي (activityBox، promoBox، بطاقة "ملخص سريع" بحالة
الحساب والعمولة، chartsBox). نفس الفحص لبقية التبويبات (orders/balance/
topups/ledger): كل استدعاء دالة موجود بالنسخة الميتة (`reqTable`،
`balancePage`، `topupTable`، `ledgerTable`) موجود بنفس الاستدعاء بالضبط
بالنسخة الحيّة، فقط بإضافة رأس `dashboardHero` فوقه. تحقّقت أيضاً أن لا
شيء بين الإعلانين يلتقط مرجعاً للنسخة الأولى قبل استبدالها (`__oldTechDash`
يلتقط الثانية فقط، بعد إعلانها). **لا فقدان أي حقل أو بيانات — النسخة
الحيّة مجموعة شاملة تماماً (superset) للنسخة الميتة.**

## الحل
حُذفت النسخة الميتة بالكامل (سطر واحد، تعريف الدالة الكامل). حزمة
Playwright الكاملة (403 اختباراً) تعمل بلا أي فشل جديد بعد الحذف — كان
كوداً ميتاً فعلاً، لا أثر تشغيلي له.

## الإثبات
403/403 اختباراً ناجحاً بعد الحذف، مطابق للعدد قبله تماماً.

---

# [FIX-NPMCI-01] النشر يستخدم الآن `npm ci` بدل `npm install` — يفشل بوضوح عند أي تعارض بدل حل صامت

من قائمة `[DEFERRED-AUDIT-10]` المؤجَّلة (البند "`nixpacks.toml` ينشر بـ`npm install`
لا `npm ci`") — أول بند من جولة متابعة طلبها صاحب المنتج صراحة لحل كل
المشاكل المتبقية بالتطبيق واحداً واحداً.

## الخطورة
متوسط — `npm install` يسمح لنفسه بتحديث `package-lock.json` أثناء
التثبيت لو وجد تعارضاً (حتى لو بسيطاً)، فيحل التعارض صامتاً بدل الفشل
الواضح. عملياً هذا يعني: لا ضمان أن نسخ الحزم المثبَّتة فعلياً بأي نشر
تطابق ما هو مُثبَّت بملف القفل المُراجَع والمُختبَر — يشمل ذلك الأصفار
الدقيقة التي ثبَّتها تدقيق تبعيات سابق (`SEC-FIX-DEPAUDIT-01`) عمداً.

## الاكتشاف
`nixpacks.toml`, `[phases.install]`: `cmds = ["npm install"]`. `package-lock.json`
موجود ومتتبَّع فعلياً (لا استثناء بـ`.gitignore`)، بصيغة `lockfileVersion: 3`
حديثة — كل الشروط اللازمة لـ`npm ci` متوفرة أصلاً، لم يكن يُستخدَم فقط.

## الحل
`cmds = ["npm ci"]` بدل `["npm install"]`. `npm ci` يثبِّت حرفياً ما هو
مُدوَّن بملف القفل أو يفشل بوضوح بلا أي حل تعارضات صامت — نفس السلوك
المتوقَّع من أي نشر إنتاجي حقيقي.

## الإثبات
تحقُّق مباشر: حذف `node_modules` بالكامل ثم تشغيل `npm ci` فعلياً من
الصفر — نجح نظيفاً (345 حزمة، بلا أي خطأ، فقط تحذيرات معتادة غير متعلقة
بهذا التغيير: فرق إصدار Node المحلي عن `nodejs_20` المحدَّد بـ`nixpacks.toml`
نفسه، وتحذيرات `deprecated` معتادة لحزم فرعية). حزمة Playwright الكاملة
(403 اختباراً) تعمل بعدها بلا أي فشل جديد — نفس النتيجة قبل وبعد، لأن
`node_modules` الناتجة مطابقة تماماً لما كان `npm install` ينتجه أصلاً؛
الفرق فقط بسلوك أي عدم تطابق مستقبلي (فشل بدل حل صامت).

## نطاق متروك عمداً
لا شيء بهذا البند تحديداً. باقي بنود `[DEFERRED-AUDIT-10]` تُعالَج
بترتيب منفصل، كل واحد بمُدخَل مستقل عند اكتماله.

---

# [FIX-ADMINENVRESET-01] كلمة سر الأدمن لم تعد تعود تلقائياً لقيمة `.env` بكل إعادة نشر

من قائمة `[DEFERRED-AUDIT-10]` المؤجَّلة (البند "كلمة سر الأدمن ترجع
تلقائياً لقيمة `.env` بكل إعادة نشر") — ثاني بند من نفس جولة المتابعة
(`FIX-NPMCI-01` كان الأول).

## الخطورة
متوسط-مرتفع — كل إقلاع للسيرفر (يعني كل إعادة نشر بالإنتاج، حدث شائع
جداً بدورة نشر نشطة) كان `migrate()` يعيد كتابة `email`/`password_hash`
لحساب الأدمن من `ADMIN_EMAIL`/`ADMIN_PASSWORD` بملف `.env` بلا أي شرط،
طالما الحساب موجود أصلاً. لو غيّر الأدمن كلمة سره من داخل التطبيق نفسه
(مسار مقصود وسليم تماماً) فإن أول إعادة نشر لاحقة تُرجعها صامتاً للقيمة
القديمة بـ`.env` — بلا أي تنبيه أو أثر بالسجلات يوضّح لماذا "توقفت" كلمة
السر الجديدة عن العمل فجأة.

## الاكتشاف
`config/migrate.js`، قسم مزامنة حساب الإدارة: عند وجود صف أدمن أصلاً
(`existingAdmin`)، كان التنفيذ `UPDATE users SET email=?, password_hash=?,
is_active=1, is_super_admin=1 WHERE id=?` بلا أي شرط يميّز "المُشغِّل غيّر
`.env` فعلاً" عن "مجرد إعادة تشغيل عادية بـ`.env` كما هو". النية الأصلية
الموثَّقة بتعليق الكود («غيّر `.env` ثم أعد التشغيل») هي آلية استرجاع
صريحة عند فقدان الوصول لحساب الأدمن — لا إعادة ضبط دورية بكل إقلاع.

## الحل
عمود جديد `env_admin_fingerprint` (نص) على صف الأدمن، يخزّن
`SHA-256(adminEmail + ' ' + adminPassword)` لقيمتَي `.env` المُطبَّقتين
آخر مرة. عند كل إقلاع، ثلاث حالات:
1. **البصمة الحالية مطابقة للمخزَّنة** (`.env` لم يتغيّر): لا يُلمَس
   `email`/`password_hash` إطلاقاً — فقط `is_active=1, is_super_admin=1`
   يُفرَضان كالمعتاد (نفس منطق `FIX-SUPERADMIN-01` الأصلي).
2. **لا بصمة مخزَّنة بعد** (`NULL` — حالة الترقية: حساب موجود من قبل هذا
   الإصلاح): تُسجَّل البصمة الحالية كخط أساس فقط، بلا لمس `email`/
   `password_hash` الحيَّين — تفادياً لإعادة ضبط غير متوقَّعة على أول
   إقلاع بعد نشر هذا الإصلاح نفسه.
3. **البصمة تغيّرت فعلاً عن آخر قيمة مسجَّلة**: تغيير متعمَّد من
   المُشغِّل بـ`.env` — تُطبَّق القيمة الجديدة وتُسجَّل بصمتها.

العمود يُضاف عبر `ALTER TABLE users ADD COLUMN env_admin_fingerprint
TEXT` مغلَّف بـ`try/catch` فارغ (نفس نمط بقية أعمدة `migrate.js`) — آمن
على التشغيل المتكرر.

## الإثبات
اختباران جديدان بـ`tests/db-integrity.spec.js` ضمن نفس مجموعة
`[DB] أمان تشغيل migrate() أكثر من مرة`:
- كلمة سر مُغيَّرة يدوياً (محاكاة تغيير من داخل التطبيق) تبقى كما هي عبر
  `migrate()` متكرر طالما `ADMIN_EMAIL`/`ADMIN_PASSWORD` لم يتغيّرا، ثم
  تتغيّر فعلاً فقط بعد تغيير حقيقي لقيمتَي `.env`.
- حساب أدمن بلا `env_admin_fingerprint` (محاكاة حساب من قبل هذا الإصلاح)
  يسجّل بصمة أساسية على أول `migrate()` بلا لمس كلمة سره الحالية، وتبقى
  محفوظة على الإقلاعات اللاحقة.

حزمة Playwright الكاملة (405 اختباراً، يشمل الاختبارين الجديدين) تعمل
بلا أي فشل — 405/405 ناجحة.

## نطاق متروك عمداً
لا شيء بهذا البند تحديداً. باقي بنود `[DEFERRED-AUDIT-10]` تُعالَج
بترتيب منفصل، كل واحد بمُدخَل مستقل عند اكتماله.

---

# [SCHED-FIX-CRONSTAGGER-01] النسخ الاحتياطي وتنظيف الرفوعات اليتيمة لم يعودا يصطدمان بنفس اللحظة

من قائمة `[DEFERRED-AUDIT-10]` المؤجَّلة (البند "مهمتا النسخ الاحتياطي
وتنظيف الرفوعات مجدولتان لتنطلقا معاً، للأبد") — ثالث بند من جولة المتابعة
(`FIX-NPMCI-01` ثم `FIX-ADMINENVRESET-01`).

## الخطورة
متوسط — تنافس مستمر على I/O القرص المحدود (1GB على Render، مشترَك مع كل
الوسائط المرفوعة)، يتكرر كل 6 ساعات للأبد طوال عمر الخدمة، ويتضاعف عند كل
إقلاع بسبب `[SCHED-FIX-BOOTRUN-01]`.

## الاكتشاف
`config/db.js`: `createDbBackup` (نسخ ملف `.sqlite` كامل عبر Online Backup
API + رفع خارجي اختياري) و`cleanupOrphanUploads` (قراءة/فحص/حذف عبر 4
مجلدات رفع) كلاهما مسجَّل بـ`setInterval(..., 6 * 60 * 60 * 1000)` بلا أي
تباعد بينهما، وكلاهما أيضاً يُشغَّل فوراً عند الإقلاع (`SCHED-FIX-BOOTRUN-01`)
بنفس اللحظة تقريباً — أي أنهما يصطدمان عند كل إقلاع، ثم كل 6 ساعات بعدها،
للأبد، بلا أي تدهور تدريجي يُنبِّه — فقط تنافس I/O صامت يتراكم أثره مع نمو
البيانات.

## الحل
ثابت تباعد جديد `UPLOAD_CLEANUP_STAGGER_MS` (30 دقيقة) يُطبَّق على
`cleanupOrphanUploads` فقط — على كل من تشغيلتها الفورية عند الإقلاع
(عبر `setTimeout` بدل الاستدعاء المباشر) وعلى طور `setInterval` نفسه (عبر
`setTimeout` يغلّف تسجيل الـ`setInterval`)، حتى يبقى التباعد ثابتاً للأبد لا
لمرة واحدة فقط. نصف ساعة لا نصف الدورة (3 ساعات) عمداً — حتى لا يُضعِف ضمان
`SCHED-FIX-BOOTRUN-01` الأصلي (تشغيلة مضمونة قريبة من الإقلاع حتى على دورة
نشر أسرع من الفاصل الزمني). `createDbBackup` لم يتغيّر إطلاقاً — يبقى
المرجع الزمني الثابت الذي يُقاس التباعد بالنسبة له.

## الإثبات
اختبار جديد بـ`tests/scheduled-jobs-boot.spec.js` (`[SCHED-FIX-CRONSTAGGER-01]`):
ملف رفع يتيم (بلا مرجع بقاعدة البيانات، عمره أكثر من يوم — هدف مضمون
للحذف لو نُفِّذت الدالة فوراً) يبقى موجوداً بعد الإقلاع مباشرة رغم ظهور
ملف النسخة الاحتياطية بنفس الفترة — يثبت مباشرة أن التنظيف لم يعد يعمل
بنفس لحظة النسخ الاحتياطي كما كان قبل الإصلاح. حزمة Playwright الكاملة
(406 اختباراً، يشمل الاختبار الجديد) تعمل بلا أي فشل — 406/406 ناجحة.

## نطاق متروك عمداً
لا اختبار مباشر لانتظار الـ30 دقيقة الفعلية وإثبات أن التنظيف يعمل فعلاً
بعدها (غير عملي بوقت اختبار آلي) — الإثبات يقتصر على الجانب السلبي (لم
يعد يعمل فوراً بنفس اللحظة)، وهو المطلوب فعلياً لإغلاق هذا البند. باقي
بنود `[DEFERRED-AUDIT-10]` تُعالَج بترتيب منفصل، كل واحد بمُدخَل مستقل عند
اكتماله.

---

# [FEAT-UPLOADQUOTA-01] حصة تخزين تراكمية لكل مستخدم — لم يعد بإمكان حساب واحد أن يملأ القرص

من قائمة `[DEFERRED-AUDIT-10]` المؤجَّلة (البند "لا حصة تخزين (quota) على
أي مستخدم — عميل واحد موثَّق يقدر يملأ قرص Render") — رابع بند من جولة
المتابعة (`FIX-NPMCI-01`، `FIX-ADMINENVRESET-01`، `SCHED-FIX-CRONSTAGGER-01`).

## الخطورة
متوسط — وفق تقدير جولة التدقيق العاشرة، رفع وسائط شات مستمر ضد نفسه (لا
حاجة لأي طرف آخر أو ثغرة) يقدر يملأ القرص المحدود (1GB على Render، مشترَك
مع كل مستخدمي المنصّة) خلال ~7 دقائق تقريباً. لا فحص حصة لكل مستخدم بأي
مسار رفع بالمشروع قبل هذا الإصلاح.

## الاكتشاف
5 مسارات رفع مصادَق عليها (`POST /me/profile` [avatar]، `POST /requests`
[problem_image]، `POST /requests/:id/images` [image]، `POST /requests/:id/audio`
[audio]، `POST /api/topups` [receipt]) — كلها تمر عبر `middleware/upload.js`
(multer)، ولا واحد منها يتحقق من مجموع ما رفعه المستخدم نفسه سابقاً قبل
قبول ملف جديد. الحد الوحيد الموجود كان لكل *ملف* على حدة (3MB صور، 5MB
صوت) — لا لكل *مستخدم* عبر كل رفوعاته.

## الحل
عمود تراكمي جديد `users.total_upload_bytes` (لا يُخفَّض أبداً، حتى لو حُذف
الملف لاحقاً بتنظيف دوري أو استبدال صورة شخصية) + middleware جديدة
`enforceUploadQuota` (`middleware/upload.js`) تُشغَّل بعد multer وبعد
`verifyImageMagicBytes` حيثما وُجدت، على كل مسارات الرفع الخمسة المصادَق
عليها أعلاه (لا مسار التسجيل — لا حساب موجود بعد وقت رفع صورة التسجيل).
سقف 100MB لكل مستخدم عبر حياة حسابه كاملة — سخي لاستخدام طبيعي، بينما يُبقي
أسوأ سيناريو لحساب واحد عند ~10% فقط من القرص الكلي. تجاوز الحصة يحذف
الملف المرفوض فوراً من القرص (لا يبقى يتيماً) ويرفض الطلب بـ413/
`STORAGE_QUOTA_EXCEEDED` (`utils/errors.js`'s `QuotaExceededError`، بنفس
نمط `ForbiddenError` تماماً — قرار متوقَّع ومقصود، لا خطأ غير متوقَّع، فيُترجَم
مباشرة عبر `apiErrorHandler` بلا تسجيل/تنبيه إزعاجي).

## نطاق متروك عمداً
`total_upload_bytes` عدّاد تراكمي **لا يُخفَّض أبداً** — ليس تتبّعاً حياً
لمساحة المستخدم المشغولة فعلياً الآن، بل ضماناً رياضياً بسيطاً: مستحيل لأي
حساب واحد أن يتسبَّب بأكثر من 100MB من الكتابة للقرص إجمالاً عبر حياة
الحساب، بغض النظر عمّا يُنظَّف لاحقاً (تنظيف دوري، استبدال صورة، إلخ). بناء
تتبّع "استخدام حي" دقيق يتطلَّب تعديل كل مسار حذف ملف بالمشروع (تنظيف
الرفوعات اليتيمة، استبدال الصورة الشخصية، وأي مسار إداري مستقبلي) لخصم
الحجم عند كل حذف — تعقيد غير مبرَّر للتهديد الموثَّق فعلياً (رفع سريع متكرر
يملأ القرص خلال دقائق)، الذي يحلّه السقف الصلب البسيط أعلاه بالكامل. لا
تغيير على مسار رفع صورة التسجيل (`POST /auth/register`) — التهديد الموثَّق
تحديداً هو "عميل موثَّق" (حساب موجود فعلاً)، لا تسجيلات مجهولة (مُخفَّفة أصلاً
بـ`registerLimiter` وتنظيف `pending_users` المنتهية). باقي بنود
`[DEFERRED-AUDIT-10]` تُعالَج بترتيب منفصل، كل واحد بمُدخَل مستقل عند
اكتماله.

## الإثبات
اختبار جديد `tests/upload-quota.spec.js` (3 اختبارات): رفع تحت الحصة ينجح
ويزيد `total_upload_bytes` بحجم الملف الفعلي المرفوع؛ رفع يتجاوز الحصة
المتبقية (بايت واحد أقل من السقف، مضبوط مباشرة بقاعدة البيانات لتفادي رفع
100MB فعلياً بالاختبار) يُرفض بـ413/`STORAGE_QUOTA_EXCEEDED`، بلا أي زيادة
على العدّاد وبلا إنشاء أي طلب شحن من الرفع المرفوض؛ رفع بفارق كافٍ متبقٍ
ينجح عادياً. حزمة Playwright الكاملة (409 اختباراً، يشمل الثلاثة الجديدة)
تعمل بلا أي فشل — 409/409 ناجحة (اكتُشف أثناء التشغيل الكامل أن
`tests/crash-safety.spec.js`'s `buildMinimalAuthApp` يبني `deps.middleware`
مصغَّراً بيدويّاً لا يتضمَّن `enforceUploadQuota` الجديدة — أُضيفت هناك بنفس
نمط `verifyImageMagicBytes` المزيَّفة المجاورة تماماً).

---

# [FEAT-RETENTION-01] تنظيف دوري لـ messages/audit_logs/chat_violations — لم تعد تنمو للأبد

من قائمة `[DEFERRED-AUDIT-10]` المؤجَّلة (البند "لا تنظيف دوري لأي جدول عدا
notifications/pending_users — messages/audit_logs/chat_violations تنمو
للأبد") — خامس بند من جولة المتابعة (بعد `FIX-NPMCI-01`،
`FIX-ADMINENVRESET-01`، `SCHED-FIX-CRONSTAGGER-01`، `FEAT-UPLOADQUOTA-01`).

## الخطورة
متوسط — لا خطورة أمنية مباشرة، لكن نمو غير محدود لثلاثة جداول (أكبرها
`messages`، مصدر أكبر حجم كتابة بالمشروع فعلياً) على قرص محدود (1GB على
Render) يتفاقم مع الوقت، وأداء الاستعلامات ضدها يتدهور تدريجياً.

## قرار المنتج (لا افتراض هندسي)
بخلاف بقية بنود هذه الجولة (قرارات هندسية بحتة بلا أي أثر على بيانات
مستخدمين حقيقية)، هذا البند يحذف بيانات فعلية يستخدمها المستخدمون
(محادثات) والإدارة (سجل تدقيق، سجل مخالفات) — فطُرح على صاحب المنتج صراحة
قبل أي تنفيذ (لم يُفترَض شيء). المدد المعتمدة، كما حدَّدها صاحب المنتج:
- **`messages`**: تُحذَف رسائل أي طلب **أسبوعاً بعد إغلاقه فعلياً** (الفني
  خلّص من العميل والطلب صار "مكتمل"، أو "ملغي") — لا حسب عمر الرسالة نفسها.
  طلب لا يزال نشطاً (لم يُغلَق بعد) لا تُلمَس رسائله إطلاقاً مهما طال عمرها.
- **`audit_logs`**: **6 أشهر (180 يوماً)** من تاريخ كل سجل — تعليق حساب،
  حذف، إلخ.
- **`chat_violations`**: **سنتان (730 يوماً)**، باستثناء أي مخالفة لا تزال
  `status='مفتوح'` (لم يراجعها الأدمن بعد) — تبقى هذه بغض النظر عن عمرها،
  لا يجوز حذف حالة تأديبية لم يُبتّ فيها بعد.

## الاكتشاف
لا فحص لأي من الشروط أعلاه بأي مكان بالمشروع قبل هذا الإصلاح — الجداول
الثلاثة كانت تُدرَج فقط، بلا أي `DELETE` دوري (بعكس `notifications` —
`NOTIF-RETENTION-01` — و`pending_users`).

## الحل
ثلاث دوال تنظيف دورية جديدة بـ`config/db.js` (`cleanupOldChatMessages`،
`cleanupOldAuditLogs`، `cleanupOldChatViolations`)، بنفس نمط
`cleanupOldNotifications` الموجود تماماً: فاصل 24 ساعة (`setInterval`) +
تشغيلة فورية عند الإقلاع (`SCHED-FIX-BOOTRUN-01`). `cleanupOldChatMessages`
تحديداً لا تقارن على `messages.created_at` مباشرة، بل تحذف كل رسائل أي
طلب حيث `requests.status IN ('مكتمل','ملغي') AND requests.updated_at <
الحد` (عمود `updated_at` يُضبَط عند كل تغيير حالة فعلي — راجع
`routes/requests.routes.js`) — يضمن عملياً أن رسائل أي طلب نشط لا تُلمَس
مطلقاً بغض النظر عن عمرها، ويطابق حرفياً "أسبوع بعد ما الفني خلّص من
العميل" لا "أسبوع من إرسال الرسالة". فهرسان جديدان (`config/migrate.js`)
يخدمان شرطَي `WHERE` هذين تحديداً: `idx_requests_status_updated` و
`idx_chat_violations_status_created` (`idx_audit_logs_created` كان موجوداً
أصلاً).

## نطاق متروك عمداً
'ملغي' (طلب ملغي) أُضيفت لشرط `cleanupOldChatMessages` إلى جانب 'مكتمل'
(المذكورة صراحة) — تفسير معقول لنفس النية المعلَنة ("الطلب خلص") بدل قراءة
حرفية ضيقة تترك محادثات الطلبات الملغاة تنمو للأبد رغم أنها بنفس درجة
"الانتهاء" فعلياً. باقي بنود `[DEFERRED-AUDIT-10]` تُعالَج بترتيب منفصل، كل
واحد بمُدخَل مستقل عند اكتماله.

## الإثبات
3 اختبارات جديدة بـ`tests/data-retention.spec.js` (نفس أسلوب
`tests/scheduled-jobs-boot.spec.js` — إقلاع إنتاج حقيقي على قاعدة بيانات
مؤقتة معزولة): رسائل طلب أُغلق منذ أكثر من أسبوع تُحذَف، رسائل طلب أُغلق
حديثاً أو لا يزال نشطاً تبقى؛ سجل تدقيق أقدم من 6 أشهر يُحذَف والأحدث يبقى؛
مخالفة شات مغلقة أقدم من سنتين تُحذَف، المفتوحة تبقى مهما قدُمت (حتى لو
أقدم بكثير من الحد)، والأحدث تبقى. حزمة Playwright الكاملة (412 اختباراً،
يشمل الثلاثة الجديدة) تعمل بلا أي فشل — 412/412 ناجحة.

---

# [SEC-FIX-CHATCHOKEPOINT-SCANFIX-01] فحص chokepoint الشات: تغطية JOIN + استعلام متعدد الأسطر + اختبار مباشر للأداة نفسها

من قائمة `[DEFERRED-AUDIT-10]` المؤجَّلة (البند "`scripts/check-chat-content-chokepoint.js`
— regex الفحص يمكن تجاوزه بصياغة SQL شائعة، لا استثنائية؛ لا اختبار يغطي
أداة الفحص نفسها") — سادس بند من جولة المتابعة.

## الخطورة
متوسط — الأداة (`SEC-FIX-CHATACCESS-CHOKEPOINT-01`) موجودة أصلاً بالضبط
لمنع قراءة محتوى شات خارج نقطة الوصول المفروضة (`utils/db-helpers.js`)، لكن
تجاوزها لا يتطلَّب أي حيلة تحايل متعمَّدة — مجرد صياغة SQL عادية تماماً قد
يكتبها مطوّر بلا أي قصد للتحايل، فتمرّ عبر CI بصمت.

## الاكتشاف
النمط الأصلي `/FROM\s+messages\b/i` يُقارَن **سطراً سطراً** (`lines.forEach`)،
لا على محتوى الملف كاملاً. فجوتان حقيقيتان مؤكَّدتان بقراءة الكود مباشرة:
1. **`JOIN messages`**: الكلمة التي تلي `FROM` مباشرة اسم جدول آخر (مثلاً
   `FROM requests r JOIN messages m ON ...`)، لا `messages` — لا يطابق النمط
   إطلاقاً رغم أنه استعلام مباشر حقيقي على الجدول.
2. **استعلام مقسَّم على أكثر من سطر** (شائع جداً بـtemplate literals متعددة
   الأسطر لاستعلامات SQL طويلة): `FROM` بسطر، `messages` بالسطر التالي —
   المقارنة سطراً بسطر لا تمنح `\s+` فرصة مطابقة فاصل الأسطر بين عنصرين
   منفصلين بمصفوفة `lines`.

بالإضافة: لا اختبار مباشر إطلاقاً لهذا الملف قبل هذا الإصلاح — الأداة نفسها
كانت بلا تغطية.

## الحل
- النمط أصبح `/\b(?:FROM|JOIN)\s+messages\b/gi` — يغطي `JOIN` أيضاً.
- الفحص أصبح على **محتوى الملف كاملاً دفعة واحدة** (لا سطراً سطراً) — `\s+`
  بجافاسكربت يطابق أصلاً فواصل الأسطر ضمنياً، فيحل مشكلة الاستعلام متعدد
  الأسطر تلقائياً بلا أي منطق إضافي مطلوب، بمجرد التوقف عن التقطيع لسطور
  منفصلة قبل المطابقة. رقم السطر المُبلَّغ عنه يُحسَب من موضع بداية المطابقة
  الفعلي (`FROM`/`JOIN`)، لا موضع `messages` نفسها.
- منطق الفحص الأساسي استُخرِج كدالة مُصدَّرة (`scanForViolations`) قابلة
  للاستدعاء المباشر من اختبار جديد على شجرة ملفات مؤقتة معزولة — بدل أن يكون
  الملف بأكمله كتلة تنفيذ فورية (`process.exit`) عند أي `require()`، ما كان
  يمنع اختباره بمعزل عن المستودع الحقيقي بالكامل. سلوك الـCLI (فحص المستودع
  الحقيقي، طباعة، exit code) يبقى كما هو تماماً، محصور بشرط
  `require.main === module`.

## نطاق متروك عمداً
هذا يبقى فحص نصّي (grep موسَّع)، لا تحليل شجرة كود (AST) حقيقياً — تعليق
الملف الأصلي يوثِّق هذا القرار صراحة (بديل رخيص متعمَّد عن تبنّي ESLint لهذا
الغرض فقط). صياغات SQL أغرب (مثلاً `messages` باسم مستعار غير قياسي، أو
استعلام مبني ديناميكياً من أجزاء نصية متفرّقة بجافاسكربت) تبقى قادرة نظرياً
على التحايل — هذا الإصلاح يغلق الفجوتين الموثَّقتين فعلياً (JOIN، تعدد
الأسطر)، لا كل تحايل ممكن نظرياً. باقي بنود `[DEFERRED-AUDIT-10]` تُعالَج
بترتيب منفصل، كل واحد بمُدخَل مستقل عند اكتماله.

## الإثبات
اختبار جديد `tests/chat-chokepoint-checker.spec.js` (5 اختبارات) يستدعي
`scanForViolations` مباشرة على شجرة ملفات مؤقتة: `FROM messages` بسطر واحد
(السلوك الأصلي) لا يزال يُكتشَف؛ `JOIN messages` (الثغرة الأولى) يُكتشَف
الآن؛ استعلام مقسَّم على أكثر من سطر (الثغرة الثانية) يُكتشَف الآن؛ ملفات
`ALLOWLIST` تبقى مستثناة فعلياً؛ ملف بلا أي مطابقة يمر بأمان. تحقُّق إضافي:
تشغيل الأداة كـCLI فعلياً ضد المستودع الحقيقي (`npm run check:chat-chokepoint`)
لا يزال ينجح بلا أي مخالفة جديدة بعد توسيع النمط (لا صياغة `JOIN messages`
حقيقية بالكود الفعلي حالياً). حزمة Playwright الكاملة (417 اختباراً، يشمل
الخمسة الجديدة): **417/417 ناجحة**.

---

# [FIX-BODYLIMIT-01] حد حجم الجسم أصبح متماثلاً بين JSON وurlencoded (1MB لكليهما) — وتبيّن أن الفجوة كانت تُفعَّل فعلياً بصمت

من قائمة `[DEFERRED-AUDIT-10]` المؤجَّلة (البند "منخفض/تافه": "حدود حجم
الجسم غير متماثلة عبر المسارات المختلفة — لم تُفحَص كفجوة أمنية فعلية،
فقط عدم اتساق ملحوظ") — من نفس جولة المتابعة (بالتوازي مع `FIX-API404-01`
— كلاهما من بندي "منخفض/تافه" المتبقيين، بمُدخَلين مستقلين وPR منفصلين).

## الخطورة
كانت مُصنَّفة "منخفض/تافه" بجولة التدقيق العاشرة (عدم اتساق نظري فقط، لم
يُفحَص أثره الفعلي). **التحقق أثناء هذا الإصلاح رفعها فعلياً لمتوسط**: عدم
الاتساق لم يكن نظرياً فقط — اختبار موجود أصلاً
(`tests/security-hardening.spec.js`، "حمولة نصية ضخمة... تُرفض بـ400") كان
يصطدم فعلياً بالحد الضمني القديم (100kb) بصمت، بتعليق يدّعي خطأً أن فحص
التحقق الصريح بالراوت (حد 1000 حرف) هو من يرفضه.

## الاكتشاف
`app.js`: `express.json({ limit: '1mb' })` مضبوط صراحة، بينما
`express.urlencoded({ extended: true })` **بلا أي `limit`** — يرث افتراضي
`body-parser` (100kb، تحقَّق مباشرة من كود الحزمة). تشغيل الاختبار المذكور
أعلاه بمعزل (`-g`) أثبت مباشرة: الاستجابة الفعلية كانت `PayloadTooLargeError`
من `body-parser`، لا فحص طول الوصف بـ`routes/requests.routes.js` كما ادّعى
تعليق الاختبار — يمرّ عبر مسار "خطأ غير متوقَّع" بـ`apiErrorHandler`
(تسجيل + تنبيه عبر `alertError`) بدل مسار تحقق نظيف متوقَّع بلا أي تنبيه،
لسيناريو مستخدم عادي تماماً (وصف طلب طويل، لا هجوم).

## الحل
`express.urlencoded({ extended: true, limit: '1mb' })` — نفس حد
`express.json()` تماماً، حداً واحداً متسقاً بغض النظر عن نوع محتوى الطلب.

## نطاق متروك عمداً
اختبار "200 ألف حرف" الموجود أصلاً تبيَّن أنه **لم يكن يختبر ما يدّعيه
تعليقه أصلاً** — نص عربي 200 ألف حرف يُشفَّر (urlencoded) لنحو 1.2 ميجابايت
فعلياً (كل حرف عربي بايتان بـUTF-8، كل بايت غير ASCII يتحوّل لـ`%XX` أي 3
أحرف عند urlencode)، أكبر حتى من الحد الجديد (1MB) — لا يزال يصطدم بحد حجم
الجسم بعد هذا الإصلاح، لا بفحص التحقق المقصود. صُحِّح لاستخدام 5 آلاف حرف
(~30 كيلوبايت مُشفَّرة، بأمان تحت 1MB، تتجاوز حد الـ1000 حرف بوضوح) ليصل
فعلاً لفحص التحقق الذي يدّعي اختباره، مع تحقُّق إضافي صريح على `body.code`
(`REQUEST_DESCRIPTION_TOO_LONG`) بدل الاكتفاء بحالة 400 وحدها (كانت لتنجح
بنفس الشكل حتى لو الرفض من مصدر مختلف تماماً، كما ثبت). باقي بند
"منخفض/تافه" المتبقي بـ`[DEFERRED-AUDIT-10]` (`X-Frame-Options` مُضعَّفة
فعلياً بواسطة CSP) يُعالَج بمُدخَل مستقل عند اكتماله.

## الإثبات
اختبار جديد بـ`tests/malformed-input-safety.spec.js`: جسم urlencoded بحجم
500 كيلوبايت (بين الحد القديم 100kb والجديد 1MB) يصل الآن فعلياً لراوت
`/auth/login` (يُثبَت بوصوله لفحص طول كلمة السر الصريح هناك —
`401`/`AUTH_INVALID_CREDENTIALS` — بدل الاصطدام بحد حجم الجسم أولاً). اختبار
"200 ألف حرف" المُصحَّح (الآن 5 آلاف) يتحقق صراحة من `body.code` الصحيح.
حزمة Playwright الكاملة (418 اختباراً): **418/418 ناجحة**.
